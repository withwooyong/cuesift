# CLAUDE.md — cuesift

이 파일은 이 저장소에서 작업할 때 매 세션 컨텍스트에 실린다.
**여기 적힌 것은 코드를 읽어서는 알 수 없는 것들이다.**

## 이 프로젝트가 무엇인가

**번역기가 아니라 검수 트리아지 엔진이다.** 번역은 수단이고 목적은
"사람이 정말 봐야 할 자막을 고르는 것"이다. 이 구분이 모든 설계 결정을 지배한다.

핵심 지표는 **Recall @ Budget** — "검수 예산 X%를 썼을 때 실제 오류 중 몇 %를 큐에 담았는가".
README 최상단에 들어갈 숫자는 **무작위 베이스라인 대비 배수**이고,
이것이 "AI 래퍼가 아니다"를 증명하는 유일한 자료다(요구사항정의서 §9.1, §11 R4).

**비용은 3계층으로 통제한다**(§4):

| 계층 | 내용 | 비용 |
| --- | --- | --- |
| Tier 0 | 결정론적 신호 9종 — 전량 적용 | 0 |
| Tier 1 | 자가일관성·역번역 — Tier 0이 좁힌 후보만 | LLM 호출 |
| Tier 2 | QE 모델 (v0.2, 플러그인) | 모델 추론 |

## 문서 지도

| 문서 | 역할 |
| --- | --- |
| `docs/요구사항정의서.md` | **단일 진실 원천.** FR·NFR 번호가 코드 주석의 근거로 쓰인다 |
| `docs/WBS.md` | 작업 분해와 진척. **요구사항정의서의 파생물이지 병렬 출처가 아니다** — FR이 여기서만 바뀌면 갈라진다 |
| `docs/AI_자막검수_오픈소스_비교.md` | 공통 용어 정의(§0)의 단일 출처. 조사 질문 §5 |
| `docs/번역관리_TMS_솔루션_비교.md` | 상용 TMS 조사 |
| `docs/superpowers/specs/` | 설계 스펙 |
| `docs/superpowers/plans/` | 구현 계획. **"구현 중 바뀐 결정" 절이 있으면 본문 코드 블록보다 그쪽이 최신이다** |
| `HANDOFF.md` | 직전 세션 인수인계 |
| `CHANGELOG.md` | Keep a Changelog 형식 |

**용어 정의는 단일 출처로 둔다.** 공통 용어의 상세는 `AI_자막검수_오픈소스_비교.md` §0에만 두고
`요구사항정의서.md` §0.5는 한 줄 요약 + 포인터만 갖는다. 같은 용어를 두 문서에서 각각 정의하면
한쪽만 고쳐져 반드시 갈라진다.

**요구사항정의서 §12에서 Q가 닫히면 조사 문서 §5도 함께 봐야 한다.**
두 문서가 서로를 링크하므로 한쪽만 갱신하면 독자가 상반된 두 주장을 만난다.

**파생 문서에서 읽은 사실은 원본에서 읽은 것보다 약하다.** 같은 문장이 여러 문서에
있는 것은 독립 확인이 여러 번이라는 뜻이 아니라 **한 번 쓰이고 복사됐다**는 뜻이다.
근거로 삼기 전에 코드로 확인한다 — "`spans[].side`가 이미 실려 있다"가 요구사항정의서·
WBS·HANDOFF 셋에 복사돼 FR-7.3을 "렌더러 하나"로 오인하게 만든 적이 있다.
셋 다 참이었다(스키마에 실려 있었다). 거짓인 것은 읽히는 함의였다 —
`grep -rn "Span(" src/`가 **0건**이었다.

## 개발 환경

**Python 실행은 반드시 `.venv/Scripts/python.exe`를 쓴다.** 시스템 Python은 3.14라 다르다.

**로컬 게이트는 CI가 돌리는 명령과 대상이 같아야 한다.** 아래는 `.github/workflows/ci.yml`에서
그대로 옮긴 것이다 — 임의로 좁히지 말 것.

```bash
.venv/Scripts/python.exe -m ruff check .
.venv/Scripts/python.exe -m ruff format --check .
.venv/Scripts/python.exe -m pytest --cov=cuesift --cov-report=term-missing
python scripts/check_links.py
npx --yes markdownlint-cli2
```

**`src tests`로 좁히면 안 된다.** 2026-07-29부터 8월 1일까지 CI가 **5회 연속 실패**했는데
아무도 몰랐다 — 로컬은 `ruff format --check src tests`를 돌려 통과했고 CI는 `.`을 돌려
실패했다. 원인은 ruff 0.16이 마크다운의 파이썬 코드 블록을 포매팅 대상에 넣은 것이었고,
지금은 `[tool.ruff.format] exclude = ["*.md"]`로 닫혀 있다.
**"통과했나"가 아니라 "무엇을 대상으로 통과했나"가 이 사건의 교훈이다.**

**의존성은 고정이다.** 런타임 4개(`typer`·`pysubs2`·`pyyaml`·`httpx`), dev 3개(`pytest`·`pytest-cov`·`ruff`).
추가하지 않는다 — `scripts/check_links.py`가 표준 라이브러리만 쓰는 것도 같은 이유다.

CI는 5잡이다: `test 3.11`·`3.12`·`3.13`·`3.14` · `docs`(markdownlint + 링크 검사).
**매트릭스는 `pyproject.toml`의 `requires-python`(`>=3.11`)과 같은 집합이다** —
한쪽만 바꾸면 지원한다고 선언한 버전을 아무도 검사하지 않는 상태가 된다.
2026-08-28까지 실제로 그랬다: 3.13·3.14가 선언에는 있고 CI에는 없었으며,
3.14는 개발자 로컬에서 우연히 돌고 있었을 뿐이다.

**로컬이 3.14인 것은 이제 매트릭스 안에 있지만, 그것으로 CI가 대체되지 않는다.**
OS가 다르고(Windows vs ubuntu-latest) 환경변수가 다르다 — 로컬과 CI가 갈린
유일한 실측 사례는 버전이 아니라 `FORCE_COLOR`였다(rich가 CI에서만 색을 켜
`--help` 출력의 옵션 이름을 쪼갰다).

## PR 절차 — 작업 패키지 단위

**PR은 리뷰를 받기 위한 것이 아니라 게이트와 기록을 위한 것이다.** 이 구분이 절차를 정한다.
혼자 개발해도 아래 넷은 유효하다.

| 목적 | 혼자일 때 |
| --- | --- |
| 남의 리뷰를 받는다 | ❌ 무효 — 리뷰어가 없다 |
| **CI를 머지 전에 돌린다** | ✅ 유효 — `test 3.11`~`3.14`·`docs`가 다른 OS·다른 환경변수에서 도는 것을 검증한다 |
| **변경 묶음의 서술을 남긴다** | ✅ 유효 — 커밋 여러 개를 "무엇을 왜"로 요약한 문서가 생긴다 |
| **되돌리기 단위를 만든다** | ✅ 유효 — "이 PR을 revert"가 한 번에 된다 |
| **미래의 자신에게 설명한다** | ✅ 유효 — 6개월 뒤엔 남이다 |

```bash
git checkout -b feat/xxx          # 1. 브랜치를 판다
# ... 작업하고 커밋 ...
git push -u origin feat/xxx       # 2. 푸시
gh pr create --base main          # 3. PR 생성
gh pr checks --watch              # 4. CI 통과 대기
gh pr merge --squash              # 5. 머지 (또는 --merge)
git checkout main && git pull && git branch -d feat/xxx   # 6. 로컬 정리
```

**`main`에 직접 푸시하지 않는다.** CI의 `push` 트리거는 `branches: [main]`뿐이라
직접 푸시하면 **머지된 뒤에야** CI가 돈다 — 게이트가 아니라 사후 통보가 된다.
5회 연속 실패가 숨은 것이 정확히 이 구조 때문이다.

PR 본문에는 **무엇을 · 근거 문서 · 게이트 수치**를 담는다. 게이트 수치는 개수를 그대로 적는다.

## 코드 규약

- 모든 모듈 첫 줄에 `from __future__ import annotations`
- 독스트링과 주석은 **한국어**, 근거 FR·§ 번호를 병기한다 (예: `FR-6.1`, `§7.3`)
- ruff: `line-length = 100`, 규칙 `E,F,I,UP,B,SIM`
- 커밋 메시지는 **한국어**
- **푸시는 사용자가 명시적으로 요청할 때만.** 커밋과 푸시를 한 명령에 묶지 않는다

### 주석에 무엇을 적는가

"왜 이 값인가"가 아니라 **"이 값이 아니면 무엇이 깨지는가"** 를 적는다.
현재 코드의 상수들이 그렇게 돼 있다(`_UNTRANSLATED_MIN_CHARS`·`_DEGENERATION_MAX_TOKENS`·
`_RATIO_MIN_RELATIVE_DEVIATION`). 경계 조건이 코드에 명시돼 있으면 틀린 것도 눈에 보인다.

## 이 프로젝트의 규율

### 검사하지 않고 통과하는 게이트는 없는 게이트보다 나쁘다

**"통과했나"가 아니라 "무엇을 대상으로 통과했나"를 본다.**
`pytest`의 수집 개수, markdownlint의 `Linting: N files`, 링크 체커의 `상대 링크 N개`를
매번 읽는다. 0개 수집은 통과가 아니라 설정 오류다.

이 습관이 실제로 버그를 잡았다 — 링크 체커 첫 버전이 19개를 셌는데 기대값은 21개였고,
원인은 README 뱃지의 중첩 대괄호였다. `0 broken`만 봤다면 통과로 읽혔을 누락이다.

**문서를 추가했으면 `git add` 뒤에 링크 체커를 돌린다.** markdownlint는 `gitignore`
규칙을, `scripts/check_links.py`는 `git ls-files`를 대상으로 삼아 **추적되기 전의 새
문서는 링크 검사를 아예 받지 않는다** — 실측으로 32개 vs 31개로 갈렸다.
`깨진 링크 없음`만 읽으면 통과로 보인다. **두 도구의 파일 개수가 같은지를 본다.**

**게이트를 만들면 반드시 실패시켜 봐야 한다.** 회귀 테스트는 버그 코드에서 실제로
실패하는 것을 확인한 뒤에야 회귀 테스트다. 이 규율이 실행 중 한 번 발동했다 —
길이비 회귀 테스트가 버그 버전에서도 통과해 데이터를 다시 짰다.

### hard fail 신호에서는 미탐이 오탐보다 낫다

FR-6.2에 따라 hard fail은 **검수 예산을 우회**한다. 따라서 오탐은 단순한 품질 문제가 아니라
**실제 검수 비율을 부풀려 Recall@Budget 지표 자체를 파괴한다.**
Tier 0 구조 신호의 임계값이 전부 보수적인 것은 이 때문이다.

### 배수는 요청 예산이 아니라 실제 검수 비율로 나눈다

hard fail이 예산을 우회하므로 "예산 10% 요청"과 "실제 10% 검수"는 다르다.
`review_ratio()`가 후자를 낸다. 여기서 부풀리면 프로젝트의 핵심 주장이 무너진다.

### 가중치는 튜닝하지 않는다

`DEFAULT_WEIGHTS`는 전부 1.0이다. 같은 데이터에서 맞춘 가중치는 새 데이터에서 재현되지 않는다.
**점수 스케일을 통해 몰래 가중을 넣는 것도 금지다** — 실제로 한 번 발생했고
공용 `_violation_score`로 통일했다.

### 외부 URL은 아무도 검사하지 않는다

링크 체커는 내부 상대 링크만 본다(외부는 남의 서버 사정으로 간헐 실패하고,
무시되는 게이트는 없는 게이트와 같다). **착수 시점에 사람이 출처를 재확인해야 한다.**
실제로 `translations.ted.com`이 DNS 해석에 실패해 TED 프로파일 값을
검색 색인 스냅샷으로만 확인한 사례가 있다 — 그 사실을 YAML 주석과 테스트에 남겼다.

### click은 설정 파일 값에도 타입 검증을 적용한다

`typer`/`click` 옵션의 `exists=True`·`dir_okay=False`는 명령줄 인자만 검사하는 것이 아니라
`default_map`(= `cuesift.yaml`)이 채운 값에도 걸린다. **그래서 설정에서 온 값이 본문의 양보
로직보다 먼저 터지고, FR-8.4의 "명령줄이 설정을 이긴다"가 무력화된다.**
실제로 `input.media`에서 발생했고, 요구사항정의서 §8.2의 예시 YAML을 채우자 문서 게이트에서
발동했다 — **설정 예시를 실제로 실행하는 테스트가 이 부류를 잡는 유일한 게이트다.**

## 서브에이전트로 작업할 때

- **구현자가 도는 동안 작업트리는 내 것이 아니다.** 리포 파일을 편집하면 구현자가
  `git add`로 쓸어 담는다. 실제로 한 번 발생했다
- 브리프는 계획 문서에서 잘라낸 것이라 **앞선 태스크에서 배운 것을 모른다.**
  컨트롤러가 손으로 옮겨야 한다
- **리뷰어에게 구체적 위험을 지목하라.** "잘 리뷰해 달라"고만 하면 diff를 읽고
  "계획대로 구현됨"으로 승인한다. 다만 지목이 검증을 보장하지는 않으므로
  **검증 방법까지** 적는 편이 낫다
- 리뷰어의 지적이 옳아도 **해법은 틀릴 수 있다.** 이 프로젝트의 언어 조건(ko→en/ja)에서
  동작하는지 반드시 확인할 것 — 제안된 `\b` 단어 경계가 CJK를 전부 깨뜨려 폐기된 적이 있다

## 확정된 결정 (요구사항정의서 §12)

| # | 결정 |
| --- | --- |
| Q1 | 프로젝트명 `cuesift` |
| Q2 | 초기 언어쌍 **ko→en/ja** |
| Q3 | 로컬 LLM은 **OpenAI 호환 엔드포인트로 일원화.** 단 능력은 균일하지 않아 탐지·명시가 필요하다 |
| Q4 | **미해결** — 자가일관성 유사도 측정 수단. 벤치마크로 판정한다 |
| Q5 | 규격 프로파일 1차 출처는 **Netflix TTSG**, 벤치마크는 `specs/ted-*.yaml` 분리 |
| Q6 | LLM 연동은 **자체 얇은 어댑터** |

**이어지는 제약**: `logprobs`는 백엔드에 따라 조용히 사라진다(Ollama 미지원).
자가일관성 샘플링은 `n>1` 단일 호출이 아니라 **N회 개별 호출**이어야 이식성이 유지된다.
