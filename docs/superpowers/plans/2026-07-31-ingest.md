# 인제스트 구현 계획 (WP4)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 자막 파일(SRT·WebVTT·ASS/SSA)을 `Segment` 리스트로 바꾸는 `cuesift.ingest` 계층을 만든다.

**Architecture:** pysubs2 1.8.1에 포맷 판별과 태그 정규화를 위임하고, 이 모듈은 **경계 판단만** 한다 — 무엇을 거를지(주석·드로잉), 무엇을 실패로 볼지(0큐·역전 타임코드·디코딩 실패), 원본을 어떻게 보존할지(`subs` + `event_index`). 포맷별 분기는 없다.

**Tech Stack:** Python 3.11+ · pysubs2 1.8.1 · pytest · ruff

**설계 근거:** [2026-07-31-ingest-design.md](../specs/2026-07-31-ingest-design.md) — 절 번호는 전부 이 스펙을 가리킨다.

## Global Constraints

- **런타임 의존성 4개 고정** — `typer` · `pysubs2` · `pyyaml` · `httpx`. dev 3개 — `pytest` · `pytest-cov` · `ruff`. **추가 금지**
- **Python 실행은 반드시 `.venv/Scripts/python.exe`** — 시스템 Python은 3.14라 다르다
- 모든 모듈 첫 줄에 `from __future__ import annotations`
- 독스트링과 주석은 **한국어**, 근거 FR·§ 번호를 병기한다
- 주석에는 "왜 이 값인가"가 아니라 **"이 값이 아니면 무엇이 깨지는가"** 를 적는다
- ruff: `line-length = 100`, 규칙 `E,F,I,UP,B,SIM`
- 커밋 메시지는 **한국어**. **푸시는 하지 않는다** — 사용자가 명시적으로 요청할 때만
- **게이트 출력의 개수를 읽는다** — `pytest`의 수집 개수, markdownlint의 `Linting: N files`, 링크 체커의 `상대 링크 N개`. 0개 수집은 통과가 아니라 설정 오류다

### 착수 시점 기준값

| 게이트 | 현재 값 |
| --- | --- |
| `pytest -q` | **282 passed** |
| markdownlint | `Linting: 16 files` · 0 issues |
| `check_links.py` | 마크다운 16개 · 상대 링크 48개 · 깨진 링크 0 |

**모든 태스크 종료 시 이 값이 줄어들지 않았는지 확인한다.**

## File Structure

| 파일 | 책임 | 태스크 |
| --- | --- | --- |
| `src/cuesift/ingest/__init__.py` | 공개 API 재수출 | 2 |
| `src/cuesift/ingest/loader.py` | 전부 — 로드 · 필터 · 변환 · 오류 번역 | 2~6 |
| `tests/fixtures/ingest/*` | 픽스처 10개 | 1 |
| `tests/test_ingest_fixtures.py` | **픽스처가 심었다고 선언한 성질을 갖는지** 검증 | 1 |
| `tests/test_ingest.py` | 로더 동작·오류 계약 | 2~6 |
| `tests/test_ingest_contamination.py` | 오염 차단 음성 테스트 | 7 |
| `.gitattributes` | `*.ssa` 줄바꿈 변환 금지 추가 | 1 |
| `docs/WBS.md` · `CHANGELOG.md` | 진척 반영 | 8 |

`loader.py` 하나에 모으는 이유는 설계 §1.2다 — **포맷별 분기가 0개**라 쪼갤 축이 없다.

---

## Task 1: 픽스처와 픽스처 자체 검증

**Files:**

- Create: `tests/fixtures/ingest/` 아래 10개 (아래 목록)
- Create: `tests/test_ingest_fixtures.py`
- Modify: `.gitattributes`

**Interfaces:**

- Consumes: 없음
- Produces: `tests/fixtures/ingest/` 경로. 이후 모든 태스크가 이 디렉터리를 `FIXTURES`로 참조한다

**왜 픽스처를 먼저 검증하는가.** 픽스처는 검사 대상이 아니라 **검사 도구**다. 편집기나 git이 줄바꿈·인코딩을 바꾸면 이후 테스트가 **다른 이유로 통과**하고, 그 사실은 드러나지 않는다.

- [ ] **Step 1: `.gitattributes`에 `*.ssa` 추가**

`*.srt` · `*.vtt` · `*.ass`는 이미 `-text`로 보호돼 있다. `.ssa`만 빠져 있어 `* text=auto eol=lf`가 적용되고, 나중에 `.ssa` 픽스처를 만들면 CRLF가 조용히 LF로 바뀐다. FR-1.1이 ASS/SSA를 함께 요구하므로 지금 막는다.

```gitattributes
*.srt -text
*.vtt -text
*.ass -text
*.ssa -text
*.ttml -text
```

- [ ] **Step 2: 실패하는 픽스처 검증 테스트 작성**

`tests/test_ingest_fixtures.py`:

```python
"""픽스처가 심었다고 선언한 경계를 실제로 갖는지 검증한다.

픽스처는 검사 대상이 아니라 **검사 도구**다. 편집기나 git이 줄바꿈·인코딩을
바꾸면 이후 테스트가 **다른 이유로 통과**하고 그 사실은 드러나지 않는다.
`.gitattributes`의 `*.srt -text`가 실제로 작동하는지도 여기서 확인된다.
"""

from __future__ import annotations

from pathlib import Path

import pytest

FIXTURES = Path(__file__).parent / "fixtures" / "ingest"

EXPECTED = (
    "minimal.srt",
    "crlf_bom.srt",
    "overlap.vtt",
    "tags.ass",
    "empty_cue.srt",
    "reversed.srt",
    "all_comments.ass",
    "not_subtitle.txt",
    "cp949.srt",
    "multiline.vtt",
)


def test_all_fixtures_exist():
    """개수를 고정한다 — 파일이 사라져도 다른 테스트는 조용히 건너뛸 수 있다."""
    actual = sorted(p.name for p in FIXTURES.iterdir() if p.is_file())
    assert actual == sorted(EXPECTED)


def test_crlf_bom_fixture_keeps_crlf_and_bom():
    """git이 줄바꿈을 정규화하면 이 픽스처는 의미를 잃는다."""
    raw = (FIXTURES / "crlf_bom.srt").read_bytes()
    assert raw.startswith(b"\xef\xbb\xbf"), "BOM이 없다"
    assert b"\r\n" in raw, "CRLF가 LF로 정규화됐다"


def test_minimal_fixture_is_lf_only():
    """대조군 — 이 파일에 CRLF가 있으면 위 테스트가 무의미해진다."""
    assert b"\r\n" not in (FIXTURES / "minimal.srt").read_bytes()


def test_cp949_fixture_is_not_utf8():
    """utf-8로 읽히면 decode 오류 경로를 검증할 수 없다."""
    raw = (FIXTURES / "cp949.srt").read_bytes()
    with pytest.raises(UnicodeDecodeError):
        raw.decode("utf-8")
    raw.decode("cp949")  # 예외가 나지 않아야 한다


def test_tags_ass_has_styles_section():
    """[V4+ Styles]가 없으면 pysubs2가 FormatAutodetectionError를 낸다(설계 §12).

    그러면 이 픽스처는 태그 처리가 아니라 파싱 실패를 테스트하게 된다.
    """
    text = (FIXTURES / "tags.ass").read_text(encoding="utf-8")
    assert "[V4+ Styles]" in text
```

- [ ] **Step 3: 실행해서 실패 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest_fixtures.py -v
```

기대: 5건 모두 FAIL (`FileNotFoundError` — `tests/fixtures/ingest`가 없다)

- [ ] **Step 4: 평문 픽스처 7개 생성 (UTF-8 · LF)**

`tests/fixtures/ingest/minimal.srt`:

```text
1
00:00:01,000 --> 00:00:03,000
안녕하세요
두 번째 줄

2
00:00:03,500 --> 00:00:06,000
세 번째 큐
```

`tests/fixtures/ingest/overlap.vtt` — 겹침 1000ms를 심는다:

```text
WEBVTT

00:00:01.000 --> 00:00:04.000
첫 번째 큐

00:00:03.000 --> 00:00:05.000
겹치는 큐
```

`tests/fixtures/ingest/tags.ass` — 드로잉은 **69자·2초**여야 CPS를 넘는다(Task 7 참고):

```text
[Script Info]
ScriptType: v4.00+

[V4+ Styles]
Format: Name, Fontname, Fontsize
Style: Default,Arial,20

[Events]
Format: Layer, Start, End, Style, Name, MarginL, MarginR, MarginV, Effect, Text
Dialogue: 0,0:00:01.00,0:00:03.00,Default,,0,0,0,,{\an8}{\i1}기울임{\i0} 대사
Comment: 0,0:00:03.00,0:00:05.00,Default,,0,0,0,,화면에 안 나오는 줄
Dialogue: 0,0:00:05.00,0:00:07.00,Default,,0,0,0,,{\p1}m 0 0 l 100 0 100 100 0 100 b 20 20 40 40 60 60 l 80 20 90 90 10 80 c{\p0}
Dialogue: 0,0:00:07.00,0:00:09.00,Default,,0,0,0,,두 번째 대사
```

`tests/fixtures/ingest/empty_cue.srt`:

```text
1
00:00:01,000 --> 00:00:03,000
있는 텍스트

2
00:00:03,500 --> 00:00:05,000

```

`tests/fixtures/ingest/reversed.srt` — 역전은 **2번째 큐**에 심는다(1-based 보고 검증):

```text
1
00:00:01,000 --> 00:00:03,000
정상 큐

2
00:00:05,000 --> 00:00:04,000
역전된 큐
```

`tests/fixtures/ingest/all_comments.ass` — `empty`의 유일한 경로(설계 §5.4):

```text
[Script Info]
ScriptType: v4.00+

[V4+ Styles]
Format: Name, Fontname, Fontsize
Style: Default,Arial,20

[Events]
Format: Layer, Start, End, Style, Name, MarginL, MarginR, MarginV, Effect, Text
Comment: 0,0:00:01.00,0:00:03.00,Default,,0,0,0,,주석뿐인 파일
Comment: 0,0:00:03.00,0:00:05.00,Default,,0,0,0,,표시할 큐가 없다
```

`tests/fixtures/ingest/not_subtitle.txt` — `parse`를 낸다:

```text
이건 자막이 아니다
그냥 텍스트다
```

`tests/fixtures/ingest/multiline.vtt`:

```text
WEBVTT

00:00:01.000 --> 00:00:04.000
첫째 줄
둘째 줄
셋째 줄
```

- [ ] **Step 5: 특수 인코딩 픽스처 3개 생성**

편집기로는 BOM·CRLF·cp949를 안정적으로 만들 수 없다. 바이트로 쓴다.

```bash
.venv/Scripts/python.exe - <<'PY'
from pathlib import Path

d = Path("tests/fixtures/ingest")
body = "1\r\n00:00:01,000 --> 00:00:03,000\r\n안녕하세요\r\n두 번째 줄\r\n\r\n"

(d / "crlf_bom.srt").write_bytes(body.encode("utf-8-sig"))
(d / "cp949.srt").write_bytes(body.encode("cp949"))
print("crlf_bom.srt", (d / "crlf_bom.srt").stat().st_size, "bytes")
print("cp949.srt   ", (d / "cp949.srt").stat().st_size, "bytes")
PY
```

`minimal.srt`는 Step 4에서 LF로 만들었다. 만약 편집기가 CRLF로 저장했다면 아래로 되돌린다.

```bash
.venv/Scripts/python.exe -c "
from pathlib import Path
p = Path('tests/fixtures/ingest/minimal.srt')
p.write_bytes(p.read_bytes().replace(b'\r\n', b'\n'))
"
```

- [ ] **Step 6: 실행해서 통과 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest_fixtures.py -v
```

기대: **5 passed**

- [ ] **Step 7: 커밋**

```bash
git add tests/fixtures/ingest tests/test_ingest_fixtures.py .gitattributes
git commit -m "테스트: 인제스트 픽스처 10종과 픽스처 자체 검증"
```

---

## Task 2: 최소 로더 — 자막 파일이 Segment가 된다

**Files:**

- Create: `src/cuesift/ingest/__init__.py`
- Create: `src/cuesift/ingest/loader.py`
- Create: `tests/test_ingest.py`

**Interfaces:**

- Consumes: `cuesift.segment.Segment`
- Produces:
  - `load_subtitle(path: Path, *, source_lang: str = "ko") -> IngestResult`
  - `IngestResult` — 필드 `segments: list[Segment]` · `source_path: Path` · `format: str` · `source_lang: str` · `subs: pysubs2.SSAFile` · `event_index: dict[str, int]`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_ingest.py`:

```python
"""인제스트 (요구사항정의서 FR-1.1·1.3·1.5).

설계: docs/superpowers/specs/2026-07-31-ingest-design.md
"""

from __future__ import annotations

from pathlib import Path

from cuesift.ingest import load_subtitle

FIXTURES = Path(__file__).parent / "fixtures" / "ingest"


def test_srt_becomes_segments():
    result = load_subtitle(FIXTURES / "minimal.srt")

    assert result.format == "srt"
    assert result.source_path == FIXTURES / "minimal.srt"
    assert [s.id for s in result.segments] == ["00000", "00001"]
    assert [s.index for s in result.segments] == [0, 1]
    assert result.segments[0].start_ms == 1000
    assert result.segments[0].end_ms == 3000
    assert result.segments[0].source_text == "안녕하세요\n두 번째 줄"
    assert result.segments[1].source_text == "세 번째 큐"


def test_segments_leave_translation_fields_empty():
    """번역은 WP7이 채운다. 인제스트가 미리 채우면 미번역 신호가 눈이 먼다."""
    result = load_subtitle(FIXTURES / "minimal.srt")

    assert all(s.target_text is None for s in result.segments)
    assert all(s.speaker is None for s in result.segments)
    assert all(s.meta == {} for s in result.segments)


def test_source_lang_defaults_to_ko_and_is_overridable():
    """FR-1.5 — 값을 받아 기록만 한다. 우선순위 해결은 WP6."""
    assert load_subtitle(FIXTURES / "minimal.srt").source_lang == "ko"
    assert load_subtitle(FIXTURES / "minimal.srt", source_lang="ja").source_lang == "ja"


def test_crlf_and_bom_do_not_leak_into_text():
    """파이썬 텍스트 모드가 CRLF를 처리한다(설계 §12).

    `\\r`이 남으면 규격 검사가 `split("\\n")`할 때 줄 끝마다 1자씩
    더 세고, 마지막에 빈 줄이 하나 생겨 line_count가 +1 된다.
    """
    result = load_subtitle(FIXTURES / "crlf_bom.srt")

    assert result.segments[0].source_text == "안녕하세요\n두 번째 줄"
    assert "\r" not in result.segments[0].source_text
    assert "﻿" not in result.segments[0].source_text  # BOM은 눈에 안 보이므로 이스케이프로 쓴다


def test_vtt_multiline_keeps_newlines():
    """`\\N`이 `\\n`으로 정규화돼야 spec.check_text의 줄 분리가 맞는다."""
    result = load_subtitle(FIXTURES / "multiline.vtt")

    assert result.format == "vtt"
    assert result.segments[0].source_text == "첫째 줄\n둘째 줄\n셋째 줄"
```

- [ ] **Step 2: 실행해서 실패 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest.py -v
```

기대: 5건 모두 collection error (`ModuleNotFoundError: No module named 'cuesift.ingest'`)

- [ ] **Step 3: 구현**

`src/cuesift/ingest/loader.py`:

```python
"""자막 파일을 세그먼트 리스트로 만든다 (요구사항정의서 FR-1.1·1.3·1.5).

**이 모듈이 pysubs2를 아는 유일한 곳이다** (§7.2 모듈 경계).
외부 라이브러리의 표현이 여기서 멈추고 아래로는 순수한 `Segment`만 흐른다.
경계가 흐려지면 `spec`·`risk` 같은 순수 모듈의 테스트가 파일 I/O에 묶인다.
"""

from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path

import pysubs2

from cuesift.segment import Segment


@dataclass(frozen=True, slots=True)
class IngestResult:
    """인제스트 산출물 (설계 §2).

    `subs`와 `event_index`는 **WP5(FR-7.1) 라운드트립 전용**이다.
    FR-7.1이 "입력과 동일 포맷으로 출력"을 요구하는데 배치 태그(`{\\an8}`)와
    VTT cue settings는 `Segment`에 없다. 여기서 버리면 WP5가 원본을 다시
    파싱해야 하고, 그 순간 같은 파일의 두 표현이 갈릴 수 있다.
    """

    segments: list[Segment]
    source_path: Path
    format: str
    source_lang: str
    subs: pysubs2.SSAFile
    event_index: dict[str, int]


def load_subtitle(path: Path, *, source_lang: str = "ko") -> IngestResult:
    """자막 파일 하나를 읽어 `IngestResult`로 만든다 (FR-1.1).

    `source_lang`은 값을 받아 기록만 한다 (FR-1.5). CLI·설정 파일의
    우선순위 해결은 WP6의 몫이며 이 모듈은 둘 다 읽지 않는다.
    """
    subs = pysubs2.load(path, encoding="utf-8")
    segments, event_index = _to_segments(list(enumerate(subs)))
    return IngestResult(
        segments=segments,
        source_path=path,
        format=subs.format,
        source_lang=source_lang,
        subs=subs,
        event_index=event_index,
    )


def _to_segments(
    events: list[tuple[int, pysubs2.SSAEvent]],
) -> tuple[list[Segment], dict[str, int]]:
    """이벤트를 `Segment`로 바꾸고 원본 위치 대응표를 함께 만든다 (설계 §6).

    `index`는 **필터 후 0부터 연속 재부여**한다. 구멍이 있으면 리포트와
    정렬이 혼란스러워진다. 원본 위치는 `event_index`가 보존하므로
    라운드트립에 필요한 정보는 잃지 않는다.
    """
    segments: list[Segment] = []
    event_index: dict[str, int] = {}
    for index, (raw_index, event) in enumerate(events):
        seg_id = f"{index:05d}"
        segments.append(
            Segment(
                id=seg_id,
                index=index,
                start_ms=event.start,
                end_ms=event.end,
                source_text=event.plaintext,
            )
        )
        event_index[seg_id] = raw_index
    return segments, event_index
```

`src/cuesift/ingest/__init__.py`:

```python
"""자막 파일 인제스트 (요구사항정의서 FR-1.1·1.3·1.5)."""

from __future__ import annotations

from cuesift.ingest.loader import IngestResult, load_subtitle

__all__ = ["IngestResult", "load_subtitle"]
```

- [ ] **Step 4: 실행해서 통과 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest.py -v
.venv/Scripts/python.exe -m ruff check src tests
.venv/Scripts/python.exe -m ruff format --check src tests
```

기대: **5 passed** · ruff 통과

- [ ] **Step 5: 커밋**

```bash
git add src/cuesift/ingest tests/test_ingest.py
git commit -m "추가: 인제스트 최소 로더 (FR-1.1)"
```

---

## Task 3: 입력 종류 판별 — `not_found` · `video_input`

**Files:**

- Modify: `src/cuesift/ingest/loader.py`
- Modify: `src/cuesift/ingest/__init__.py`
- Modify: `tests/test_ingest.py`

**Interfaces:**

- Consumes: Task 2의 `load_subtitle`
- Produces: `IngestError(reason: str, message: str)` — `.reason` 속성이 계약이다

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/test_ingest.py` 끝에 덧붙인다. 상단 import도 함께 고친다.

```python
import pytest

from cuesift.ingest import IngestError, load_subtitle
```

```python
def test_missing_file_raises_not_found(tmp_path):
    with pytest.raises(IngestError) as exc:
        load_subtitle(tmp_path / "없는파일.srt")

    assert exc.value.reason == "not_found"


def test_video_input_raises_video_input(tmp_path):
    """FR-1.3 — 영상은 STT 경로이고 v0.1에 STT가 없다.

    확장자로 먼저 거르지 않으면 mp4가 텍스트로 열려 UnicodeDecodeError가 나고,
    사용자에게 "utf-8로 변환하라"는 **틀린 조언**이 간다.
    """
    video = tmp_path / "episode01.mp4"
    video.write_bytes(b"\x00\x00\x00\x20ftypisom")

    with pytest.raises(IngestError) as exc:
        load_subtitle(video)

    assert exc.value.reason == "video_input"


@pytest.mark.parametrize("suffix", [".mkv", ".mov", ".webm", ".m4v", ".avi", ".mp3", ".m4a", ".wav"])
def test_all_media_suffixes_are_rejected(tmp_path, suffix):
    media = tmp_path / f"episode01{suffix}"
    media.write_bytes(b"\x00\x01\x02")

    with pytest.raises(IngestError) as exc:
        load_subtitle(media)

    assert exc.value.reason == "video_input"
```

- [ ] **Step 2: 실행해서 실패 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest.py -v
```

기대: collection error (`ImportError: cannot import name 'IngestError'`)

- [ ] **Step 3: 구현**

`loader.py`의 import 아래에 추가한다.

```python
# 영상·오디오를 명시적으로 거른다 (FR-1.3, 설계 §7.2).
# 이 목록이 없으면 mp4가 텍스트로 열려 UnicodeDecodeError가 나고,
# 사용자에게 "utf-8로 변환하라"는 **틀린 조언**이 간다.
#
# 자막 확장자 화이트리스트는 두지 않는다 — pysubs2가 내용으로 판별하므로
# 확장자가 없거나 `.vtt`인데 SRT 내용이어도 제대로 읽는다(실측 설계 §12).
_MEDIA_SUFFIXES = frozenset(
    {".mp4", ".mkv", ".mov", ".webm", ".m4v", ".avi", ".mp3", ".m4a", ".wav"}
)


class IngestError(Exception):
    """인제스트 실패 (설계 §5).

    **`reason`이 계약이고 메시지는 사람용이다.** 테스트는 `reason`만 단언한다 —
    문구를 고정하면 메시지를 개선할 때 회귀 테스트가 함께 실패해 개선을 방해한다.
    """

    def __init__(self, reason: str, message: str) -> None:
        super().__init__(message)
        self.reason = reason
```

`load_subtitle` 첫 줄 앞에 판별을 넣는다.

```python
def load_subtitle(path: Path, *, source_lang: str = "ko") -> IngestResult:
    """(독스트링은 Task 2 그대로)"""
    _reject_non_subtitle(path)
    subs = pysubs2.load(path, encoding="utf-8")
    ...
```

```python
def _reject_non_subtitle(path: Path) -> None:
    """읽기 전에 걸러야 하는 입력 (FR-1.3).

    FR-1.3의 문구는 "자막과 영상이 모두 주어지면 자막 우선"이지만 v0.1의 CLI는
    입력을 하나만 받는다(설계 §7.1). 여기서는 **입력이 영상이면 자막 경로가
    아님을 알린다**로 구현하고, 진짜 "둘 다 주어짐"은 WP9에서 다시 본다.
    """
    if not path.is_file():
        raise IngestError("not_found", f"{path}: 파일이 없다")
    if path.suffix.lower() in _MEDIA_SUFFIXES:
        raise IngestError(
            "video_input",
            f"{path}: 영상·오디오 입력이다. STT는 v0.1에 없다(WBS WP9). "
            "FR-1.3에 따라 자막 파일이 있으면 그것을 넣는다.",
        )
```

`__init__.py`:

```python
from cuesift.ingest.loader import IngestError, IngestResult, load_subtitle

__all__ = ["IngestError", "IngestResult", "load_subtitle"]
```

- [ ] **Step 4: 실행해서 통과 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest.py -v
.venv/Scripts/python.exe -m ruff check src tests
```

기대: **15 passed** (기존 5 + 신규 2 + parametrize 8)

- [ ] **Step 5: 커밋**

```bash
git add src/cuesift/ingest tests/test_ingest.py
git commit -m "추가: 인제스트 입력 종류 판별 (FR-1.3)"
```

---

## Task 4: 로드 실패 번역 — `decode` · `parse`

**Files:**

- Modify: `src/cuesift/ingest/loader.py`
- Modify: `tests/test_ingest.py`

**Interfaces:**

- Consumes: Task 3의 `IngestError`
- Produces: `_load(path: Path) -> pysubs2.SSAFile` (내부 함수)

- [ ] **Step 1: 실패하는 테스트 추가**

```python
def test_cp949_file_raises_decode():
    """설계 §5.3 — utf-8 고정이다. `--encoding` 플래그가 없으므로
    설정할 수 없는 인자를 만들지 않는다. 진단 메시지가 변환을 안내한다.
    """
    with pytest.raises(IngestError) as exc:
        load_subtitle(FIXTURES / "cp949.srt")

    assert exc.value.reason == "decode"


def test_non_subtitle_text_raises_parse():
    """`load()`는 자동 판별을 하므로 FormatAutodetectionError를 던진다(설계 §12).

    초판 스펙은 이것을 "0큐"로 적었으나 그 측정은 `format_`을 강제한 것이었다.
    """
    with pytest.raises(IngestError) as exc:
        load_subtitle(FIXTURES / "not_subtitle.txt")

    assert exc.value.reason == "parse"
```

- [ ] **Step 2: 실행해서 실패 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest.py -k "decode or parse" -v
```

기대: 2건 FAIL — `UnicodeDecodeError`와 `FormatAutodetectionError`가 그대로 새어 나온다

- [ ] **Step 3: 구현**

import에 추가:

```python
from pysubs2.exceptions import Pysubs2Error
```

`load_subtitle`의 `pysubs2.load(...)` 호출을 `_load(path)`로 바꾸고 아래를 추가한다.

```python
def _load(path: Path) -> pysubs2.SSAFile:
    """파일을 읽어 pysubs2 표현으로 만들고, 실패를 `IngestError`로 번역한다.

    번역하지 않으면 호출자가 pysubs2 예외 계층을 알아야 하고,
    그 순간 §7.2의 "외부 의존을 인터페이스 뒤로 격리"가 무너진다.
    """
    try:
        return pysubs2.load(path, encoding="utf-8")
    except UnicodeDecodeError as exc:
        # UnicodeDecodeError는 ValueError의 하위다 — 아래 절보다 먼저 와야 한다.
        # 순서를 바꾸면 cp949 파일이 decode가 아니라 parse로 보고되고,
        # 사용자는 "인코딩을 바꿔라"라는 조언을 못 받는다.
        raise IngestError(
            "decode",
            f"{path}: utf-8로 읽을 수 없다 (바이트 {exc.start}). "
            "파일을 utf-8로 변환한 뒤 다시 시도한다.",
        ) from exc
    except (Pysubs2Error, ValueError) as exc:
        raise IngestError("parse", f"{path}: 자막으로 해석할 수 없다 — {exc}") from exc
```

- [ ] **Step 4: 실행해서 통과 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest.py -v
.venv/Scripts/python.exe -m ruff check src tests
```

기대: **17 passed**

- [ ] **Step 5: 커밋**

```bash
git add src/cuesift/ingest tests/test_ingest.py
git commit -m "추가: 인제스트 로드 실패를 IngestError로 번역 (decode·parse)"
```

---

## Task 5: 이벤트 필터와 `empty`

**Files:**

- Modify: `src/cuesift/ingest/loader.py`
- Modify: `tests/test_ingest.py`

**Interfaces:**

- Consumes: Task 4의 `_load`
- Produces: `_keep_displayed(subs) -> list[tuple[int, pysubs2.SSAEvent]]` (내부 함수)

- [ ] **Step 1: 실패하는 테스트 추가**

```python
def test_comments_and_drawings_are_excluded_from_segments():
    """설계 §4 — 화면에 나오지 않는 것은 세그먼트가 아니다."""
    result = load_subtitle(FIXTURES / "tags.ass")

    assert result.format == "ass"
    assert [s.source_text for s in result.segments] == ["기울임 대사", "두 번째 대사"]


def test_filtered_events_survive_in_subs_for_roundtrip():
    """설계 §2.1 — FR-7.1이 원본 포맷 출력을 요구하므로 원본은 온전해야 한다."""
    result = load_subtitle(FIXTURES / "tags.ass")

    assert len(result.subs) == 4
    assert any(e.is_comment for e in result.subs)
    assert any(e.is_drawing for e in result.subs)


def test_event_index_maps_to_the_right_original_event():
    """설계 §2.2 — 필터가 segments와 subs를 어긋나게 한다.

    이 대응표가 틀리면 WP5가 번역문을 한 칸씩 밀어서 되쓰고,
    **예외 없이 조용히** 밀린다.
    """
    result = load_subtitle(FIXTURES / "tags.ass")

    assert result.event_index == {"00000": 0, "00001": 3}
    for seg in result.segments:
        assert result.subs[result.event_index[seg.id]].plaintext == seg.source_text


def test_empty_text_cue_is_preserved():
    """설계 §4 — FR-3.2가 hard fail로 잡을 대상을 인제스트가 미리 지우지 않는다."""
    result = load_subtitle(FIXTURES / "empty_cue.srt")

    assert len(result.segments) == 2
    assert result.segments[1].source_text == ""


def test_all_comment_file_raises_empty():
    """설계 §5.4 — 파싱은 됐는데 표시할 큐가 0개인 유일한 실측 경로.

    CLAUDE.md의 "0개 수집은 통과가 아니라 설정 오류다"가 이 지점이다.
    """
    with pytest.raises(IngestError) as exc:
        load_subtitle(FIXTURES / "all_comments.ass")

    assert exc.value.reason == "empty"
```

- [ ] **Step 2: 실행해서 실패 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest.py -k "excluded or roundtrip or event_index or empty" -v
```

기대: 필터가 없으므로 `tags.ass`가 4개 세그먼트를 내고, `all_comments.ass`는 예외 없이 2개를 낸다 — 5건 중 4건 FAIL, `test_empty_text_cue_is_preserved`만 PASS

- [ ] **Step 3: 구현**

`load_subtitle` 본문을 바꾼다.

```python
def load_subtitle(path: Path, *, source_lang: str = "ko") -> IngestResult:
    """(독스트링은 Task 2 그대로)"""
    _reject_non_subtitle(path)
    subs = _load(path)
    events = _keep_displayed(subs)
    if not events:
        raise IngestError(
            "empty",
            f"{path}: 표시할 자막 큐가 0개다 (포맷 {subs.format}). "
            "0개 수집은 통과가 아니라 입력 오류다.",
        )
    segments, event_index = _to_segments(events)
    return IngestResult(
        segments=segments,
        source_path=path,
        format=subs.format,
        source_lang=source_lang,
        subs=subs,
        event_index=event_index,
    )
```

```python
def _keep_displayed(subs: pysubs2.SSAFile) -> list[tuple[int, pysubs2.SSAEvent]]:
    """화면에 나오는 이벤트만 남기고 원본 위치를 함께 돌려준다 (설계 §4).

    `is_comment`는 ASS의 `Comment:` 줄, `is_drawing`은 벡터 드로잉이다.
    드로잉을 남기면 `m 0 0 l 100 0`이 **자막 문자로 세어져** CPS를 부풀리고,
    그 오탐은 hard fail이라 FR-6.2에 따라 검수 예산을 우회한다 —
    실제 검수 비율이 부풀면 Recall@Budget 지표 자체가 무너진다.

    둘 다 SRT·VTT에서는 항상 False이므로(실측 §12) 포맷 분기 없이 적용한다.
    **텍스트가 빈 큐는 남긴다** — FR-3.2가 hard fail로 잡을 대상이다.
    """
    return [(i, e) for i, e in enumerate(subs) if not e.is_comment and not e.is_drawing]
```

`empty` 검사가 필터 **뒤에** 있어야 한다. 앞에 두면 전부 주석인 ASS 파일이 조용히 통과한다.

- [ ] **Step 4: 실행해서 통과 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest.py -v
.venv/Scripts/python.exe -m ruff check src tests
```

기대: **22 passed**

- [ ] **Step 5: 커밋**

```bash
git add src/cuesift/ingest tests/test_ingest.py
git commit -m "추가: 인제스트 이벤트 필터와 empty 검사 (설계 §4·§5.4)"
```

---

## Task 6: `bad_timecode`

**Files:**

- Modify: `src/cuesift/ingest/loader.py`
- Modify: `tests/test_ingest.py`

**Interfaces:**

- Consumes: Task 5의 `_to_segments`
- Produces: 변경 없음 (`_to_segments`가 `path`를 추가로 받는다)

- [ ] **Step 1: 실패하는 테스트 추가**

```python
def test_reversed_timecode_raises_bad_timecode_with_cue_number():
    """pysubs2는 역전 타임코드를 통과시킨다(설계 §12).

    `Segment.__post_init__`가 ValueError를 던지지만 그 메시지에는
    **몇 번째 큐인지가 없어** 사람이 파일에서 찾을 수 없다.
    1-based는 SRT 파일의 인덱스 표기와 맞춘 것이다.
    """
    with pytest.raises(IngestError) as exc:
        load_subtitle(FIXTURES / "reversed.srt")

    assert exc.value.reason == "bad_timecode"
    assert "2" in str(exc.value), "몇 번째 큐인지가 메시지에 없다"
```

`"2" in str(...)`는 문구가 아니라 **큐 번호가 들어 있다는 계약**을 고정한다.

- [ ] **Step 2: 실행해서 실패 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest.py -k bad_timecode -v
```

기대: FAIL — `ValueError: end_ms(4000)가 start_ms(5000)보다 작다`가 `IngestError`가 아니라 그대로 올라온다

- [ ] **Step 3: 구현**

`_to_segments` 시그니처에 `path`를 추가하고 루프 안에 검사를 넣는다.
`load_subtitle`의 호출도 `_to_segments(events, path)`로 바꾼다.

```python
def _to_segments(
    events: list[tuple[int, pysubs2.SSAEvent]], path: Path
) -> tuple[list[Segment], dict[str, int]]:
    """(독스트링은 Task 2 그대로 + 아래 문단 추가)

    역전 타임코드는 여기서 잡는다. `Segment`에 맡기면 `ValueError`가 나지만
    **몇 번째 큐인지가 메시지에 없어** 사람이 파일에서 찾을 수 없다.
    """
    segments: list[Segment] = []
    event_index: dict[str, int] = {}
    for index, (raw_index, event) in enumerate(events):
        if event.end < event.start:
            raise IngestError(
                "bad_timecode",
                f"{path}: {raw_index + 1}번째 큐의 타임코드가 역전됐다 "
                f"(start={event.start}ms > end={event.end}ms)",
            )
        seg_id = f"{index:05d}"
        segments.append(
            Segment(
                id=seg_id,
                index=index,
                start_ms=event.start,
                end_ms=event.end,
                source_text=event.plaintext,
            )
        )
        event_index[seg_id] = raw_index
    return segments, event_index
```

- [ ] **Step 4: 실행해서 통과 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest.py -v
.venv/Scripts/python.exe -m ruff check src tests
.venv/Scripts/python.exe -m ruff format --check src tests
```

기대: **23 passed**

- [ ] **Step 5: 커밋**

```bash
git add src/cuesift/ingest tests/test_ingest.py
git commit -m "추가: 역전 타임코드를 큐 번호와 함께 보고 (bad_timecode)"
```

---

## Task 7: 오염 차단 음성 테스트

**Files:**

- Create: `tests/test_ingest_contamination.py`

**Interfaces:**

- Consumes: `cuesift.ingest.load_subtitle` · `cuesift.spec.check_text` · `cuesift.spec.load_builtin`
- Produces: 없음 (검증 전용)

**왜 별도 파일인가.** 이 테스트는 인제스트의 동작이 아니라 **인제스트가 없을 때 무엇이 깨지는지**를 고정한다. 성격이 달라 섞으면 나중에 "왜 여기서 spec을 임포트하지" 하고 지워질 수 있다.

- [ ] **Step 1: 테스트 작성**

`tests/test_ingest_contamination.py`:

```python
"""필터가 없으면 무엇이 깨지는지 수치로 고정한다 (설계 §9.2 축 6).

이 프로젝트의 규율: **게이트를 만들면 반드시 실패시켜 봐야 한다.**
길이비 회귀 테스트가 버그 버전에서도 통과해 데이터를 다시 짠 전례가 있다.

여기서 고정하는 사실은 "필터가 있으니 안전하다"가 아니라
**"필터가 없으면 CPS가 실제로 넘친다"** 이다. 후자가 반증 가능하다.
"""

from __future__ import annotations

from pathlib import Path

from cuesift.ingest import load_subtitle
from cuesift.spec import check_text, load_builtin, text_width

FIXTURES = Path(__file__).parent / "fixtures" / "ingest"


def test_filtered_segments_have_no_spec_violations():
    """필터를 지난 세그먼트는 ko 프로파일에서 깨끗하다 — 대조군."""
    profile = load_builtin("ko")
    result = load_subtitle(FIXTURES / "tags.ass")

    for seg in result.segments:
        kinds = {v.kind for v in check_text(seg.source_text, seg.duration_ms, profile)}
        assert kinds == set(), f"{seg.id}에서 예상치 못한 위반: {kinds}"


def test_unfiltered_drawing_would_violate_cps_and_line_length():
    """드로잉을 세그먼트로 넣었다면 CPS와 줄 길이가 둘 다 터진다.

    실측값(2026-07-31, `ko` 프로파일 max_cps=12.0 · max_chars=16.0):
      텍스트 69자 → latin_half 폭 34.5 → 2000ms 기준 CPS 17.25

    **드로잉이 짧으면 이 테스트는 성립하지 않는다.** 27자짜리 드로잉은
    폭 13.5 · CPS 6.75로 한계를 넘지 못해 `tags.ass`의 드로잉을
    69자로 만들었다. 픽스처를 줄이면 여기가 먼저 깨진다.
    """
    profile = load_builtin("ko")
    result = load_subtitle(FIXTURES / "tags.ass")

    drawing = next(e for e in result.subs if e.is_drawing)
    width = text_width(drawing.plaintext, profile.char_counting)
    cps = width / (drawing.duration / 1000)

    assert drawing.duration == 2000
    assert width == 34.5
    assert cps > profile.max_cps

    kinds = {v.kind for v in check_text(drawing.plaintext, drawing.duration, profile)}
    assert "cps" in kinds
    assert "line_length" in kinds


def test_comment_line_would_be_spec_checked_if_kept():
    """주석 줄은 화면에 안 나오는데 필터가 없으면 규격 검사를 받는다."""
    result = load_subtitle(FIXTURES / "tags.ass")

    comment = next(e for e in result.subs if e.is_comment)
    assert comment.plaintext not in [s.source_text for s in result.segments]
```

- [ ] **Step 2: 실행해서 통과 확인**

```bash
.venv/Scripts/python.exe -m pytest tests/test_ingest_contamination.py -v
```

기대: **3 passed**

**만약 `test_unfiltered_drawing_would_violate_cps_and_line_length`가 실패하면** 픽스처의 드로잉이 짧거나 지속시간이 길다. 값을 확인한다.

```bash
.venv/Scripts/python.exe -c "
from pathlib import Path
from cuesift.ingest import load_subtitle
from cuesift.spec import load_builtin, text_width
p = load_builtin('ko')
r = load_subtitle(Path('tests/fixtures/ingest/tags.ass'))
d = next(e for e in r.subs if e.is_drawing)
w = text_width(d.plaintext, p.char_counting)
print(f'len={len(d.plaintext)} width={w} dur={d.duration} cps={w/(d.duration/1000):.2f} max={p.max_cps}')
"
```

- [ ] **Step 3: 필터를 꺼서 게이트가 실제로 실패하는지 확인**

**이 단계를 건너뛰면 위 테스트는 회귀 테스트가 아니다.** `_keep_displayed`를 임시로 무력화한다.

**백업 파일을 만들지 않는다.** 커밋된 상태가 있으므로 `git checkout`이 원복 수단이다 —
`.py.bak`을 만들면 단계가 중간에 끊겼을 때 추적되지 않는 파일이 남는다.

```bash
.venv/Scripts/python.exe -c "
import pathlib
p = pathlib.Path('src/cuesift/ingest/loader.py')
s = p.read_text(encoding='utf-8')
assert 'if not e.is_comment and not e.is_drawing' in s, '치환 대상을 못 찾았다'
p.write_text(s.replace('if not e.is_comment and not e.is_drawing', 'if True'), encoding='utf-8')
print('필터 무력화 완료')
"
.venv/Scripts/python.exe -m pytest tests/test_ingest_contamination.py tests/test_ingest.py -q
```

기대: **`test_filtered_segments_have_no_spec_violations`가 FAIL** (드로잉이 세그먼트로 들어와 cps·line_length 위반) + `test_comments_and_drawings_are_excluded_from_segments` 등도 FAIL

- [ ] **Step 4: 원복하고 다시 통과 확인**

```bash
git checkout -- src/cuesift/ingest/loader.py
.venv/Scripts/python.exe -m pytest -q
git status --short
```

기대: **전체 통과** · `git status`가 비어 있을 것(추적되지 않는 잔여 파일 없음)

- [ ] **Step 5: 커밋**

```bash
git add tests/test_ingest_contamination.py
git commit -m "테스트: 필터가 없을 때 CPS가 넘치는 것을 수치로 고정"
```

---

## Task 8: 문서 갱신

**Files:**

- Modify: `docs/WBS.md`
- Modify: `CHANGELOG.md`

**Interfaces:**

- Consumes: Task 1~7의 커밋 해시
- Produces: 없음

- [ ] **Step 1: 전체 게이트 실행하고 개수를 기록**

```bash
.venv/Scripts/python.exe -m pytest -q
.venv/Scripts/python.exe -m ruff check src tests
.venv/Scripts/python.exe -m ruff format --check src tests
.venv/Scripts/python.exe scripts/check_links.py
npx --yes markdownlint-cli2
```

착수 시점 대비: `pytest` **282 → 313** (Task 1의 5 + Task 2~6의 23 + Task 7의 3), 마크다운 **16 → 17**(이 계획서), 나머지는 유지.

**수치가 다르면 그대로 적는다.** 기대와 다른 것이 곧 정보다.

- [ ] **Step 2: `docs/WBS.md` 갱신**

세 곳을 고친다.

1. 작업 패키지 표의 WP4 행 — 상태 `⬜` → `✅`, 규모 `M` → `S`, 산출물에 실제 경로와 **근거 커밋 해시**를 적는다
2. "현재 위치" 진척 막대 — WP4를 채우고 완료 개수를 `16 → 19`(FR-1.1·1.3·1.5)로 고친다
3. Mermaid 노드 `WP3` — 아직 `🔨 진행 중`이고 style이 노란색이다. 표와 진척 막대는 이미 `✅`이므로 **도식만 뒤처져 있다.** `✅ 완료`와 초록 style로 맞추고, WP4도 같이 갱신한다

```mermaid
    WP3["WP3 융합·검출 정정<br/>✅ 완료"]
    WP4["WP4 인제스트<br/>✅ 완료"]
```

```mermaid
    style WP3 fill:#e6f4ea,stroke:#34a853
    style WP4 fill:#e6f4ea,stroke:#34a853
```

- [ ] **Step 3: `CHANGELOG.md`에 항목 추가**

Keep a Changelog 형식이다. `Added`에 인제스트 계층, `Fixed`에 WBS 도식 정정을 넣는다.

- [ ] **Step 4: 문서 게이트 재실행**

```bash
.venv/Scripts/python.exe scripts/check_links.py
npx --yes markdownlint-cli2
```

**두 게이트의 파일 개수를 대조한다.** 어긋나면 새 파일이 `git add`되지 않은 것이다 — 링크 체커는 `git ls-files` 기준이라 미추적 파일을 건너뛴다.

- [ ] **Step 5: 커밋**

```bash
git add docs/WBS.md CHANGELOG.md
git commit -m "문서: WP4 완료 반영과 WBS 도식의 WP3 상태 정정"
```

---

## Self-Review 결과

**1. 스펙 커버리지**

| 스펙 절 | 태스크 |
| --- | --- |
| §2 공개 API · `IngestResult` | 2 |
| §2.2 `event_index` | 5 |
| §3 데이터 흐름 ①~⑦ | 3 · 4 · 5 · 6 |
| §4 이벤트 필터 | 5 |
| §5 오류 계약 6종 | 3(`not_found`·`video_input`) · 4(`decode`·`parse`) · 5(`empty`) · 6(`bad_timecode`) |
| §6 Segment 매핑 | 2 · 6 |
| §7 입력 판별 | 3 |
| §8 언어 값 | 2 |
| §9.1 픽스처 10종 | 1 |
| §9.2 검증 축 1~6 | 2(축1·2) · 5(축3·4) · 3~6(축5) · 7(축6) |
| §10 남는 위험 | 문서에만 — 코드 변경 없음 |

**빠진 것 없음.**

**2. 플레이스홀더 스캔** — "적절히", "TBD", "비슷하게" 없음. 모든 코드 단계에 실제 코드가 있다.

**3. 타입 일관성**

| 이름 | 정의 | 사용 |
| --- | --- | --- |
| `load_subtitle` | Task 2 | 2~7 전부 동일 시그니처 |
| `IngestResult` | Task 2 | 필드 6개, 이후 변경 없음 |
| `IngestError(reason, message)` | Task 3 | 4·5·6에서 동일 |
| `_to_segments` | Task 2 | **Task 6에서 `path` 인자 추가** — 호출부도 함께 바꾸는 것을 Task 6 Step 3에 명시 |
| `_keep_displayed` | Task 5 | 5·7 |
| `_load` | Task 4 | 4·5 |

`_to_segments`의 시그니처 변경이 유일한 진화 지점이고, 해당 태스크에 호출부 수정을 명시했다.

## 남는 위험

| 위험 | 어느 태스크에서 드러나나 | 징후 |
| --- | --- | --- |
| `empty_cue.srt`의 빈 큐를 pysubs2가 큐로 안 셀 수 있다 | Task 5 | `len(result.segments) == 2`가 1로 실패 → 픽스처의 빈 줄 개수를 조정 |
| 편집기가 `tags.ass`의 백슬래시를 이스케이프할 수 있다 | Task 1 | `test_tags_ass_has_styles_section`은 통과하는데 Task 5에서 드로잉이 안 잡힘 → 파일을 바이트로 확인 |
| `subs.format`이 `None`일 수 있다 | Task 2 | `result.format == "srt"` 실패 → `pysubs2.load`가 포맷을 못 채운 경우이며 `parse`로 처리해야 한다 |
