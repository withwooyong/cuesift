# Changelog

All notable changes to this project are documented in this file.
Format follows [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/).

## [Unreleased]

### Added

- **Tier 0 신호 엔진** — 요구사항정의서 §4의 무료 결정론적 계층 전체. 순수 모듈 6개(`segment`·`spec`·`glossary`·`signals`·`risk`·`triage`)로 네트워크·LLM 의존이 0이다. 테스트 172건 (`845a9a9`..`cace3d4`)
- `segment/models.py` — `Segment`·`Signal`·`Span`·`SegmentRisk` (§7.3). 타임코드는 §8.4 `review.json` 계약에 맞춰 정수 밀리초를 쓴다. 네 모델 모두 생성 시점에 범위·정합성을 검증한다 (`198e90a`, `cace3d4`)
- `spec/counting.py` — 문자 폭 계산 3종 (FR-5.2). `grapheme`·`latin_half`·`fullwidth`로 언어마다 다른 "한 자"의 뜻을 표현한다 (`47de1a9`)
- `spec/profile.py` + `specs/*.yaml` 6종 — Netflix TTSG 기반 ko·en·ja와 TED 벤치마크용 ted-ko·ted-en·ted-ja. 수치와 출처 URL만 두고 가이드 본문은 복제하지 않는다 (`c79f752`)
- `spec/check.py` — 줄 길이·줄 수·CPS·노출시간·겹침 판정 (FR-5.1) (`1822d7b`)
- `glossary/` — 용어집 로드와 대응어 누락 판정 (FR-3.7). 룩어라운드 경계 매칭으로 짧은 약어의 부분 문자열 오탐을 막는다 (`426c268`, `0784e20`)
- `signals/base.py` — 수집기 프로토콜 2종(세그먼트 단위·배치)과 레지스트리 (FR-6.5, NFR-5). v0.2에서 QE 모델을 코드 수정 없이 꽂을 자리다 (`a49c3e5`)
- `signals/structural.py` — 구조 신호 5종 (FR-3.1~3.5): 미번역 잔존·빈 값·반복 붕괴·숫자 누락·태그 손실. 전부 hard fail이라 검수 예산을 우회한다 (`ef7c839`)
- `signals/derived.py` — 파생 신호 4종 (FR-3.6~3.8, FR-5.1): 규격 위반·용어집 위반·길이비 이상치·세그먼트 겹침. 모두 hard fail이 아니다 (`1864326`, `ad76a8c`)
- `risk/fuse.py` — 신호 융합 (FR-6.1, FR-6.2, FR-6.4). **noisy-or**(`1 - ∏(1 - sᵢ)^wᵢ`, 아래 Changed의 `78921d4` 참고)이며 hard fail은 그것을 우회한다. 기본 가중치는 **10종** 균등(무튜닝, WP8a가 `llm.self_consistency`를 더해 **9종에서** 늘었다 — 옛 이 줄이 "8종"이라 적었던 것은 `spec.overlap` 추가 때 갱신을 놓친 오기였다(`git show 48f9133:src/cuesift/risk/fuse.py` 확인, 최종 브랜치 리뷰 A3). 아래 "Tier 1 신호 라이브러리 계층" 항목 참고) (`501533e`)
- `triage/policy.py` — 검수 큐 선별 (FR-6.3). 예산·임계값 두 정책과 실제 검수 비율 산출. 전체 목록을 반환하고 선별된 것에 `selected` 플래그를 붙인다 (`86bdd6b`, `bfa0f72`)
- `CLAUDE.md` — 프로젝트 작업 규약. 코드를 읽어서는 알 수 없는 것들(문서 지도, 검증 습관, 확정된 결정)을 기록한다
- `scripts/check_links.py` — 마크다운 상대 링크가 실제 파일을 가리키는지 검사한다. 표준 라이브러리만 사용하며 대상은 `git ls-files` 기준(markdownlint의 `gitignore: true`와 동일 집합). 외부 URL과 앵커는 검사하지 않는다 — 외부 URL은 남의 서버 사정으로 CI가 간헐 실패하면 게이트가 무시되고, 앵커 생성 규칙은 렌더러마다 달라 한국어 제목에서 갈린다. 코드 펜스와 인라인 코드 스팬은 제외한다(링크 문법을 설명하는 문장이 오탐되지 않도록)
- CI `docs` 잡에 링크 검사 스텝 추가 — 검사 대상 0개면 통과가 아니라 실패로 처리하고, 깨진 링크는 `::error file=…,line=…::` 애노테이션으로 표시한다
- **TED2020 벤치마크 하네스** (벤치마크 하네스 설계 스펙 §3~§10) — `bench/` 패키지 9개 모듈. 코퍼스 획득(`scripts/fetch_ted2020.py`)·필터링(`corpus.py`)·타임코드 합성(`timing.py`)·트랙 빌드(`build_track.py`)·직렬화(`track_io.py`)·오류 주입 7종(`inject.py`)·Recall@Budget 측정과 불변식 4종(`measure.py`)·리포트 생성(`report.py`)·전체 오케스트레이션(`run.py`). LLM 의존 없이 결정론적으로 실행하며 테스트 259건 (`dd2826c`..`dfa12ca`)
- `bench/run.py` — `python -m bench.run --pair {en-ko,ja-ko}`. Tier 0 신호 9종의 **첫 Recall@Budget 실측치**를 낸다 — 실제 검수 비율 10%에서 무작위 대비 **en-ko 6.96x · ja-ko 7.06x** (오류 포착률 69.6%·70.6%). hard fail 자연 오탐률은 en-ko 0.96% / ja-ko 0.91%로 목표(§9.1 "≈ 0")에 부합
- `bench/results/{en-ko,ja-ko}-2026-07-29.{md,json}` — 재현 정보(코퍼스 SHA-256·시드·커밋)·예산 스윕(1~30%)·유형별 Recall·오라클 대비 달성률·신호별 ablation을 담은 실측 리포트. 결과는 숫자라 리포에 커밋한다(가공 코퍼스와 달리 저작권 문제가 없다)
- `bench/report.py`에 "오라클 대비 달성률" 절 추가 — 예산이 낮을수록(hard fail 위주) 오라클 대비 달성률이 높고 예산 10% 부근(가중평균 순 채움 구간)에서 저점을 찍는 **U자 곡선**을 표로 남긴다. 계획 A 재리뷰가 남긴 미결 항목("가중평균 vs noisy-or 융합")에 대한 정량 근거다
- `bench/report.py`에 "`negation` 무작위 기준선과의 비교" 절 추가, `bench/run.py`가 유형별 무작위 기준선을 계산해 넘긴다(기존 `random_baseline`을 negation 라벨로만 좁힌 `error_ids`로 재사용). **Tier 0는 의미 반전(`negation`)에서 무작위보다 못하다** — 예산 10%에서 1.41% vs 무작위 9.61%(en-ko), 예산 20%에서도 9.86%/11.27% vs 무작위 19.49%/19.85%(en-ko/ja-ko, 100시드 재측정). Tier 0가 다른 오류를 상위로 올리며 문법적으로 완벽한 문장(의미만 뒤집힌 것)을 오히려 큐에서 밀어내기 때문이다
- `bench/build_track.py` — 트랙과 나란히 **사이드카** `{pair}.clean.stats.json`을 쓴다(스펙 §4.4 부수 산출물). `_corpus_stats()`가 콘솔 출력과 같은 공식으로 계산해 두 출처가 갈라지지 않는다. **실측: en-ko 필터 후 389,598쌍 중 194,463쌍(49.91%)이, ja-ko는 필터 후 350,634쌍 중 168,613쌍(48.09%)이 TED 규격(21자×2줄)에 물리적으로 담기지 않아 트랙에서 빠졌다** — FR-5.4(v0.2 규격 자동 교정)를 정량적으로 정당화하는 숫자다
- `bench/report.py`에 "코퍼스 제외 (스펙 §4.4)" 절 추가 — `RunMeta.corpus_stats`(사이드카에서 읽은 값)가 있을 때만 렌더링하고, 비율은 리포트가 다시 계산하지 않는다
- **주입 감사 산출물** (스펙 §5.7) — `bench/track_io.py`의 `dump_audit()`이 변조 트랙(`data/bench/{pair}.injected.json`)과 정답 라벨(`{pair}.labels.json`)을 **한 번의 호출로 함께** 쓴다. 둘을 각각 쓰는 함수 두 개로 두지 않은 이유는 짝이 아닌 산출물이 감사에 쓸 수 없기 때문이다 — 함수가 둘이면 한쪽만 호출하는 실수가 구조적으로 가능하다. 변조 트랙은 `dump_track`과 같은 형식이라 `load_track`으로 그대로 읽힌다(감사자가 새 파서를 짜야 한다면 감사 수단이 아니다). 라벨 파일에는 시드·커밋과 **변조 트랙의 SHA-256**이 함께 기록되고 `load_labels()`가 이를 대조한다 — 해시를 적기만 하고 아무도 대조하지 않으면 없는 게이트와 같다. `bench.run`은 **측정 전에** 이 산출물을 남긴다(불변식에서 실패한 실행도 무엇을 주입했는지는 남아야 한다). `--audit-dir`로 위치를 바꿀 수 있고 기본값은 트랙과 같은 디렉터리다. **실행 검증**: en-ko·ja-ko 양쪽에서 깨끗한 트랙과 변조 트랙의 차집합이 라벨 500건과 양방향으로 완전히 일치했고, Recall 값은 기존 리포트와 동일했다(산출물 추가가 측정을 바꾸지 않았다)
- `README.md`에 **"재현"** 절 추가 — 실측 절이 배수 7배를 인용하면서 독자가 재현할 명령이 없었다. 3단계 파이프라인(`scripts.fetch_ted2020` → `bench.build_track` → `bench.run`)을 Mermaid 흐름도·표·실행 명령으로 싣고, `python -m`으로 실행해야 하는 이유(`sys.path`에서 리포 루트가 빠진다)와 용어집 회귀 게이트 `scripts/glossary_verify.py`를 함께 문서화했다. 적은 명령 4개는 전부 실제로 실행해 동작을 확인했다
- **설계 스펙 — noisy-or 융합과 숫자 NFKC 정규화** (`411b14d`). `docs/superpowers/specs/2026-07-29-noisy-or-fusion-nfkc-design.md`. HANDOFF의 미결 항목 둘(융합 방식·전각 숫자 미탐)을 한 스펙으로 묶는다 — 성격은 다르지만 둘 다 재측정을 요구하므로 따로 처리하면 벤치를 두 번 돌린다. **noisy-or 채택을 재측정 결과에 걸지 않기로 확정했다**: 벤치 숫자를 보고 융합식을 고르면 그것이 벤치 데이터로 제품을 튜닝하는 것이고, 재측정의 목적은 판정이 아니라 기록이다(Recall이 내려가도 그 숫자를 싣는다). 가중치는 지수로 넣는다(`1 - ∏(1-s)^w`) — 점수 스케일 가중은 `w·s > 1`에서 곱의 부호가 뒤집혀 `w ≤ 1` clamp를 강제하고 그러면 신호 강화를 표현할 수 없다. NFKC는 추출된 숫자 문자열에만 적용한다 — 텍스트 전체를 정규화하면 `½` → `1⁄2`로 없던 숫자가 생기고 재측정 결과의 원인 귀속이 흐려진다
- **작업 분해 구조(WBS)** (`a3d234a`). `docs/WBS.md`. 전체 진척이 보이지 않는다는 지적에 대한 대응 — 단위 작업은 따라갈 수 있는데 "얼마나 더 하면 끝나는가"를 아무도 답할 수 없었다. 작업 패키지를 FR 번호에 대응시킨 이유는 **진척 주장을 반증 가능하게** 만들기 위해서다("인제스트 60% 완료"는 반박할 수 없지만 "FR-1.1 완료"는 코드와 테스트로 확인된다). v0.1 대상 FR 42개 중 16개 완료(38%), WP 9개. 의존 그래프가 두 가지를 드러냈다 — **WP4(인제스트)가 병목이고**(출력·번역·STT 셋이 걸린다), **`check` 서브커맨드는 번역·STT 없이 완결된다**(인제스트 + FR-8.2 배선만으로 쓸 수 있는 제품이 나오는 유일한 지점). 갱신 규칙에 "상태 칸을 고칠 때 근거 커밋을 함께 적는다"를 넣었다 — 근거 없는 상태 표시는 검사하지 않고 통과하는 게이트와 같다
- **구현 계획 — noisy-or·NFKC** (`c746098`). `docs/superpowers/plans/2026-07-29-noisy-or-fusion-nfkc.md`. 태스크 6개(0~5), 코드 변경은 두 파일 약 30줄이고 나머지는 측정과 기록이다. 계획을 쓰며 **스펙 결함 하나와 누락 하나**를 찾았다 — 스펙 §5가 "`w=0` 테스트는 현재 코드에서 FAIL"이라 적었으나 실제는 PASS이고(가중 평균에서 `w=0`인 신호는 분자·분모에 각각 0을 더해 평균에서 제외되며 noisy-or에서도 `(1-s)^0 = 1`로 곱에서 제외된다), 스펙이 신규 테스트만 세고 **기존 테스트 2건이 가중 평균을 값으로 고정하고 있다**는 것을 놓쳤다. 신규·교체 테스트 5건의 현재 코드 반환값을 실제로 실행해 확인하고 표로 실었다 — 추측한 숫자를 계획에 남기면 구현자가 그것을 신뢰한다. 이 과정에서 `HANDOFF.md`의 테스트 총계 271이 낡았다는 것(실제 277)도 드러났다
- **인제스트 계층** (FR-1.1·1.3·1.5, WBS WP4). `src/cuesift/ingest/{__init__,loader}.py` — 최소 로더. pysubs2로 SRT·WebVTT·ASS/SSA를 공통 `IngestResult`(`Segment` 목록·포맷·언어 등 필드 6개)로 통일한다. **포맷별 분기가 0개다** — pysubs2가 포맷 판별과 태그 정규화를 전부 하기 때문이며, 이 사실 하나로 WBS 규모 추정이 M에서 S로 내려갔다(설계 §1.2). 로드 실패를 오류 계약 6종(`not_found`·`video_input`·`decode`·`parse`·`empty`·`bad_timecode`)의 `IngestError`로 번역하고, 이벤트 필터(설계 §4·§5.4)로 화면에 표시되지 않는 큐를 걸러 CPS 과다 계산을 막으며, 역전 타임코드는 큐 번호와 함께 보고한다. `tests/fixtures/ingest/`에 픽스처 12종을 두고 픽스처 자체를 검증하는 테스트를 별도로 뒀다(§9.1) — 픽스처가 틀리면 그 위의 모든 테스트가 틀린 것을 옳다고 확인하기 때문이다. 테스트 34건 신규, 전체 282 → 316 (`42a126f`·`bb105b7`·`be51706`·`52acf5b`·`d51cb3c`·`1cbc21a`·`26f7517`·`6ad9366`·`50e62cf`)
- **인제스트 회귀선 3종** (`50e62cf`). 최종 전체 브랜치 리뷰가 태스크별 리뷰 8회가 모두 놓친 빈 칸을 찾아 메웠다. ① `basic.ssa` — FR-1.1이 이름을 걸어 요구한 SSA가 픽스처도 단언도 없이 WBS에서 ✅ 처리돼 있었다(`.gitattributes`의 `*.ssa -text`는 존재하지 않는 픽스처를 위한 죽은 설정이었다). 동작은 정상이었으므로 **깨진 게 아니라 무증거**였고, 이 리포가 스스로 금지한 "검사하지 않고 통과하는 게이트"에 해당한다. ② `comment_then_reversed.ass` — `bad_timecode`의 큐 번호가 `raw_index`(원본 위치)인지 `index`(필터 후 순번)인지 기존 픽스처로는 **구분되지 않았다**(`reversed.srt`에 주석이 없어 두 값이 같다). 주석을 앞에 두어 두 값을 갈라 놓고, `raw_index + 1`을 `index + 1`로 일시 개악해 새 테스트만 실패하는 것을 확인했다. ③ `overlap.vtt`를 `spec.check_overlaps`에 연결 — 픽스처는 있었으나 **로드하는 테스트가 하나도 없었다.** 겹침이 있는 실제 트랙이 처음으로 파이프라인에 들어오며, `spec.overlap`의 기여도 `+0.0%`가 "쓸모없다"가 아니라 "측정하지 못했다"였던 상태가 풀릴 준비가 됐다
- **설계 스펙 — `check` 배선** (`9ef4869`, FR-8.2·FR-7.5, WBS WP6 부분). `docs/superpowers/specs/2026-08-03-check-cli-design.md`. 핵심 결정은 **`check`가 신호 엔진(`collect_all`→`fuse`→`triage`)을 통과하지 않는다**는 것이다 — `SpecViolationSignal`·`OverlapSignal`은 `spec/check.py`의 얇은 래퍼이고 신호 계층이 얹는 넷(점수화·`hard_fail`·융합·트리아지)을 `check`가 하나도 쓰지 않는다(심각도 단일 등급, 예산 없음, 순위 없음). 직접 호출은 HANDOFF가 경고한 함정을 **회피가 아니라 소멸**시킨다 — `struct.*` 수집기가 아예 실행되지 않는다. 실측이 HANDOFF 기록을 두 군데 정정했다: ① 함정이 `struct.empty` 하나가 아니라 원문에 숫자·태그가 있으면 `struct.number_missing`·`struct.tag_lost`도 함께 hard fail한다(이전 세션 픽스처에 숫자·태그가 없어 안 보였다), ② HANDOFF가 제안한 미러링 대응은 `struct.degeneration`이 "네 네 네" 같은 정상 한국어 반복에서 hard fail을 내므로 안전하지 않다. **심각도는 단일 등급으로 두고 등급을 발명하지 않았다** — 프로파일 수치를 재 보니 `ja`↔`ted-ja`가 CPS 2.50배·줄폭 1.62배로 두 축 모두 흔들려 등급 배정의 출처가 없고, 이는 §11 R8("출처 없는 수치를 기본값으로 넣지 않음")에 걸린다. 빈 큐가 **현재 어느 경로로도 탐지되지 않는 사각지대**임을 확인하고 `check_empty_cues`로 닫기로 했다(`check_text` 안에 넣으면 `translate` 경로에서 `struct.empty`와 이중 계산된다). 종료 코드는 `2`(호출이 틀림)와 `66`(파일이 틀림)을 분리한다

- **`cuesift check`가 실제로 동작한다** (FR-8.2·FR-7.5, WBS WP6 부분 · WP5 부분). `cuesift check <자막파일> --spec <프로파일>`이 규격 위반 6종과 빈 큐를 판정하고 **종료 코드 0·1·2·66을 구분해 낸다.** 이것이 WBS가 지목한 "가장 짧은 쓸 수 있는 제품" 경로다 — 번역(WP7)도 STT(WP9)도 없이 완결되는 유일한 지점이고, **v0.1 전체를 기다리지 않고 나오는 첫 중간 산출물**이다. `spec/check.py`에 `TrackViolation`·`check_track()`이, `cli.py`에 `check()` 본문·`_resolve_profile()`·`_format_report()`가 들어왔다. 테스트 316 → **481**건 (`90c6a7d`·`c376ad0`·`a580a66`·`5c5dd5a`·`95e182a`·`3330773`·`f16bb0f`·`1be2c84`·`8845d5a`·`70f5aef`·`800d17c`·`ca29ef5`·`7d3e5f6`·`4899fec`·`59c7b51`·`955b4cd`·`6b45f95`·`c61a067`·`819b861`)
- **빈 큐 판정 `check_empty_cues`** (`a580a66`). 설계 §12.3이 실측한 **사각지대**를 닫는다 — `empty_cue.srt`를 세 구성(그대로·미러링·`check_text` 직접 호출)으로 돌려 **셋 다 놓치는 것**을 확인한 뒤 추가했다. `struct.empty`는 `source_text`가 비면 `None`을 반환하고 `check_text`는 빈 텍스트의 줄·CPS 검사를 건너뛴다. 배포 자막에 텍스트 없는 큐가 남는 것은 명백한 결함인데 어느 경로로도 잡히지 않고 있었다. **`check_text` 안에 넣지 않은 것이 설계다**(D6) — 넣으면 `translate` 경로에서 `struct.empty`와 **이중 계산**되고, 그 부풀림은 `spec.violation` 점수를 통해 위험도로 흘러 검수 비율을 밀어 올린다. 검수 비율은 §9.1 배수의 **분모**다
- **`check`는 신호 엔진을 통과하지 않는다** (설계 D3, `4899fec`). `collect_all`→`fuse`→`triage`가 얹는 넷(점수화·`hard_fail`·융합·트리아지)을 `check`가 **하나도 쓰지 않는다** — 심각도가 단일 등급이고 예산도 순위도 없다. 직접 호출은 HANDOFF가 경고한 함정을 **회피가 아니라 소멸**시킨다: `struct.*` 수집기가 아예 실행되지 않아 `target_text`가 전 구간 `None`으로 남는다. 규격 판정의 원천은 `spec/check.py` 하나이고 `translate` 경로와 `check`가 양쪽 다 그것을 쓰므로 두 경로가 갈라지지 않는다
- **`--limit N`과 요약 이중 출력** (FR-8.2 표면 확장). 위반 목록을 N건까지만 출력한다. **기본값 `0`은 무제한**이라 지정하지 않으면 기존과 동일하다 — 상한을 기본으로 켜면 전체 목록을 파이프로 받아 grep하던 쓰임이 조용히 잘린다. 함께 **요약 줄을 헤더 바로 아래와 맨 끝 양쪽**에 낸다: 이전 판은 맨 아래 한 줄이라, 위반 682건이면 686줄이 나가고 26화 × 3언어 매트릭스에서 프로파일을 잘못 물리면 약 5만 줄이 쌓이는데 **로그를 앞에서 남기고 뒤를 자르는 CI에서 가장 중요한 한 줄이 가장 먼저** 사라졌다. 셋을 계약으로 못 박았다 — ① **종료 코드는 `--limit`에 영향받지 않는다**(판정의 결과이지 출력의 결과가 아니다. 3건만 보여준다고 위반이 3건인 것은 아니다), ② **요약은 언제나 전체 기준**이다(자른 뒤에 세면 `--limit 3`이 "위반 3건"이라는, 종료 코드와 모순되지 않아 사용자가 검증할 수 없는 거짓말을 낸다), ③ 절단은 `_format_report` **안**에서 한다(호출부에서 `lines[:N]`으로 자르면 요약 줄까지 함께 잘려 목적이 정확히 무너진다). 생략 고지 `... N건 생략 (전체는 --limit 0)`은 상한이 위반 수보다 클 때는 내지 않는다 — `0건 생략`은 그 자체로 거짓말이다. 음수 인자는 typer `min=0`이 본문에 닿기 전에 종료 코드 2로 거른다. `_format_report`가 순수 함수인 것이 여기서 배당금을 냈다(설계 §7.4) — 절단·요약 배치 계약 넷을 `CliRunner` 없이 문자열 입출력으로 고정했다

- **번역 엔진 계층 `cuesift.translate`** (FR-2.1~2.6·2.8, WBS WP7a). 배치 번역·개별 폴백·프로바이더 추상화·재시도 정책. 모듈 5개(`provider`·`batch`·`prompt`·`engine`·`openai_compat`)와 `Glossary.terms_in`이 들어왔고 테스트 499 → **813**건 (`10d3b31`·`8f0ea4a`·`cdfbcf5`·`751f72a`·`2a0abe9`·`f6e0ec6`·`76c63dd`·`c52b93f`·`0ee7e76`·`f191afe`·`ae6f891`·`c337cb2`·`abf3235`·`3c23c0d`·`684e1ef`·`1b4ea6e`). 설계의 축은 **"실패를 두 갈래로 가르는 것"** 하나다(설계 §4.2) — `RetryableProviderError`(**408**·429·5xx·타임아웃·연결 끊김)와 `FatalProviderError`(**3xx 리다이렉트**·400·401·403·404·그 밖의 4xx)를 나누지 않으면 API 키 오타 하나가 **세그먼트 800건의 실패 리포트**로 둔갑해 사용자가 원인을 못 찾는다. 이 축은 `check`가 exit 2("명령줄이 틀림")와 exit 66("파일 내용이 틀림")을 가른 것과 같은 것이다. **재개(FR-2.7)와 캐시(NFR-3)는 일부러 넣지 않았다** — WP7b가 감싼다. 넣었다면 엔진의 모든 테스트가 임시 디렉터리를 들고 다녀야 했다
- **FR-2.1을 "한 호출에 여러 언어"가 아니라 "한 실행에 여러 언어"로 읽는다** (설계 §3.1). `translate_segments`는 대상 언어를 **하나만** 받고 복수 언어는 호출자가 루프를 돈다. `Segment.target_text`가 단수이고 `Glossary`와 spec이 한 언어 계약이라, 다르게 읽으면 세 모듈을 동시에 깨야 한다. 실패한 세그먼트의 `target_text`에는 `None`을 **넣는다** — 들어온 값을 그대로 두면 en으로 채운 세그먼트를 ja로 다시 넣었을 때 실패분에 영어가 남고 그것이 ja 결과로 보고된다
- **배치 검증 실패가 개별 폴백의 방아쇠다** (FR-2.4, `f191afe`·`c337cb2`). `parse_translations`가 개수·번호를 검사해 어긋나면 `InvalidResponseError`를 던지고, 엔진이 그 배치만 세그먼트 단위 호출로 강등한다. **다만 재시도 소진 후에는 폴백하지 않는다** — 폴백은 "모델이 지시를 어김"의 처방이지 "서버가 죽음"의 처방이 아니라, 네트워크가 끊긴 상태에서 강등하면 실패 1회가 실패 10회로 는다. 검증의 한계도 못 박았다: 개수와 번호가 맞은 채 **내용만 한 칸 밀린 것**은 구조 검증이 잡지 못하고 Tier 0 길이비 신호의 몫이다(설계 §7.2). **FR-2.4는 translate 혼자 지키는 요구사항이 아니다**
- **`SegmentFailure.reason` 3종이 서로 다른 처방으로 이어진다** (FR-2.6). `provider_error`는 다시 돌리면 되고, `invalid_response`는 모델을 바꿔야 하고, `empty_translation`은 그 자막이 원래 번역할 것이 없었을 수 있다. 뭉뚱그리면 "실패 800건"에서 무엇을 해야 할지 알 수 없다. `attempts`를 함께 남겨 한 번 만에 죽었는지 네 번 버텼는지가 리포트에서 사라지지 않게 했다
- **`OpenAICompatibleProvider` — OpenAI 호환 엔드포인트 어댑터** (FR-2.5, §12 Q3, `3c23c0d`·`1b4ea6e`). 번역 계층의 **유일한 I/O**다. `logprobs`도 `n`도 `response_format`도 보내지 않는다 — Ollama가 앞의 둘을 지원하지 않고 `response_format`은 서버마다 조용히 무시되거나 400을 내므로, 프롬프트로 JSON을 요구하고 파싱 실패를 정상 경로(개별 폴백)로 다루는 쪽이 이식성이 높다. **`ProviderError` 밖의 예외가 어떤 경로로도 나가면 안 된다**는 것이 이 모듈의 계약이다 — 엔진의 폴백은 `except RetryableProviderError` / `except FatalProviderError` 둘뿐이라 `TypeError`·`ValueError`가 새면 폴백을 통째로 우회해 파이프라인이 트레이스백으로 죽는다. 실측으로 닫은 누수: `httpx.DecodingError`는 `TransportError`의 자손이 **아니라서**(httpx 0.28.1) `except httpx.TransportError`를 지나쳤고, gzip 본문이 깨진 프록시에서 실제로 도달한다
- **설정 오류는 `ProviderError`가 아니라 맨 `ValueError`다.** **여섯 자리**다 — `base_url`이 URL로 읽히지 않음(`http://[::1`, `httpx.InvalidURL`을 감싼다)·스킴 누락·호스트 누락·쿼리나 프래그먼트 포함, `api_key`의 비-ASCII, `timeout`+`client` 동시 지정. **이 목록은 `cuesift/translate/__init__.py` 독스트링과 두 곳에 있고 초판은 둘 다 `InvalidURL` 하나를 빠뜨렸다** — 복제된 목록이 갈라지는 실례이며, 지금은 `test_설정_오류는_ProviderError로_잡히지_않는다`가 여섯 자리를 모두 돈다. **`ProviderError`는 "프로바이더 호출 실패의 최상위"인데 이것들은 호출이 아니라 설정이다** — 재시도해도 소용없고 세그먼트 단위로 강등할 대상도 아니다. WP7b가 CLI를 배선할 때 `except ProviderError`만 달면 `base_url` 오타가 트레이스백으로 나가므로, 이 구분을 `cuesift.translate` 독스트링과 `tests/test_translate_api.py`에 **양쪽 다** 못 박았다
- **공개 API `cuesift.translate.__all__` 21개와 `live` 마커** (`tests/test_translate_api.py`·`tests/test_translate_live.py`, 테스트 813 → **859**). 공개 기준을 "호출자가 지금 쓰는 것"이 아니라 **"하위 모듈의 밑줄 없는 최상위 이름 전부"** 로 잡았다 — 전자는 검사할 수 없어 새 심볼이 조용히 빠지고, 그러면 호출자가 하위 모듈 경로를 직접 파고들어 파사드가 무의미해진다. ast로 정의문만 훑어 이 규약을 강제한다(감추려면 `_` 접두사). 실 API 테스트는 `-m live` opt-in이고 `CUESIFT_LIVE_BASE_URL`로 **로컬 Ollama도 겨눌 수 있다**(§12 Q3) — 프론티어 모델은 배치 지시를 거의 어기지 않아 폴백 경로가 실사용에서 한 번도 실행되지 않은 채 "통과"되기 때문이다(설계 §9.3)

- **계층 경계의 계약 4건을 문서로 못 박았다** (최종 브랜치 리뷰). `translate/` 안에서는 전부 옳은데 **경계 밖에서만 보이는** 공백이었다 — 한 문장으로 "번역 계층의 산출물이 다음 계층에서 무엇이 되는지가 어디에도 적혀 있지 않았다". ① **`failures`의 세그먼트를 triage에 넣으면 안 된다** — 실패분은 `target_text=None`이고 `struct.empty`가 그것을 `hard_fail=True`로 판정하는데, hard fail은 검수 예산을 우회해 quota를 소진한다. 실측(200큐·진짜 오류 20건·예산 10%): 번역 실패 20건에서 **Recall이 100% → 0%**, 30건에서는 실제 검수 비율이 15%로 부풀어 **§9.1 배수의 분모까지 오염된다.** 오염이 오류가 아니라 **"번역이 실패했다"는 사실 자체**에서 온다 — 번역 안 된 자막은 검수 대상이 아니라 재실행 대상이다. ② **`iter_batches`의 `segments`는 트랙의 연속 구간이어야 한다** — 맥락을 인접 슬라이스로 잡으므로 비연속 부분집합에서 **예외 없이 맥락이 조용히 틀린다.** 실측: 같은 세그먼트의 프롬프트 SHA-256이 전체 실행과 `track[6:]` 재개 실행에서 다르다(`27077083` vs `8cb1100c`). **WP7b 재개(FR-2.7)와 WP8 Tier 1(FR-4.3)이 정면으로 걸리는 자리**이고, 후자는 자가일관성이 모델 분산 대신 **프롬프트 차이**를 재게 되어 Q4 판정을 오염시킨다. ③ **`Provider.complete`에 예외 계약을 적었다**(그전에는 독스트링이 없었다) — 맨 `ProviderError`를 던지는 구현은 **재시도도 폴백도 없이 실행 전체를 죽이고**(실측: 호출 1회), `Completion(text=None)`은 `AttributeError`가 되는데 둘 다 `ProviderError` 밖이다. NFR-5가 약속한 확장점이라 계약이 시그니처만 말하면 서드파티가 폴백을 통째로 우회할 수 있다. 특성 테스트 2건으로 고정했다. ④ **맨 `ValueError` 표에 `translate_segments` 경로 5자리를 더했다** — 생성자 6자리만 세면 WP7b가 CLI 종료 코드를 가를 때 `max_retries`·`batch_size`·`context_window`·빈 배치·번호 충돌을 놓친다
- **거대 정수 토큰 수를 `2**53`에서 자른다** (NFR-2, `openai_compat.py`). `math.isfinite`를 int에서 건너뛴 판단은 그 함수 안에서는 옳았지만 **"크래시 없음"이 "안전"은 아니었다** — 값이 클램프 없이 `TokenUsage`로 나가고 **비용 = 토큰 x 단가는 float 연산이다.** 실측: `{"prompt_tokens": 10**309}`가 310자리 정수로 통과한 뒤 단가를 곱하면 `OverflowError: int too large to convert to float`가 나고, 그것은 `ArithmeticError`라 **`ProviderError` 밖이다.** 이 모듈이 전체를 걸어 막고 있는 바로 그 부류의 누수이고 터지는 자리만 engine이 아니라 WP7b의 비용 리포트다. 상한을 `2**53`(float64가 정수를 정확히 담는 마지막 값)으로 잡은 이유도 "이 값이 아니면 무엇이 깨지는가"로 적었다 — 더 크면 정밀도를 잃어 비용이 **조용히** 틀리고, 더 작으면 정상 실행이 잘린다(실사용 1e7 토큰 대비 약 1e9배 여유)
- **`ChatMessage.content`가 `str`인지 검사한다.** Task 1에서 "문자열 타입은 검사하지 않는다"로 기각했던 판정을 뒤집었다 — `role` 가드가 **같은 클래스에서** 막는 실패 모드와 동일하기 때문이다. 막지 않으면 `{"role": "user", "content": 123}`이 요청 본문에 그대로 실리고(실측), 서버의 400은 `FatalProviderError`가 되어 실행 전체를 중단시키는데 **원인이 프롬프트 조립 코드라는 사실은 보이지 않는다.** `role` 가드의 독스트링이 이미 그 근거를 다 적어 뒀고 한쪽 필드만 막은 비대칭이었다. `bool`이 `int`의 하위라 `isinstance(content, str)` 형태여야 한다
- **LLM 응답 캐시 저장소** (`store/cache.py`, NFR-3). 요청 지문(`cache_identity`·프롬프트 SHA·`temperature`·`max_tokens`)으로 키를 만들어 성공한 호출을 디스크에 원자적으로 남긴다. **캐시는 최적화이지 정확성의 근거가 아니다** — JSON 파싱·필드 타입·`_matches()`가 하나라도 어긋나면 미스로 떨어뜨린다(손상된 캐시가 있어도 재실행이 조용히 실패하지 않는다) (`742ac52`, `d1cb573`)
- **`CachingProvider` — 캐시를 끼운 프로바이더, 재개의 실체** (FR-2.7, NFR-3). `Provider`를 그대로 감싸 히트는 네트워크를 타지 않고 미스만 실제로 호출한 뒤 저장한다. 흡수 한정성 게이트(캐시 저장 실패가 원래 번역 성공을 가리지 않는다)와 tmp 파일 정리를 `finally`로 옮겨 `KeyboardInterrupt`에서도 잔해가 남지 않게 했다 (`eb7a08b`, `18f631c`, `b4e8972`, `290b146`, `9217658`)
- **번역된 자막 파일 쓰기** (`ingest/writer.py`, FR-7.1). 실패한 세그먼트는 원문을 그대로 유지해 낸다 — 실패가 빈 줄로 나가면 배포 자막에 구멍이 뚫린다 (`4dc5571`, `eeb4d33`)
- **`cuesift translate` 배선** (FR-8.1, FR-7.1, WBS WP7b). 캐시·재개·다국어 순차 번역·자막 출력이 실제로 동작한다. 파이썬 예외가 커맨드 본문을 빠져나가 exit 1("규격 위반 발견"과 같은 코드)로 오보되던 누수 6종을 막았다. `python -m cuesift` 진입점이 생겨 콘솔 스크립트가 PATH에 없어도 서브프로세스로 부를 수 있다 (`f4d3a70`, `26ad847`, `7a62825`, `1b8bf29`, `05ca745`, `a022dcb`)
- 종료 코드 `69`(`EX_UNAVAILABLE`) — LLM 서버가 요청을 거부한 경우(인증 실패·모델 없음 등). `66`(파일 사정)·`70`(미구현)과 나란히, CI가 "설정이 틀렸다"와 "서비스가 거부했다"를 구분할 수 있게 한다
- **`--dry-run` 실제 구현** (NFR-2). `build_messages`를 실제로 불러 배치 수·문자 수를 정확히 세고, 캐시 키를 계산해 히트를 실측한다. **토큰·비용은 추정하지 않는다** — 문자에서 토큰으로 가는 계수가 모델마다 다르고 출처가 없다(요구사항정의서 §11 R8, "출처 없는 수치를 기본값으로 넣지 않음"). "호출 필요 N개"는 배치가 한 번에 성공한다고 가정한 **하한**이라고 명시한다 — 개별 폴백이 발동하면 실제 호출은 몇 배로 는다 (`3509b96`, `4685be1`)
- **live 검증 — 실제 프로세스로 재개를 확인한다** (`tests/test_translate_live.py::test_cli가_실제_프로세스로_동작한다`, WP7b Task 7). `typer.Exit`이 실제 프로세스 종료 코드가 되는지는 `CliRunner`가 완전히 증명하지 못한다 — `python -m cuesift`를 서브프로세스로 두 번 실행해, 1회차가 실제로 파일을 내고(exit 0/1) 2회차는 캐시 히트라 "실제 호출 0개"가 찍히는 것을 실측한다. Ollama `qwen2.5:3b`로 실행 확인: 1회차 exit 0·실제 호출 1개(2.71초), 2회차 exit 0·캐시 히트 1개·실제 호출 0개(0.38초)
- **Tier 1 신호 라이브러리 계층** (FR-4.1, FR-4.3 라이브러리, FR-6.5, WBS WP8a). `signals/llm.py`의 `llm.self_consistency` — 같은 세그먼트를 N회(기본 3, `temperature=1.0`) 재번역해 결과 간 상호 유사도로 "이 구간이 번역하기 어려운가"를 잰다. `signals/base.py`가 Tier 0/Tier 1 실행 경로를 완전히 분리한다 — `collect_all()`은 tier 0만 돌아 LLM 호출이 0이고(설계 §10.1 게이트, 변이 삽입 후 실제로 죽는 것을 확인했다), `collect_tier1()`은 `select_tier1_candidates`(FR-4.3, `triage/policy.py`)가 **예산 컷라인 아래 회색지대**에서 고른 후보에만 돈다 — hard fail은 `risk_score=1.0`이 고정이라, 이미 선별된 세그먼트는 예산을 이미 확보했으므로 Tier 1을 태워도 낭비다. `signals/similarity.py`가 `difflib.SequenceMatcher`(NFKC 정규화, `autojunk=False`) 기반 **문자 단위** 유사도를 낸다 — ja에 공백이 없어 단어 분할이 위험하다(CJK를 깨뜨린 `\b` 전례가 이 저장소에 있다). `store/cache.py`의 `CacheRequest.attempt`가 자가일관성의 N회 호출을 캐시 키에서 가르되 `attempt=0`은 키 문자열에서 생략해 **기존 캐시가 바이트 단위로 유효하다**(WP7b가 실물로 증명한 재개가 헛돌지 않는다, 회귀 테스트로 고정). `tier1.py`의 `triage_with_tier1()`이 2라운드(Tier 0 → 예산 적용 → Tier 1 후보 선별 → 재융합 → 예산 재적용) 오케스트레이션을 묶는다 — noisy-or 융합(아래 `risk/fuse.py` 항목)이 "신호가 붙을수록 점수가 오르기만 한다"는 성질을 보장하므로, 회색지대에만 Tier 1을 적용해도 적용받은 쪽이 부당하게 낮아지지 않는다(가중 평균이었다면 반대로 갔을 자리). **FR-4.2(역번역)는 보류했다** — 착수 시점 실측(7쌍, 요구사항정의서 §12 Q4)이 negation(유사도 0.727~0.930)과 paraphrase(0.759~0.800)를 문자 단위 유사도로는 분리하지 못하는 것을 보였다 — 정상 변이가 오류보다 높은 점수를 받으므로, 그 신호를 그대로 구현했다면 위험도를 잘못된 방향으로 밀어 올렸을 것이다. **길이 편향도 실측으로 남겼다**(지금은 완화하지 않는다) — 같은 정도의 편집이 세그먼트 길이에 따라 점수를 크게 다르게 만든다(짧을수록 높다). 배수는 예문에 따라 흩어져(독립 측정 4회, en 8~12배·ja 5~7배) 단일 수치로 특정하지 않는다 — 상세는 `SelfConsistency`의 독스트링이 단일 출처다. **live 검증**(`test_자가일관성이_실제_엔드포인트에서_신호를_낸다`, Task 7): Ollama `qwen2.5:3b`에서 `llm.self_consistency`가 실제로 발화하는 것을 확인했다. 이 과정에서 **신규 결함**을 하나 더 찾았다 — 단일 세그먼트 호출에서 `index != 0`이면 이 모델이 프롬프트의 `[N]` 마커를 무시하고 항상 `id: 0`을 답해 파싱이 거부됐다(실측 6/6 재현). 시스템 프롬프트 예시가 `{"id": 0, ...}`라는 리터럴 0을 보여 주고 항목이 하나뿐이면 그 0을 그대로 베끼는 것으로 보인다. `_retranslate`가 세그먼트를 로컬 `index=0`으로 재번호해 보내고 결과는 `segment.id`로 매칭하는 방식(원본 index는 참조하지 않는다)으로 닫았다(`9d57e9a`, Ruling P13) — `loader.py`가 `id=f"{index:05d}"`로 매기고 동점 처리가 작은 id를 먼저 큐에 담으므로 **실전 Tier 1 후보는 거의 항상 index != 0**이라, 재번호 없이는 이 신호가 실물 모델에서 조용히 거의 항상 `None`이 될 뻔했다(Q3 무음 열화). **재리뷰가 가짜 프로바이더(`AlwaysZeroProvider`)로 재현하며 수정 전 비용도 함께 실측했다** — 시도당 호출이 2회였다(배치 호출이 실패하면 `_fallback_individually` → `_run_single`로 폴백하는데 그쪽도 같은 이유로 실패해서다). 즉 수정 전에는 **신호는 조용히 0건인데 호출은 2배**로 나가는, 침묵과 낭비가 겹친 상태였다. **`engine.py:384`의 `_run_single`(일반 번역의 개별 폴백 경로)에도 같은 코드 패턴
(`parse_translations(text, [segment.index])`, 원본 전역 index 사용)이 있고, 최종
브랜치 리뷰가 실물 Ollama로 확인했다** — `index=1`·`5`·`9`는 3/3 전부 실패하고 `index=0`만
3/3 성공한다(P13과 정확히 같은 형태). 이 패턴은 이 브랜치가 만든 것이 아니라 `48f9133`
(WP7b)에 이미 있었다 — WP8a는 발견만 했고 고치지 않았다(범위 밖, HANDOFF.md로 넘긴다).
**신호 라이브러리 계층 완성 후 최종 브랜치 리뷰가 후속 수정 2건을 더 냈다**: ① `signals/llm.py`
의 실측 문구에 조건(원문 16자)을 명시하고 "6.5배"를 정확한 값 "6.45배"로 고쳤다 — 문자 수
자체는 원래도 정확했고 원문 길이 L의 선형함수(`9840+30L`/`1119+30L`)라 L을 밝히지 않은
것이 "재현 실패"로 잘못 읽혔을 뿐이다. ② `tier1.py`의 "## 비용" 절에 결론을 추가했다 —
기준선 대비 배수는 `30 × max_ratio`이고 이 저장소 표준 예산(10%)과 대칭인
`max_ratio=0.10`이 요구사항정의서 §4의 "3배는 감당 불가" 한도에 정확히 걸린다. 회귀
게이트도 늘었다 — `tests/fakes/provider.py`에 `AlwaysZeroProvider`(요청 id 무시하고 항상
0으로 답하는 실물 재현)를 추가하고, `tests/test_signals_llm.py`에 그것으로 만든 **증상**
게이트(메커니즘이 아니라 `collect_tier1`이 실제로 신호를 내는지)를 추가했다 — 재번호를
되돌려 실제로 죽는 것을 확인한 뒤 복원했다. [설계 스펙](docs/superpowers/specs/2026-08-17-tier1-signals-design.md) · [구현 계획](docs/superpowers/plans/2026-08-17-tier1-signals.md). 테스트 978 → **1050**건 (`989e9ee`..HEAD, Task 7 live 테스트·증상 게이트 각 1건 포함)
- **검수 트리아지가 CLI에서 처음으로 도달 가능해졌다** (FR-6.1~6.3, WBS WP6). 이 저장소의 목적이 트리아지 엔진인데 **신호 수집기도 융합도 CLI에서 한 번도 호출되지 않고 있었다** — `--review-budget`은 경고만 내고 값을 버렸고, `check`는 설계상 신호 엔진을 통과하지 않으므로(위 참고) 라이브러리에 다 있는 `collect_all`→`fuse`→`select_by_*`가 사용자에게 닿는 경로가 **하나도 없었다.** 지금은 `cuesift translate ep01.ko.srt --to en,ja --review-budget 10%`가 번역 직후 트리아지를 돌린다. **`--review-threshold`(0.0~1.0)를 신설**했고 `--review-budget`과 **상호배타**다 — FR-6.3은 "두 방식으로 지정할 수 있다"이지 "동시에"가 아니고, 합성하면 어느 쪽이 이겼는지가 출력에서 사라진다. **예산은 비율만 받는다**(`10%`·`0.1`) — 개수(`50`)는 종료 코드 2이며, 라이브러리에 개수 기반 선별이 없고 `k/n` 환산은 `ceil`과 hard fail 소진 때문에 정확히 K개를 내지 못해 **옵션이 거짓말을 하게 된다.** `0`·`0%`는 유효값이고("hard fail만 보기"는 의미 있는 요청이다) `%` 없는 `1`은 전량 검수다 — `0.0 <= x <= 1.0` 한 규칙의 결과이며, 규칙을 좁혀 `1`을 거부하면 `0`도 함께 막힌다. **`--review-threshold`에는 NaN 가드를 따로 뒀다**: click의 `min`/`max`는 `lt(nan, 0.0)`·`gt(nan, 1.0)`이 **둘 다 False**라 NaN을 통과시키고, 그러면 `select_by_threshold`의 `ValueError`가 미처리 traceback이 되어 **exit 1**("규격 위반 발견")로 나가 설정 실수가 자막 결함으로 오보된다. `inf`는 click이 잡으므로 여기 오지 않는다(실측). `_parse_review_budget`은 `not 0.0 <= nan <= 1.0`이 True가 되어 **극성이 반대**로 거부한다
- **규격 프로파일을 대상 언어 코드에서 자동 유도한다 — 새 옵션이 0개다** (설계 D6, `spec/profile.py`의 `available_builtins`·`load_builtin` 재사용). `--to en,ja`가 내장 `en`·`ja`를 쓴다. `check --spec`(검사 대상 자막의 규격)과 **이름이 같아도 다른 것이다** — 여기서는 **대상 언어**의 규격이고, 프로파일을 읽는 신호 2종(`spec.violation`·`length.ratio`)이 그것을 번역문에 적용한다. **프로파일이 없는 언어는 경고하고 그 언어의 트리아지만 건너뛴다**(번역은 그대로 나간다) — 전량 거부하면 프로파일이 **있는** 언어의 트리아지까지 잃고 요구사항정의서 §8.1 S3의 문서화된 호출(`--to en,ja,th,vi --review-budget 10%`)이 깨진다(`th`·`vi` 프로파일은 없다). **단 한 언어도 못 돌면 종료 코드 2다** — 그때는 요청이 통째로 무시된 것인데 경고만 내고 0으로 끝나면 CI가 "트리아지했다"로 읽는다. **검사는 언어별 루프 전에 전량 돈다**(D13) — 루프 안에서만 보면 `--to en,ja,fr`이 en·ja의 LLM 비용을 실제로 쓴 뒤 fr에서 죽는다. 조회만 소문자로 접는다: `--to EN`이 Windows에서는 `en.yaml`을 찾아내고 **CI의 Linux에서는 못 찾아** 같은 명령의 종료 코드가 플랫폼마다 갈린다(출력 파일명은 원본 태그를 유지한다)
- **번역 실패분을 트리아지 입력에서 제외한다** (설계 D12 · `TranslationResult` 독스트링의 계약). 실패분은 `segments`에 `target_text=None`으로 **남아 있고**, 그대로 넘기면 `struct.empty`가 `hard_fail=True`를 내는데 hard fail은 예산을 우회하므로 **프로바이더 장애 하나가 진짜 오류를 큐에서 밀어낸다.** 실측(200큐·진짜 오류 20건·요청 예산 10%): 실패 20건에서 Recall@10%가 **0%**(quota 전량 소진), 30건에서는 실제 검수 비율이 **15%**로 부풀어 §9.1 배수의 **분모까지** 망가진다. **오염이 오류에서 오지 않고 "번역이 실패했다"는 사실 자체에서 온다** — 번역 안 된 자막은 검수 대상이 아니라 재실행 대상이다. 이 계약은 `tier1.py`의 `triage_with_tier1()`이 이미 지키고 있었고 **CLI만 어기고 있던 상태**였다. 실물이 그 가치를 우연히 실증했다 — Ollama `qwen2.5:3b`가 배치 10개에서 1개만 번역해 실패가 9건 났고, 제외하지 않았다면 hard fail 9건이 예산 30%(`quota = ceil(10 × 0.3) = 3`)를 **완전히 소진**해 진짜 오류 1건이 밀려났을 것이다
- **요약이 요청 예산과 실제 검수 비율을 나란히 낸다** (FR-7.4 · 설계 D8). `[en] 트리아지 (예산 10%, 프로파일 en)` 아래로 대상 세그먼트 수(실패 제외 건수를 괄호로, **0건이면 괄호를 내지 않는다**)·검수 대상 수와 **실제 비율**·hard fail 수·신호별 적발 건수(이름순 정렬, NFR-3)가 나온다. **둘을 함께 내는 이유**는 hard fail이 quota를 소진하고 `ceil` 하나로도 어긋나기 때문이다(7건에 10%면 실제 14.3%) — 요청만 찍으면 사용자가 README 배수를 **틀린 분모로** 재계산한다. **프로파일 이름을 출력하는 것이 값 검증의 유일한 수단이다**: 리뷰가 `profiles[target] = load_builtin("ko")` 변이로 실측했는데 키 집합만 검증되고 값은 검증되지 않아 **전 스위트가 통과했다.** 비율은 `_format_ratio`를 재사용한다 — `f"{x:.1%}"`로 직접 찍으면 2001개 중 1개(0.05%)가 `"0.0%"`가 되어 검수자가 "0%니까 통과"로 읽는다. **`review_ratio`에는 선별분이 아니라 전체 목록을 넘긴다** — 선별분만 넘기면 값이 언제나 1.0이 되고, 그 값이 README 배수의 분모다. **세그먼트 목록은 출력하지 않는다**(D9) — FR-7.2(`review.json`)가 WP5의 명시적 산출물이고, 화면 목록과 리포트가 갈라지면 한쪽만 고쳐진다. **종료 코드는 0을 유지한다**(D10) — 검수 대상이 있는 것은 실패가 아니다. 2는 사용법 오류에만 쓴다. **`--dry-run`과의 관계도 게이트로 고정했다**(최종 리뷰 I4): dry-run은 번역을 하지 않으므로 트리아지도 돌지 않고(`--review-budget`을 **주고도** 요약이 없다), **프로파일 검증은 dry-run에서도 돈다**(`--to fr --review-budget 10% --dry-run`이 exit 2). 둘 다 README가 "실측"으로 약속한 동작인데 단언이 없었다 — 각각 변이(dry-run 경로에 요약을 얹기 · 프로파일 검사를 `and not dry_run`으로 미루기)를 넣어 **해당 테스트만 죽는 것을 확인한 뒤 되돌렸다.** 테스트 1050 → **1096**건. [설계 스펙](docs/superpowers/specs/2026-08-18-triage-cli-design.md)
- **검수 큐가 처음으로 파일이 됐다 — `--review-out DIR`과 `review.json`** (FR-7.2, WBS WP5). 트리아지는 이미 CLI에서 돌았지만 결과가 **화면 요약으로만** 나가고 세그먼트 단위 정보는 함수 안에서 소멸했다(`_run_triage`가 `list[str]`을 반환했다). 지금은 `cuesift translate ep01.ko.srt --to en --review-budget 30% --review-out reports`가 `reports/ep01.en.review.json`을 낸다. **파일명이 `{stem}.{대상언어}.review.json`인 것이 설계다**(D2) — 고정 이름(`review.en.json`)이면 `ep01`과 `ep02`를 같은 디렉터리로 낼 때 뒤엣것이 앞엣것을 **조용히 지우고 종료 코드는 0이며 경고도 없다.** `_output_path`가 자막 파일에서 이미 같은 사고를 막고 있다. **`--review-out` 단독은 exit 2다**(D10) — 리포트를 기대하고 명령을 짰는데 파일이 조용히 안 나오는 것이 최악이고, 그 사실은 다음 단계(배포 스크립트·CI)에서야 드러난다. **`--dry-run`에서도 이 조합 검증은 돈다**(D11, 실측: 단독+dry-run이 exit 2) — 파일은 쓰지 않지만 옵션 오류는 LLM을 부르기 전에 알아야 한다. **규격 프로파일이 없는 대상 언어는 리포트를 내지 않는다**(D7, 실측: `--to en,th`에서 `th` 파일 없음·번역은 나감) — 빈 파일은 소비자가 "검수했고 걸린 것이 없다"로 읽는데 실제로는 판정 자체를 못 한 것이다
- **신설 패키지 `src/cuesift/report/`** (설계 D9) — `models.py`의 `TriageOutcome`(수치의 단일 출처)과 `json_report.py`의 `build_review`/`write_review`. `cli.py`가 1686줄이었고 FR-7.3(`report.html`)이 같은 자리를 다시 요구한다. **`TriageOutcome`이 이 설계의 분기점이다**(D8): `_run_triage`가 이것을 반환하고 화면 요약과 JSON이 **같은 객체**를 읽으므로 수치가 어긋날 자리가 구조적으로 없다. 갈라져도 프로그램은 정상 종료하고 파일도 정상이며 종료 코드도 0이라 **어떤 게이트에도 걸리지 않는** 부류의 실패다. 파생 수치(`total`·`selected`·`hard_fail_count`·`signal_hits`·`review_ratio`)는 프로퍼티로 한 번만 정의하고, **`risks`는 선별분이 아니라 전량을 담는다** — 선별분만 담으면 `review_ratio`가 언제나 1.0이 되는데 그 값이 README 배수의 분모다. `segments[]`에는 반대로 **선별분만** 담는다(D3) — FR-7.2가 "검수 **대상** 세그먼트 목록"이고 분모는 `summary`가 따로 낸다. 커버리지 `json_report.py` **100%** · `cli.py` **97%**, 테스트 1096 → **1153**건. [설계 스펙](docs/superpowers/specs/2026-08-18-review-json-design.md)
- **`review.json`이 FR-6.4를 함께 닫았다** (요구사항정의서 §0.1 축 1). FR-6.4("왜 선별되었는지 설명 가능하게 한다")는 **자기 코드가 한 줄도 바뀌지 않은 채** 부분 충족에서 완료가 됐다 — 라이브러리는 `SegmentRisk.reasons`를 세그먼트마다 기록하고 있었으나 목적절의 수혜자인 **검수자에게 가는 통로가 없었고**, 그 통로가 정확히 FR-7.2였다. `signals[].detail`을 **통째로** 싣는 것(D4)이 여기서 값을 낸다 — `signals/llm.py`의 주석이 이미 review.json을 소비자로 명시해 뒀고(`self_consistency.samples`는 "왜 이 번역이 불안정하다고 봤는가"의 유일한 증거다), 잘라내면 FR-6.4가 요구한 설명력이 반쪽이 된다. **v0.1 완료 개수가 30 → 32로 올랐다**(`30 + FR-7.2 + FR-6.4`)
- **Tier 1 자가일관성이 CLI에서 돈다 — `--tier1` 외 3종** (FR-4.3, WBS WP8b). `cuesift translate ep01.ko.srt --to en --review-budget 10% --tier1`이 컷라인 아래 회색지대에서 후보를 골라 세그먼트마다 N회 재번역하고 그 상호 유사도를 신호로 낸다. **FR-4.3이 "라이브러리에 있음"에서 "사용자가 설정할 수 있음"으로 넘어갔다** — WP8a가 만든 `max_ratio`는 라이브러리 파라미터로만 존재해 CLI에서 도달할 수 없었고, 요구사항정의서 §0.1의 축 1(층)이 요구한 사용자 통로가 이번에 생겼다. 옵션은 스위치 `--tier1`과 `--tier1-max-ratio`(기본 `0.05`)·`--tier1-samples`(기본 `3`)·`--tier1-temperature`(기본 `1.0`) 넷이고, **스위치를 분리한 것이 설계다**(D1·D2) — `--tier1-*`만 주면 종료 코드 2로 거부해 "켠 줄 알았는데 안 켜진" 실행을 없앤다. `--review-threshold`와의 조합도 거부한다(D9, `triage_with_tier1`이 `select_by_budget`을 고정으로 쓴다). [설계 스펙](docs/superpowers/specs/2026-08-25-tier1-cli-design.md)
- **비용 상한을 `samples`가 아니라 곱에 건다** (설계 D4, 요구사항정의서 §4). 번역 기준선 대비 배수는 `samples x max_ratio x DEFAULT_BATCH_SIZE(10)`이고 세그먼트 수가 약분돼 **트랙 크기에 무관하다.** §4가 "3배는 감당 불가"라 적었으므로 게이트는 `--tier1-samples x --tier1-max-ratio < 0.3`이고 **경계를 포함해 거부한다** — 3.0배가 곧 §4가 막으려던 값이다. 부동소수 때문에 `>`만으로는 새는데(`2 x 0.15`와 `30 x 0.01`은 정확히 `0.3`이라 통과한다) `math.isclose`를 함께 걸어 막았다. **`samples` 단독 상한이었다면 `--tier1-samples 10 --tier1-max-ratio 0.5`(배수 50)가 통과한다** — 상한이 잘못된 축에 걸리면 막으려던 것을 못 막는다. 기본값 `3 x 0.05 = 0.15`는 한도의 절반인 1.5배다
- **`--dry-run`이 Tier 1 상한을 낸다** (설계 D10). `Tier 1 재번역 요청 최대 3회 (재시도·폴백 제외 · 후보 상한 비율 0.05 · 샘플 3)` — LLM을 부르기 전에 얼마를 부를지 본다. **"호출"이 아니라 "재번역 요청"인 것은 의도한 것이다**: 같은 화면의 형제 줄(`호출 필요 N개 이상`)이 프로바이더 호출을 세고 이 줄은 Tier 1 재번역 요청을 센다 — 재시도와 개별 폴백이 얹히면 실제 호출은 이 수를 넘으므로 두 낱말을 섞으면 화면이 거짓말을 한다. 실측(26 세그먼트·기본값): `floor(26 x 0.05) = 1` 후보 x 샘플 3 = **3회**
- **후보가 0건이어도 사유를 낸다** (설계 D12). `floor(세그먼트 수 x max_ratio)`가 0이거나 회색지대가 비면 유료 계층이 통째로 안 도는데, 그때 화면이 같으면 사용자는 Tier 1이 돈 줄로 읽는다. 실측: `[en] Tier 1: 세그먼트 수(3)에 비해 max_ratio(0.05)가 작아 Tier 1 상한이 내림(floor)으로 0이 됐다`. **Tier 1 프로바이더가 죽으면 종료 코드 69**(D14)이고 문구에 `Tier 1`을 넣어 번역 경로의 69와 구별한다 — 그물이 없으면 401이 exit 1이 되는데 이 리포에서 1은 "규격 위반 발견"이라 **설정 실수가 자막 결함으로 오보된다**
- **`cost`가 Tier 1 토큰을 합산한다 — FR-7.4가 함께 닫혔다** (WBS WP5 나머지의 절반). `review.json`의 `cost.includes`가 **`--tier1`을 켜고 Tier 1이 실제로 돈 실행**에서 `["translation", "tier1"]`이 된다(live 실측, Ollama `qwen2.5:3b`). **판정은 스위치가 아니라 "그 계층이 돌았나"다** — 스위치를 켜도 Tier 1이 한 번도 안 도는 경로가 **둘**이고(번역 전량 실패 · **후보 0건**) 둘 다 `["translation"]`이 나간다. 후보 0건 쪽은 최종 브랜치 리뷰가 **실주행으로** 찾았다 — 화면은 `Tier 1: 회색지대가 비었다`인데 파일은 `["translation", "tier1"]`이라 말했고 그 `calls`는 전부 번역분이었다. `triage_with_tier1`이 후보 0건에서만 `warn`을 부르고 즉시 반환하므로 호출부가 그 신호로 계층을 범위에서 뺀다. **호출 수로 판정하지 않는다** — `CountingProvider`가 캐시 안쪽이라 전량 캐시 히트 재실행에서 `calls == 0`인데 그때 Tier 1은 돌았고, 호출 수를 기준으로 삼으면 같은 입력의 두 실행이 다른 범위를 내 NFR-3이 깨진다. `tier1_usage`의 기본값을 `None`으로 둔 것이 그 자리를 옳게 만든다 — `TokenUsage(0, 0, 0)`을 넘기면 `calls`가 0이라 수치는 안 부푸는데 `includes`만 "Tier 1을 셌다"고 말해 **화면 어디에도 신호가 없는 거짓말**이 된다. 범위·판정·합계는 `report/models.py`의 `resolve_cost_scope`가 **함께** 낸다 — 셋을 호출부가 손으로 적으면 계층을 늘린 쪽이 판정을 빠뜨리고 그 계층의 무음 열화가 `tokens_reported`에 안 실린다. `cost.basis`가 계층마다 어떻게 셌는지를 밝히는 것도 같은 이유다(번역은 캐시 적중분 포함, Tier 1은 캐시 안쪽이라 실제 전송분만). **FR-7.4의 마지막 빈칸이던 "소요 토큰"이 채워졌다** — 축 2(완전성)가 닫혔고, 합산 자체는 `test_tier1이_쓴_토큰이_usage에_더해진다`가 `calls`와 `completion_tokens` 양쪽으로 건다
- **v0.1 완료 개수가 32 → 34로 올랐다** (`32 + FR-4.3 + FR-7.4`). 규칙이 바뀌어서가 아니라 구현이 FR을 닫았고, 두 축으로 교차 검산했다 — 요구사항정의서 §5.4·§5.6·§5.7의 상태 열 있는 **13개**를 전수 대조하면 ✅ **10개**이고, 상태 열 없는 **29개**의 완료 **24개**는 이 작업으로 움직이지 않아 `24 + 10 = 34`가 WBS의 WP 축 합계와 같은 값을 낸다. **두 값이 갈라지면 산수가 아니라 판정이 틀린 것이다.** 테스트 **1267 passed · 1 skipped · 3 deselected**(CI 기준 — `data/`가 `.gitignore`에 있어 깨끗한 체크아웃에는 벤치 트랙이 없고 `tests/test_bench_glossary.py`가 1건을 skip한다. 트랙이 있는 작업트리는 1268 passed · 0 skipped)
- **설계 스펙과 구현 계획 — `report.html` 검수 리포트** (`3cdac38`·`df9a67b`, FR-7.3, WBS WP5 나머지). `docs/superpowers/specs/2026-08-27-report-html-design.md`와 `docs/superpowers/plans/2026-08-27-report-html.md`. **착수 조사가 문서 셋의 공통 오류를 찾았다** — 요구사항정의서 §5.7·WBS·HANDOFF가 모두 "`spans[].side`가 이미 실려 있다"고 적었는데, 셋 다 참이지만(스키마에 실려 있다) 읽히는 함의(값이 있다)가 거짓이다. `grep -rn "Span(" --include=*.py src/ tests/`가 **프로덕션 코드에서 0건**을 냈다 — `Span` 클래스도, `side`의 런타임 검증도, `review.json`의 직렬화도, "`side`를 뺄 수 없다"는 주석도 전부 있는데 **채우는 쪽이 없어 `Signal.spans`가 언제나 `()`였다.** 스키마·검증·직렬화·주석이 갖춰지면 "구현됐다"는 착각이 만들어지고, 파생 문서가 원문을 복사하면 검증이 아니라 증폭이 일어난다. 그래서 범위를 렌더러 하나가 아니라 **둘**로 잡았다 — 수집기 3종(`glossary.miss`·`struct.number_missing`·`struct.tag_lost`, 나머지 7종은 구간 개념이 성립하지 않는다)이 판정하는 그 자리에서 `Span`을 함께 내고, `report/highlight.py`의 순수 함수가 겹침을 경계점으로 쪼개며, `string.Template`이 HTML을 조립한다. 결정 10건 중 **D7(이스케이프는 분할 뒤에)**이 가장 위험하다 — `html.escape`는 길이를 보존하지 않아 (`<` 1자 → `&lt;` 4자) 순서를 뒤집으면 오프셋이 전부 밀리는데 **예외가 나지 않는다.** 자막에 태그가 실제로 들어오는 것은 `struct.tag_lost`가 태그를 세고 있다는 사실이 증명한다. 태스크 10개에 게이트 수치를 못 박았고(착수 1270 → 완료 1332), **자동 게이트가 없는 것 둘(필터 동작·`noscript` 폴백)을 완료 판정에 명시했다** — pytest는 브라우저를 띄우지 않으므로 live 1회로 대신하며, 검사하지 않는 것을 검사한다고 적지 않는다
- README에 `CUESIFT_API_KEY` 안내 한 줄 — `cli.py`가 실제로 읽는(`_resolve_endpoint`) 환경변수 3종 중 이것만 문서에 없었다. 로컬 Ollama 예시에는 불필요하지만 원격 API 사용자는 인증 방법을 알 길이 없었다. 718줄대의 `CUESIFT_LIVE_*` 표는 live 테스트 전용이라 별개다
- **검수 리포트가 브라우저에서 열린다 — `--review-format json|html|both`와 `report.html`** (FR-7.3, WBS WP5 나머지). `cuesift translate ep01.ko.srt --to en --review-budget 30% --review-out reports --review-format both`가 `reports/ep01.en.review.json`과 `reports/ep01.en.report.html`을 나란히 낸다. HTML은 **의존성 0개의 단일 파일**이다 — CSS·JS를 안에 넣어 검수자에게 파일 하나만 넘기면 된다(설계 D5, `string.Template`). 본문이 요구한 셋을 전부 낸다: 세그먼트마다 원문과 번역문을 나란히 놓고, 신호가 지목한 구간을 `<mark>`로 칠하며, 신호 이름·hard fail로 행을 줄이는 필터가 붙는다. **기본값이 `json`이라 기존 동작은 바뀌지 않는다**(D1) — `HTML`이나 `BOTH`가 기본이면 `--review-format`을 준 적 없는 CI 파이프라인의 리포트 디렉터리에 `.report.html`이 새로 쌓이는데 종료 코드는 그대로 0이다. **`--review-format` 단독은 종료 코드 2다** — 낼 곳이 없는 형식 지정을 조용히 넘기면 사용자는 나오지 않는 파일을 찾아 헤매고 스크립트는 성공으로 읽는다(`--review-out` 단독이 막히는 것과 같은 사고). **`--dry-run`의 예고도 형식을 따른다** — 산출물 판단을 `_review_artifact_paths` 한 곳에 모으기 전에는 `--dry-run --review-format html`이 나오지도 않을 `.review.json`을 예고했다(리뷰 라운드 1 실측, 두 리뷰어가 독립으로 지목). 파일명은 `{stem}.{대상언어}.report.html`로 `review.json`과 **같은 stem 규칙**이다 — 갈라지면 `both`가 낸 두 파일이 한 실행의 산출물임을 소비자가 눈으로 맞추지 못한다. [설계 스펙](docs/superpowers/specs/2026-08-27-report-html-design.md)
- **수집기 3종이 `Span`을 낸다 — 하이라이트의 입력이 처음으로 채워졌다** (FR-7.3). `glossary.miss`·`struct.number_missing`·`struct.tag_lost`가 판정하는 **그 자리에서** 구간을 함께 낸다(설계 D8). **렌더러가 나중에 `find`로 되찾을 수 없기 때문이다** — `detail`의 값은 NFKC 정규화·천 단위 구분자 제거·`casefold()`를 거쳐 원문과 다르다: `５０`이 `"50"`이 되어 `source_text.find("50")`이 **-1**을 내고, 그러면 예외 없이 하이라이트만 조용히 빈다. **나머지 7종에 `spans`가 없는 것은 결함이 아니라 성질이다** — 판정 단위가 세그먼트 전체다. 겹치는 구간은 `report/highlight.py`의 `split_spans`가 **경계점으로 평평하게 쪼갠다** — 중첩(`A=[0,5)`·`B=[3,8)`)으로는 유효한 HTML을 만들 수 없다(D6). **이스케이프는 분할 뒤에 조각 단위로 건다**(D7) — `html.escape`는 길이를 보존하지 않아(`<` 1자 → `&lt;` 4자) 먼저 걸면 이후 오프셋이 전부 밀리는데 **예외가 나지 않는다.** 자막에 태그가 실제로 들어오는 것은 `struct.tag_lost`가 태그를 세고 있다는 사실이 증명한다. **부수 효과로 `review.json`의 `spans`도 채워져 나가기 시작했다** — `json_report.py`는 한 줄도 바뀌지 않았다(§8.4의 계약이 이미 그 자리를 비워 두고 있었다)
- **v0.1 완료 개수가 34 → 35로 올랐다** (`34 + FR-7.3`). **앞의 두 번과 달리 이번엔 FR 하나만 닫혔고, 그것도 규칙대로다** — FR-7.3은 ⬜(아무것도 없음)이라 부족한 축이 없었고, 요구사항정의서 §0.1이 후속 작업을 지정해 두는 것은 🟡에서만이다. **함께 닫히는 FR은 🟡에서 나온다.** 교차 검산: 상태 열 있는 **13개** 중 ✅ **11개**, 상태 열 없는 **29개** 중 **24개**로 `24 + 11 = 35`가 WBS의 WP 축 합계와 같다. 테스트 **1379 passed · 3 deselected**(작업트리 기준. CI는 `data/`가 `.gitignore`에 있어 벤치 트랙이 없고 `tests/test_bench_glossary.py`가 1건을 skip하므로 **1378 passed · 1 skipped**). 계획서는 착수 1270에서 완료 1332를 예고했는데 실측이 **47건 많다** — 부족분은 전부 **변이 실측이 생존자를 보여준 뒤에야** 드러났다
- **자동 게이트가 없는 것 둘을 완료 판정에 명시했다** (FR-7.3). 필터 동작과 `noscript` 폴백은 브라우저가 있어야 재는데, 헤드리스 브라우저를 넣으면 dev 의존성 고정(3개)이 깨진다(D3). 파이썬이 보장하는 것은 **마크업 계약까지**이고 그 뒤는 live 1회다 — Chrome에서 hard fail 토글로 4행이 2행이 되고 카운터가 `표시 중 2 / 4`가 되는 것, `allow-scripts` 없는 `sandbox` iframe에서 `noscript` 안내가 뜨고 4행이 전부 남는 것, 원문에 `<i>백서</i>`와 `2024`가 함께 든 세그먼트에서 **칠해진 것이 정확히 `2024`이고 `<i>`는 기울임이 아니라 글자인 것**(D7의 실물 확인)을 봤다. CLI 전 경로도 Ollama `qwen2.5:3b`로 돌려 두 파일이 함께 나오는 것을 확인했다. **다만 그 실행에서 하이라이트는 보지 못했다** — 낸 신호 2종(`spec.violation`·`struct.untranslated`)에 구간 개념이 없고, 구간을 내는 3종은 번역이 성공해야 발화하는데 10건 중 9건이 원문 그대로 되돌아왔다. **"LLM 실행 → 구간 있는 신호 → 하이라이트"의 전 구간은 아직 한 번도 통과시킨 적이 없고, 그것을 통과로 적지 않는다**

- **설계 스펙과 구현 계획 — `cuesift.yaml` 설정 파일** (`ea84f91`·`82794de`·`acc55d2`, FR-8.4, WBS WP6 나머지). `docs/superpowers/specs/2026-08-28-config-file-design.md`와 `docs/superpowers/plans/2026-08-28-config-file.md`. **착수 조사가 설계를 가장 많이 줄였다** — "click이 해 줄 것이다"는 근거가 아니므로 폐기용 탐침 4개로 실측했고(P1~P4), `ctx.default_map`이 서브커맨드 파라미터에 먹으며 **타입 변환과 값 검증까지** 한다는 것이 확인됐다(`"./x/y"` → `WindowsPath`, `"xml"` → 종료 코드 2). 옵션 23개의 변환·검증 코드가 통째로 사라졌고(D5), 검증 규칙이 CLI와 설정 파일에서 갈릴 자리도 함께 사라졌다. P3(`default_map`이 **필수** 옵션을 만족시킨다)이 없었다면 `--to`·`--spec`을 선택 인자로 바꾸는 파괴적 변경이 필요했다. 요구도 셋으로 갈렸다 — 로더 · 우선순위 · **`signals.weights`의 비(非)옵션 통로**. 셋째가 있는 이유는 그것이 CLI 옵션이 아니어서 click을 거치지 않기 때문이고, 앞의 둘만 만들면 FR-8.4는 닫히지만 FR-6.1의 빈칸은 그대로 남는다
- **`cuesift.yaml`로 모든 옵션을 지정할 수 있다 — `--config`와 자동 탐색** (FR-8.4, WBS WP6 나머지). `cuesift --config ./ci.yaml translate ep01.ko.srt`가 파일의 값을 읽고, 경로를 주지 않아도 현재 디렉터리의 `./cuesift.yaml`을 자동으로 읽는다. 신설 패키지는 `src/cuesift/config/`(`schema.py`·`loader.py`)이고 `cli.py`는 `ctx.default_map`에 싣기만 한다 — **병합 코드를 쓰지 않았다**(설계 D1). **`schema.py`의 매핑표가 단일 출처다**(§4.1): 허용 키 목록을 따로 두면 "허용은 되는데 아무 데도 가지 않는 키"가 생기고 그것은 조용히 무시되는 설정이라, 허용 키를 매핑표에서 파생시킨다. 우선순위는 **CLI 인자 > 환경변수 > `cuesift.yaml` > 기본값**이다(D3) — `_resolve_llm`의 `base_url or os.environ.get(...)`이 설정 값을 `or`의 왼쪽에서 참으로 만들어 환경변수를 이기던 것을 `ctx.get_parameter_source`로 막았다. **이 결함은 테스트 없이 절대 드러나지 않는다**: 어느 쪽이 이기든 값은 나오고 종료 코드는 0이다. 테스트 1379 → **1480**건(구현 1475 + 문서 게이트 5), 커버리지 **99%**. [설계 스펙](docs/superpowers/specs/2026-08-28-config-file-design.md)
- **모르는 키는 종료 코드 2로 거부하고 가까운 키를 제시한다** (설계 D4). `review_budgt` 오타 하나가 조용히 무시되면 사용자는 예산 10%로 검수됐다고 믿는데 트리아지가 아예 안 돌고 **종료 코드는 0이다** — 이 저장소가 1급으로 금지한 "검사하지 않고 통과하는 게이트"다. 후보 제시는 표준 라이브러리 `difflib.get_close_matches`로 하며 의존성을 늘리지 않는다. 메시지에 em dash(U+2014)를 쓰지 않는다 — cp949가 그것을 인코딩하지 못해 리다이렉트 시 종료 코드가 2에서 1로 바뀐 실측이 `spec/profile.py`에 남아 있고, 1은 이 저장소에서 "규격 위반 발견"이다
- **자동 탐색은 현재 디렉터리 한 칸뿐이다 — 상위로 올라가지 않는다** (설계 D2). 상위를 읽으면 사용자가 존재를 모르는 파일이 검수 기준(hard fail 임계·신호 가중치)을 바꾸고, 그것은 README 최상단 배수의 근거인 Recall@Budget을 조용히 오염시킨다. 설정을 적용하면 **출처 한 줄이 stderr에 나간다**(D7) — click의 오류 메시지가 `Invalid value for '--review-format'`이라, 그 옵션을 친 적 없는 사용자가 명령줄을 노려보게 되는 오진을 막는다
- **신호 가중치를 사용자가 설정할 수 있다 — FR-6.1의 빈칸이 채워졌다** (설계 D6·D9). `signals.weights`는 CLI 옵션이 아니라 `ctx.obj`로 가서 `fuse()` 호출 **3곳**(`cli.py` 1 · `tier1.py` 2)에 전부 내려간다. **`tier1.py`의 둘을 빠뜨리면 `--tier1` 유무로 순위가 달라지고**, 더 나쁜 것은 재점수 쪽이다 — 사용자 가중치로 고른 후보를 기본 가중치로 다시 세우게 되어 **가중치를 설정한 사용자에게만** 결과가 어긋난다. 키는 축약명이 아니라 `review.json`의 `signals[].name`과 같은 **실제 신호 이름 10종**이다 — 축약명 4개는 `struct.*` 5종을 아예 가리키지 못했고, 그대로 뒀다면 사용자가 리포트와 설정에서 서로 다른 어휘를 배워야 했다. 부분 지정을 허용한다(명시하지 않은 신호는 1.0). **`DEFAULT_WEIGHTS`는 여전히 전부 1.0이고 튜닝하지 않는다** — 사용자가 바꿀 수 있는 것과 저장소가 맞추는 것은 다르며, `bench/`는 설정 파일을 읽지 않으므로 README의 배수는 기본 가중치 수치다(§11 R3)
- **매핑표가 CLI 옵션 집합과 어긋나면 테스트가 죽는다** (설계 R5). `typer.main.get_command`에서 파라미터 이름을 뽑아 위치인자를 뺀 집합이 매핑표의 대상 집합과 **정확히 같은지** 본다 — 부분집합이 아니라 상등이다. 한쪽 방향만 보면 매핑표에 남은 죽은 행을 못 잡는다. 사람이 표를 갱신하는 것을 잊는 것은 확실히 일어나고, 잊으면 "설정 파일로 모든 옵션을 지정할 수 있다"가 조용히 거짓이 된다
- **요구사항정의서 §8.2의 예시 블록을 테스트가 실제로 로드한다** (`tests/test_docs_config_example.py`). 문서에서 `yaml` 코드 블록을 뽑아 `load_config()`에 먹인다. 문서가 표류하면 D4가 **즉시 하드 에러**를 내는데 지금까지 아무도 검사하지 않았고, 실제로 개명 2건과 폐기 1건이 문서에 남아 있었다. 게이트를 실패시켜 확인했다 — §8.2에 `consistency_n`을 되돌려 넣으면 `모르는 키 'signals.tier1.consistency_n'. 가까운 키: signals.tier1.samples`로 죽는다
- **v0.1 완료 개수가 35 → 36으로 올랐다** (`35 + FR-8.4`). **함께 닫힌 FR은 없다.** 요구사항정의서 §8.2를 FR-6.1(`signals.weights`)과 FR-4.3(`signals.tier1.max_ratio`)이 이름을 걸어 지목하고 있어 "설정 파일이 열리면 둘도 닫힌다"로 읽혔지만, 착수 시점에 **둘 다 이미 ✅**였다 — FR-6.1은 수식어라 판정을 좌우하지 않았고 FR-4.3은 `--tier1-max-ratio`로 축 1이 먼저 닫혀 있었다. **지목받는 것과 막혀 있는 것은 다르다**
- **설계 스펙 — 진행 표시와 비대화형 감지** (`580d3bb`, FR-8.5, WBS WP6 나머지). `docs/superpowers/specs/2026-08-29-progress-display-design.md`. **착수 조사가 범위를 두 배로 늘렸다** — `grep -rn "callback\|on_progress\|progress" src/cuesift/translate/ src/cuesift/tier1.py`가 **0건**을 냈다. 진행 표시는 렌더러 하나가 아니라 **라이브러리에 관측 지점을 뚫는 일**이 절반이고, 이는 FR-7.3에서 `Span`을 채우는 코드가 0건이었던 것과 같은 구조다. 이음매는 `translate_segments`와 `collect_tier1` **둘뿐**이며 `triage_with_tier1`은 전달만 한다(D1) — `warn: Callable[[str], None]`이라는 선례가 이미 `tier1.py`에 있어 새 패턴을 만들지 않았다. **Tier 1의 분모는 후보 수 × 수집기 수다**(D4): `collect_tier1`이 `for name → for seg` 이중 루프라 후보 수를 분모로 쓰면 수집기가 2종이 되는 날 200%가 찍히는데, 오늘은 수집기가 하나뿐이라 **틀린 정의를 골라도 전 스위트가 통과한다** — 가짜 수집기를 등록하는 테스트가 이 결정을 코드에 고정한다. 렌더러는 `rich`를 쓰지 않고 자체 구현한다(D6) — rich는 typer의 전이 의존이고, 이 리포에는 rich가 `FORCE_COLOR`로 **비TTY인 CI에서 색을 켜** `--help`를 깨뜨린 실측 전례가 있다. **진행 표시는 stderr에 커서 위치라는 상태를 만든다**(§4.3 ②) — 그래서 `_echo(err=True)`가 쓰기 전에 진행 줄을 지우는 계약(D11)이 필요하고, 이것이 이 작업을 bounded가 아니라 architectural로 분류한 근거다. 쓰기 실패는 리포터를 영구 비활성화하고 예외를 전파하지 않는다(D10) — 닫힌 파이프에서 예외가 새면 `_TolerantOutput`이 지켜 온 종료 코드 계약이 깨진다. 게이트 영향은 CLI 옵션 23 → **24**, `BINDINGS` 1행 추가(`output.progress`), 요구사항정의서 §8.2 예시에 키 추가다

### Changed

- **`glossary.miss` 신호의 `spans`가 원문 구간으로 채워진다 — 스키마는 그대로이고 값이 바뀐다** (FR-7.3, FR-3.7). 이전에는 **언제나 빈 튜플**이었으므로 `review.json`의 `signals[].spans`가 항상 `[]`였고, 지금은 위반 용어가 원문에서 차지하는 구간이 `side: "source"`로 실린다(번역문에 대응어가 **없다**는 판정이라 칠할 곳이 원문뿐이다). **필드 추가가 아니라서 스키마 diff에 나타나지 않는다** — `[]`를 "구간 없음"으로 해석하도록 짠 소비자는 코드 변경 없이 동작이 바뀐다. 배열 순서는 용어집 등재 순이 아니라 **원문 위치 오름차순**이다(NFR-3 · 설계 D9 — 같은 입력이 같은 파일을 내야 한다). **구간은 겹치거나 중복될 수 있고 그것이 의도다**: 용어집에 `오픈`·`오픈소스`가 함께 등재되고 둘 다 위반이면 `(5, 9)`와 `(7, 9)`가 **함께** 실리고, 같은 `source`가 두 번 등재되면 **동일 구간이 2개** 실린다. 여기서 dedup하지 않는 이유는 `spans`가 판정(`detail.terms`)과 1:1로 대응해야 하기 때문이다 — `API`·`api`가 둘 다 등재되고 텍스트에 `API`가 하나면 terms 2 · spans 1이 되어 "위반으로 잡힌 용어는 반드시 위치를 갖는다"는 계약이 깨진다. **겹침은 HTML 렌더러(FR-7.3 `split_spans`)가 평평하게 쪼개어 흡수한다.** JSON을 직접 읽는 소비자는 조각을 이어붙일 때 **중복 렌더에 주의해야 한다** — 구간을 그대로 감싸면 같은 글자가 두 번 나온다
- **`struct.number_missing` 신호의 `spans`가 원문 숫자 구간으로 채워진다 — 값으로는 위치를 되찾을 수 없어서 매치 자리에서 함께 낸다** (FR-7.3, FR-3.4). `_numbers()`는 숫자를 **추출한 뒤** NFKC 정규화와 천 단위 구분자 제거를 하므로 `detail.missing`의 값과 원문 표기가 다르다(`５０` → `50`, `1,000` → `1000`). 렌더러가 값으로 `str.find`를 걸면 **-1이 돌아오고 예외는 나지 않는다** — 하이라이트만 조용히 비고 검수자는 칠해지지 않은 것을 "문제 없음"으로 읽는다. 그래서 `_number_matches()`를 신설해 정규식 매치가 이미 갖고 있는 오프셋을 값과 함께 반환하고, **`_numbers()`를 그 위에 얹어 추출 규칙을 하나로 유지한다** — 둘로 갈리면 판정한 숫자와 칠하는 숫자가 어긋난다. 구간은 `side: "source"`이고(누락이라 번역문에는 칠할 것이 없다) **누락된 것만** 덮는다 — 원문 `3시 15분 20초` / 번역문 `3 minutes 15`면 span은 `20` 하나다. **후행 쉼표는 구간에서 잘라낸다**: `_NUMBER`의 `[\d,]*`가 `"3, 4"`에서 `"3,"`까지 먹는데 값은 쉼표를 지워 `"3"`이 되므로, 자르지 않으면 `detail`이 말하는 것과 칠해지는 것이 어긋나 위험 구간에 문장 부호가 섞인다. **판정(`is None`·`hard_fail`·`score`·`detail`)은 한 건도 바뀌지 않았다** — 변경 전후 `collect()`에 큐레이트 999쌍과 퍼즈 2만 건을 흘려 불일치 0건, `_numbers()` 반환값도 21,574 입력에서 불일치 0건으로 확인했다. 이 신호는 hard fail이라 오탐이 하나만 늘어도 검수 예산을 우회해 §9.1의 배수를 파괴하므로(FR-6.2) 값 계산을 건드리지 않는 것이 이 변경의 제약이었다. **전각 콤마(`１，２３４` → `['1', '234']`)는 여전히 갈라진다** — NFKC가 추출 뒤에 오므로 `[\d,]`가 ASCII 쉼표만 보기 때문이고, 이것은 이번 변경 이전부터 있던 hard fail 오탐이라 판정을 바꾸는 별도 사이클로 남긴다
- **요구사항정의서 §8.4 `review.json` 스키마를 정정했다 — 명세가 바뀐 것이지 구현이 명세를 벗어난 것이 아니다.** 초판은 0으로 채운 골격이었고 넷을 고쳤다. ① **`cost.estimated_usd`를 키째 제거** — NFR-2가 통화 환산을 v0.1 범위 밖으로 못 박았고, `0.0`은 출처 없는 수치이며 `null`은 "언젠가 채워질 자리"로 읽혀 **명시적 범위 결정을 숨긴다**(§11 R8). ② **`cost`를 `prompt_tokens`·`completion_tokens`·`calls`·`includes` 넷으로** — 화면이 이미 앞의 셋을 내므로 파일이 하나로 뭉치면 같은 실행의 두 산출물이 다른 해상도를 갖는다. **`includes`가 이 블록의 요점이다**: 지금 Tier 1은 CLI에서 돌지 않아 번역 토큰이 곧 전체이지만, WP8b가 `--tier1-*`를 붙이는 순간 같은 코드가 **과소 보고를 시작하는데 그것을 알릴 수단이 없다** — `["translation"]`에 `"tier1"`이 없다는 사실이 그 부재를 파일에서 드러낸다. ③ **`summary`에 재현성 필드 5개**(`source_lang`·`target_lang`·`profile`·`policy.kind`·`policy.value`) — 초판은 **파일만 보고 "무엇을 어느 규격으로 어떤 정책에서 걸렀나"를 알 수 없었다.** 리포트는 옮겨지고 첨부되고 며칠 뒤에 열린다. 프로파일 이름이 값 검증의 유일한 수단이라는 것은 이미 실측됐다(`profiles[target] = load_builtin("ko")` 변이에서 **전 스위트가 통과했다**). `policy.value`는 **언제나 비율이지 퍼센트가 아니다** — 화면의 `_format_ratio`가 `× 100`을 요구하는 것과 반대 방향이라 퍼센트를 넣으면 소비자가 100배 틀린다. ④ **세그먼트 수를 셋으로 쪼갬**(`total_segments`·`triaged_segments`·`excluded_failures`) — `total = triaged + excluded`가 **파일 안에서 검산되고** `review_ratio`의 분모가 `triaged_segments`임이 드러난다. 하나로 두면 `total_segments`가 "트랙 전체"로 읽히며 배수의 분모가 조용히 틀리는데, 그것이 §9.1 배수의 분모다
- **`_run_triage`의 반환 타입이 `list[str]` → `TriageOutcome`으로 바뀌었다** (설계 D8). 내부 함수라 공개 API 파기는 아니지만, 화면 포매터(`_format_triage_summary`)도 이 객체를 받도록 함께 바뀌었다. 이전에는 요약 문자열 목록만 돌아와 **세그먼트 단위 정보가 함수 경계에서 소멸**했고, 그래서 JSON이 수치를 다시 셌다면 화면과 파일이 갈라질 수 있었다
- **종료 코드 `70`의 발신처가 둘이 됐다.** 이전에는 `transcribe`(미구현) 하나였고 지금은 **검수 리포트 직렬화 실패**가 더해졌다 — 따라서 CI는 "아직 안 만든 명령"과 "리포트 직렬화가 죽었다"를 **종료 코드만으로는 구별하지 못한다**(구별이 필요하면 stderr를 본다. 예외 타입명을 병기한다). 새 코드를 만들지 않은 것은 설계 §8.1의 순서 1번("기존 코드 중 내부 결함에 해당하는 것이 있으면 그것을 쓴다")을 따른 것이다. **1이나 2로 보내지 않는 것이 요점이다** — 1은 이 CLI에서 "규격 위반 발견"이라 미처리 traceback이 1이 되면 **우리 쪽 결함이 자막 결함으로 오보되고** 사용자는 멀쩡한 자막을 고치려 든다. 66도 아니다(66은 "사용자가 준 파일이 틀렸다"). 쓰기 실패(`OSError`)는 그대로 **66**이다 — 디스크 상태의 문제이고 `write_subtitle`과 같은 등급이다. 상수 이름 `EXIT_NOT_IMPLEMENTED`는 이제 발신처의 절반만 말하므로 `cli.py`에 그 사실을 주석으로 남겼다
- **`--review-budget`의 하위 호환이 두 자리에서 깨진다 — 의도된 것이지만 예고 없는 파기다.** 이전 버전은 이 옵션을 **경고만 내고 무시**했으므로 무엇을 주든 종료 코드 0으로 번역 산출물이 나왔다. 지금은 값을 검증하고 실행을 중단시킬 수 있다.

  | 호출 | 이전 | 지금 |
  | --- | --- | --- |
  | `--review-budget abc` | 경고 · exit 0 · **번역 산출** | **exit 2 · 산출 없음** |
  | `--to th --review-budget 10%` | 경고 · exit 0 · th 번역 산출 | **exit 2 · 산출 없음** (`th` 프로파일이 없고 다른 대상 언어도 없다) |

  둘 다 설계 D5·D7의 의도된 결과다 — 읽을 수 없는 값을 조용히 무시하면 사용자는 예산이 적용된 줄 알고, 트리아지가 한 언어도 못 돌았는데 0으로 끝나면 CI가 "트리아지했다"로 읽는다. **그럼에도 예고 없는 파기인 것은 맞다**: `README.md`가 이 플래그를 "지정해도 경고만 내고 무시됩니다"라고 명시했고 그 문장을 읽고 스크립트를 짠 사용자가 있을 수 있다. 그래서 README에 이 두 줄을 **파기 목록으로 따로** 실었다 — 문서가 no-op이라 말한 플래그가 번역 산출물을 통째로 없애는 상태를 남기지 않는다
- **v0.1 FR 완료 개수를 32에서 30으로 정정했다 (76% → 71%) — 기능이 빠진 것이 아니라 세는 법이 틀려 있었다.** `docs/WBS.md`가 FR-6.3·6.4를 **라이브러리만 있는 상태로 WP1에서 ✅로 세고 있었다.** FR-6.3은 "트리아지 정책을 **지정할 수 있다**"인데 착수 시점에 CLI에서 아무것도 도달할 수 없었고, FR-6.4는 `SegmentRisk.reasons`를 세그먼트마다 기록하지만 사용자에게 가는 통로(FR-7.2 `review.json`)가 미구현이다. 이번 작업은 FR을 하나도 잃지 않았고 오히려 FR-6.3의 CLI 절반을 새로 냈다 — 내려간 2는 **이전 숫자가 실제보다 높았다**는 뜻이다. FR-5.3이 같은 함정에 걸렸을 때 이미 배운 것("라이브러리에 있음 ≠ 사용자가 쓸 수 있음")을 이 저장소가 **세 번째로** 다시 밟은 자리다
- **`docs/WBS.md`에 담당 WP가 없는 FR이 있었다 — FR-6.3의 CLI 절반이다.** WP5는 FR-7.1~7.5만, WP6은 FR-8.1~8.5만 담당하는데 `cli.py`의 옛 경고는 "아직 구현되지 않았습니다 (WP5)"라고 적었다 — **WP5의 범위에 그런 항목이 없다.** 그 경고가 가리킨 WP가 실재하지 않았다. 담당을 **WP6**으로 명시하고(FR-8 계열과 성격이 같은 배선 작업이다), 의존 그래프의 `WP6 --> WP8b` 화살표에 **"FR-6.3 CLI → FR-4.3 CLI"** 라벨을 붙였다 — `triage_with_tier1()`이 `budget_ratio`를 **기본값 없는 필수 인자**로 요구하므로 예산 배선 없이 Tier 1만 얹을 수 없다. **구조적 원인은 WBS가 FR 번호로 WP를 나눈다는 것이다**: 라이브러리와 CLI 두 계층에 걸친 FR은 WP1에서 라이브러리 절반만 만들고 ✅로 닫히며 CLI 절반이 미아가 된다(FR-5.3·FR-4.3·FR-6.3, 세 번째다). "남은 1순위는 WP8b 하나다"라는 서술도 정정했다 — 그 문장을 쓴 시점에 WP8b의 선행 하나가 이 문서에 **없었고**, 없는 항목은 1순위 경쟁에 나오지 않으므로 **문서가 자기 결론이 틀렸다는 것을 감지할 수단까지 함께 잃고 있었다**
- **`README.md`가 거짓을 말하고 있었다** — "`--review-budget`은 아직 구현되지 않았습니다(WP5) — 지정해도 경고만 내고 무시됩니다". 그 한 줄을 지우는 것으로는 부족해 `#### 검수 트리아지` 절을 새로 썼다: 두 옵션의 실제 동작과 상호배타, 비율만 받는다는 것과 `0`·`1`의 의미, 프로파일 자동 유도와 부분 적용/전량 실패의 종료 코드, 요약 출력 실물, 번역 실패분 제외의 근거, 그리고 위의 **하위 호환 파기 2건**이다. 종료 코드 표의 `2` 행에도 트리아지 사유 3종을 더했다. **이 절이 필요한 이유는 이 프로젝트의 목적이 여기 있기 때문이다** — README가 "번역기가 아니라 트리아지 엔진"이라 선언하면서 정작 트리아지를 켜는 법을 "구현되지 않았습니다"라고 적고 있었다
- **상태 칸 본문에서 판정 기호를 없앴다 — 개수를 기계적으로 검산할 수 있어야 한다** (최종 리뷰 I1). FR-6.1·FR-6.4의 상태 칸이 본문에서 다른 FR을 가리키며 `✅`·`🟡`를 쓰고 있었고, 그 결과 `✅`를 세면 **6이 아니라 7**이 나왔다(실측). 판정 기호는 **칸의 맨 앞 하나**만 남기고 본문에서는 낱말로 쓴다("FR-6.1은 ✅다" → "완료 판정이다"). 13개 전부를 훑어 본문 기호 0건을 확인했고, 지금은 `✅ 6 · 🟡 4 · ⬜ 3`이 스크립트로 재현된다. 이 저장소가 "게이트 수치를 그대로 적는다"를 규율로 두는 것과 같은 이유다 — **검산할 수 없는 숫자는 주장이지 수치가 아니다**
- **`--to en-US`가 하위 호환 파기 목록에서 빠져 있었다** (최종 리뷰 I5). 실측: `--to en-US` 단독은 지금도 exit 0인데 `--review-budget`을 붙이면 **exit 2 + 산출물 없음**이다. 프로파일 이름을 **언어 태그 전체**로 찾으므로 `en-US`는 `en-us.yaml`을 찾다 없어서 건너뛰고, 그것이 유일한 대상 언어면 "적용할 수 있는 대상 언어가 없다"가 된다. README의 파기 목록을 표로 바꾸고 이 행을 더했다. **서브태그 폴백(`en-US` → `en`)은 만들지 않았다** — `ted-en` 같은 내장 이름과의 충돌 규칙을 함께 정해야 하는 설계 판단이라 후속으로 미루고, 현재 동작과 그 이유만 문서에 적었다
- **절 번호 자기모순 2곳** (최종 리뷰 I3). `docs/WBS.md`가 한 줄 아래에서 자신을 반박했고(`§5.6·§5.7` vs `§5.4·§5.6·§5.7`), 트리아지 CLI 설계 스펙 §11도 같은 자리에서 어긋났다 — Ruling 17이 절 번호를 고칠 때 **계획서만 고치고 설계 스펙을 놓쳤다.** **두 곳의 올바른 값이 서로 다르다** — WBS는 상태 열이 있는 절 전체를 가리키므로 `§5.4·§5.6·§5.7`이고, 설계 스펙 §11은 **이 작업이 열을 더한 두 절**을 가리키므로 `§5.6·§5.7`이다(§5.4는 WP8a가 이미 넣었다). 각각 그 값으로 맞췄다. **FR 번호는 안정적이지만 절 번호는 문서 구조에 종속된다**는 것을 이 브랜치에서 두 번 밟았다
- **`_risk_free` 독스트링이 틀린 기제를 적고 있었다** (`tests/test_cli_triage.py`, 최종 리뷰 triage #3). 두 가지가 사실이 아니었다. ① "숫자를 보존한다"의 근거로 `large.srt`의 원문을 들었으나 이 헬퍼를 쓰는 두 테스트의 픽스처는 **`ten_cues.srt`이고 거기엔 숫자가 하나도 없다** — 결과는 10큐 전부 `"Line  of the talk"`라는 한 문자열이다(실측). ② `length.ratio`가 잠잠한 이유를 "MAD도 평균절대편차도 0"이라 적었으나 **MAD는 0.1518이다.** 실제 기제는 원문 폭이 7·8 두 가지뿐이라 비율이 2.4286·2.125 둘로 갈리고 **모든 편차가 MAD와 정확히 같아져** z가 전 큐에서 `_MAD_SCALE`(0.6745)에 고정되는 것이다 — 임계 3.5의 약 1/5로, 실제 여유가 독스트링이 말하던 것보다 크다. 이 저장소는 주석에 **"이 값이 아니면 무엇이 깨지는가"**를 적으므로 틀린 기제는 다음 사람이 잘못된 전제로 데이터를 바꾸게 한다 — 원문 길이가 두 가지를 넘으면 z가 흩어지고 하나라도 3.5를 넘는 순간 헬퍼 이름이 거짓이 되어 정책 테스트가 **조용히** 오염된다는 것까지 적었다
- **`docs/요구사항정의서.md` §5.6·§5.7에 상태 열을 추가했다.** 두 표는 `ID │ 요구사항 │ 우선순위` 셋뿐이라 **갱신이 아니라 열 추가**였다 — §5.4(Tier 1)가 이미 쓰는 표기(✅ / 🟡 / ⬜)를 따랐다. FR-6.1·6.2·6.5 ✅, FR-6.3 🟡("상위 K개"가 남는다), FR-6.4 🟡(집계만), FR-7.1·7.5 ✅, FR-7.2·7.3 ⬜, FR-7.4 🟡(소요 토큰이 남는다 — 번역 토큰은 이미 나오지만 Tier 1의 `TranslationResult.usage`를 올려 보낼 통로가 없다). **역할을 한정해 적는다 — "FR 상태의 단일 출처는 요구사항정의서"는 사실이 아니었다**: 상태 열은 §5.4·§5.6·§5.7 **세 절에만** 있고 실측 45개 FR 행 중 13행만 상태를 갖는다(✅ 6 · 🟡 4 · ⬜ 3). 그러므로 요구사항정의서는 **상태 열이 있는 FR의 판정과 근거**를 내고, **v0.1 전수 완료 개수는 WBS가 낸다.** 빈칸을 "미완료"로 읽으면 개수가 실제보다 낮게 나오므로 그 사실을 §5 머리에 못 박았다 — **정정이 설계 스펙에만 있으면 §5의 빈칸을 만나는 독자는 도달하지 못한다**
- **완료(✅) 판정 기준을 요구사항정의서 §0.1에 명시했다.**
  > **이 항목이 인용하는 수치(29·31·30) · FR 상태 · 아래 본문에 복제된 판정 규칙 전문은 전부 그 시점의 값이다.
  > 현재 값도 현재 규칙도 요구사항정의서 §0.1이 낸다.**
  > 변경 로그는 "무엇이 왜 바뀌었나"를 적는 자리이지 규칙의 사본을 두는 자리가 아니다 — 이 항목이 규칙 전문을
  > 복제한 탓에 `HANDOFF.md`가 못 박은 **"판정 규칙은 세 곳에 복제돼 있으니 셋을 함께 고친다"** 에서
  > **셋째만 안 고쳐진 상태**가 실제로 났다(Task 8 리뷰 I2). 지금 수치를 현재값으로 갈아 끼우지 **않는** 것은,
  > 과거 항목이 현재 상태를 흉내 내면 이력이 이력이기를 그만두고 다음에 또 갈라지기 때문이다.
  > 대신 이 줄로 **셋째 출처이기를 그만둔다.** 아래 본문은 그대로 둔다.
  >
  > 이후 실제로 갈라진 자리 둘: ① 아래가 "수식어 규칙이 없으면 **하나 낮게(29)**"라 적은 것은 FR-7.1을 빠뜨린
  > 셈이라 **둘 낮게(30)** 로 정정됐다(당시에도 이미 오기였다) ② 아래가 FR-6.4를 🟡로 적지만 FR-7.2(`review.json`)가
  > 통로를 열어 **✅**가 됐고, 그래서 "수혜자 규칙이 없으면 하나 높게 센다"는 예시 자체가 성립하지 않게 됐다.
  > 현재 기준 개수는 **32**이고 흔들리는 방향은 30(수식어 규칙 부재)·34(축 2 부재)다.

  없으면 같은 문서가 잣대를 두 개 쓴다 — FR-4.3은 §8.2 `cuesift.yaml`의 `signals.tier1.max_ratio`가 미구현이라 🟡인데 FR-6.1은 **같은 §8.2 블록**의 `signals.weights`가 미구현인 채로 ✅였고, 규칙이 없으면 다음 사람이 FR-6.1을 🟡로 내려 개수를 하나 낮게 센다(리뷰가 지적한 실제 가능성이다). 규칙은 **두 축**이다 — **✅ = 층 도달 + 완전 · 🟡 = 층은 도달했으나 불완전 또는 층이 부족 · ⬜ = 아무것도 없음**. **축 1(층)**: 어느 층인지는 **목적절의 수혜자가 어디에 있는가**가 가른다 — 수혜자가 **파이프라인 안**이면(다음 단계가 그 값을 쓰거나, 라이브러리를 직접 호출하는 개발자가 그 인터페이스를 쓴다) 라이브러리로 충족되고, **라이브러리 밖의 사람**이면(검수자·CLI 사용자·CI) 사용자 통로(CLI·설정 파일·리포트)까지 있어야 한다. **뒤쪽이 곧 "미완료"는 아니다** — FR-7.1·FR-7.5·FR-5.3은 통로가 완료 조건인데 그 통로가 실제로 있어 완료다. **축 2(완전성)**: 그 층에서 FR이 말한 것을 **전부** 하는가 — FR-6.3은 CLI에 도달했으나 "상위 K개"가 없고 FR-7.4는 요약이 나오지만 소요 토큰이 없어 둘 다 부분 충족이다. **축 2가 보는 것은 본문이 열거한 항목이고 수식어·괄호로 붙은 속성은 두 축 모두에서 판정을 좌우하지 않는다**(FR-7.1의 "(…변환 지정 가능)"이 미구현이지만 판정 불변 — 상태 칸에 명시만 한다). **상태 칸에 어느 축이 부족한지 적는다** — "층 미도달"은 배선을, "불완전"은 기능 추가를 부르는 **다른 후속 작업**이다. **어미가 아니라 수혜자의 위치가 가른다** — FR-6.5의 "QE 모델을 꽂을 수 **있게 한다**"와 FR-6.4의 "설명 가능하**게 한다**"는 같은 어미인데 전자는 ✅(통로가 `signals/base.py` 프로토콜 자신이다) 후자는 🟡(검수자는 라이브러리 안에 도달할 수단이 없다)다. 수식어로 붙은 속성(FR-6.1의 "**설정 가능한** 가중치")은 별도 FR로 분리되지 않는 한 판정을 좌우하지 않되, **미구현 통로는 상태 칸에 명시한다** — 판정을 바꾸지 않는 것과 감추는 것은 다르다. **이 규칙은 네 번 깨졌고 그때마다 고쳤다 — "상태 13개 전수 대조"가 네 번 다 찾아냈다.** ① 1판(동사 목록만)은 **FR-6.4**의 "기록한다"를 라이브러리 동사로 읽어 규칙이 완료를 내는데 표기는 부분 충족이었다 — 닫으려던 "잣대가 두 개"가 FR-6.1/FR-6.4 쌍에서 재현된 것이다. ② 2판(수혜자를 "시스템 / 사람"으로)은 **FR-6.5**의 "꽂을 수 **있게 한다**"에서 어긋났다 — 수혜자가 사람이지만 그 사람은 라이브러리를 직접 호출하는 개발자다. ③ 3판(수혜자의 위치)은 **FR-6.3·FR-7.4**에서 어긋났다 — 층은 도달했는데 부분 충족이라, 규칙이 층만 다루고 완전성을 안 다뤘다. ④ 4판 초안(두 축)은 **FR-7.1**에서 어긋났다 — 괄호의 "변환 지정 가능"이 미구현이라 축 2가 부분 충족을 내는데 표기는 완료다. 축 2의 범위를 "본문 열거"로 한정한 뒤에야 13개 전부와 일치했다. **규칙만 읽으면 네 판 다 그럴듯해 보인다 — 대조해야 보인다.** **개수가 틀리는 방향도 둘이다**: 수식어 규칙이 없으면 29, 수혜자 규칙이 없으면 31. 함께 있어야 30이 유지된다. **대조해서 30이 움직이면 표기가 아니라 규칙을 의심한다** — 네 번 다 틀린 쪽은 규칙이었다
- **`pytest`의 기본 수집이 `-m "not live"`로 좁혀졌다** (`pyproject.toml`). 이 줄이 들어간 뒤로 `pytest -q`의 마지막 줄은 `N passed, M deselected` 형태이므로 **수치를 읽는 법이 바뀌었다** — `passed`만 보면 live 테스트가 통째로 사라진 것을 눈치채지 못한다. 변이 실측(2026-08-16)이 이 게이트의 비대칭을 드러냈다: 마커 **미등록**은 `--strict-markers` 때문에 `Interrupted: 1 error during collection`으로 exit 2를 내고 **나머지 858개가 실행조차 안 되는** 반면, `-m "not live"`나 `pytestmark` **삭제**는 키 없는 환경에서 `deselected`가 `skipped`로 바뀔 뿐 **아무 테스트도 죽지 않는다.** 조용한 쪽이 위험하다 — 키가 설정된 개발자 머신에서 평범한 `pytest` 한 줄이 유료 엔드포인트를 치기 시작하고 CI는 끝까지 초록이다. 그래서 실행 결과가 아니라 **설정 그 자체**를 단언한다. 검사 자리는 둘로 나뉜다 — ini 파일 정체·`--strict-markers`·`addopts`의 `-m` 개수는 `tests/conftest.py`의 `pytest_configure`가 보고, `markers` 등록과 live 파일들의 `pytestmark`는 테스트가 본다. **설정 검사를 테스트에 두면 자기를 무력화하는 변이에 같이 쓸려 나가기 때문이다** — `addopts` 끝에 `-m live`를 덧붙이면 감시자 자신이 832개와 함께 deselect되고 exit 0으로 초록이 난다(**훅을 넣기 전 실측**이라 지금은 재현되지 않는다). 훅은 수집 전에 돌아 deselect의 대상이 아니라서 그 자리만이 안전하다. **방어는 결국 세 겹이 됐고 각 겹이 서로 다른 것에 무력하다** — ① `conftest` 모듈 최상위(`_check_on_import`)는 임포트만으로 돌아 **개명할 이름도 없고 deselect 대상도 아니지만** `config`가 없어 **pytest가 어느 ini를 골랐는지 모른다**, ② `pytest_configure` 훅은 그것을 보지만 **함수 개명으로 배선이 끊긴다**, ③ 테스트는 ①②의 배선과 로직에 더해 `markers` 등록·live 파일들의 `pytestmark`를 보지만 **deselect되면 안 돈다**. **①과 ②는 판정 술어를 공유하지 않는다** — 한때 공유 헬퍼로 묶었더니 그 함수 하나를 무력화하는 것만으로 두 층이 **동시에** 눈이 멀었고, 거기에 `-m live`를 덧붙이면 ③은 deselect되어 **0건이 죽었다**(실측). `_REQUIRED`가 `__all__`을 일부러 중복하는 것과 같은 논거다 — 검사와 검사 대상이 같은 출처를 쓰면 그것은 검사가 아니다. **남은 구멍도 적어 둔다**: 훅 개명 + `pytest.ini` 추가 조합은 ①이 pyproject 원문만 보므로 지나간다(실측). **세 겹이 된 이유는 변이 하나가 정확히 그 틈으로 들어갔기 때문이다** — 훅 함수에서 `pytest_` 접두사를 떼면 pluggy가 등록하지 않는데 `839 passed`로 조용히 지나가고(접두사를 **유지하는** 개명은 pluggy가 exit 3으로 막지만 **버리면** 그 검증을 지나간다), 거기에 `-m live` 덧붙이기를 더하면 등록을 감시하는 테스트마저 함께 deselect되어 `1 skipped, 839 deselected` exit 0이 난다. live 파일 판정도 파일명에서 **소스에 엔드포인트 환경변수가 있는가**로 바꿨다 — `test_*live*.py` 규칙은 파일을 `test_ollama_roundtrip.py`로 이름 짓는 것만으로 뚫렸고(`840 passed`, 0건 사망), **WP7b가 추가할 live 테스트의 이름이 `live`를 포함할 보장이 없다.** 우회 아홉 가지가 **각각 수정 전에 0건을 죽이는 것을 확인한 뒤** 막았다
- **위험도 융합을 가중 평균에서 noisy-or로** (`78921d4`, FR-6.1). `1 - ∏(1 - sᵢ)^wᵢ`. 가중 평균은 **문제를 하나 더 찾을 때 위험도를 내렸다** — 규격 위반(1.0) 하나는 1.0인데 용어 위반(0.5)이 붙으면 0.75가 되어 검수 큐에서 밀려난다. 벤치가 그 비용을 쟀고(오라클 대비 달성률이 예산 10%에서만 69.6%로 꺼짐), 그 구간이 기본 운영점이다. **가중치는 지수로 들어간다** — `(1-s)^w`는 "이 신호를 w번 관측했다"로 읽혀 `w=0`이 ablation의 신호 끄기와 의미가 일치한다. 점수 스케일 가중(`1 - ∏(1 - w·s)`)은 `w·s > 1`에서 곱의 부호가 뒤집혀 `w ≤ 1` clamp를 강제하고, 그러면 신호 강화를 표현할 수 없다. FR-6.1은 "설정 가능한 가중치"만 요구하고 가중 평균을 명시하지 않으므로 **요구사항 변경이 없다**. 범위 정합성이 이제 `Signal.score ∈ [0,1]`의 생성 시점 검증에 의존한다(밑이 [0,1]이고 지수가 음수가 아니면 곱도 [0,1]) — 가중 평균 시절에는 clamp가 마지막 방어선이었으나 이제 산식 자신이 보장하고 그 보장이 상류 모델을 전제로 한다. D-22(총합 유한성) 검사는 남기되 주석을 정정했다: noisy-or는 곱셈이라 그 실패 모드가 사라졌지만 `inf` 총합은 설정 오타의 신호다. **채택은 재측정 결과에 걸지 않았다** — 벤치 숫자를 보고 융합식을 고르면 그것이 벤치 데이터로 제품을 튜닝하는 것이다(스펙 §1.1)
- **재측정 — 두 변경의 효과를 분리해 귀속했다** (`57bbded`). 시드·코퍼스·트랙 동일(`seed=20260729`). A2와 A1을 각각 커밋한 뒤 2단계로 측정했다 — 두 변경의 효과가 반대 방향일 수 있어 1회 측정은 상쇄되면 "변화 없음"으로 읽힌다

  | 예산 10% | 기준 | +A2 (NFKC) | +A1 (noisy-or) |
  | --- | --- | --- | --- |
  | en-ko Recall | 69.60% | 69.60% | **72.40%** |
  | en-ko 배수 | 6.96x | 6.96x | **7.24x** |
  | ja-ko Recall | 70.60% | 73.20% | **75.20%** |
  | ja-ko 배수 | 7.06x | 7.32x | **7.52x** |
  | hard fail 오탐률 (en/ja) | 0.956% / 0.911% | 0.956% / **0.778%** | 0.956% / 0.778% |
  | `negation` Recall @10% | 1.41% | 1.41% | 1.41% |

  **en-ko가 A2에서 한 자리도 움직이지 않은 것이 진단의 검증이다** — NFKC 결함이 일본어 전각 숫자 현상이라면 en-ko는 변하면 안 된다. `negation`은 전 단계에서 소수점까지 동일해 **결정론적 신호를 어떻게 조합하든 의미 판단에는 닿지 못한다**는 것을 재확인했다(Tier 1 투자 근거 유지). ablation `struct.number_missing`이 두 언어쌍 모두 음수 → 양수로 뒤집혔고(en-ko −0.0240 → +0.0040, ja-ko −0.0140 → +0.0320), en-ko는 A2로 전혀 안 움직였는데 A1만으로 부호가 바뀌었다 — **이 신호가 해로웠던 것은 검출 정확도가 아니라 희석 때문**이었다. **스펙 §7.1의 예측은 빗나갔다**: "noisy-or에서 `length.ratio`의 해악이 악화될 가능성이 실재한다"고 적었으나 실제로는 완화됐다(ja-ko −0.0880 → −0.0680). 가중 평균에서는 오탐이 진짜 신호를 **희석해 끌어내렸고** noisy-or에서는 **자리만 차지한다**
- `bench/report.py`의 hard fail 오탐 원인 목록을 **실제 잔여 한계로 교체** (`1c6473f`). NFKC 정규화가 붙으면서 "검출기가 전각 숫자를 다른 값으로 본다"는 서술이 거짓이 됐다. **지우기만 하면 안 된다** — 오탐은 여전히 남고, 남은 것의 정체를 독자가 귀속할 수 없으면 숫자만 있고 설명이 없는 절이 된다. 한자 수사(`10대` → `十代`)로 갈아 끼웠다: NFKC로 해결되지 않는 진짜 잔여 한계이고(`十分に` ≠ 10분이라 파서를 붙이면 새 오탐이 생긴다), 이전 판이 원인 목록에서 통째로 빠뜨렸던 갈래다. **이전 판의 "ja-ko 31.7%"는 과대평가로 드러났다** — 전각 숫자를 포함한 16건 중 정규화로 해소된 것은 8건이고 나머지 8건은 단위 환산(`80마일` → `1３0キロ`)·표기 체계(`30%` → `３割`)라 전각과 무관했다. **"전각 숫자가 있다"와 "전각 때문에 오탐이다"는 다르다.** 테스트는 한 줄도 고칠 필요가 없었다 — I-2에서 `assert "billion" in md`를 값 연동으로 바꾸면서 "검출기의 알려진 한계"라는 **계약**만 고정해 뒀기 때문이다. `assert "NFKC" in md`로 고정했다면 이 수정과 함께 실패했을 것이다
- `pyproject.toml` — `specs/`를 휠에 `force-include` 한다. `src/cuesift/` 밖이라 그대로 두면 설치본에서 `load_builtin`이 깨진다 (`c79f752`)
- `README.md` 상태 경고 — "요구사항 정의서와 CLI 골격만 있다"는 서술을 실제 상태(Tier 0 엔진 구현, CLI 미배선)로 고쳤다. **갱신**: Tier 0 실측 완료를 반영하고 최상단에 "실측: Recall @ Budget" 절 추가 — 헤드라인 배수는 요청 예산과 실제 검수 비율이 일치하는 첫 지점인 **예산 10%**에서 뽑는다(en-ko 6.96x · ja-ko 7.06x). 더 낮은 예산(1~5%, 8.6x)이 배수는 크지만 hard fail만으로 채워져 실제 검수 비율이 요청 예산보다 훨씬 높아 인용에 부적절하다고 판단했다. 한계 절에 **"이 배수는 의미 오류에는 적용되지 않는다"**를 명시 — `negation`만 보면 Tier 0는 무작위보다 못하다(아래 참고)
- 구현 계획 문서에 **"구현 중 바뀐 결정"** 절 추가 (`e2c7d39`). 리뷰가 걸러낸 결함 17건 중 15건이 계획 자체의 결함이었고, 각각의 원인·결정·근거를 D-1~D-21로 기록했다. 본문 코드 블록은 고치지 않았다 — 최종 코드로 갈아 끼우면 계획이 구현과 같은 것을 두 번 적은 문서가 된다
- `docs/AI_자막검수_오픈소스_비교.md` §5 조사 질문 #2("경량 신호만으로 트리아지가 성립하는가") — 🔬 측정 필요 → **✅ 닫힘**. "미결정 Q4와 같은 실험에서 답이 나온다"던 이전 서술을 정정했다 — Q4(자가일관성 유사도)는 LLM 재번역이 필요한 Tier 1 질문이라 이 벤치의 측정 범위 밖이고, 두 질문은 별개로 닫힌다. §5 한계 목록의 "벤치마크 전용 `specs/ted.yaml`을 분리하기로 했다"는 서술도 실제 구조(언어별 `ted-ko`·`ted-en`·`ted-ja` 세 파일)로 정정
- `docs/요구사항정의서.md` §12 Q4 — 정량 근거 추가·강화. 의미 반전 Recall이 예산 10%에서 1.41%에 그친다는 실측에 이어, **같은 예산의 무작위 기준선(9.61%)보다도 낮다**는 재측정 결과를 반영했다. **Q4 자체는 이 벤치로 닫히지 않는다**는 것을 명시 — Tier 1(자가일관성·역번역)을 구현해야 답할 수 있는 질문이기 때문이다
- **`--fail-on` 값을 `hard\|soft\|never`에서 `hard\|any\|none`으로** (`90c6a7d`, FR-7.5). 요구사항정의서가 단일 진실 원천이므로 문서가 아니라 **코드를 고쳤다.** 마침 v0.1이 단일 등급을 택했으므로 `soft`는 **존재하지 않는 등급을 가리키는 이름**이 되어 더 맞지 않았다. CLI가 배선 전이라 호환 부담이 없었다. **`hard`와 `any`가 v0.1에서 같은 결과를 낸다는 사실을 `--help`와 README에 명시했다** — 조용히 같으면 사용자는 둘이 다르다고 믿는다(설계 §11 R1). 등급을 발명하지 않은 근거는 배정의 출처가 없다는 것이다: 프로파일 수치를 재 보니 `ja`↔`ted-ja`가 CPS 2.50배·줄폭 1.62배로 **두 축 모두 흔들려** 프로파일 독립인 축이 없고, 1차 출처인 Netflix TTSG에 위반 등급 구분이 없다(§11 R8 "출처 없는 수치를 기본값으로 넣지 않음")
- **`--spec`이 `.yaml`/`.yml` 경로를 받는다** (`f16bb0f`, FR-5.3). `load_profile(path)`는 있었으나 **CLI에서 도달할 방법이 없어** 必 요구사항인 "사용자가 규격을 덮어쓸 수 있다"가 닫혀 있었다. **존재 여부가 아니라 확장자로 가른다** — 존재 여부로 가르면 오타 난 파일 경로가 "내장 이름이 없다"는 **틀린 진단**을 받는다(인제스트가 mp4를 `decode` 오류로 보고하지 않으려고 확장자를 먼저 본 것과 같은 판단). `.yml`을 함께 받는 것은 실측으로 정정한 설계 결함이다 — `.yaml`만 가르면 D10이 막으려던 그 틀린 진단이 `.yml` 사용자에게 그대로 난다. **라우팅만 소문자로 본다**: Windows는 파일명 대소문자를 구분하지 않아 `my-spec.YAML`이 정상인 파일명인데 CI의 Linux는 구분하므로 `load_profile`에는 원본을 넘긴다
- **규격 프로파일 로더가 잘못된 내용을 전부 `ValueError`로 정규화한다** (`8845d5a`·`70f5aef`). 값 타입 오류·`bool`·`NaN`·`inf`·표현 범위를 넘는 정수·YAML 문법 오류를 거부한다. **`cli.py`가 예외를 열거하지 않는 것이 이 계약 덕분이다** — 열거는 계약이 아니라 관찰이라 로더가 새 예외를 낼 때마다 뒤처지고, 뒤처진 쪽으로 샌 예외는 종료 코드 1("규격 위반 발견")이 된다. 실제로 그 튜플이 **두 번 넓어지고도 세 번째 누락이 남아 있었다**
- **`check` 출력 헤더의 낱말을 `큐 N개`에서 `검사 큐 N개`로** (`7d3e5f6`). `cue_total`은 필터 **후** 세그먼트 수인데 `#N`은 **원본** 큐 번호라, 주석이 있는 파일에서는 `큐 2개` 아래 `#4`가 찍혀 자기모순으로 읽힌다. **분모를 원본 이벤트 수로 되돌리는 것은 오답이다** — 검사 대상이 아닌 주석·드로잉까지 세면 위반 비율이 과소평가되고 그것은 Recall@Budget 지표를 직접 건드린다. 낱말만 좁혀 모호함을 없앴다. 같은 커밋에서 `profile_name`을 **`profile_label`** 로 바꿨다: `--spec ./our-spec.yaml`인데 그 파일이 `name: ko`면 헤더가 내장 `ko`와 **바이트 단위로 같아져**, 헤더를 둔 이유("엉뚱한 프로파일로 통과한 것을 알 수 없다")가 하필 FR-5.3 경로에서 정확히 무효화된다
- **`README.md`가 `translate`의 낡은 계약을 말하고 있었다** (WP7b Task 7, Task 6 리뷰어 실측). ① 종료 코드 표가 여전히 "`cuesift translate`는 종료 코드 70(미구현)을 반환합니다"라 적혀 있었다 — 이제 거짓이다. `src/cuesift/cli.py` 모듈 독스트링의 표를 단일 출처로 삼아 다시 맞췄고, 빠져 있던 `69`(`EX_UNAVAILABLE`, LLM 프로바이더가 요청을 거부)를 추가했다. ② `--dry-run` 예제의 `# 비용 추정` 주석이 §11 R8과 충돌했다 — dry-run은 배치 수·문자 수·캐시 히트만 실측하고 토큰·비용은 내지 않으므로 `# 몇 번 더 불러야 하나`로 정정했다. ③ `## CLI` 절 헤더와 상단 경고를 "`check`·`translate` 구현 완료 · `transcribe` 설계 확정"으로 갱신하고, `cuesift translate` 실사용 절(캐시·재개·`--dry-run` 예제)을 신설했다

### Fixed

- **`--dry-run`이 "파일을 만들지 않는다"를 지키는 게이트가 이름 아는 쓰기만 막고 있었다** (WP7b Task 7, Task 6 리뷰어 실측). 기존 테스트는 `cuesift.cli.write_subtitle`·`cuesift.store.provider.store` 두 **이름**을 계측했는데, `cache_dir.mkdir(...)`처럼 함수 밖에서 직접 부르는 다른 쓰기는 새어 나가도 죽지 않았다 — 이 변이가 기존 975건을 전부 통과했다(실측). `Path.mkdir` 자체를 계측해 이름과 무관하게 "디스크에 뭔가 만들었다"를 막는 한 줄을 같은 테스트에 더했고, `_dry_run_report` 안에 `cache_dir.mkdir(parents=True, exist_ok=True)`를 임시로 넣어 새 계측이 실제로 죽는 것을 확인한 뒤 되돌렸다(변이가 실패하는 것을 보지 않은 게이트는 게이트가 아니라는 이 저장소의 규율)
- **폴백의 실물 관찰 — 소형 모델 실측 기록** (설계 §9.3, FR-2.6). 코드 변경은 없고 **관찰 결과만** 남긴다. WP7a 이래 폴백 경로는 가짜 프로바이더로만 검증돼 있었고, 이 저장소가 이미 여러 번 물린 "변이 배터리는 자기가 상정한 변이만 잡는다"가 그대로 적용되는 자리였다. `qwen2.5:3b`·`qwen2.5:1.5b` 두 모델로 실물 관찰했다. **① live 테스트(세그먼트 3개)로는 폴백을 관찰할 수 없다** — 3b·1.5b 둘 다 `calls=1`로 배치 한 번에 성공한다. 세그먼트가 3개면 배치도 1개(`DEFAULT_BATCH_SIZE=10`)이고 `before`/`after`가 **빈 튜플**이라, 설령 폴백이 발동해도 `_fallback_individually`의 **맥락 재계산**(`local[max(0, pos-cw):pos]`)이 실질적으로 돌지 않는다. 그 자리는 독스트링이 "배치 `[10,11,12]`에서 11을 개별 처리할 때 앞 맥락이 `[7][8][9]`가 된다"며 길게 경고한 곳이다. **② 세그먼트 15개(배치 10+5)로 늘리자 폴백 구제 경로가 처음으로 돌았다** — `qwen2.5:1.5b`에서 호출 **17회**(= 배치 1 + 개별 10 + 배치 1 + 개별 5, 상한 계산과 정확히 일치), 15건 중 **5건을 폴백이 구제**했다(폴백이 없었다면 15건 전부 실패). 모든 호출에 맥락이 붙어 재계산 경로도 함께 돌았다. **지금까지 실물에서 본 폴백은 전부 실패로 끝났고, "배치 실패 → 개별 성공 → 구제"가 관찰된 것은 이번이 처음이다.** ③ **1.5b의 실패 양식은 전역 인덱스 추종 실패다** — 개별 호출 `ids=1`·`ids=10`·`ids=13`에 전부 `{"id": 0`으로 답했고, **배치 `ids=10~14`도 `{"id": 0`부터 셌다.** 프롬프트가 `[10] 우리에게 남은…`으로 번호를 명시해도 따라오지 못한다. `_run_single`의 주석이 "0부터 다시 세면 두 번째 배치의 폴백이 전부 누락 판정을 받는다"고 예견한 상황이 실물로 나타났다. ④ **시스템 프롬프트 예시를 `{"id": <번호>}` 자리표시자로 되돌리면 더 나빠진다** — 위 ③을 보고 "예시의 구체적 `0`을 소형 모델이 복사하는 것 아닌가"를 의심해 두 판을 비교했고, **가설은 기각됐다**: 현재 판(예시가 정수 `0`) 성공 5/15·개별 id 정확 5/15 대 자리표시자 판 성공 **1/15**·개별 id 정확 **0/15**(2회 반복 동일). 자리표시자는 타입 문장이 함께 있어도 열등하다. ⑤ 부수 관찰 둘 — 번역문에 번호 마커가 새어 들어간다(`"text": "[0] That night, I met him…"`). `text`는 문자열이면 계약을 통과하므로 **id가 맞았다면 그대로 살아남았을 것**이고, 이것은 파서가 아니라 Tier 0 신호의 몫이다(설계 §7.2가 나눈 그 경계다). 그리고 15건 중 10건 실패(67%)는 `TranslationResult` 독스트링이 경고한 triage 오염을 실물로 재현한다. **실용 결론: 번역에는 3b를 쓰고 1.5b는 폴백 관찰 도구로만 쓴다.** 파킹 항목 S-4(`_escape_newlines`가 단사가 아님)가 "Ollama로 폴백률을 관측한 뒤가 순서다"라고 걸어 둔 선행 조건이 이로써 풀렸다
- **첫 live 실행에서 번역이 전량 실패했다 — 프롬프트가 파서의 계약을 전달하지 않았다** (FR-2.4, WBS WP7a). 로컬 Ollama(`qwen2.5:3b`)를 붙여 `tests/test_translate_live.py`를 **처음으로 실제 실행**하자 세그먼트 3/3이 `invalid_response`로 죽었다. 원인은 모델이 `{"id": "0", "text": "Hello."}`처럼 **번호를 문자열로** 낸 것이고, `_check_contract`의 `isinstance(item_id, int)`가 이를 거부했다. 모델은 그 외 전부를 맞혔다 — JSON 유효, 구조 정확, id 3개 전부 존재, 순서 유지, 번역 정상. **어긋난 것은 JSON 타입 하나였다.** 직접 원인은 모델이 아니라 프롬프트다: 파서는 정수를 요구하는데 시스템 프롬프트는 `{"id": <번호>}`라는 자리표시자만 주고 **타입을 말하지 않았다.** 실측(각 3회, `temperature=0`) — 현행 0/3, `- id는 따옴표 없는 정수다` 한 줄 추가 3/3, 예시를 `{"id": 0, ...}` 정수 리터럴로 교체 3/3. **둘 다** 넣었다(각각 단독으로 충분했으나 어느 쪽이 듣는지는 모델마다 다를 수 있고, 하나만 남기면 다른 모델에서 되돌아갈 여지가 있다). **이 실패 양식에서 개별 폴백은 무력하다** — 타입 습관은 세그먼트 1개짜리 호출에서도 그대로 재현되므로 배치 1회 + 개별 3회 = `calls` 4로 호출만 (1+N)배가 되고 결과는 같다. 설계 §6.2가 상정한 폴백의 사각지대다. 파서에도 그물을 남겼다(프롬프트는 지시이지 강제가 아니다) — `_normalize_id`가 **정수로 왕복하는 문자열만** 정수로 바꾼다. **경계를 `int()` 성공이 아니라 `str(int(s)) == s` 왕복으로 잡은 것이 이 수정의 전부다**: 파이썬의 `int()`는 JSON 숫자 문법보다 넓어 `"1_0"`(언더스코어) → 10, `" 0"`·`"+0"` → 0, `"٠"`(아랍-인도 숫자) → 0까지 읽어 낸다. 변이 검증으로 왕복을 뺀 사본을 만들자 경계 테스트 8건 중 **5건이 실패했고**, 그중 `" 0"`·`"+0"`·`"٠"` 셋은 예외도 폴백도 없이 **0번 세그먼트로 조용히 접혔다**(`DID NOT RAISE`) — id 집합 검사는 접힌 뒤의 값만 보므로 이 오매핑을 잡지 못한다. 나머지 둘(`"01"`·`"1_0"`)은 "여분의 id"로 시끄럽게 걸렸다. 문자열이 아닌 값은 손대지 않는다 — 실수 `0.0`을 0으로 접으면 `1.5`가 정수 자리에 들어올 길이 열리고 그때 어느 세그먼트인지를 반올림이 정한다. **거부하는 쪽이 보수적이라는 직관이 여기서는 틀린다**: 번역 실패는 `struct.empty`가 hard fail로 판정해 검수 예산을 우회하고, 실제 검수 비율을 부풀려 §9.1 배수의 **분모**를 망가뜨린다(`engine.py`의 `TranslationResult` 독스트링 표 — 실패 30건이면 요청 예산 10%가 실측 15%가 된다). 수정 후 live 재실행은 `calls=1`(폴백 미발동)·`prompt_tokens=240`으로 통과했다. 전체 스위트 858 → **868**건. **이 결함이 813건의 테스트를 전부 통과하며 살아남은 이유**를 적어 둔다 — 단위 테스트는 가짜 프로바이더를 쓰므로 응답을 **테스트 작성자가 만든다.** 작성자는 파서 계약을 알고 있어서 언제나 `{"id": 0}`을 냈고, 프롬프트와 파서가 어긋났다는 사실이 드러날 자리가 구조적으로 없었다. `test_translate_live.py` 독스트링이 이미 이 위험을 이름 붙여 두었고("가짜 프로바이더로 상정해 만든 변이가 진짜 변이를 전부 덮는다는 보장이 없다") 상정한 변이는 번호 누락·코드펜스·설명 덧붙임이었는데, **실물이 낸 첫 변이는 셋 다 아니었다.** 이 파일은 첫 실제 실행에서 자기 존재 이유를 증명했다
- **음수 타임코드가 exit 0으로 통과했다** (FR-1.1). `(-5000, -1000)`처럼 규격 위반이 없는 트랙은 **"위반 없음"이라는 보고와 함께 CI를 지나갔다**(실측). 부호를 보는 검사가 아무 데도 없었다 — `_require_int_timecodes`는 타입만, `_to_segments`의 `event.end < event.start`는 역전만, `Segment.__post_init__`도 역전만 봤다. 위반이 있는 경우에도 `_format_timecode`의 `max(ms, 0)`이 `00:00:00.000`을 찍어 **좌표를 날조했다** — 검수자가 그 자리를 믿고 찾아가면 아무것도 없다. `_require_non_negative_timecodes`(reason `negative_timecode`, exit 66)를 인제스트 경계에 넣고 검사 순서를 **타입 → 부호 → 역전**으로 고정했다(역전이 먼저면 `(1000, -3000)`에서 음수가 메시지에서 사라지고 사용자는 역전만 고친 채 재생 불가능한 파일을 얻는다). `_format_timecode`의 클램프는 부호 보존으로 바꿨다(`-3000` → `-00:00:03.000`). **규격 위반(exit 1)이 아니라 66인 근거**: CPS·줄길이는 검수자가 그 큐의 텍스트를 고치면 되지만 음수 좌표는 싱크·변환 파이프라인의 사고다. 섞으면 CI가 두 사고에 같은 대응을 한다. 이 결함은 **주석의 거짓 전제로 유지되고 있었다** — `_format_timecode` 독스트링이 클램프를 남긴 근거로 "음수는 `Segment.__post_init__`과 인제스트 경계가 이미 막는다"를 들었는데 사실이 아니었고, 기존 테스트 `test_format_timecode_clamps_a_negative_to_zero`가 그 동작을 **계약으로 고정**하고 있었다. 이전 세션이 남긴 "주석·계획서의 주장은 테스트와 같은 기준으로 검증한다"에 사례가 하나 더 늘었다. **리뷰 2명(위험 축: 종료 코드 계약 / 오탐 회귀)이 독립적으로 이 커밋의 거짓 전제 둘을 더 찾았고 반영했다.** ① **음수의 진입로는 json 하나가 아니다** — pysubs2가 ASS·SAMI(`.smi`)·MPL2에서 음수를 의도적으로 파싱한다(`substation.py`의 `# handle negative timestamps` 분기, `mpl2.py` 정규식의 `(-?\d+)`, `sami.py`의 `int()`). SRT·VTT·MicroDVD·TMP는 부호 자리가 없어 앞의 `-`를 조용히 무시한다. **쓰기 경로는 pysubs2가 전부 클램프하므로** "우리 도구로 왕복시켜 보니 괜찮더라"는 검증이 이 위험을 통째로 비켜 간다 — 라이브러리 경계는 read 경로와 write 경로를 따로 봐야 한다. `negative_timecode.ass` 픽스처로 덮었다. ② **`< 0` 경계를 지키는 게이트가 없었다** — `<= 0` 변이가 497건 전체 스위트를 통과했고, 독스트링이 근거로 든 "`minimal.srt`를 비롯한 픽스처가 `0`을 실제로 쓴다"가 **거짓**이었다(13종 중 `0`을 지나가는 것이 하나도 없었다). `starts_at_zero.srt` 픽스처를 새로 만들어 닫았고, 변이 사본에서 실제로 실패하는 것을 확인했다. **감수한 대가를 적어 둔다**: 한 큐가 걸리면 파일 전체가 66이라 나머지 큐의 규격 위반이 가려진다(실측: 800큐 ASS에서 `-10ms` 하나가 위반 17건을 덮었다). 그럼에도 이 판정을 고른 것은 대안이 더 나쁘기 때문이다 — 허용 임계값은 출처가 없어 §11 R8에 걸리고, 해당 큐만 빼는 것은 `검사 큐 N개` 헤더를 거짓으로 만들며, 0으로 클램프하는 것은 이 커밋이 방금 폐기한 "그럴듯한 거짓"과 같아진다

아래 여덟은 **`check` 배선이 사용자에게 보이는 형태로 고친 것**이다. 전부 실측으로 재현한 뒤 닫았다.

- **테스트 5건이 `rich`의 렌더링에 의존해 Linux CI에서만 실패했다.** 로컬(Windows·Python 3.14)에서 485 passed였는데 CI의 `test 3.11`·`test 3.12`가 둘 다 실패했다 — **이 브랜치의 35커밋은 그때까지 CI를 한 번도 거치지 않았다**(원격이 설계 커밋에 멈춰 있었다). 원인은 `rich`가 **호스트 플랫폼에 따라 다른 박스 문자**를 쓰는 것이다.

  | 환경 | `rich`의 모서리 | cp949 |
  | --- | --- | --- |
  | Windows (`legacy_windows=True`) | `┌┐└┘` (U+250C·2510·2514·2518) | **인코딩 된다** |
  | Linux CI (`legacy_windows=False`) | `╭╮╰╯` (U+256D~2570) | **인코딩 안 된다** |
  | **실제 실행 + 리다이렉트** | **박스를 아예 안 그린다** | 무관 |

  `test_help_output_is_encodable_in_the_cp949_locale`은 렌더된 출력을 통째로 `cp949`로 인코딩해 봤고, 독스트링은 그것을 "rich가 둥근 모서리로 바꾸면 Windows 사용자에게 깨지는 회귀"라고 정당화했다. **실측으로 셋 다 틀렸다**: Windows의 rich는 애초에 둥근 모서리를 안 쓰고, 사용자가 `cuesift --help > help.txt`를 하면 rich는 **박스를 아예 안 그리며**(실측: 1381바이트에 박스 문자 0개, exit 0), 따라서 그 단언은 **보호하려던 사용자에게 일어날 수 없는 조건**을 검사한 것이었다. 박스 문자는 `CliRunner`가 만들어 낸 산물이다. 테두리·ANSI를 걷어내고 **우리가 쓴 문자열만** 검사하도록 좁히고, 실제로 데였던 원인(em dash U+2014)을 **이름으로 지목하는 테스트를 따로** 뒀다 — `encode("cp949")`는 "무언가 인코딩되지 않는다"까지만 말해 실패 메시지가 원인을 가리키지 못한다. 같은 이유로 `rich`가 렌더한 stderr에 **긴 문자열·경로를 통째로 단언하지 않는다**: 강제 개행 위치가 임시 디렉터리 경로 길이에 좌우돼 **로컬에서는 `utf-8로 읽을 수 없다`가, CI에서는 `cp949-spec.yaml`이 끊겼다.** 정규화 헬퍼를 `tests/conftest.py`에 두고 양쪽에서 쓴다. **고친 뒤 em dash를 다시 심어 두 렌더링 모두에서 RED을 확인했다** — 단언을 약하게 만들어 통과시킨 것이 아니다

- **`--config`가 조용히 무시되던 것을 고쳤다** (설계 D12). 옵션은 그룹 콜백에 **처음부터 있었고** `--config x.yaml check a.srt --spec ko`가 **아무 말 없이 exit 0**으로 끝났다. 게다가 `--help`가 **거짓을 현재형으로 약속**했다 — "기본: `./cuesift.yaml`"과 "CLI 인자가 설정 파일보다 우선합니다"는 둘 다 사실이 아니다(그 파일은 읽히지 않고 우선순위 해결은 존재하지 않는다). **사용자가 자기 규격을 지정했다고 믿는데 내장 프로파일로 검사되고 종료 코드 0으로 통과한다** — 이 저장소가 1급으로 금지한 "검사하지 않고 통과하는 게이트"이며, D12가 적은 근거와 정확히 같은 문장이다. 그룹 콜백에서 stderr로 경고하고(산출물이 아니므로 stdout을 오염시키지 않는다) `--help` 문구를 미래형으로 고쳤다. **이 결함은 "구현하지 않는다"는 판단이 거짓 전제 위에 있어서 생겼다** — 근거로 든 실측 `check x.srt --config c.yaml` → exit 2는 **옵션이 없는 위치에서만** 시험한 것이었다(`--config`는 그룹 옵션이라 서브커맨드 **앞**에 와야 한다). 회귀 테스트는 경고 유무뿐 아니라 **stdout 산출물과 종료 코드가 `--config` 유무와 무관하게 같은 것**까지 고정한다
- **비-utf-8 프로파일의 진단이 자막 경로보다 나빴다.** `profile.py`의 `read_text`가 `try` **밖**에 있어 `'utf-8' codec can't decode byte 0xc0 in position 6`이 그대로 나갔다 — **어느 파일인지도, 무엇을 하라는 것인지도 알 수 없고 한국어도 아니다.** `UnicodeDecodeError`가 `ValueError`의 자식이라 종료 코드는 이미 2로 맞았으므로 **증상이 메시지 품질에만 나타나 눈에 띄지 않았다.** `loader.py`의 `decode` 오류와 같은 형태(파일 · 바이트 위치 · 해법)로 정규화해 두 경로의 진단을 일치시켰다. `load_profile` 독스트링의 "내용이 잘못된 경우는 전부 `ValueError`" 계약과도 맞다

- **프로파일·자막 파일 문제가 종료 코드 1("규격 위반 발견")로 오보되던 것을 고쳤다** (`1be2c84`·`8845d5a`·`70f5aef`·`59c7b51`·`955b4cd`·`c61a067`). 파이썬 예외가 커맨드 본문을 빠져나가면 미처리 traceback이 되어 **exit 1**이 나가는데, 이 저장소에서 1은 "규격 위반 발견"이다. **설정 사고가 자막 결함으로 읽혀 사용자가 멀쩡한 자막을 고치려 든다.** 실측으로 닫은 누수 경로가 **12종을 넘는다.**

  | 무엇이 틀렸나 | 옛 종료 코드 | 지금 |
  | --- | --- | --- |
  | 알 수 없는 내장 프로파일 이름 · 없는 `.yaml` 경로 | 1 | **2** |
  | YAML 문법 오류 · 값 타입 오류 · `bool` | 1 | **2** |
  | `NaN` · `inf` · 표현 범위를 넘는 정수 | 1(또는 조용한 통과) | **2** |
  | 읽을 수 없는 파일 (권한 · 편집기·OneDrive 잠금) | 1 | **66** |
  | JSON 껍데기 `{"info": {}}` 계열 5변종 | 1 | **66** |
  | 확장자만 `.srt`인 JSON | 1 | **66** |
  | 타임코드가 정수가 아님 (`float`·`str`·`bool`·혼합 5변종) | 1 | **66** |
  | 텍스트가 문자열이 아님 (`.json`·`.srt`) | 1 | **66** |

  표의 오른쪽 열이 이 수정의 전부다 — **"0이 아님"으로 뭉뚱그리면 어느 줄도 검증되지 않는다.** `pysubs2`가 JSON을 **내용으로** 판별하므로 확장자로는 막을 수 없고, 타임코드 타입은 `_format_timecode`의 `{hours:02d}`에서 터지는데 **위반이 0건이면 그 코드에 닿지도 않아** 수정 전에도 `exit 0 · "위반 없음"`으로 조용히 통과했다(그래서 회귀 픽스처의 본문을 일부러 규격 위반으로 만들었다)
- **파이프가 먼저 닫히면 종료 코드가 120 또는 1로 덮어써지던 것을 고쳤다** (`59c7b51`·`6b45f95`·`819b861`). `cuesift check ... | head -20`은 `head`가 정상 종료하며 파이프를 닫는 흔한 사용법이다. **예외를 나중에 잡는 방식은 "누가 썼는가"에 따라 구멍이 났다** — exit 2는 click의 `UsageError.show()`가, 70은 `typer.secho`가 쓰므로 커맨드 본문의 방어가 닿지 않는다. 실측된 회귀: `cuesift check nope.srt --spec ko 2>&1 | head -0`이 2가 아니라 **0**으로, `transcribe`도 70이 아니라 **0**으로 나갔다. **120은 시끄럽지만 0은 조용히 CI를 통과시킨다.** 쓰기 지점 자체를 무해하게 만드는 프록시(`_TolerantOutput`)로 어느 코드 경로가 쓰든 같은 결과가 되게 했고, fd를 `os.devnull`로 갈아 끼워 **인터프리터 종료 시의 재-flush**가 120을 만들지 못하게 했다(파이썬 객체만 바꾸면 부족하다 — 실측: 그대로 두면 120, `dup2`하면 0). **`errno`가 플랫폼마다 다른 것이 함정이었다**: POSIX는 `BrokenPipeError`(EPIPE)지만 **Windows는 평범한 `OSError` errno 22(EINVAL)** 이고 `isinstance(exc, BrokenPipeError)`가 False다. `except BrokenPipeError`만 다는 해법은 Linux에서만 동작하고 개발 플랫폼에서는 조용히 안 먹는다. **`ENOSPC`는 어느 층도 삼키지 않는다** — 디스크가 차서 잘린 출력이 성공으로 보고되면 그것이야말로 "검사하지 않고 통과하는 게이트"다
- **비-cp949 파일명에서 깨끗한 자막이 exit 1로 나가던 것을 고쳤다** (`4899fec`). **출력에 실리는 것은 우리 리터럴만이 아니다** — 사용자가 준 파일 경로와 `--spec` 경로가 그대로 헤더에 들어간다. Windows 기본 로케일(cp949)에서 인코딩할 수 없는 문자가 그 안에 있으면 리다이렉트 시 `UnicodeEncodeError`로 프로세스가 죽고 **종료 코드 1**이 나가, **위반 0건인 파일이 CI에서 "규격 위반"으로 읽힌다.** 실측된 사례: `Amélie.srt`(U+00E9) · `S01E01 – ko.srt`(U+2013) — 자막 파일명에 흔한 문자들이다. **em dash 금지 규칙은 우리가 쓰는 리터럴만 통제하고 사용자 입력이 흐르는 이 경로는 못 막으므로**, 규칙이 아니라 스트림 설정(`errors="backslashreplace"`)으로 닫았다. `--help`·`--version`은 eager 옵션이라 그룹 콜백보다 먼저 렌더되어 여기가 닿지 않고, 그쪽은 리터럴에서 em dash를 빼는 것으로만 막힌다(전용 테스트가 고정한다)
- **`max_cps: .nan` 같은 값이 로드에 성공해 CPS 위반이 영원히 발화하지 않던 것을 고쳤다** (`8845d5a`). YAML이 `.nan`을 파싱하므로 **설정 파일에서 도달 가능하다.** NaN과의 모든 비교가 False라 **증상 없이 신호만 죽는다** — 게이트가 통과하는데 검사는 하지 않는, 이 저장소가 가장 나쁘다고 규정한 형태다. `risk/fuse.py`의 `nan` 가중치 사고(`95b5ff3`)와 같은 계열이 규격 프로파일 쪽에도 열려 있었다
- **요구사항정의서 §3.2 S3와 §8.1의 예시가 실행되지 않았다.** `cuesift check episode01.th.srt --spec th`는 **내장 프로파일에 `th`가 없어** exit 2로 끝난다(`en`·`ja`·`ko`·`ted-en`·`ted-ja`·`ted-ko` 여섯뿐이다). Q2가 초기 언어쌍을 ko→en/ja로 확정했으므로 태국어는 v0.1 범위 밖이고, 이 예시는 **Q2 확정 이전의 잔재**였다. `--spec ja`로 정정하며 **파일명도 함께 바꿨다** — `--spec`만 고치면 `episode01.th.srt --spec ja`가 되어 "태국어 파일을 일본어 규격으로 검사"하는 더 이상한 명령이 된다. **S1의 `--to en,ja,th,vi`는 손대지 않았다**: 그것은 번역 대상 언어이지 규격 프로파일이 아니고, 목록에 `ja`가 있어 "S1에서 번역한 것 중 하나를 S3에서 검사한다"는 시나리오 연결이 유지된다. 문서에 적힌 명령이 실행되지 않는 것은 **문서가 검증되지 않았다는 신호**이고, 이 저장소에서 그것은 "검사하지 않고 통과하는 게이트"와 같은 부류다 (사용자 승인 2026-08-13)

- **`struct.number_missing`이 전각 숫자를 놓쳤다** (`2314fc6`, FR-3.4). `_numbers()`가 NFKC 정규화를 하지 않아 `'５０' != '50'`이었다. 집합 비교에서 누락 판정 → 두 자리라 `multi_digit` → **hard fail**. hard fail은 검수 예산을 우회하므로(FR-6.2) 이 오탐이 실제 검수 비율을 부풀려 §9.1의 배수를 파괴한다. **추출한 뒤에 정규화한다** — 텍스트 전체를 먼저 정규화하면 `½`(카테고리 `No`)가 `1⁄2`가 되어 **원문에 없던 숫자 1과 2가 생긴다**. `\d`는 `Nd`만 잡으므로 추출 후 정규화는 그 경로를 열지 않는다. 회귀 테스트 2건 중 두 번째("진짜 누락은 여전히 hard fail")가 없으면 첫 번째는 `_numbers`가 빈 리스트를 반환해도 녹색이 된다 — **오탐을 없애는 수정에는 미탐 방지선이 짝으로 붙어야 한다.** 한자 수사(`十代`)는 파서가 필요하고 `十分に`(≠ 10분)·`万一`(≠ 10001) 같은 관용구에서 새 오탐을 만들어 범위 밖으로 뒀다(독스트링에 한계로 명시)
- 위 수정이 **오탐 하나가 우연히 잡고 있던 진짜 오류를 드러냈다.** ja-ko 낮은 예산 Recall이 50.00% → 49.80%로 1건 떨어졌는데, 그 1건은 `glossary` 주입 오류였다(`160년` → `１６０年前と変わらぬ器具を使ってthingamajigを`). **숫자는 애초에 누락되지 않았고** spurious hard fail이 다른 유형의 오류를 우연히 잡고 있었다. **이것은 수정의 결함이 아니라 정직해진 것이다** — 이전 50.00%는 검출기가 옳아서 나온 숫자가 아니었다

이하는 전부 **계획 자체의 결함**이며 구현자가 계획을 정확히 따른 결과였다. 거의 전부 "흔한 입력에서는 맞는" 코드였고, 계획에 쓴 테스트가 그 흔한 경우만 담아 자체 검토로 걸러지지 않았다.

- `latin_half`가 East Asian Width `A`(Ambiguous)를 전각으로 세어 **같은 글자의 대소문자가 다른 폭**을 가졌다 — `é`(A)=1.0, `É`(N)=0.5. 유니코드의 Ambiguous 등급은 역사적 인코딩을 반영해 라틴 악센트 문자를 자의적으로 가른다 (`e3b5352`)
- 겹침 탐지가 인접 쌍만 비교해 포함 관계를 놓쳤다. 긴 자막이 여러 개를 덮으면 사이에 끼지 않은 것이 통째로 검사에서 빠졌다. 겹침량도 과대 보고했다(실제 100ms → 9900ms) — 이 값은 `review.json`에 실려 검수자가 본다 (`740f433`)
- 용어집 부분 문자열 매칭이 `AI`를 `rain` 안에서 찾아 위반을 통째로 놓쳤다. **계획의 예시 용어집이 `AI`를 써서 계획 자신의 데이터로 재현됐다.** 리뷰어의 `\b` 제안은 CJK 문자가 전부 `\w`라 조사 인접 시 경계가 생기지 않아 기각하고 룩어라운드로 대체했다 (`0784e20`)
- 반복 붕괴가 전체 최빈 토큰을 세어 `the cat sat on the mat with the dog` 같은 평범한 영어 문장에서 hard fail이 났다. hard fail은 예산을 우회하므로 이 오탐 하나로 Recall@Budget 지표가 무의미해진다 (`f3e0445`)
- 그 수정이 O(n²)를 낳았고 **하필 이 신호가 잡으려는 입력이 최악 케이스**였다(5000토큰 2479ms). 어절 상한 200으로 상수 시간이 됐다 (`34f4abc`)
- 태그 비교가 재직렬화 표기 차이(따옴표 스타일, `<br>` vs `<br/>`)를 마크업 손실로 오판했다 (`f3e0445`)
- 숫자 검사가 천 단위 콤마에서 `1,000`을 `['1','000']`으로 쪼개 보존된 숫자를 누락으로 잡았다. 한 자리 수를 단어로 옮긴 정상 번역(`3시` → `three`)의 hard fail도 해제했다 (`f3e0445`)
- 미번역 판정이 짧은 세그먼트에서 과민했다(`Hi 가` = 25%). 최소 문자 수를 함께 요구한다 (`f3e0445`)
- 길이비 척도가 MAD 미소 시 붕괴해 중앙값 대비 0.4% 편차가 z=4.72가 됐다. **첫 수정(평균절대편차 병용)이 틀려** 벤치마크 주입률 10%에서 재현율이 절반으로 떨어졌고, MAD 복귀 + 실질 편차 게이트로 다시 고쳤다 (`318229d`, `3b82943`, `198cd29`)
- 신호 간 점수 스케일이 어긋나 균등 가중 평균에 **암묵적 가중치**가 생겼다(규격 1건=0.33 vs 용어 1건=1.0). 공용 `_violation_score`로 통일했다 (`318229d`)
- `nan` 가중치가 `nan < 0 == False`로 음수 검사를 통과하고 `max(0.0, nan) == 0.0`이라 **최대 위험이 최소 위험으로 뒤집혔다.** YAML이 `.nan`을 파싱하므로 설정 파일에서 도달 가능하다 (`95b5ff3`)
- `review_ratio(select_by_budget(...))`가 항상 1.0을 냈다 — 이 값이 README 배수의 분모다. 계획의 테스트가 손으로 재조립하던 것이 결함의 증거였다 (`bfa0f72`)
- `check_overlaps`가 어떤 신호에도 연결되지 않아 **FR-5.1의 중첩 금지가 위험도에 도달하지 않았다.** 개별 태스크 리뷰가 구조상 볼 수 없는 결함이다 (`ad76a8c`)
- `length.ratio`가 짧은 원문에서 계통 오탐했다 — 깨끗한 400건 트랙에서 33건 발화, 전부 오탐(`네` → `Yes`가 비율 3.0) (`3718e3a`)
- `select_by_threshold`에 인자 검증이 없어 `threshold=nan`이 hard fail 외 전량을 조용히 검수에서 뺐다 (`8b9af8f`)
- `docs/번역관리_TMS_솔루션_비교.md:5`의 깨진 링크 처리 — `글로벌_OTT_플랫폼.md`는 이 저장소에 존재한 적이 없다. 링크에서 일반 텍스트로 강등하고 외부 문서임을 명시했다
- `bench/report.py`의 `RunMeta.excluded` → `injection_skipped`로 이름을 바로잡았다(fix 라운드 1, 팀장 지적). 옛 이름과 절 제목 "제외 건수"는 `glossary 4,041`을 "5,000건 트랙에서 4,041건이 빠졌다"로 읽히게 했다 — 실은 "해당 유형의 주입 조건을 만족하지 않아 건너뛴 세그먼트 수"이며, 그 세그먼트들은 뒤에 처리되는 유형의 주입 후보로 다시 검토되고 끝까지 배정되지 않으면 **오류 없이 깨끗한 상태로 트랙에 남는다**(fix 라운드 2에서 "다른 유형으로는 정상 주입됐다"는 과장된 서술을 이 표현으로 정정했다). 또한 §4.4 부수 산출물(코퍼스 규격 미충족률)이 어느 리포트에도 실리지 않았던 것을 사이드카 도입으로 바로잡았다
- **가중치 총합 오버플로가 최대 위험을 최소 위험으로 뒤집었다** (D-22, `097e1ec`). D-16이 `nan`·`inf`를 막았으나 `math.isfinite`를 항목 하나씩만 적용해, `1e308` 두 개는 개별로는 유한해 통과하고 합이 `inf`가 되면 `weighted / total_weight = inf / inf = nan` → `min(1.0, max(0.0, nan)) = 0.0`이 됐다. **D-16 주석이 "막았다"고 선언한 실패 모드가 합계 경로로 그대로 재현됐고**, `cace3d4`의 `SegmentRisk` 범위 검증도 clamp가 먼저 NaN을 삼켜 발동하지 않았다. 회귀 테스트는 수정 전 코드에서 `DID NOT RAISE`로 실패하는 것을 확인한 뒤 붙였다
- **`Span.side`에 런타임 검증이 없었다** (D-23, `341b357`). `cace3d4`가 `SegmentRisk`에 "형제 세 모델과 같은 방어"를 근거로 범위 검증을 추가하면서 **같은 커밋이 새로 넣은 필드는 무방비로 뒀다** — `Literal`은 타입 힌트일 뿐이라 `"both"`·`None`·`0`이 전부 통과했다. 바로 두 줄 아래 `start`/`end`는 이미 막고 있었다
- **리포트가 실재하는 검출기 결함을 부인했다** (최종 브랜치 리뷰 I-2, `b6285ca`). hard fail 오탐 설명이 언어쌍과 무관하게 "**검출기 버그가 아니라** 만·억 vs billion 때문"이라 단언했는데, 원인별 실측 분류에서 **ja-ko는 41건 중 0건**이 그 원인이었고 **31.7%가 진짜 검출기 결함**이었다 — `struct.number_missing`의 숫자 추출이 NFKC 정규화를 하지 않아 전각 `５０`과 반각 `50`을 다른 값으로 본다. 그리고 `assert "billion" in md`가 그 거짓 문장을 회귀 방지선으로 고정하고 있었다. 문장을 실측에 맞는 사실 나열로 바꾸고 NFKC 한계를 명시했으며, 테스트를 값 연동 단언으로 교체했다. **제품 코드는 고치지 않았다** — 측정을 끝낸 뒤 제품을 고치면 그 측정이 무효가 되므로 `HANDOFF.md`에 재측정 과제로 인계했다
- ablation 음수값이 무설명이었다 (I-3, `b6285ca`). 표 제목이 "Recall 하락폭"인데 `length.ratio`가 두 언어쌍 모두에서 **−6.0%p**다 — 내림차순 정렬 맨 아래라 "가장 덜 유용함"으로 보이지만 실제로는 "가장 해로움"이다. 기여도 `+0.0%`인 `spec.overlap`에는 오독 방지 절을 줬으면서 음수에는 한 줄도 없던 비대칭을 해소했다
- 코퍼스 제외율의 분모가 `CHANGELOG`·`HANDOFF` 두 곳에서 틀렸다 (I-4, `b6285ca`). 49.91%의 분모는 **필터 후 389,598**인데 "원본 399,414쌍 중"으로 적혀 있었다(그 조합이면 48.69%다). 리포트 본문과 README는 표로 분모를 함께 보여 정확했다 — **리포트는 고쳐졌는데 기록만 틀린 조합을 담은** 상태였다
- `inject`의 가드가 "실주입 0건"만 보고 quota 부분 미달은 보지 않았다 (`b6285ca`). 용어집 자격이 89건(quota 72)으로 여유가 1.24배뿐이라, 표본이나 용어집이 바뀌면 라벨 총계가 조용히 목표에 미달해 오류율이 10% 아래로 내려가고 오라클 상한·배수의 분모 관계가 바뀐다. `achieved[k] < quota[k]`도 같은 예외로 올렸다 — 스펙 §5.5가 "0 broken이 통과로 읽혔던 것처럼"이라며 세운 원칙의 연장이다
- 리포트의 커밋 스탬프가 항상 한 커밋 뒤처졌다 (I-1, `f4b32ca`). `report.py`를 고치는 커밋에서는 리포트 재생성이 커밋보다 먼저라 스탬프가 직전 커밋을 가리켰고, **그 커밋의 `report.py`는 방금 지운 거짓 문장을 렌더링한다.** 리포트만 재생성하는 기계적 커밋으로 구조적으로 닫았다
- `scripts/__init__.py`에 `from __future__ import annotations`가 없었고, docstring "표준 라이브러리만 쓴다"가 같은 패키지의 `glossary_verify.py`(`bench.corpus`·`cuesift.glossary` 임포트)에 대해 거짓이었다 (`b6285ca`)
- `docs/WBS.md`의 Mermaid 의존 그래프에서 `WP3` 노드가 `🔨 진행 중`(노란 style)으로 남아 있었다 — 작업 패키지 표와 "현재 위치" 진척 막대는 이미 `✅`였으나 도식만 갱신되지 않은 것이다. `✅ 완료`와 초록 style로 맞추고, 이번에 완료한 `WP4`도 함께 반영했다(도식이 최신 상태를 반영하지 못하면 표·막대·도식 세 표현이 서로 다른 진척을 주장하게 된다)

- **인제스트 설계 §2.1의 라운드트립 보존 주장 두 개가 거짓이었다** (`f9c51d8`). 최종 리뷰가 실행해 반증했다 — VTT cue settings(`line:0 position:20%`)는 `SSAEvent`에 담을 필드가 아예 없어 **로드 시점에 버려지고**, ASS 오버라이드 태그(`{\an8}`)는 `SSAEvent.plaintext` setter가 텍스트를 통째로 대체하므로 **번역을 되쓰는 이벤트에서는 사라진다**(건드리지 않은 이벤트에서만 남는다). 원본 `SSAFile`을 들고 다니는 결정 자체는 여전히 옳다 — 스크립트 헤더·스타일 정의·주석 줄·타임코드·포맷은 실제로 보존되고 라운드트립이 SRT·VTT·ASS·SSA 4포맷 전부에서 성립하는 것을 실측했다. **결론은 맞고 근거로 든 예시가 틀린 경우**이며, 그대로 뒀다면 WP5(출력)가 잘못된 목표를 세웠을 것이다. §10 위험표에 세 줄을 추가했는데 그중 하나가 **WP6이 밟을 함정**이다 — 인제스트 결과를 신호 엔진에 바로 넣으면 `target_text`가 `None`이라 `struct.empty`가 전 세그먼트에서 발화하고, hard fail은 FR-6.2에 따라 검수 예산을 우회하므로 검수 비율이 100%가 되어 Recall@Budget이 무의미해진다
- `docs/superpowers/plans/2026-07-31-ingest.md`의 Task 5 RED 예고치가 틀렸다 (`0480167`). "5건 중 4건 FAIL"로 적었으나 실측은 3건이다 — `test_filtered_events_survive_in_subs_for_roundtrip`은 원본 `subs`만 보는 **불변조건 테스트**라 필터 유무와 무관하게 통과한다. 구현자가 불일치를 은폐하지 않고 보고했고, 리뷰가 "그 테스트는 무엇을 막는가"에 답을 냈다 — 필터된 사본을 `subs`에 담고 `event_index`를 사본 기준으로 재계산하는 변경은 `event_index` 루프 단언을 **통과시키므로** `len(result.subs) == 4`만이 잡는다

아래 한 건은 성격이 다르다 — **구현이 아니라 테스트 자체의 결함**이며 대상 코드는 처음부터 옳았다.

- **리포트 테스트 2건이 독스트링의 주장과 달리 하드코딩 회귀를 못 잡았다.** `test_report_states_hard_fail_false_positive_rate`는 픽스처 `META` 하나로만 렌더해 `render_markdown`이 `"0.96%"`를 리터럴로 박아도 통과했다. `test_ablation_explains_negative_drops_as_harmful_not_just_unhelpful`은 **두 가지가 겹쳐** 무력했다 — 검증에 쓴 신호 이름 `length.ratio`가 실측에서 실제로 음수가 나오는 이름이라 제품 코드가 하드코딩할 수 있었고, 잘라내는 지점(`## 신호별 기여도` 이후)이 ablation **표**를 포함해 어떤 이름을 넣어도 거기 나타났다. HANDOFF가 남긴 수정 제안("픽스처 이름을 `zz.synthetic.probe`로", 1줄)은 전자만 고쳐 **뮤테이션이 여전히 통과했다** — 지적이 옳아도 해법은 틀릴 수 있다. 고친 뒤 두 결함을 실제로 `bench/report.py`에 심어 RED(2 failed)를 확인하고 복원했다. **교훈: 값 연동만으로는 부족하고 값이 두 가지 이상이어야 하며, 잘라내는 범위도 함께 검증해야 한다** — 픽스처가 하나면 "값에서 뽑은 것"과 "그 값을 박은 것"이 구분되지 않는다

### Removed

- (없음)

---

## [2026-07-28] 문서 구조 정리 · 용어 풀이 · 조사 질문 동기화

### Added

- `docs/AI_자막검수_오픈소스_비교.md` §0 용어 풀이 — QE·트리아지 두 축 심층 설명 + 용어 28개(음성·자막 생성 5 / 품질 측정 9 / 자막 규격·포맷 10 / 파이프라인 운영 4). 각 그룹을 §2의 계층(C·D·E)에 대응시켰다 (`cc8f109`)
- `docs/요구사항정의서.md` §0 용어 풀이 — 문서 표기 규약(`FR-`·`NFR-`·`必`·`Qn`·`★` 상자), 척추 4개념(세그먼트→신호→위험도→트리아지), 고유 용어 14개, 측정 용어 6개, 공통 용어 포인터 9개. 각 용어에 근거 FR·Q 번호를 병기해 요구사항 변경 시 역추적되게 했다 (`cc8f109`)

### Changed

- 조사 문서 3종을 `docs/`로 이동 — 최상위를 README·CHANGELOG·HANDOFF·라이선스로 정리. 세 문서는 형제 상대경로로 상호 참조하므로 함께 옮겨 링크가 유지된다. README·HANDOFF의 유입 링크만 수정 (`5ffda8d`)
- 용어 정의를 단일 출처로 통일 — 공통 용어(QE·MQM·CPS·화자분리·자막 포맷)는 AI 문서 §0에만 두고, 요구사항정의서 §0.5는 한 줄 요약과 포인터만 갖는다. 같은 용어를 두 문서에서 각각 정의하면 한쪽만 고쳐져 갈라진다 (`cc8f109`)
- 검증 수준 표기에 색상 이름 명시 — `docs/AI_자막검수_오픈소스_비교.md` 최상단의 🟢 → 🟢 **초록**, 🟡 → 🟡 **노랑** (`cc8f109`)
- 문서 버전 0.3 → 0.4 (`cc8f109`)
- `docs/AI_자막검수_오픈소스_비교.md` §5 조사 질문 6개에 상태와 근거를 부여 — #3은 ✅ 닫힘(Q6), #1·#4·#5는 ⏸ 연기(Q2·`FR-1.6`·`FR-5.5` = v0.3), #6은 ⚪ 불필요(§4에서 채택 후보 아님), #2는 🔬 측정 필요(Q4와 동일 실험)로 재분류. 상태 기호는 문서 전반의 검증 수준 🟢·🟡과 의미가 달라 재사용하지 않고 범례에 명시 (`11dbfe4`)
- §5 한계 목록의 각 항목에 대응처를 명시 — 모델 크기 → `NFR-1`, 평가 데이터 → §9.2, 샷 체인지 → `FR-5.5`(v0.3) (`11dbfe4`)

### Fixed

- §5 한계 항목의 "CometKiwi·xCOMET의 저자원 언어(th·vi·id) 성능이 **최우선 검증 대상**" 서술 정정 — Q2가 초기 언어쌍을 ko→en/ja로 확정한 이후 사실이 아니다. 조사 문서가 요구사항 결정 이전 상태로 굳어 있던 사례 (`11dbfe4`)

---

## [2026-07-27] 저장소 구축 및 미결정 사항 정리

### Added

- 프로젝트 골격 초기화 — Apache-2.0 라이선스·NOTICE, `pyproject.toml`(Typer·hatchling·src 레이아웃), `src/cuesift/cli.py`, 테스트 6건, `.gitignore`, `.gitattributes` (`66cf564`)
- GitHub Actions CI — Python 3.11/3.12 매트릭스에서 ruff·pytest 실행 (`66cf564`)
- CI에 `docs` 잡 추가 — markdownlint 게이트. 버전 고정, `set -eo pipefail`, `Linting: 0 files` 가드 포함 (`7956a0a`)
- `.markdownlint-cli2.jsonc` — 프로젝트 마크다운 관례를 명시 (`7af39a7`)
- 요구사항정의서 §8.3.1 — ko·en·ja 규격 프로파일 기본값과 1차 출처 (`7af39a7`)

### Changed

- CI 액션을 Node 24 기반 최신 메이저로 상향 — `actions/checkout` v4 → v7, `actions/setup-python` v5 → v7 (`48d127c`)
- 미결정 사항 Q2 해결 — 초기 대상 언어쌍을 ko→en/ja로 확정 (`9e9ee83`)
- 미결정 사항 Q3 해결 — 로컬 LLM은 OpenAI 호환 엔드포인트로 일원화하되 프로바이더 능력 탐지를 요구사항에 추가 (`9e9ee83`)
- 미결정 사항 Q5 해결 — Netflix Timed Text Style Guide를 규격 프로파일 1차 출처로 확정 (`7af39a7`)
- 미결정 사항 Q6 해결 — PySubtrans 대신 자체 얇은 어댑터로 결정 (`9e9ee83`)
- `char_counting` 스키마 재정의 — `grapheme`\|`cjk_width` → `grapheme`\|`latin_half`\|`fullwidth`. 기존 두 값으로는 한국어를 표현할 수 없었다 (`7af39a7`)
- 문서 버전 0.2 → 0.3 (`9e9ee83`)
- markdownlint가 `.gitignore`를 따르도록 설정 — 로컬 5개 / CI 4개로 어긋나던 검사 대상을 일치시킴 (`852cb68`)

### Fixed

- §7.4의 PySubtrans 배제 근거 정정 — "GUI가 딸려온다"는 사실이 아니다. `pyside6`는 `gui` extra로 분리돼 있으며, 실제 사유는 도메인 모델 동반이다 (`9e9ee83`)
- markdownlint 경고 76건 수정 — bare URL 42건, 목록·제목·코드펜스 주변 공백, 코드펜스 언어 지정, 인용문 병합, 제목 레벨 (`7af39a7`)
