# Changelog

All notable changes to this project are documented in this file.
Format follows [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/).

## [Unreleased]

### Added

- **Tier 0 신호 엔진** — 요구사항정의서 §4의 무료 결정론적 계층 전체. 순수 모듈 6개(`segment`·`spec`·`glossary`·`signals`·`risk`·`triage`)로 네트워크·LLM 의존이 0이다. 테스트 172건 (`845a9a9`..`cace3d4`)
- `segment/models.py` — `Segment`·`Signal`·`Span`·`SegmentRisk` (§7.3). 타임코드는 §8.4 `review.json` 계약에 맞춰 정수 밀리초를 쓴다. 네 모델 모두 생성 시점에 범위·정합성을 검증한다 (`198e90a`, `cace3d4`)
- `spec/counting.py` — 문자 폭 계산 3종 (FR-5.2). `grapheme`·`latin_half`·`fullwidth`로 언어마다 다른 "한 자"의 뜻을 표현한다 (`47de1a9`)
- `spec/profile.py` + `specs/*.yaml` 6종 — Netflix TTSG 기반 ko·en·ja와 TED 벤치마크용 ted-ko·ted-en·ted-ja. 수치와 출처 URL만 두고 가이드 본문은 복제하지 않는다 (`c79f752`)
- `spec/check.py` — 줄 길이·줄 수·CPS·노출시간·겹침 판정 (FR-5.1) (`1822d7b`)
- `glossary/` — 용어집 로드와 대응어 누락 판정 (FR-3.7). 룩어라운드 경계 매칭으로 짧은 약어의 부분 문자열 오탐을 막는다 (`426c268`, `0784e20`)
- `signals/base.py` — 수집기 프로토콜 2종(세그먼트 단위·배치)과 레지스트리 (FR-6.5, NFR-5). v0.2에서 QE 모델을 코드 수정 없이 꽂을 자리다 (`a49c3e5`)
- `signals/structural.py` — 구조 신호 5종 (FR-3.1~3.5): 미번역 잔존·빈 값·반복 붕괴·숫자 누락·태그 손실. 전부 hard fail이라 검수 예산을 우회한다 (`ef7c839`)
- `signals/derived.py` — 파생 신호 4종 (FR-3.6~3.8, FR-5.1): 규격 위반·용어집 위반·길이비 이상치·세그먼트 겹침. 모두 hard fail이 아니다 (`1864326`, `ad76a8c`)
- `risk/fuse.py` — 신호 융합 (FR-6.1, FR-6.2, FR-6.4). 가중 평균이며 hard fail은 가중합을 우회한다. 기본 가중치는 8종 균등(무튜닝) (`501533e`)
- `triage/policy.py` — 검수 큐 선별 (FR-6.3). 예산·임계값 두 정책과 실제 검수 비율 산출. 전체 목록을 반환하고 선별된 것에 `selected` 플래그를 붙인다 (`86bdd6b`, `bfa0f72`)
- `CLAUDE.md` — 프로젝트 작업 규약. 코드를 읽어서는 알 수 없는 것들(문서 지도, 검증 습관, 확정된 결정)을 기록한다
- `scripts/check_links.py` — 마크다운 상대 링크가 실제 파일을 가리키는지 검사한다. 표준 라이브러리만 사용하며 대상은 `git ls-files` 기준(markdownlint의 `gitignore: true`와 동일 집합). 외부 URL과 앵커는 검사하지 않는다 — 외부 URL은 남의 서버 사정으로 CI가 간헐 실패하면 게이트가 무시되고, 앵커 생성 규칙은 렌더러마다 달라 한국어 제목에서 갈린다. 코드 펜스와 인라인 코드 스팬은 제외한다(링크 문법을 설명하는 문장이 오탐되지 않도록)
- CI `docs` 잡에 링크 검사 스텝 추가 — 검사 대상 0개면 통과가 아니라 실패로 처리하고, 깨진 링크는 `::error file=…,line=…::` 애노테이션으로 표시한다
- **TED2020 벤치마크 하네스** (벤치마크 하네스 설계 스펙 §3~§10) — `bench/` 패키지 9개 모듈. 코퍼스 획득(`scripts/fetch_ted2020.py`)·필터링(`corpus.py`)·타임코드 합성(`timing.py`)·트랙 빌드(`build_track.py`)·직렬화(`track_io.py`)·오류 주입 7종(`inject.py`)·Recall@Budget 측정과 불변식 4종(`measure.py`)·리포트 생성(`report.py`)·전체 오케스트레이션(`run.py`). LLM 의존 없이 결정론적으로 실행하며 테스트 259건 (`dd2826c`..`dfa12ca`)
- `bench/run.py` — `python -m bench.run --pair {en-ko,ja-ko}`. Tier 0 신호 9종의 **첫 Recall@Budget 실측치**를 낸다 — 실제 검수 비율 10%에서 무작위 대비 **en-ko 6.96x · ja-ko 7.06x** (오류 포착률 69.6%·70.6%). hard fail 자연 오탐률은 en-ko 0.96% / ja-ko 0.91%로 목표(§9.1 "≈ 0")에 부합
- `bench/results/{en-ko,ja-ko}-2026-07-29.{md,json}` — 재현 정보(코퍼스 SHA-256·시드·커밋)·예산 스윕(1~30%)·유형별 Recall·오라클 대비 달성률·신호별 ablation을 담은 실측 리포트. 결과는 숫자라 리포에 커밋한다(가공 코퍼스와 달리 저작권 문제가 없다)
- `bench/report.py`에 "오라클 대비 달성률" 절 추가 — 예산이 낮을수록(hard fail 위주) 오라클 대비 달성률이 높고 예산 10% 부근(가중평균 순 채움 구간)에서 저점을 찍는 **U자 곡선**을 표로 남긴다. 계획 A 재리뷰가 남긴 미결 항목("가중평균 vs noisy-or 융합")에 대한 정량 근거다
- `bench/report.py`에 "`negation` 무작위 기준선과의 비교" 절 추가, `bench/run.py`가 유형별 무작위 기준선을 계산해 넘긴다(기존 `random_baseline`을 negation 라벨로만 좁힌 `error_ids`로 재사용). **Tier 0는 의미 반전(`negation`)에서 무작위보다 못하다** — 예산 10%에서 1.41% vs 무작위 9.61%(en-ko), 예산 20%에서도 9.86%/11.27% vs 무작위 19.49%/19.85%(en-ko/ja-ko, 100시드 재측정). Tier 0가 다른 오류를 상위로 올리며 문법적으로 완벽한 문장(의미만 뒤집힌 것)을 오히려 큐에서 밀어내기 때문이다
- `bench/build_track.py` — 트랙과 나란히 **사이드카** `{pair}.clean.stats.json`을 쓴다(스펙 §4.4 부수 산출물). `_corpus_stats()`가 콘솔 출력과 같은 공식으로 계산해 두 출처가 갈라지지 않는다. **실측: en-ko 필터 후 389,598쌍 중 194,463쌍(49.91%)이, ja-ko는 필터 후 350,634쌍 중 168,613쌍(48.09%)이 TED 규격(21자×2줄)에 물리적으로 담기지 않아 트랙에서 빠졌다** — FR-5.4(v0.2 규격 자동 교정)를 정량적으로 정당화하는 숫자다
- `bench/report.py`에 "코퍼스 제외 (스펙 §4.4)" 절 추가 — `RunMeta.corpus_stats`(사이드카에서 읽은 값)가 있을 때만 렌더링하고, 비율은 리포트가 다시 계산하지 않는다
- **주입 감사 산출물** (스펙 §5.7) — `bench/track_io.py`의 `dump_audit()`이 변조 트랙(`data/bench/{pair}.injected.json`)과 정답 라벨(`{pair}.labels.json`)을 **한 번의 호출로 함께** 쓴다. 둘을 각각 쓰는 함수 두 개로 두지 않은 이유는 짝이 아닌 산출물이 감사에 쓸 수 없기 때문이다 — 함수가 둘이면 한쪽만 호출하는 실수가 구조적으로 가능하다. 변조 트랙은 `dump_track`과 같은 형식이라 `load_track`으로 그대로 읽힌다(감사자가 새 파서를 짜야 한다면 감사 수단이 아니다). 라벨 파일에는 시드·커밋과 **변조 트랙의 SHA-256**이 함께 기록되고 `load_labels()`가 이를 대조한다 — 해시를 적기만 하고 아무도 대조하지 않으면 없는 게이트와 같다. `bench.run`은 **측정 전에** 이 산출물을 남긴다(불변식에서 실패한 실행도 무엇을 주입했는지는 남아야 한다). `--audit-dir`로 위치를 바꿀 수 있고 기본값은 트랙과 같은 디렉터리다. **실행 검증**: en-ko·ja-ko 양쪽에서 깨끗한 트랙과 변조 트랙의 차집합이 라벨 500건과 양방향으로 완전히 일치했고, Recall 값은 기존 리포트와 동일했다(산출물 추가가 측정을 바꾸지 않았다)
- `README.md`에 **"재현"** 절 추가 — 실측 절이 배수 7배를 인용하면서 독자가 재현할 명령이 없었다. 3단계 파이프라인(`scripts.fetch_ted2020` → `bench.build_track` → `bench.run`)을 Mermaid 흐름도·표·실행 명령으로 싣고, `python -m`으로 실행해야 하는 이유(`sys.path`에서 리포 루트가 빠진다)와 용어집 회귀 게이트 `scripts/glossary_verify.py`를 함께 문서화했다. 적은 명령 4개는 전부 실제로 실행해 동작을 확인했다
- **설계 스펙 — noisy-or 융합과 숫자 NFKC 정규화** (`411b14d`). `docs/superpowers/specs/2026-07-29-noisy-or-fusion-nfkc-design.md`. HANDOFF의 미결 항목 둘(융합 방식·전각 숫자 미탐)을 한 스펙으로 묶는다 — 성격은 다르지만 둘 다 재측정을 요구하므로 따로 처리하면 벤치를 두 번 돌린다. **noisy-or 채택을 재측정 결과에 걸지 않기로 확정했다**: 벤치 숫자를 보고 융합식을 고르면 그것이 벤치 데이터로 제품을 튜닝하는 것이고, 재측정의 목적은 판정이 아니라 기록이다(Recall이 내려가도 그 숫자를 싣는다). 가중치는 지수로 넣는다(`1 - ∏(1-s)^w`) — 점수 스케일 가중은 `w·s > 1`에서 곱의 부호가 뒤집혀 `w ≤ 1` clamp를 강제하고 그러면 신호 강화를 표현할 수 없다. NFKC는 추출된 숫자 문자열에만 적용한다 — 텍스트 전체를 정규화하면 `½` → `1⁄2`로 없던 숫자가 생기고 재측정 결과의 원인 귀속이 흐려진다
- **작업 분해 구조(WBS)** (`a3d234a`). `docs/WBS.md`. 전체 진척이 보이지 않는다는 지적에 대한 대응 — 단위 작업은 따라갈 수 있는데 "얼마나 더 하면 끝나는가"를 아무도 답할 수 없었다. 작업 패키지를 FR 번호에 대응시킨 이유는 **진척 주장을 반증 가능하게** 만들기 위해서다("인제스트 60% 완료"는 반박할 수 없지만 "FR-1.1 완료"는 코드와 테스트로 확인된다). v0.1 대상 FR 42개 중 16개 완료(38%), WP 9개. 의존 그래프가 두 가지를 드러냈다 — **WP4(인제스트)가 병목이고**(출력·번역·STT 셋이 걸린다), **`check` 서브커맨드는 번역·STT 없이 완결된다**(인제스트 + FR-8.2 배선만으로 쓸 수 있는 제품이 나오는 유일한 지점). 갱신 규칙에 "상태 칸을 고칠 때 근거 커밋을 함께 적는다"를 넣었다 — 근거 없는 상태 표시는 검사하지 않고 통과하는 게이트와 같다
- **구현 계획 — noisy-or·NFKC** (`c746098`). `docs/superpowers/plans/2026-07-29-noisy-or-fusion-nfkc.md`. 태스크 6개(0~5), 코드 변경은 두 파일 약 30줄이고 나머지는 측정과 기록이다. 계획을 쓰며 **스펙 결함 하나와 누락 하나**를 찾았다 — 스펙 §5가 "`w=0` 테스트는 현재 코드에서 FAIL"이라 적었으나 실제는 PASS이고(가중 평균에서 `w=0`인 신호는 분자·분모에 각각 0을 더해 평균에서 제외되며 noisy-or에서도 `(1-s)^0 = 1`로 곱에서 제외된다), 스펙이 신규 테스트만 세고 **기존 테스트 2건이 가중 평균을 값으로 고정하고 있다**는 것을 놓쳤다. 신규·교체 테스트 5건의 현재 코드 반환값을 실제로 실행해 확인하고 표로 실었다 — 추측한 숫자를 계획에 남기면 구현자가 그것을 신뢰한다. 이 과정에서 `HANDOFF.md`의 테스트 총계 271이 낡았다는 것(실제 277)도 드러났다
- **인제스트 계층** (FR-1.1·1.3·1.5, WBS WP4). `src/cuesift/ingest/{__init__,loader}.py` — 최소 로더. pysubs2로 SRT·WebVTT·ASS/SSA를 공통 `IngestResult`(`Segment` 목록·포맷·언어 등 필드 6개)로 통일한다. **포맷별 분기가 0개다** — pysubs2가 포맷 판별과 태그 정규화를 전부 하기 때문이며, 이 사실 하나로 WBS 규모 추정이 M에서 S로 내려갔다(설계 §1.2). 로드 실패를 오류 계약 6종(`not_found`·`video_input`·`decode`·`parse`·`empty`·`bad_timecode`)의 `IngestError`로 번역하고, 이벤트 필터(설계 §4·§5.4)로 화면에 표시되지 않는 큐를 걸러 CPS 과다 계산을 막으며, 역전 타임코드는 큐 번호와 함께 보고한다. `tests/fixtures/ingest/`에 픽스처 12종을 두고 픽스처 자체를 검증하는 테스트를 별도로 뒀다(§9.1) — 픽스처가 틀리면 그 위의 모든 테스트가 틀린 것을 옳다고 확인하기 때문이다. 테스트 34건 신규, 전체 282 → 316 (`42a126f`·`bb105b7`·`be51706`·`52acf5b`·`d51cb3c`·`1cbc21a`·`26f7517`·`6ad9366`·`50e62cf`)
- **인제스트 회귀선 3종** (`50e62cf`). 최종 전체 브랜치 리뷰가 태스크별 리뷰 8회가 모두 놓친 빈 칸을 찾아 메웠다. ① `basic.ssa` — FR-1.1이 이름을 걸어 요구한 SSA가 픽스처도 단언도 없이 WBS에서 ✅ 처리돼 있었다(`.gitattributes`의 `*.ssa -text`는 존재하지 않는 픽스처를 위한 죽은 설정이었다). 동작은 정상이었으므로 **깨진 게 아니라 무증거**였고, 이 리포가 스스로 금지한 "검사하지 않고 통과하는 게이트"에 해당한다. ② `comment_then_reversed.ass` — `bad_timecode`의 큐 번호가 `raw_index`(원본 위치)인지 `index`(필터 후 순번)인지 기존 픽스처로는 **구분되지 않았다**(`reversed.srt`에 주석이 없어 두 값이 같다). 주석을 앞에 두어 두 값을 갈라 놓고, `raw_index + 1`을 `index + 1`로 일시 개악해 새 테스트만 실패하는 것을 확인했다. ③ `overlap.vtt`를 `spec.check_overlaps`에 연결 — 픽스처는 있었으나 **로드하는 테스트가 하나도 없었다.** 겹침이 있는 실제 트랙이 처음으로 파이프라인에 들어오며, `spec.overlap`의 기여도 `+0.0%`가 "쓸모없다"가 아니라 "측정하지 못했다"였던 상태가 풀릴 준비가 됐다
- **설계 스펙 — `check` 배선** (`9ef4869`, FR-8.2·FR-7.5, WBS WP6 부분). `docs/superpowers/specs/2026-08-03-check-cli-design.md`. 핵심 결정은 **`check`가 신호 엔진(`collect_all`→`fuse`→`triage`)을 통과하지 않는다**는 것이다 — `SpecViolationSignal`·`OverlapSignal`은 `spec/check.py`의 얇은 래퍼이고 신호 계층이 얹는 넷(점수화·`hard_fail`·융합·트리아지)을 `check`가 하나도 쓰지 않는다(심각도 단일 등급, 예산 없음, 순위 없음). 직접 호출은 HANDOFF가 경고한 함정을 **회피가 아니라 소멸**시킨다 — `struct.*` 수집기가 아예 실행되지 않는다. 실측이 HANDOFF 기록을 두 군데 정정했다: ① 함정이 `struct.empty` 하나가 아니라 원문에 숫자·태그가 있으면 `struct.number_missing`·`struct.tag_lost`도 함께 hard fail한다(이전 세션 픽스처에 숫자·태그가 없어 안 보였다), ② HANDOFF가 제안한 미러링 대응은 `struct.degeneration`이 "네 네 네" 같은 정상 한국어 반복에서 hard fail을 내므로 안전하지 않다. **심각도는 단일 등급으로 두고 등급을 발명하지 않았다** — 프로파일 수치를 재 보니 `ja`↔`ted-ja`가 CPS 2.50배·줄폭 1.62배로 두 축 모두 흔들려 등급 배정의 출처가 없고, 이는 §11 R8("출처 없는 수치를 기본값으로 넣지 않음")에 걸린다. 빈 큐가 **현재 어느 경로로도 탐지되지 않는 사각지대**임을 확인하고 `check_empty_cues`로 닫기로 했다(`check_text` 안에 넣으면 `translate` 경로에서 `struct.empty`와 이중 계산된다). 종료 코드는 `2`(호출이 틀림)와 `66`(파일이 틀림)을 분리한다

- **`cuesift check`가 실제로 동작한다** (FR-8.2·FR-7.5, WBS WP6 부분 · WP5 부분). `cuesift check <자막파일> --spec <프로파일>`이 규격 위반 6종과 빈 큐를 판정하고 **종료 코드 0·1·2·66을 구분해 낸다.** 이것이 WBS가 지목한 "가장 짧은 쓸 수 있는 제품" 경로다 — 번역(WP7)도 STT(WP9)도 없이 완결되는 유일한 지점이고, **v0.1 전체를 기다리지 않고 나오는 첫 중간 산출물**이다. `spec/check.py`에 `TrackViolation`·`check_track()`이, `cli.py`에 `check()` 본문·`_resolve_profile()`·`_format_report()`가 들어왔다. 테스트 316 → **481**건 (`90c6a7d`·`c376ad0`·`a580a66`·`5c5dd5a`·`95e182a`·`3330773`·`f16bb0f`·`1be2c84`·`8845d5a`·`70f5aef`·`800d17c`·`ca29ef5`·`7d3e5f6`·`4899fec`·`59c7b51`·`955b4cd`·`6b45f95`·`c61a067`·`819b861`)
- **빈 큐 판정 `check_empty_cues`** (`a580a66`). 설계 §12.3이 실측한 **사각지대**를 닫는다 — `empty_cue.srt`를 세 구성(그대로·미러링·`check_text` 직접 호출)으로 돌려 **셋 다 놓치는 것**을 확인한 뒤 추가했다. `struct.empty`는 `source_text`가 비면 `None`을 반환하고 `check_text`는 빈 텍스트의 줄·CPS 검사를 건너뛴다. 배포 자막에 텍스트 없는 큐가 남는 것은 명백한 결함인데 어느 경로로도 잡히지 않고 있었다. **`check_text` 안에 넣지 않은 것이 설계다**(D6) — 넣으면 `translate` 경로에서 `struct.empty`와 **이중 계산**되고, 그 부풀림은 `spec.violation` 점수를 통해 위험도로 흘러 검수 비율을 밀어 올린다. 검수 비율은 §9.1 배수의 **분모**다
- **`check`는 신호 엔진을 통과하지 않는다** (설계 D3, `4899fec`). `collect_all`→`fuse`→`triage`가 얹는 넷(점수화·`hard_fail`·융합·트리아지)을 `check`가 **하나도 쓰지 않는다** — 심각도가 단일 등급이고 예산도 순위도 없다. 직접 호출은 HANDOFF가 경고한 함정을 **회피가 아니라 소멸**시킨다: `struct.*` 수집기가 아예 실행되지 않아 `target_text`가 전 구간 `None`으로 남는다. 규격 판정의 원천은 `spec/check.py` 하나이고 `translate` 경로와 `check`가 양쪽 다 그것을 쓰므로 두 경로가 갈라지지 않는다
- **`--limit N`과 요약 이중 출력** (FR-8.2 표면 확장). 위반 목록을 N건까지만 출력한다. **기본값 `0`은 무제한**이라 지정하지 않으면 기존과 동일하다 — 상한을 기본으로 켜면 전체 목록을 파이프로 받아 grep하던 쓰임이 조용히 잘린다. 함께 **요약 줄을 헤더 바로 아래와 맨 끝 양쪽**에 낸다: 이전 판은 맨 아래 한 줄이라, 위반 682건이면 686줄이 나가고 26화 × 3언어 매트릭스에서 프로파일을 잘못 물리면 약 5만 줄이 쌓이는데 **로그를 앞에서 남기고 뒤를 자르는 CI에서 가장 중요한 한 줄이 가장 먼저** 사라졌다. 셋을 계약으로 못 박았다 — ① **종료 코드는 `--limit`에 영향받지 않는다**(판정의 결과이지 출력의 결과가 아니다. 3건만 보여준다고 위반이 3건인 것은 아니다), ② **요약은 언제나 전체 기준**이다(자른 뒤에 세면 `--limit 3`이 "위반 3건"이라는, 종료 코드와 모순되지 않아 사용자가 검증할 수 없는 거짓말을 낸다), ③ 절단은 `_format_report` **안**에서 한다(호출부에서 `lines[:N]`으로 자르면 요약 줄까지 함께 잘려 목적이 정확히 무너진다). 생략 고지 `... N건 생략 (전체는 --limit 0)`은 상한이 위반 수보다 클 때는 내지 않는다 — `0건 생략`은 그 자체로 거짓말이다. 음수 인자는 typer `min=0`이 본문에 닿기 전에 종료 코드 2로 거른다. `_format_report`가 순수 함수인 것이 여기서 배당금을 냈다(설계 §7.4) — 절단·요약 배치 계약 넷을 `CliRunner` 없이 문자열 입출력으로 고정했다

### Changed

- **위험도 융합을 가중 평균에서 noisy-or로** (`78921d4`, FR-6.1). `1 - ∏(1 - sᵢ)^wᵢ`. 가중 평균은 **문제를 하나 더 찾을 때 위험도를 내렸다** — 규격 위반(1.0) 하나는 1.0인데 용어 위반(0.5)이 붙으면 0.75가 되어 검수 큐에서 밀려난다. 벤치가 그 비용을 쟀고(오라클 대비 달성률이 예산 10%에서만 69.6%로 꺼짐), 그 구간이 기본 운영점이다. **가중치는 지수로 들어간다** — `(1-s)^w`는 "이 신호를 w번 관측했다"로 읽혀 `w=0`이 ablation의 신호 끄기와 의미가 일치한다. 점수 스케일 가중(`1 - ∏(1 - w·s)`)은 `w·s > 1`에서 곱의 부호가 뒤집혀 `w ≤ 1` clamp를 강제하고, 그러면 신호 강화를 표현할 수 없다. FR-6.1은 "설정 가능한 가중치"만 요구하고 가중 평균을 명시하지 않으므로 **요구사항 변경이 없다**. 범위 정합성이 이제 `Signal.score ∈ [0,1]`의 생성 시점 검증에 의존한다(밑이 [0,1]이고 지수가 음수가 아니면 곱도 [0,1]) — 가중 평균 시절에는 clamp가 마지막 방어선이었으나 이제 산식 자신이 보장하고 그 보장이 상류 모델을 전제로 한다. D-22(총합 유한성) 검사는 남기되 주석을 정정했다: noisy-or는 곱셈이라 그 실패 모드가 사라졌지만 `inf` 총합은 설정 오타의 신호다. **채택은 재측정 결과에 걸지 않았다** — 벤치 숫자를 보고 융합식을 고르면 그것이 벤치 데이터로 제품을 튜닝하는 것이다(스펙 §1.1)
- **재측정 — 두 변경의 효과를 분리해 귀속했다** (`57bbded`). 시드·코퍼스·트랙 동일(`seed=20260729`). A2와 A1을 각각 커밋한 뒤 2단계로 측정했다 — 두 변경의 효과가 반대 방향일 수 있어 1회 측정은 상쇄되면 "변화 없음"으로 읽힌다

  | 예산 10% | 기준 | +A2 (NFKC) | +A1 (noisy-or) |
  | --- | --- | --- | --- |
  | en-ko Recall | 69.60% | 69.60% | **72.40%** |
  | en-ko 배수 | 6.96x | 6.96x | **7.24x** |
  | ja-ko Recall | 70.60% | 73.20% | **75.20%** |
  | ja-ko 배수 | 7.06x | 7.32x | **7.52x** |
  | hard fail 오탐률 (en/ja) | 0.956% / 0.911% | 0.956% / **0.778%** | 0.956% / 0.778% |
  | `negation` Recall @10% | 1.41% | 1.41% | 1.41% |

  **en-ko가 A2에서 한 자리도 움직이지 않은 것이 진단의 검증이다** — NFKC 결함이 일본어 전각 숫자 현상이라면 en-ko는 변하면 안 된다. `negation`은 전 단계에서 소수점까지 동일해 **결정론적 신호를 어떻게 조합하든 의미 판단에는 닿지 못한다**는 것을 재확인했다(Tier 1 투자 근거 유지). ablation `struct.number_missing`이 두 언어쌍 모두 음수 → 양수로 뒤집혔고(en-ko −0.0240 → +0.0040, ja-ko −0.0140 → +0.0320), en-ko는 A2로 전혀 안 움직였는데 A1만으로 부호가 바뀌었다 — **이 신호가 해로웠던 것은 검출 정확도가 아니라 희석 때문**이었다. **스펙 §7.1의 예측은 빗나갔다**: "noisy-or에서 `length.ratio`의 해악이 악화될 가능성이 실재한다"고 적었으나 실제로는 완화됐다(ja-ko −0.0880 → −0.0680). 가중 평균에서는 오탐이 진짜 신호를 **희석해 끌어내렸고** noisy-or에서는 **자리만 차지한다**
- `bench/report.py`의 hard fail 오탐 원인 목록을 **실제 잔여 한계로 교체** (`1c6473f`). NFKC 정규화가 붙으면서 "검출기가 전각 숫자를 다른 값으로 본다"는 서술이 거짓이 됐다. **지우기만 하면 안 된다** — 오탐은 여전히 남고, 남은 것의 정체를 독자가 귀속할 수 없으면 숫자만 있고 설명이 없는 절이 된다. 한자 수사(`10대` → `十代`)로 갈아 끼웠다: NFKC로 해결되지 않는 진짜 잔여 한계이고(`十分に` ≠ 10분이라 파서를 붙이면 새 오탐이 생긴다), 이전 판이 원인 목록에서 통째로 빠뜨렸던 갈래다. **이전 판의 "ja-ko 31.7%"는 과대평가로 드러났다** — 전각 숫자를 포함한 16건 중 정규화로 해소된 것은 8건이고 나머지 8건은 단위 환산(`80마일` → `1３0キロ`)·표기 체계(`30%` → `３割`)라 전각과 무관했다. **"전각 숫자가 있다"와 "전각 때문에 오탐이다"는 다르다.** 테스트는 한 줄도 고칠 필요가 없었다 — I-2에서 `assert "billion" in md`를 값 연동으로 바꾸면서 "검출기의 알려진 한계"라는 **계약**만 고정해 뒀기 때문이다. `assert "NFKC" in md`로 고정했다면 이 수정과 함께 실패했을 것이다
- `pyproject.toml` — `specs/`를 휠에 `force-include` 한다. `src/cuesift/` 밖이라 그대로 두면 설치본에서 `load_builtin`이 깨진다 (`c79f752`)
- `README.md` 상태 경고 — "요구사항 정의서와 CLI 골격만 있다"는 서술을 실제 상태(Tier 0 엔진 구현, CLI 미배선)로 고쳤다. **갱신**: Tier 0 실측 완료를 반영하고 최상단에 "실측: Recall @ Budget" 절 추가 — 헤드라인 배수는 요청 예산과 실제 검수 비율이 일치하는 첫 지점인 **예산 10%**에서 뽑는다(en-ko 6.96x · ja-ko 7.06x). 더 낮은 예산(1~5%, 8.6x)이 배수는 크지만 hard fail만으로 채워져 실제 검수 비율이 요청 예산보다 훨씬 높아 인용에 부적절하다고 판단했다. 한계 절에 **"이 배수는 의미 오류에는 적용되지 않는다"**를 명시 — `negation`만 보면 Tier 0는 무작위보다 못하다(아래 참고)
- 구현 계획 문서에 **"구현 중 바뀐 결정"** 절 추가 (`e2c7d39`). 리뷰가 걸러낸 결함 17건 중 15건이 계획 자체의 결함이었고, 각각의 원인·결정·근거를 D-1~D-21로 기록했다. 본문 코드 블록은 고치지 않았다 — 최종 코드로 갈아 끼우면 계획이 구현과 같은 것을 두 번 적은 문서가 된다
- `docs/AI_자막검수_오픈소스_비교.md` §5 조사 질문 #2("경량 신호만으로 트리아지가 성립하는가") — 🔬 측정 필요 → **✅ 닫힘**. "미결정 Q4와 같은 실험에서 답이 나온다"던 이전 서술을 정정했다 — Q4(자가일관성 유사도)는 LLM 재번역이 필요한 Tier 1 질문이라 이 벤치의 측정 범위 밖이고, 두 질문은 별개로 닫힌다. §5 한계 목록의 "벤치마크 전용 `specs/ted.yaml`을 분리하기로 했다"는 서술도 실제 구조(언어별 `ted-ko`·`ted-en`·`ted-ja` 세 파일)로 정정
- `docs/요구사항정의서.md` §12 Q4 — 정량 근거 추가·강화. 의미 반전 Recall이 예산 10%에서 1.41%에 그친다는 실측에 이어, **같은 예산의 무작위 기준선(9.61%)보다도 낮다**는 재측정 결과를 반영했다. **Q4 자체는 이 벤치로 닫히지 않는다**는 것을 명시 — Tier 1(자가일관성·역번역)을 구현해야 답할 수 있는 질문이기 때문이다
- **`--fail-on` 값을 `hard\|soft\|never`에서 `hard\|any\|none`으로** (`90c6a7d`, FR-7.5). 요구사항정의서가 단일 진실 원천이므로 문서가 아니라 **코드를 고쳤다.** 마침 v0.1이 단일 등급을 택했으므로 `soft`는 **존재하지 않는 등급을 가리키는 이름**이 되어 더 맞지 않았다. CLI가 배선 전이라 호환 부담이 없었다. **`hard`와 `any`가 v0.1에서 같은 결과를 낸다는 사실을 `--help`와 README에 명시했다** — 조용히 같으면 사용자는 둘이 다르다고 믿는다(설계 §11 R1). 등급을 발명하지 않은 근거는 배정의 출처가 없다는 것이다: 프로파일 수치를 재 보니 `ja`↔`ted-ja`가 CPS 2.50배·줄폭 1.62배로 **두 축 모두 흔들려** 프로파일 독립인 축이 없고, 1차 출처인 Netflix TTSG에 위반 등급 구분이 없다(§11 R8 "출처 없는 수치를 기본값으로 넣지 않음")
- **`--spec`이 `.yaml`/`.yml` 경로를 받는다** (`f16bb0f`, FR-5.3). `load_profile(path)`는 있었으나 **CLI에서 도달할 방법이 없어** 必 요구사항인 "사용자가 규격을 덮어쓸 수 있다"가 닫혀 있었다. **존재 여부가 아니라 확장자로 가른다** — 존재 여부로 가르면 오타 난 파일 경로가 "내장 이름이 없다"는 **틀린 진단**을 받는다(인제스트가 mp4를 `decode` 오류로 보고하지 않으려고 확장자를 먼저 본 것과 같은 판단). `.yml`을 함께 받는 것은 실측으로 정정한 설계 결함이다 — `.yaml`만 가르면 D10이 막으려던 그 틀린 진단이 `.yml` 사용자에게 그대로 난다. **라우팅만 소문자로 본다**: Windows는 파일명 대소문자를 구분하지 않아 `my-spec.YAML`이 정상인 파일명인데 CI의 Linux는 구분하므로 `load_profile`에는 원본을 넘긴다
- **규격 프로파일 로더가 잘못된 내용을 전부 `ValueError`로 정규화한다** (`8845d5a`·`70f5aef`). 값 타입 오류·`bool`·`NaN`·`inf`·표현 범위를 넘는 정수·YAML 문법 오류를 거부한다. **`cli.py`가 예외를 열거하지 않는 것이 이 계약 덕분이다** — 열거는 계약이 아니라 관찰이라 로더가 새 예외를 낼 때마다 뒤처지고, 뒤처진 쪽으로 샌 예외는 종료 코드 1("규격 위반 발견")이 된다. 실제로 그 튜플이 **두 번 넓어지고도 세 번째 누락이 남아 있었다**
- **`check` 출력 헤더의 낱말을 `큐 N개`에서 `검사 큐 N개`로** (`7d3e5f6`). `cue_total`은 필터 **후** 세그먼트 수인데 `#N`은 **원본** 큐 번호라, 주석이 있는 파일에서는 `큐 2개` 아래 `#4`가 찍혀 자기모순으로 읽힌다. **분모를 원본 이벤트 수로 되돌리는 것은 오답이다** — 검사 대상이 아닌 주석·드로잉까지 세면 위반 비율이 과소평가되고 그것은 Recall@Budget 지표를 직접 건드린다. 낱말만 좁혀 모호함을 없앴다. 같은 커밋에서 `profile_name`을 **`profile_label`** 로 바꿨다: `--spec ./our-spec.yaml`인데 그 파일이 `name: ko`면 헤더가 내장 `ko`와 **바이트 단위로 같아져**, 헤더를 둔 이유("엉뚱한 프로파일로 통과한 것을 알 수 없다")가 하필 FR-5.3 경로에서 정확히 무효화된다

### Fixed

- **음수 타임코드가 exit 0으로 통과했다** (FR-1.1). `(-5000, -1000)`처럼 규격 위반이 없는 트랙은 **"위반 없음"이라는 보고와 함께 CI를 지나갔다**(실측). 부호를 보는 검사가 아무 데도 없었다 — `_require_int_timecodes`는 타입만, `_to_segments`의 `event.end < event.start`는 역전만, `Segment.__post_init__`도 역전만 봤다. 위반이 있는 경우에도 `_format_timecode`의 `max(ms, 0)`이 `00:00:00.000`을 찍어 **좌표를 날조했다** — 검수자가 그 자리를 믿고 찾아가면 아무것도 없다. `_require_non_negative_timecodes`(reason `negative_timecode`, exit 66)를 인제스트 경계에 넣고 검사 순서를 **타입 → 부호 → 역전**으로 고정했다(역전이 먼저면 `(1000, -3000)`에서 음수가 메시지에서 사라지고 사용자는 역전만 고친 채 재생 불가능한 파일을 얻는다). `_format_timecode`의 클램프는 부호 보존으로 바꿨다(`-3000` → `-00:00:03.000`). **규격 위반(exit 1)이 아니라 66인 근거**: CPS·줄길이는 검수자가 그 큐의 텍스트를 고치면 되지만 음수 좌표는 싱크·변환 파이프라인의 사고다. 섞으면 CI가 두 사고에 같은 대응을 한다. 이 결함은 **주석의 거짓 전제로 유지되고 있었다** — `_format_timecode` 독스트링이 클램프를 남긴 근거로 "음수는 `Segment.__post_init__`과 인제스트 경계가 이미 막는다"를 들었는데 사실이 아니었고, 기존 테스트 `test_format_timecode_clamps_a_negative_to_zero`가 그 동작을 **계약으로 고정**하고 있었다. 이전 세션이 남긴 "주석·계획서의 주장은 테스트와 같은 기준으로 검증한다"에 사례가 하나 더 늘었다

아래 여덟은 **`check` 배선이 사용자에게 보이는 형태로 고친 것**이다. 전부 실측으로 재현한 뒤 닫았다.

- **테스트 5건이 `rich`의 렌더링에 의존해 Linux CI에서만 실패했다.** 로컬(Windows·Python 3.14)에서 485 passed였는데 CI의 `test 3.11`·`test 3.12`가 둘 다 실패했다 — **이 브랜치의 35커밋은 그때까지 CI를 한 번도 거치지 않았다**(원격이 설계 커밋에 멈춰 있었다). 원인은 `rich`가 **호스트 플랫폼에 따라 다른 박스 문자**를 쓰는 것이다.

  | 환경 | `rich`의 모서리 | cp949 |
  | --- | --- | --- |
  | Windows (`legacy_windows=True`) | `┌┐└┘` (U+250C·2510·2514·2518) | **인코딩 된다** |
  | Linux CI (`legacy_windows=False`) | `╭╮╰╯` (U+256D~2570) | **인코딩 안 된다** |
  | **실제 실행 + 리다이렉트** | **박스를 아예 안 그린다** | 무관 |

  `test_help_output_is_encodable_in_the_cp949_locale`은 렌더된 출력을 통째로 `cp949`로 인코딩해 봤고, 독스트링은 그것을 "rich가 둥근 모서리로 바꾸면 Windows 사용자에게 깨지는 회귀"라고 정당화했다. **실측으로 셋 다 틀렸다**: Windows의 rich는 애초에 둥근 모서리를 안 쓰고, 사용자가 `cuesift --help > help.txt`를 하면 rich는 **박스를 아예 안 그리며**(실측: 1381바이트에 박스 문자 0개, exit 0), 따라서 그 단언은 **보호하려던 사용자에게 일어날 수 없는 조건**을 검사한 것이었다. 박스 문자는 `CliRunner`가 만들어 낸 산물이다. 테두리·ANSI를 걷어내고 **우리가 쓴 문자열만** 검사하도록 좁히고, 실제로 데였던 원인(em dash U+2014)을 **이름으로 지목하는 테스트를 따로** 뒀다 — `encode("cp949")`는 "무언가 인코딩되지 않는다"까지만 말해 실패 메시지가 원인을 가리키지 못한다. 같은 이유로 `rich`가 렌더한 stderr에 **긴 문자열·경로를 통째로 단언하지 않는다**: 강제 개행 위치가 임시 디렉터리 경로 길이에 좌우돼 **로컬에서는 `utf-8로 읽을 수 없다`가, CI에서는 `cp949-spec.yaml`이 끊겼다.** 정규화 헬퍼를 `tests/conftest.py`에 두고 양쪽에서 쓴다. **고친 뒤 em dash를 다시 심어 두 렌더링 모두에서 RED을 확인했다** — 단언을 약하게 만들어 통과시킨 것이 아니다

- **`--config`가 조용히 무시되던 것을 고쳤다** (설계 D12). 옵션은 그룹 콜백에 **처음부터 있었고** `--config x.yaml check a.srt --spec ko`가 **아무 말 없이 exit 0**으로 끝났다. 게다가 `--help`가 **거짓을 현재형으로 약속**했다 — "기본: `./cuesift.yaml`"과 "CLI 인자가 설정 파일보다 우선합니다"는 둘 다 사실이 아니다(그 파일은 읽히지 않고 우선순위 해결은 존재하지 않는다). **사용자가 자기 규격을 지정했다고 믿는데 내장 프로파일로 검사되고 종료 코드 0으로 통과한다** — 이 저장소가 1급으로 금지한 "검사하지 않고 통과하는 게이트"이며, D12가 적은 근거와 정확히 같은 문장이다. 그룹 콜백에서 stderr로 경고하고(산출물이 아니므로 stdout을 오염시키지 않는다) `--help` 문구를 미래형으로 고쳤다. **이 결함은 "구현하지 않는다"는 판단이 거짓 전제 위에 있어서 생겼다** — 근거로 든 실측 `check x.srt --config c.yaml` → exit 2는 **옵션이 없는 위치에서만** 시험한 것이었다(`--config`는 그룹 옵션이라 서브커맨드 **앞**에 와야 한다). 회귀 테스트는 경고 유무뿐 아니라 **stdout 산출물과 종료 코드가 `--config` 유무와 무관하게 같은 것**까지 고정한다
- **비-utf-8 프로파일의 진단이 자막 경로보다 나빴다.** `profile.py`의 `read_text`가 `try` **밖**에 있어 `'utf-8' codec can't decode byte 0xc0 in position 6`이 그대로 나갔다 — **어느 파일인지도, 무엇을 하라는 것인지도 알 수 없고 한국어도 아니다.** `UnicodeDecodeError`가 `ValueError`의 자식이라 종료 코드는 이미 2로 맞았으므로 **증상이 메시지 품질에만 나타나 눈에 띄지 않았다.** `loader.py`의 `decode` 오류와 같은 형태(파일 · 바이트 위치 · 해법)로 정규화해 두 경로의 진단을 일치시켰다. `load_profile` 독스트링의 "내용이 잘못된 경우는 전부 `ValueError`" 계약과도 맞다

- **프로파일·자막 파일 문제가 종료 코드 1("규격 위반 발견")로 오보되던 것을 고쳤다** (`1be2c84`·`8845d5a`·`70f5aef`·`59c7b51`·`955b4cd`·`c61a067`). 파이썬 예외가 커맨드 본문을 빠져나가면 미처리 traceback이 되어 **exit 1**이 나가는데, 이 저장소에서 1은 "규격 위반 발견"이다. **설정 사고가 자막 결함으로 읽혀 사용자가 멀쩡한 자막을 고치려 든다.** 실측으로 닫은 누수 경로가 **12종을 넘는다.**

  | 무엇이 틀렸나 | 옛 종료 코드 | 지금 |
  | --- | --- | --- |
  | 알 수 없는 내장 프로파일 이름 · 없는 `.yaml` 경로 | 1 | **2** |
  | YAML 문법 오류 · 값 타입 오류 · `bool` | 1 | **2** |
  | `NaN` · `inf` · 표현 범위를 넘는 정수 | 1(또는 조용한 통과) | **2** |
  | 읽을 수 없는 파일 (권한 · 편집기·OneDrive 잠금) | 1 | **66** |
  | JSON 껍데기 `{"info": {}}` 계열 5변종 | 1 | **66** |
  | 확장자만 `.srt`인 JSON | 1 | **66** |
  | 타임코드가 정수가 아님 (`float`·`str`·`bool`·혼합 5변종) | 1 | **66** |
  | 텍스트가 문자열이 아님 (`.json`·`.srt`) | 1 | **66** |

  표의 오른쪽 열이 이 수정의 전부다 — **"0이 아님"으로 뭉뚱그리면 어느 줄도 검증되지 않는다.** `pysubs2`가 JSON을 **내용으로** 판별하므로 확장자로는 막을 수 없고, 타임코드 타입은 `_format_timecode`의 `{hours:02d}`에서 터지는데 **위반이 0건이면 그 코드에 닿지도 않아** 수정 전에도 `exit 0 · "위반 없음"`으로 조용히 통과했다(그래서 회귀 픽스처의 본문을 일부러 규격 위반으로 만들었다)
- **파이프가 먼저 닫히면 종료 코드가 120 또는 1로 덮어써지던 것을 고쳤다** (`59c7b51`·`6b45f95`·`819b861`). `cuesift check ... | head -20`은 `head`가 정상 종료하며 파이프를 닫는 흔한 사용법이다. **예외를 나중에 잡는 방식은 "누가 썼는가"에 따라 구멍이 났다** — exit 2는 click의 `UsageError.show()`가, 70은 `typer.secho`가 쓰므로 커맨드 본문의 방어가 닿지 않는다. 실측된 회귀: `cuesift check nope.srt --spec ko 2>&1 | head -0`이 2가 아니라 **0**으로, `transcribe`도 70이 아니라 **0**으로 나갔다. **120은 시끄럽지만 0은 조용히 CI를 통과시킨다.** 쓰기 지점 자체를 무해하게 만드는 프록시(`_TolerantOutput`)로 어느 코드 경로가 쓰든 같은 결과가 되게 했고, fd를 `os.devnull`로 갈아 끼워 **인터프리터 종료 시의 재-flush**가 120을 만들지 못하게 했다(파이썬 객체만 바꾸면 부족하다 — 실측: 그대로 두면 120, `dup2`하면 0). **`errno`가 플랫폼마다 다른 것이 함정이었다**: POSIX는 `BrokenPipeError`(EPIPE)지만 **Windows는 평범한 `OSError` errno 22(EINVAL)** 이고 `isinstance(exc, BrokenPipeError)`가 False다. `except BrokenPipeError`만 다는 해법은 Linux에서만 동작하고 개발 플랫폼에서는 조용히 안 먹는다. **`ENOSPC`는 어느 층도 삼키지 않는다** — 디스크가 차서 잘린 출력이 성공으로 보고되면 그것이야말로 "검사하지 않고 통과하는 게이트"다
- **비-cp949 파일명에서 깨끗한 자막이 exit 1로 나가던 것을 고쳤다** (`4899fec`). **출력에 실리는 것은 우리 리터럴만이 아니다** — 사용자가 준 파일 경로와 `--spec` 경로가 그대로 헤더에 들어간다. Windows 기본 로케일(cp949)에서 인코딩할 수 없는 문자가 그 안에 있으면 리다이렉트 시 `UnicodeEncodeError`로 프로세스가 죽고 **종료 코드 1**이 나가, **위반 0건인 파일이 CI에서 "규격 위반"으로 읽힌다.** 실측된 사례: `Amélie.srt`(U+00E9) · `S01E01 – ko.srt`(U+2013) — 자막 파일명에 흔한 문자들이다. **em dash 금지 규칙은 우리가 쓰는 리터럴만 통제하고 사용자 입력이 흐르는 이 경로는 못 막으므로**, 규칙이 아니라 스트림 설정(`errors="backslashreplace"`)으로 닫았다. `--help`·`--version`은 eager 옵션이라 그룹 콜백보다 먼저 렌더되어 여기가 닿지 않고, 그쪽은 리터럴에서 em dash를 빼는 것으로만 막힌다(전용 테스트가 고정한다)
- **`max_cps: .nan` 같은 값이 로드에 성공해 CPS 위반이 영원히 발화하지 않던 것을 고쳤다** (`8845d5a`). YAML이 `.nan`을 파싱하므로 **설정 파일에서 도달 가능하다.** NaN과의 모든 비교가 False라 **증상 없이 신호만 죽는다** — 게이트가 통과하는데 검사는 하지 않는, 이 저장소가 가장 나쁘다고 규정한 형태다. `risk/fuse.py`의 `nan` 가중치 사고(`95b5ff3`)와 같은 계열이 규격 프로파일 쪽에도 열려 있었다
- **요구사항정의서 §3.2 S3와 §8.1의 예시가 실행되지 않았다.** `cuesift check episode01.th.srt --spec th`는 **내장 프로파일에 `th`가 없어** exit 2로 끝난다(`en`·`ja`·`ko`·`ted-en`·`ted-ja`·`ted-ko` 여섯뿐이다). Q2가 초기 언어쌍을 ko→en/ja로 확정했으므로 태국어는 v0.1 범위 밖이고, 이 예시는 **Q2 확정 이전의 잔재**였다. `--spec ja`로 정정하며 **파일명도 함께 바꿨다** — `--spec`만 고치면 `episode01.th.srt --spec ja`가 되어 "태국어 파일을 일본어 규격으로 검사"하는 더 이상한 명령이 된다. **S1의 `--to en,ja,th,vi`는 손대지 않았다**: 그것은 번역 대상 언어이지 규격 프로파일이 아니고, 목록에 `ja`가 있어 "S1에서 번역한 것 중 하나를 S3에서 검사한다"는 시나리오 연결이 유지된다. 문서에 적힌 명령이 실행되지 않는 것은 **문서가 검증되지 않았다는 신호**이고, 이 저장소에서 그것은 "검사하지 않고 통과하는 게이트"와 같은 부류다 (사용자 승인 2026-08-13)

- **`struct.number_missing`이 전각 숫자를 놓쳤다** (`2314fc6`, FR-3.4). `_numbers()`가 NFKC 정규화를 하지 않아 `'５０' != '50'`이었다. 집합 비교에서 누락 판정 → 두 자리라 `multi_digit` → **hard fail**. hard fail은 검수 예산을 우회하므로(FR-6.2) 이 오탐이 실제 검수 비율을 부풀려 §9.1의 배수를 파괴한다. **추출한 뒤에 정규화한다** — 텍스트 전체를 먼저 정규화하면 `½`(카테고리 `No`)가 `1⁄2`가 되어 **원문에 없던 숫자 1과 2가 생긴다**. `\d`는 `Nd`만 잡으므로 추출 후 정규화는 그 경로를 열지 않는다. 회귀 테스트 2건 중 두 번째("진짜 누락은 여전히 hard fail")가 없으면 첫 번째는 `_numbers`가 빈 리스트를 반환해도 녹색이 된다 — **오탐을 없애는 수정에는 미탐 방지선이 짝으로 붙어야 한다.** 한자 수사(`十代`)는 파서가 필요하고 `十分に`(≠ 10분)·`万一`(≠ 10001) 같은 관용구에서 새 오탐을 만들어 범위 밖으로 뒀다(독스트링에 한계로 명시)
- 위 수정이 **오탐 하나가 우연히 잡고 있던 진짜 오류를 드러냈다.** ja-ko 낮은 예산 Recall이 50.00% → 49.80%로 1건 떨어졌는데, 그 1건은 `glossary` 주입 오류였다(`160년` → `１６０年前と変わらぬ器具を使ってthingamajigを`). **숫자는 애초에 누락되지 않았고** spurious hard fail이 다른 유형의 오류를 우연히 잡고 있었다. **이것은 수정의 결함이 아니라 정직해진 것이다** — 이전 50.00%는 검출기가 옳아서 나온 숫자가 아니었다

이하는 전부 **계획 자체의 결함**이며 구현자가 계획을 정확히 따른 결과였다. 거의 전부 "흔한 입력에서는 맞는" 코드였고, 계획에 쓴 테스트가 그 흔한 경우만 담아 자체 검토로 걸러지지 않았다.

- `latin_half`가 East Asian Width `A`(Ambiguous)를 전각으로 세어 **같은 글자의 대소문자가 다른 폭**을 가졌다 — `é`(A)=1.0, `É`(N)=0.5. 유니코드의 Ambiguous 등급은 역사적 인코딩을 반영해 라틴 악센트 문자를 자의적으로 가른다 (`e3b5352`)
- 겹침 탐지가 인접 쌍만 비교해 포함 관계를 놓쳤다. 긴 자막이 여러 개를 덮으면 사이에 끼지 않은 것이 통째로 검사에서 빠졌다. 겹침량도 과대 보고했다(실제 100ms → 9900ms) — 이 값은 `review.json`에 실려 검수자가 본다 (`740f433`)
- 용어집 부분 문자열 매칭이 `AI`를 `rain` 안에서 찾아 위반을 통째로 놓쳤다. **계획의 예시 용어집이 `AI`를 써서 계획 자신의 데이터로 재현됐다.** 리뷰어의 `\b` 제안은 CJK 문자가 전부 `\w`라 조사 인접 시 경계가 생기지 않아 기각하고 룩어라운드로 대체했다 (`0784e20`)
- 반복 붕괴가 전체 최빈 토큰을 세어 `the cat sat on the mat with the dog` 같은 평범한 영어 문장에서 hard fail이 났다. hard fail은 예산을 우회하므로 이 오탐 하나로 Recall@Budget 지표가 무의미해진다 (`f3e0445`)
- 그 수정이 O(n²)를 낳았고 **하필 이 신호가 잡으려는 입력이 최악 케이스**였다(5000토큰 2479ms). 어절 상한 200으로 상수 시간이 됐다 (`34f4abc`)
- 태그 비교가 재직렬화 표기 차이(따옴표 스타일, `<br>` vs `<br/>`)를 마크업 손실로 오판했다 (`f3e0445`)
- 숫자 검사가 천 단위 콤마에서 `1,000`을 `['1','000']`으로 쪼개 보존된 숫자를 누락으로 잡았다. 한 자리 수를 단어로 옮긴 정상 번역(`3시` → `three`)의 hard fail도 해제했다 (`f3e0445`)
- 미번역 판정이 짧은 세그먼트에서 과민했다(`Hi 가` = 25%). 최소 문자 수를 함께 요구한다 (`f3e0445`)
- 길이비 척도가 MAD 미소 시 붕괴해 중앙값 대비 0.4% 편차가 z=4.72가 됐다. **첫 수정(평균절대편차 병용)이 틀려** 벤치마크 주입률 10%에서 재현율이 절반으로 떨어졌고, MAD 복귀 + 실질 편차 게이트로 다시 고쳤다 (`318229d`, `3b82943`, `198cd29`)
- 신호 간 점수 스케일이 어긋나 균등 가중 평균에 **암묵적 가중치**가 생겼다(규격 1건=0.33 vs 용어 1건=1.0). 공용 `_violation_score`로 통일했다 (`318229d`)
- `nan` 가중치가 `nan < 0 == False`로 음수 검사를 통과하고 `max(0.0, nan) == 0.0`이라 **최대 위험이 최소 위험으로 뒤집혔다.** YAML이 `.nan`을 파싱하므로 설정 파일에서 도달 가능하다 (`95b5ff3`)
- `review_ratio(select_by_budget(...))`가 항상 1.0을 냈다 — 이 값이 README 배수의 분모다. 계획의 테스트가 손으로 재조립하던 것이 결함의 증거였다 (`bfa0f72`)
- `check_overlaps`가 어떤 신호에도 연결되지 않아 **FR-5.1의 중첩 금지가 위험도에 도달하지 않았다.** 개별 태스크 리뷰가 구조상 볼 수 없는 결함이다 (`ad76a8c`)
- `length.ratio`가 짧은 원문에서 계통 오탐했다 — 깨끗한 400건 트랙에서 33건 발화, 전부 오탐(`네` → `Yes`가 비율 3.0) (`3718e3a`)
- `select_by_threshold`에 인자 검증이 없어 `threshold=nan`이 hard fail 외 전량을 조용히 검수에서 뺐다 (`8b9af8f`)
- `docs/번역관리_TMS_솔루션_비교.md:5`의 깨진 링크 처리 — `글로벌_OTT_플랫폼.md`는 이 저장소에 존재한 적이 없다. 링크에서 일반 텍스트로 강등하고 외부 문서임을 명시했다
- `bench/report.py`의 `RunMeta.excluded` → `injection_skipped`로 이름을 바로잡았다(fix 라운드 1, 팀장 지적). 옛 이름과 절 제목 "제외 건수"는 `glossary 4,041`을 "5,000건 트랙에서 4,041건이 빠졌다"로 읽히게 했다 — 실은 "해당 유형의 주입 조건을 만족하지 않아 건너뛴 세그먼트 수"이며, 그 세그먼트들은 뒤에 처리되는 유형의 주입 후보로 다시 검토되고 끝까지 배정되지 않으면 **오류 없이 깨끗한 상태로 트랙에 남는다**(fix 라운드 2에서 "다른 유형으로는 정상 주입됐다"는 과장된 서술을 이 표현으로 정정했다). 또한 §4.4 부수 산출물(코퍼스 규격 미충족률)이 어느 리포트에도 실리지 않았던 것을 사이드카 도입으로 바로잡았다
- **가중치 총합 오버플로가 최대 위험을 최소 위험으로 뒤집었다** (D-22, `097e1ec`). D-16이 `nan`·`inf`를 막았으나 `math.isfinite`를 항목 하나씩만 적용해, `1e308` 두 개는 개별로는 유한해 통과하고 합이 `inf`가 되면 `weighted / total_weight = inf / inf = nan` → `min(1.0, max(0.0, nan)) = 0.0`이 됐다. **D-16 주석이 "막았다"고 선언한 실패 모드가 합계 경로로 그대로 재현됐고**, `cace3d4`의 `SegmentRisk` 범위 검증도 clamp가 먼저 NaN을 삼켜 발동하지 않았다. 회귀 테스트는 수정 전 코드에서 `DID NOT RAISE`로 실패하는 것을 확인한 뒤 붙였다
- **`Span.side`에 런타임 검증이 없었다** (D-23, `341b357`). `cace3d4`가 `SegmentRisk`에 "형제 세 모델과 같은 방어"를 근거로 범위 검증을 추가하면서 **같은 커밋이 새로 넣은 필드는 무방비로 뒀다** — `Literal`은 타입 힌트일 뿐이라 `"both"`·`None`·`0`이 전부 통과했다. 바로 두 줄 아래 `start`/`end`는 이미 막고 있었다
- **리포트가 실재하는 검출기 결함을 부인했다** (최종 브랜치 리뷰 I-2, `b6285ca`). hard fail 오탐 설명이 언어쌍과 무관하게 "**검출기 버그가 아니라** 만·억 vs billion 때문"이라 단언했는데, 원인별 실측 분류에서 **ja-ko는 41건 중 0건**이 그 원인이었고 **31.7%가 진짜 검출기 결함**이었다 — `struct.number_missing`의 숫자 추출이 NFKC 정규화를 하지 않아 전각 `５０`과 반각 `50`을 다른 값으로 본다. 그리고 `assert "billion" in md`가 그 거짓 문장을 회귀 방지선으로 고정하고 있었다. 문장을 실측에 맞는 사실 나열로 바꾸고 NFKC 한계를 명시했으며, 테스트를 값 연동 단언으로 교체했다. **제품 코드는 고치지 않았다** — 측정을 끝낸 뒤 제품을 고치면 그 측정이 무효가 되므로 `HANDOFF.md`에 재측정 과제로 인계했다
- ablation 음수값이 무설명이었다 (I-3, `b6285ca`). 표 제목이 "Recall 하락폭"인데 `length.ratio`가 두 언어쌍 모두에서 **−6.0%p**다 — 내림차순 정렬 맨 아래라 "가장 덜 유용함"으로 보이지만 실제로는 "가장 해로움"이다. 기여도 `+0.0%`인 `spec.overlap`에는 오독 방지 절을 줬으면서 음수에는 한 줄도 없던 비대칭을 해소했다
- 코퍼스 제외율의 분모가 `CHANGELOG`·`HANDOFF` 두 곳에서 틀렸다 (I-4, `b6285ca`). 49.91%의 분모는 **필터 후 389,598**인데 "원본 399,414쌍 중"으로 적혀 있었다(그 조합이면 48.69%다). 리포트 본문과 README는 표로 분모를 함께 보여 정확했다 — **리포트는 고쳐졌는데 기록만 틀린 조합을 담은** 상태였다
- `inject`의 가드가 "실주입 0건"만 보고 quota 부분 미달은 보지 않았다 (`b6285ca`). 용어집 자격이 89건(quota 72)으로 여유가 1.24배뿐이라, 표본이나 용어집이 바뀌면 라벨 총계가 조용히 목표에 미달해 오류율이 10% 아래로 내려가고 오라클 상한·배수의 분모 관계가 바뀐다. `achieved[k] < quota[k]`도 같은 예외로 올렸다 — 스펙 §5.5가 "0 broken이 통과로 읽혔던 것처럼"이라며 세운 원칙의 연장이다
- 리포트의 커밋 스탬프가 항상 한 커밋 뒤처졌다 (I-1, `f4b32ca`). `report.py`를 고치는 커밋에서는 리포트 재생성이 커밋보다 먼저라 스탬프가 직전 커밋을 가리켰고, **그 커밋의 `report.py`는 방금 지운 거짓 문장을 렌더링한다.** 리포트만 재생성하는 기계적 커밋으로 구조적으로 닫았다
- `scripts/__init__.py`에 `from __future__ import annotations`가 없었고, docstring "표준 라이브러리만 쓴다"가 같은 패키지의 `glossary_verify.py`(`bench.corpus`·`cuesift.glossary` 임포트)에 대해 거짓이었다 (`b6285ca`)
- `docs/WBS.md`의 Mermaid 의존 그래프에서 `WP3` 노드가 `🔨 진행 중`(노란 style)으로 남아 있었다 — 작업 패키지 표와 "현재 위치" 진척 막대는 이미 `✅`였으나 도식만 갱신되지 않은 것이다. `✅ 완료`와 초록 style로 맞추고, 이번에 완료한 `WP4`도 함께 반영했다(도식이 최신 상태를 반영하지 못하면 표·막대·도식 세 표현이 서로 다른 진척을 주장하게 된다)

- **인제스트 설계 §2.1의 라운드트립 보존 주장 두 개가 거짓이었다** (`f9c51d8`). 최종 리뷰가 실행해 반증했다 — VTT cue settings(`line:0 position:20%`)는 `SSAEvent`에 담을 필드가 아예 없어 **로드 시점에 버려지고**, ASS 오버라이드 태그(`{\an8}`)는 `SSAEvent.plaintext` setter가 텍스트를 통째로 대체하므로 **번역을 되쓰는 이벤트에서는 사라진다**(건드리지 않은 이벤트에서만 남는다). 원본 `SSAFile`을 들고 다니는 결정 자체는 여전히 옳다 — 스크립트 헤더·스타일 정의·주석 줄·타임코드·포맷은 실제로 보존되고 라운드트립이 SRT·VTT·ASS·SSA 4포맷 전부에서 성립하는 것을 실측했다. **결론은 맞고 근거로 든 예시가 틀린 경우**이며, 그대로 뒀다면 WP5(출력)가 잘못된 목표를 세웠을 것이다. §10 위험표에 세 줄을 추가했는데 그중 하나가 **WP6이 밟을 함정**이다 — 인제스트 결과를 신호 엔진에 바로 넣으면 `target_text`가 `None`이라 `struct.empty`가 전 세그먼트에서 발화하고, hard fail은 FR-6.2에 따라 검수 예산을 우회하므로 검수 비율이 100%가 되어 Recall@Budget이 무의미해진다
- `docs/superpowers/plans/2026-07-31-ingest.md`의 Task 5 RED 예고치가 틀렸다 (`0480167`). "5건 중 4건 FAIL"로 적었으나 실측은 3건이다 — `test_filtered_events_survive_in_subs_for_roundtrip`은 원본 `subs`만 보는 **불변조건 테스트**라 필터 유무와 무관하게 통과한다. 구현자가 불일치를 은폐하지 않고 보고했고, 리뷰가 "그 테스트는 무엇을 막는가"에 답을 냈다 — 필터된 사본을 `subs`에 담고 `event_index`를 사본 기준으로 재계산하는 변경은 `event_index` 루프 단언을 **통과시키므로** `len(result.subs) == 4`만이 잡는다

아래 한 건은 성격이 다르다 — **구현이 아니라 테스트 자체의 결함**이며 대상 코드는 처음부터 옳았다.

- **리포트 테스트 2건이 독스트링의 주장과 달리 하드코딩 회귀를 못 잡았다.** `test_report_states_hard_fail_false_positive_rate`는 픽스처 `META` 하나로만 렌더해 `render_markdown`이 `"0.96%"`를 리터럴로 박아도 통과했다. `test_ablation_explains_negative_drops_as_harmful_not_just_unhelpful`은 **두 가지가 겹쳐** 무력했다 — 검증에 쓴 신호 이름 `length.ratio`가 실측에서 실제로 음수가 나오는 이름이라 제품 코드가 하드코딩할 수 있었고, 잘라내는 지점(`## 신호별 기여도` 이후)이 ablation **표**를 포함해 어떤 이름을 넣어도 거기 나타났다. HANDOFF가 남긴 수정 제안("픽스처 이름을 `zz.synthetic.probe`로", 1줄)은 전자만 고쳐 **뮤테이션이 여전히 통과했다** — 지적이 옳아도 해법은 틀릴 수 있다. 고친 뒤 두 결함을 실제로 `bench/report.py`에 심어 RED(2 failed)를 확인하고 복원했다. **교훈: 값 연동만으로는 부족하고 값이 두 가지 이상이어야 하며, 잘라내는 범위도 함께 검증해야 한다** — 픽스처가 하나면 "값에서 뽑은 것"과 "그 값을 박은 것"이 구분되지 않는다

### Removed

- (없음)

---

## [2026-07-28] 문서 구조 정리 · 용어 풀이 · 조사 질문 동기화

### Added

- `docs/AI_자막검수_오픈소스_비교.md` §0 용어 풀이 — QE·트리아지 두 축 심층 설명 + 용어 28개(음성·자막 생성 5 / 품질 측정 9 / 자막 규격·포맷 10 / 파이프라인 운영 4). 각 그룹을 §2의 계층(C·D·E)에 대응시켰다 (`cc8f109`)
- `docs/요구사항정의서.md` §0 용어 풀이 — 문서 표기 규약(`FR-`·`NFR-`·`必`·`Qn`·`★` 상자), 척추 4개념(세그먼트→신호→위험도→트리아지), 고유 용어 14개, 측정 용어 6개, 공통 용어 포인터 9개. 각 용어에 근거 FR·Q 번호를 병기해 요구사항 변경 시 역추적되게 했다 (`cc8f109`)

### Changed

- 조사 문서 3종을 `docs/`로 이동 — 최상위를 README·CHANGELOG·HANDOFF·라이선스로 정리. 세 문서는 형제 상대경로로 상호 참조하므로 함께 옮겨 링크가 유지된다. README·HANDOFF의 유입 링크만 수정 (`5ffda8d`)
- 용어 정의를 단일 출처로 통일 — 공통 용어(QE·MQM·CPS·화자분리·자막 포맷)는 AI 문서 §0에만 두고, 요구사항정의서 §0.5는 한 줄 요약과 포인터만 갖는다. 같은 용어를 두 문서에서 각각 정의하면 한쪽만 고쳐져 갈라진다 (`cc8f109`)
- 검증 수준 표기에 색상 이름 명시 — `docs/AI_자막검수_오픈소스_비교.md` 최상단의 🟢 → 🟢 **초록**, 🟡 → 🟡 **노랑** (`cc8f109`)
- 문서 버전 0.3 → 0.4 (`cc8f109`)
- `docs/AI_자막검수_오픈소스_비교.md` §5 조사 질문 6개에 상태와 근거를 부여 — #3은 ✅ 닫힘(Q6), #1·#4·#5는 ⏸ 연기(Q2·`FR-1.6`·`FR-5.5` = v0.3), #6은 ⚪ 불필요(§4에서 채택 후보 아님), #2는 🔬 측정 필요(Q4와 동일 실험)로 재분류. 상태 기호는 문서 전반의 검증 수준 🟢·🟡과 의미가 달라 재사용하지 않고 범례에 명시 (`11dbfe4`)
- §5 한계 목록의 각 항목에 대응처를 명시 — 모델 크기 → `NFR-1`, 평가 데이터 → §9.2, 샷 체인지 → `FR-5.5`(v0.3) (`11dbfe4`)

### Fixed

- §5 한계 항목의 "CometKiwi·xCOMET의 저자원 언어(th·vi·id) 성능이 **최우선 검증 대상**" 서술 정정 — Q2가 초기 언어쌍을 ko→en/ja로 확정한 이후 사실이 아니다. 조사 문서가 요구사항 결정 이전 상태로 굳어 있던 사례 (`11dbfe4`)

---

## [2026-07-27] 저장소 구축 및 미결정 사항 정리

### Added

- 프로젝트 골격 초기화 — Apache-2.0 라이선스·NOTICE, `pyproject.toml`(Typer·hatchling·src 레이아웃), `src/cuesift/cli.py`, 테스트 6건, `.gitignore`, `.gitattributes` (`66cf564`)
- GitHub Actions CI — Python 3.11/3.12 매트릭스에서 ruff·pytest 실행 (`66cf564`)
- CI에 `docs` 잡 추가 — markdownlint 게이트. 버전 고정, `set -eo pipefail`, `Linting: 0 files` 가드 포함 (`7956a0a`)
- `.markdownlint-cli2.jsonc` — 프로젝트 마크다운 관례를 명시 (`7af39a7`)
- 요구사항정의서 §8.3.1 — ko·en·ja 규격 프로파일 기본값과 1차 출처 (`7af39a7`)

### Changed

- CI 액션을 Node 24 기반 최신 메이저로 상향 — `actions/checkout` v4 → v7, `actions/setup-python` v5 → v7 (`48d127c`)
- 미결정 사항 Q2 해결 — 초기 대상 언어쌍을 ko→en/ja로 확정 (`9e9ee83`)
- 미결정 사항 Q3 해결 — 로컬 LLM은 OpenAI 호환 엔드포인트로 일원화하되 프로바이더 능력 탐지를 요구사항에 추가 (`9e9ee83`)
- 미결정 사항 Q5 해결 — Netflix Timed Text Style Guide를 규격 프로파일 1차 출처로 확정 (`7af39a7`)
- 미결정 사항 Q6 해결 — PySubtrans 대신 자체 얇은 어댑터로 결정 (`9e9ee83`)
- `char_counting` 스키마 재정의 — `grapheme`\|`cjk_width` → `grapheme`\|`latin_half`\|`fullwidth`. 기존 두 값으로는 한국어를 표현할 수 없었다 (`7af39a7`)
- 문서 버전 0.2 → 0.3 (`9e9ee83`)
- markdownlint가 `.gitignore`를 따르도록 설정 — 로컬 5개 / CI 4개로 어긋나던 검사 대상을 일치시킴 (`852cb68`)

### Fixed

- §7.4의 PySubtrans 배제 근거 정정 — "GUI가 딸려온다"는 사실이 아니다. `pyside6`는 `gui` extra로 분리돼 있으며, 실제 사유는 도메인 모델 동반이다 (`9e9ee83`)
- markdownlint 경고 76건 수정 — bare URL 42건, 목록·제목·코드펜스 주변 공백, 코드펜스 언어 지정, 인용문 병합, 제목 레벨 (`7af39a7`)
