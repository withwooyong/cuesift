# Session Handoff

> Last updated: 2026-09-03 (KST)
> **Q4 spike를 실측했고, 결론보다 먼저 알아야 할 것은 벤치 코퍼스의 결함이다.**
> 미확인 전제 둘은 **둘 다 통과**했다 — Ollama는 `/v1/embeddings`를 내고(모델만
> 있으면 된다), `qwen2.5:3b`는 부정을 정확히 옮긴다. 그런데 측정 도중
> **`bench/inject.py`의 negation 주입이 대상 언어와 무관하게 영어 `not`을 끼운다**는
> 것이 드러났다 — ja-ko의 negation 라벨 **71건 전부가 의미 반전이 아니다.**
> 표본을 직접 만들어 다시 재니 **두 언어쌍 모두에서 임베딩이 문자 단위를 이겼다.**
> **코드는 한 줄도 바꾸지 않았다.** 이번 세션의 산출물은 표와 이월 항목 하나다.
> **v0.1 완료 개수는 41 그대로다**(42개 중, 98%).
> 상태 값은 여기 적힌 숫자가 아니라 아래 "현재 상태 재는 법"의 명령으로 직접 재라.
> 진척은 [WBS](docs/WBS.md), FR 번호의 출처는 [요구사항정의서](docs/요구사항정의서.md)다

## Current Status

| 단계 | 상태 | 산출물 |
| --- | --- | --- |
| FR-6.3 ① `--review-top-k` | ✅ 머지됨 | PR [#24](https://github.com/withwooyong/cuesift/pull/24) · squash `d7e927c` |
| 이월 12·13번 — 설정 채널 타입 검증 | ✅ 머지됨 | PR [#25](https://github.com/withwooyong/cuesift/pull/25) · squash `619a239` |
| 직전 인수인계 | ✅ 머지됨 | PR [#26](https://github.com/withwooyong/cuesift/pull/26) · squash `5d4b607` |
| **Q4 spike — 전제 검증 + 2×2 측정** | ✅ **끝났다** | 아래 "Q4 spike 실측". **로컬 행만 채웠다** |
| **상용 API 행** | ⬜ **미착수** | 키를 못 받아 비어 있다. 아래 "다음 세션" 참고 |
| **이 문서** | PR [#27](https://github.com/withwooyong/cuesift/pull/27) | 코드 변경 0건 — `CHANGELOG.md`를 건드리지 않았다 |

**이번 세션은 코드를 한 줄도 바꾸지 않았다.** 그래서 `CHANGELOG.md`에 넣을 것이
없다 — Keep a Changelog는 코드 변경을 적는 문서이고, 실측 결과는 여기 적는다.

## 현재 상태 재는 법

**첫 일은 직전 작업이 어디까지 갔는지 확인하는 것이다.**

```bash
gh pr list --state all --limit 5 --json number,title,state,mergedAt
git branch --show-current                     # main
git status --short                            # clean
git log --oneline -3                          # 최상단이 5d4b607
```

## 다음 세션 — 사용자가 요청한 것을 먼저 답한다

**질문: OpenAI API 키를 어떻게 전달하고, 어떤 모델을 쓰는가.**

### 키 전달 — 사용자 환경변수를 쓴다

**스크래치 디렉터리에 두면 안 된다.** 세션마다 경로가 바뀌어 다음 세션이 못 찾는다.
**리포 안에 두어도 안 된다.** 커밋에 딸려 갈 위험이 있다.

```powershell
setx OPENAI_API_KEY "sk-..."
```

`setx`는 **새로 뜨는 프로세스부터** 적용된다 — 실행한 뒤 Claude Code를 재시작해야
읽힌다. 확인은 값을 찍지 말고 길이만 본다.

```powershell
.venv/Scripts/python.exe -c "import os; k=os.environ.get('OPENAI_API_KEY'); print('len', len(k) if k else None)"
```

이 프로젝트가 이미 쓰는 방식과도 맞는다 — STT live 테스트가 `CUESIFT_LIVE_STT_*`
환경변수를 쓴다(설계 스펙 D10).

### 모델 — 두 종류가 필요하다

spike는 **역번역(chat)** 과 **임베딩** 을 따로 부른다. Q3 결정에 따라 둘 다
OpenAI 호환 엔드포인트로 `httpx`가 직접 부르며, **파이썬 패키지를 추가하지 않는다.**

| 용도 | 모델 | 왜 이것인가 |
| --- | --- | --- |
| 역번역 | `gpt-4o-mini` | 로컬 `qwen2.5:3b`의 **코드 스위칭이 사라지는지**가 이 행의 관심사다. 최상위 모델을 쓰면 "상용이라 좋다"와 "크니까 좋다"가 섞인다 |
| 임베딩 | `text-embedding-3-large` | 로컬 `bge-m3`(1024차원)의 대조군. 다국어에서 강하다 |

**규모와 비용**: 역번역 100회(en-ko 25쌍 + ja-ko 25쌍 × 2) + 임베딩 200건이다.
전부 짧은 자막 한 줄이라 비용은 소액이다.

**모델을 바꿔 달게 되면 `gpt-4o-mini` 자리에만 손댄다** — 임베딩까지 함께 바꾸면
2×2 표의 상용 행이 무엇을 재는지 다시 알 수 없게 된다.

### 착수 순서

1. 키를 환경변수에 넣고 Claude Code를 재시작한다
2. **표본을 다시 만들지 마라** — 아래 "재현 방법"의 씨앗과 규칙을 그대로 써야
   로컬 행과 비교된다
3. 상용 행 두 칸을 채우고 아래 판정표에 대입한다

## Q4 spike 실측 — 로컬 백엔드

### 전제 둘은 통과했다

| 미확인 전제 | 판정 | 근거 |
| --- | --- | --- |
| Ollama가 `/v1/embeddings`를 내는가 | ✅ **낸다** | 대조군 `/v1/nonexistent`는 **404**인데 이쪽은 **501** + `"Start it with --embeddings"`. `bge-m3` 설치 후 **200**. 서버가 아니라 **모델 능력** 문제였다 |
| `qwen2.5:3b`의 역번역 품질 | ⚠️ **조건부 통과** | 부정은 정확히 옮긴다(`"It not started"` → `"시작되지 않았습니다"`). 다만 **50건 중 19건(38%)** 에서 중국어·영어를 흘린다 |

**STT의 `/v1/audio/transcriptions` 부재와는 다른 부류다.** 그때는 경로가 없었고
이번엔 능력이 없었다 — **404와 501을 구별하지 않으면 같은 실패로 보인다.**

환경: Ollama **0.33.2** · `qwen2.5:3b`(3.1B, Q4_K_M) · `bge-m3`(566.70M, 1024차원).
`bge-m3`는 이번에 받았고 `capabilities`가 `['embedding']`이다 — 기존 두 모델은
`['completion','tools']`뿐이라 501을 냈다.

### 측정 결과 — 2×2에서 로컬 행

```text
                        문자 단위        임베딩 bge-m3
  ─────────────────────────────────────────────────────
  1차 (벤치 라벨 그대로, AUC, n=50)
    en-ko                0.636            0.858
    ja-ko                0.554            0.572   ← 표본 결함으로 무의미
  2차 (쌍 설계, 쌍 정확도, 25쌍)
    en-ko                0.800            0.920
    ja-ko                0.700            0.820
  ─────────────────────────────────────────────────────
  상용 API 행: 키 미제공으로 비어 있음
```

**두 언어쌍 · 두 측정 설계 모두에서 임베딩이 이긴다.** 1차 en-ko의
Recall@10%는 문자 1.60배 · 임베딩 **2.00배**였다.

### 왜 문자 단위가 지는가 — 백엔드를 바꿔도 안 변한다

| 실패 | 실물 | 성격 |
| --- | --- | --- |
| 문자 단위가 놓친다 | `"시작되었습니다"` vs `"시작되지 않았습니다"` → sim **0.714** | **한국어 부정은 `-지 않-` 몇 글자다.** 언어 구조라 모델을 바꿔도 그대로다 |
| 임베딩이 놓친다 | 역번역이 통째로 중국어(`'그러나,我还是不能使用洗手间。'`) → 다국어 임베딩이 원문과 매칭 | 모델 품질. **백엔드를 바꾸면 개선된다** |

**이 비대칭이 상용 행 없이도 방향을 말한다** — 문자 단위의 약점은 상용 API로
고쳐지지 않고, 임베딩의 약점은 고쳐진다.

오염을 분리해 보면 기여도가 갈린다(en-ko 1차, 오염 19건 제외 n=31):

| 무엇을 고치면 | AUC |
| --- | --- |
| 아무것도 안 고침 | 0.636 |
| 유사도 수단만 (문자 → 임베딩) | 0.858 (**+0.222**) |
| 유사도 수단 + 모델 품질 | 0.9125 (**+0.055**) |

**유사도 수단의 기여가 모델 품질의 네 배다.**

### HANDOFF 판정표에 대입하면

직전 세션이 정한 판정표의 첫 행에 해당한다.

| 결과 | Q4의 답 |
| --- | --- |
| **임베딩에서만 갈린다 (두 백엔드 공통)** | **임베딩이 필요하다. 본 설계로 넘어간다** |

**다만 "두 백엔드 공통"이 아직 아니다** — 로컬 하나뿐이다. 상용 행이 채워지기
전에는 Q4를 닫지 마라.

### 수치를 그대로 믿으면 안 되는 자리

| 유보 | 내용 |
| --- | --- |
| 오염 제외 시 en-ko가 뒤집힌다 | 2차에서 오염 없는 쌍만 보면 **n=8**에 문자 1.000 · 임베딩 0.875다. **표본이 너무 작아 판정에 못 쓴다**(같은 조건 ja-ko는 n=14에 0.643 vs 0.857로 임베딩 우위) |
| 쌍 정확도는 실전 지표가 아니다 | 실제 트리아지는 무작위 세그먼트를 **순위** 매긴다. 실전에 가까운 것은 1차의 AUC이고, 2차는 신호의 **순수 검출력**이다 |
| 코퍼스 정렬 노이즈 | `source_text`와 `target_text`가 대응하지 않는 세그먼트가 실재한다(`en-02153`·`en-03962`). 대조군 바닥을 갉는다 |
| n이 작다 | 각 칸이 25~50건이다. 방향은 재현됐지만 소수점 둘째 자리는 흔들린다 |

### 재현 방법 — 스크립트는 남기지 않았다

합의대로 throwaway였고 스크래치에만 있었다. 다시 만들 때 **아래 조건을 지켜야
이 표와 비교된다.**

| 항목 | 값 |
| --- | --- |
| 씨앗 | `20260903` |
| 표본 | 언어쌍마다 negation 25 · clean 25 (1차) / 25쌍 (2차) |
| 대조군 | **어떤 라벨도 붙지 않은** 세그먼트만. 라벨이 섞이면 "정상"의 바닥이 다른 오류로 오염된다 |
| 유사도(문자) | `src/cuesift/signals/similarity.py`의 `similarity()`를 **그대로** 쓴다 |
| 유사도(임베딩) | `/v1/embeddings`에 두 문장을 보내 코사인. `data[]`를 `index`로 정렬한 뒤 짝지운다 |
| 2차 쌍 만들기 | 무라벨 세그먼트의 `target_text`에서 **부정을 제거해** 짝을 만든다. en은 `is not→is`·`was not→was`·`never→always` 등, ja는 `ませんでした→ました`·`ません→ます`만 |
| 온도 | `temperature: 0` |

**2차에서 ja의 규칙을 둘로 좁힌 것은 의도다** — 「ない」계열은 어간 활용이 얽혀
제거하면 비문이 되고, 그러면 "의미 반전"이 아니라 "입력 파손"을 재게 된다.

## 새로 발견한 벤치 결함 — 이월 17번

**이것이 이번 세션에서 가장 오래 남을 산출물이다.**

`bench/inject.py:141`:

```python
tokens = text.split()
tokens.insert(1, "not")        # 대상 언어와 무관하게 영어 "not"
```

`_negation`은 `_NEGATIONS_EN = (" not ", " never ")`를 먼저 찾아 **제거**하고,
없으면 두 번째 토큰 자리에 영어 `not`을 **삽입**한다. 삽입 경로가 언어를 모른다.

| 언어쌍 | negation 71건의 구성 | 의미가 실제로 반전되는가 |
| --- | --- | --- |
| en-ko | `removed` 10 · `inserted` 61 | `inserted`는 비문(`"Only not men can be dads."`)이지만 모델이 부정으로 읽는다 |
| ja-ko | `removed` **0** · `inserted` 71 | ❌ **반전되지 않는다.** 일본어는 공백으로 어절이 안 나뉘어 덩어리 사이에 영어 단어가 낀다(`'さて not ナグマの話に戻りましょう'`). 역번역이 `not`을 무시한다 — 표본 6건 중 5건 |

**파급**: FR-4.2가 구현되어 negation Recall을 재는 날, **ja-ko 수치는 무의미하다.**
지금은 Tier 1 경로가 없어 드러나지 않는다. ja-ko의 1차 AUC가 0.554·0.572로
무작위에 가까웠던 것이 이 결함의 증상이고, 표본을 직접 만들자 0.700·0.820으로
올라왔다 — **유사도 수단이 못 갈라서가 아니라 갈릴 것이 없어서였다.**

**직전 인수인계의 서술도 함께 틀렸다.** Q4 착수 조사 절의 mermaid가
`"inject.py::_negation — target_text에서 부정어를 뺀다"`라고 적었는데, en-ko에서도
**61/71이 삽입**이다. 코드를 읽고 쓴 문장인데도 두 경로 중 하나만 옮겨 적혔다.

## 이월 트리아지 — **13건** (17번이 신설돼 12건에서 늘었다)

**번호는 원래 번호를 유지한다** — 다시 매기면 이전 세션의 기록이 다른 항목을 가리킨다.

| # | 무엇 | 왜 지금 안 했나 | 다시 열 조건 |
| --- | --- | --- | --- |
| 2 | `bench/track_io.py`의 `_FIELDS`가 `source_from_stt`를 모른다 — 왕복에서 **조용히 `False`로 리셋** | 도달 경로가 없다(벤치 코퍼스는 자막) | STT 트랙을 벤치에 넣을 때. `tests/test_bench_track_io.py`의 왕복 동등성이 그 순간 실패한다 |
| 3 | `pyproject.toml`에 **`filterwarnings`가 없어 경고가 게이트를 통과한다** | `["error"]`는 스위트 전체에 영향 | 별도 과제. **"검사하지 않고 통과하는 게이트" 규율에 직접 걸린다** |
| 4 | `translate/provider.py`의 `RetryableProviderError.__init__`도 `isinstance`+`isfinite`를 쓴다 — 거대 정수에서 `OverflowError` 누출 **미확인** | 기존 코드 | `translate`를 손대는 다음 패키지 |
| 5 | `Transcript.__post_init__`이 `cues`의 **컨테이너 타입만** 보고 원소 타입은 안 본다 | 범위 밖 | 프로바이더를 하나 더 붙일 때 |
| 6 | `pyproject.toml:69`의 **죽은 `stt` extra** | 의존성 고정 규율상 채울 것이 없다 | 패키징을 손볼 때 |
| 8 | FR-1.5(원문 언어 자동 감지)가 반쪽 — STT 응답의 `language`를 **기록만** 한다 | `IngestResult.source_lang`은 선언값을 쓴다 | 자막 파일 입력까지 함께 닫을 때 |
| 9 | API 키가 **공백 한 칸**이면 `Authorization` 헤더가 `Bearer` 뒤에 공백만 붙은 채 나가 401이 "키가 틀렸다"로 오독된다 | 번역 경로도 같아서 한쪽만 고치면 비대칭이 된다 | 키 처리를 손볼 때. `_resolve_stt_key`와 `_require_ascii_api_key` 양쪽을 함께 |
| 10 | `translate --media X --to ko`처럼 **대상이 원문과 같으면** 전사를 마친 뒤 exit 2 | 출력 경로 충돌 검사가 입력 이름을 알아야 한다 | 되돌릴 수 없는 비용 앞의 검사를 정리할 때 |
| 11 | 설정의 `input.media` + `--dry-run` 조합에 **CLI 탈출구가 없다** | 설정 무효화 옵션은 이 한 자리만의 문제가 아니다 | 설정 무효화 규칙을 전반적으로 정할 때 |
| 14 | `cli.py` 자막/영상 자리의 `if "input" in losers:`는 **도달 불가능한 죽은 코드** | 위치 인자는 `default_map`에 실리지 않아 `_from_config("input")`이 늘 거짓이다 | `_resolve_exclusive` 호출부를 정리할 때. 지우는 것이 맞는지부터 판정한다 |
| 15 | `review.json`에 **스키마 버전 필드가 존재한 적이 없다** | 그래서 "이번에 올리지 않았다"가 **표현 불가능한 결정이다** | 하류 소비자가 생길 때 |
| 16 | `policy_kind` 분기의 `else`가 **threshold로 조용히 떨어진다** | 지금은 넷째가 `ValueError`로 죽어 도달 불가다 | **넷째 축이 추가될 때** |
| **17** | **`bench/inject.py:141`의 negation 삽입이 언어를 모른다** — ja-ko 라벨 71건 전부가 의미 반전이 아니다 | **spike의 범위가 아니다.** 고치면 코퍼스를 다시 만들어야 하고, 그러면 기존 벤치 수치와 비교가 끊긴다 | **FR-4.2 착수와 동시에.** 역번역 신호가 들어오는 순간 이 라벨이 정답지가 된다 |

**미룬 이유가 다시 열 조건을 정한다.** 3·14는 **사람이 날을 잡아야** 하고, 15·16은
그때가 오면 **저절로 걸리며**, 나머지는 해당 영역을 손대는 작업이 함께 닫는다.
**17번은 셋 중 어느 쪽도 아니다** — 지금 고치면 비교가 끊기고, 안 고치면 FR-4.2가
잘못된 정답지 위에서 측정된다. **FR-4.2와 한 묶음으로 다뤄야 하는 이유다.**

## 승계 항목

| 항목 | 상태 |
| --- | --- |
| **Q4**(자가일관성 유사도 측정 수단) | **열려 있다.** 로컬 행은 "임베딩이 필요하다"를 가리키지만 **상용 행이 비어 있어 닫지 않았다** |
| **FR-4.2**(역번역) | 구현 안 함. **이월 17번과 한 묶음이다** |
| **FR-8.5 R3**(Windows 콘솔의 `\r`) | 구조는 확인됐고 **육안 관측이 남아 있다.** 진짜 콘솔 창이 필요하다 |
| **FR-8.3 R3**(STT live 검증) | **열려 있다.** 다만 이번에 `/v1/audio/transcriptions`가 이 서버에서 **400**을 낸다는 것을 봤다(404 아님) — 경로 자체는 라우팅된다. **400이 "요청이 틀렸다"인지 형태만 받는지는 안 갈랐다** |
| **`engine.py::_run_single`의 전역 index** | 확인됐고 안 고쳤다. `main`에 있다 |
| `segments[].reasons`의 순서 미검증 | NFR-3 재현성 문제. 열려 있다 |
| 파킹 2 — 권장 모델 `qwen2.5:3b`가 3큐 중 2큐 실패 | **이번에 성격이 좁혀졌다** — 번역 능력이 아니라 **한국어 출력 안정성**이다(38%에서 중국어·영어 혼입). 부정 자체는 정확히 옮긴다 |
| 파킹 4 — `COLUMNS=88` 아래에서 옵션 이름이 잘린다 | rich의 표 렌더링. 폭 88을 게이트로 못 박았다 |

## 이번 세션이 배운 것

### ⓐ curl 커맨드라인에 한글을 넣으면 **값이 깨져 전송된다**

첫 측정에서 `qwen2.5:3b`가 한국어 입력을 `"a mix of random characters and gibberish"`
라고 답했다. **전제 B를 "실패"로 판정할 뻔했다.** 원인은 모델이 아니라 Git Bash의
`curl -d`가 한글을 CP949로 인코딩한 것이었고, `httpx`로 같은 문장을 보내니
`"I do not trust the result."`로 정확히 번역했다.

**이 리포의 기존 메모는 "콘솔에서 한글이 깨져 보이는 것은 표시 문제"였는데, 이번
것은 표시가 아니라 입력이다.** 출력 방향만 경계하고 있으면 걸린다.

### ⓑ **404와 501은 다르다** — 없는 것과 못 하는 것

`/v1/embeddings`가 501을 냈을 때 STT의 전례(`/v1/audio/transcriptions`가 없어
WP9가 가짜 프로바이더로만 검증됨) 때문에 "또 없다"로 읽힐 뻔했다.
**대조군으로 `/v1/nonexistent`를 두드려 404를 확인**하고서야 갈렸다 — 경로는 있고
모델이 못 내는 것이었으며, `bge-m3` 하나로 200이 됐다.

**엔드포인트 부재를 주장하기 전에 없는 경로를 하나 같이 두드린다.**

### ⓒ 라벨 이름이 라벨 내용을 보증하지 않는다

`kind: "negation"`이 71건 붙어 있었지만 ja-ko에서는 **한 건도 의미 반전이
아니었다.** `detail`을 세어 보고(`inserted` 71 · `removed` 0) 실물을 열어 보고서야
드러났다. 이 리포의 "파생 문서에서 읽은 사실은 원본에서 읽은 것보다 약하다"가
**데이터 라벨에도 적용된다** — 라벨은 생성 코드의 파생물이다.

**측정 도구를 쓰기 전에 그 도구가 실제로 무엇을 만들었는지 표본을 눈으로 본다.**

### ⓓ 대조군이 무작위면 교란이 신호를 덮는다

1차는 negation군과 무작위 clean군을 비교해 en-ko AUC 0.636·ja-ko 0.554를 얻었다.
2차에서 **같은 세그먼트의 부정 있음/없음**을 쌍으로 비교하자 0.800·0.700이 됐다.
길이·주제·화자가 통제되니 문자 단위 유사도조차 살아났다.

**두 수치는 서로 다른 것을 잰다** — 1차는 실전(무작위 세그먼트를 순위 매김),
2차는 신호의 순수 검출력이다. 어느 쪽도 다른 쪽을 대체하지 않는다.

## 개발 환경 메모 (승계)

**Python 실행은 반드시 `.venv/Scripts/python.exe`를 쓴다.** 시스템 Python은 3.14라 다르다.
게이트는 CI와 같은 대상 `.`으로 돌린다 — **`src tests`로 좁히면 안 된다**(그 차이로 CI가
5회 연속 실패한 전례가 있다).

**리포 루트에 `cuesift.yaml`을 만들지 마라.** 자동 탐색이 그것을 읽는다.
`conftest.py`의 autouse가 인프로세스 테스트는 막지만 **서브프로세스는 못 막는다.**

**변이 실험은 백업본을 만들고 복원을 `finally`에 둔다.** 스크래치 사본에서 할 때는
`PYTHONPATH`를 강제한다 — editable install의 `.pth`가 사본을 가려 "생존" 오탐을 낸다.
**`git checkout --`로 복원하지 마라** — 미커밋 작업을 날린 전례가 있다.

**콘솔 출력이 cp949라 한글·한자가 `UnicodeEncodeError`로 터진다.** 판정이 필요한
출력은 **UTF-8 파일로 받아서 읽는다**(`open(..., encoding="utf-8", newline="")`).
이번 세션에서 분석 스크립트가 중국어 한자에 걸려 한 번 죽었다.

**파이썬 스크립트로 문서를 고칠 때 `newline=""`을 준다.** `Path.write_text`의 기본값이
`newline=None`이라 Windows에서 `\n`을 `\r\n`으로 번역해 **2줄 수정이 수백 줄 변경으로** 찍힌다.
**`CHANGELOG.md`·`HANDOFF.md` 둘 다 LF다**(실측).

**긴 한글 문서는 heredoc이 아니라 `Write` 도구로 쓴다.** 여러 줄 커밋 메시지는
`git commit -F <파일>`로 넘긴다 — heredoc은 조용히 깨진다.

**Bash heredoc 안의 파이썬에 Windows 경로를 넣지 마라.** 역슬래시가 한 겹 먹혀
`tests\\fixtures`가 `tests\fixtures`(폼피드)로 바뀐다. 경로가 섞인 편집은 `Edit` 도구로 한다.

### 로컬 LLM

```powershell
curl.exe http://localhost:11434/api/tags      # 설치된 모델과 capabilities
```

현재: `qwen2.5:1.5b`·`qwen2.5:3b`(둘 다 `completion`·`tools`) · **`bge-m3`**(`embedding`,
1024차원, 이번 세션에 받음). **임베딩은 `capabilities`에 `embedding`이 있는 모델만
낸다** — 없으면 `/v1/embeddings`가 501이다.

STT live 테스트는 `-m live` · `CUESIFT_LIVE_STT_*` 환경변수로 돌고
**오디오는 리포에 넣지 않는다**(D10, `CUESIFT_LIVE_AUDIO`로 받는다).

## 다음 세션 시작 절차

```bash
gh pr list --state all --limit 3 --json number,title,state,mergedAt
git branch --show-current                 # main
git status --short                        # clean
git log --oneline -3                      # 최상단이 PR #27의 squash 커밋
```

**`main`에 직접 푸시하지 않는다.** CI의 `push` 트리거가 `branches: [main]`뿐이라
직접 푸시하면 머지된 **뒤에야** CI가 돈다 — 게이트가 아니라 사후 통보다.
PR 절차는 [CLAUDE.md](CLAUDE.md)의 "PR 절차"에 있다.

**다음 작업은 Q4 spike의 상용 API 행이다.** 위 "다음 세션" 절이 키 전달 방법과
모델을 적어 두었다. **로컬 행은 이미 채워져 있으니 다시 재지 마라** — 같은 씨앗과
같은 규칙으로 상용 두 칸만 채우면 2×2가 완성되고 Q4가 닫힌다.
