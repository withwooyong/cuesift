# Session Handoff

> Last updated: 2026-09-13 (KST) · 브랜치 `feat/tier1-budget-sweep` · PR (이 커밋 다음에 채운다)
> **이월 23번을 닫았다. 결함 ②(비대칭 밀어냄)를 구조적으로 고쳤다.**
> ① **착수의 첫 질문에 답했다 - 「나머지 부류 순증이 예산의 함수인가」는 기각이다.**
> `bench/run.py` 에 `--tier1-budgets` 를 더해 예산을 다섯 지점(5·10·15·20·30%)으로 늘리자,
> 네 칸으로 보던 **-4 는 예산 10%에만 나타났고** 15·20·30%에서는 ±1 안쪽이었다.
> 앞선 세션이 「예산이 값을 정한다」로 읽은 것은 예산 지점이 둘뿐이어서 가운데가
> 보이지 않았기 때문이다.
> ② **원인을 규명했다 - 거래의 값은 밀려나는 쪽이 정한다.** 손익은
> `(승격 중 오류) - (밀려남 중 오류)` 이고 밀려나는 쪽은 언제나 컷라인 바로 위다.
> 그 구간의 오류 밀도를 정하는 것은 **컷라인의 Tier 0 점수**였다(en-ko: 점수 1.0 에서
> 84% · 0.5 에서 28% · 0 에서 2%). 점수 분포가 0 과 0.5 사이에서 비어 있어 -
> 결정론적 신호가 하나라도 발화하면 0.5 이상, 하나도 없으면 0 이다 - 규칙이 **임계 상수
> 없이** 적힌다: **Tier 1 은 Tier 0 가 아무 말도 하지 못한 칸만 가져간다.**
> ③ **`reselect_protecting_tier0` 으로 고쳤고 열 칸을 다시 쟀다.** 두 언어쌍 × 다섯 예산
> **어디에서도 전체 Recall 이 떨어지지 않는다**(en-ko 예산 10%의 -2건이 +0건이 됐다).
> 예산 15% 이상의 이득은 **여섯 칸 전부에서 한 건도 줄지 않았다.** 대가는 예산 10%의
> negation 이득(en 2건 · ja 4건)이고, 그 구간에서 Tier 1 은 큐를 한 건도 바꾸지 못하므로
> **경고를 낸다**(실제로 예산 5%·10% 두 경계에서 나왔다).
> ④ **오프라인 재구성이 예측한 스무 칸이 실제 실행과 전부 같다.** 재구성 도구가 실행을
> 재현하는지 먼저 확인한 뒤에 설계 판단에 썼다 - 첫 판은 재현하지 못했고 원인은 원자료가
> 역번역 점수만 갖고 있던 것이었다(**어긋난 것은 코드가 아니라 기록이었다**, 이월 20번과 같은 부류).
> ⑤ **예정에 없던 결함 하나를 함께 닫았다 - CLI 비용 회계가 `warn` 으로 「Tier 1 이 안
> 돌았다」를 추론하고 있었다.** ⑧ 경고를 더하자 곧바로 거짓이 되어 **LLM 을 부르고도
> `cost.includes` 에서 `tier1` 이 빠졌다**(CLI 테스트 5건이 깨졌다). 같은 함정이
> `_POLARITY_UNSUPPORTED` 에도 **이미** 있었다 - 그 언어에서도 Tier 1 은 돈다. 전용 콜백
> `on_skip` 으로 갈랐다. **좁힌 대상만 돌렸다면 못 봤다** - 정책·Tier 1 테스트는 전부 통과했다.
> 게이트는 변이 15종으로 확인했고 **열다섯 다 잡혔다**(테스트 1967 -> 1995).
> [이월 23번 절](#이월-23번---예산-10에서-tier-1-이-전체-recall-을-깎는다)이 단일 출처다.
> 아래는 직전 세션의 기록이다.
>
> **직전 세션** - 브랜치 `feat/tier1-overall-recall` · PR [#45](https://github.com/withwooyong/cuesift/pull/45)
> **v0.2 를 열었다. 이월 23번의 「착수 시 먼저 할 것」 두 갈래를 끝냈고 항목은 아직 닫지 않았다.**
> ① `render_tier1_comparison` 에 **전체 Recall 행**을 더했다 - 비율·건수·순증을 함께 내고
> 실제 검수 비율 행도 넣었다. 빠진 키는 `KeyError`, 두 조건의 정답 건수가 다르면
> `ValueError` 로 세운다. 게이트는 변이 5종으로 확인했고 **다섯 다 잡혔다**(테스트 1957 -> 1967).
> ② **ja-ko 를 쟀고 같은 부호가 나오지 않았다.** 예산 10%의 전체 순증이 en-ko 는 -2건인데
> ja-ko 는 **+0건**이고, 예산 30%는 en-ko +16건 · ja-ko +12건으로 양쪽 다 이득이다.
> **갈린 것은 대가가 아니라 이득이다** - 나머지 부류의 순증이 예산 10%에서 양쪽 다 **-4건**,
> 예산 30%에서 양쪽 다 **0건**으로 언어쌍과 무관했고, 전체 부호를 가른 것은 negation 을
> 몇 건 더 담았느냐(en 2건 대 ja 4건)였다. **다만 네 칸은 네 칸이다** - 언어쌍 2개 ×
> 예산 2개의 관찰이라 -4 가 두 번 나온 것이 우연일 수 있다.
> 그래서 다시 열 조건 중 **ja-ko 조건은 닫혔고** 남은 것은 결함 ② 의 구조적 수정 하나다.
> **교차 확인이 통과했다** - en-ko 를 다시 돌려 새 행이 `bench/pushout.py` 의 독립 측정과
> 같은 값(예산 10% 366 -> 364 · 예산 30% 440 -> 456)을 내는 것을 확인했고, 리포트 diff 는
> **코드 커밋 해시와 새 행뿐**이라 나머지 수치는 2026-09-12 판의 완전한 재현이다.
> [이월 23번 절](#이월-23번---예산-10에서-tier-1-이-전체-recall-을-깎는다) 아래의
> 「v0.2 착수」 하위 절이 단일 출처다.
> 아래는 직전 세션의 기록이다.
>
> **직전 세션** - 브랜치 `docs/close-fr-4-2` · PR [#44](https://github.com/withwooyong/cuesift/pull/44)
> **FR-4.2 를 ✅ 로 닫았다. 이월 23번은 v0.2 로 미뤘다. v0.1 대상 FR 42개가 전부 닫혔다.**
> **코드 로직은 한 줄도 바뀌지 않았다** - 문서와 `--tier1` 의 help 문자열뿐이다.
> **닫은 근거는 새 실측이 아니라 요구사항의 등급이다.** FR-4.2 는 요구사항정의서 §5.4 에서
> 우선순위 「권장」이고 `--tier1` 의 기본값이 `False` 인 선택 기능이라
> (`src/cuesift/cli.py`), 「모든 예산 구간에서 이득일 것」은 이 등급에 과도한 기준이다.
> **9일간 FR-4.2 하나에 매달린 원인은 요구사항이 늘어난 것이 아니라 합격선이 오른 것이었다** -
> 측정이 새 결함을 하나 드러낼 때마다 그것이 완료 조건으로 승격되면 FR 은 영원히 닫히지 않는다.
> **실측된 트레이드오프는 예산에 따라 부호가 갈린다.** 예산 30%에서는 negation Recall
> 19.72% -> **42.25%** 이고 전체 오류도 **+16건**(88.0% -> 91.2%)인데, 예산 10%에서는
> negation 1.41% -> **4.23%** 이지만 전체는 **-2건**(73.2% -> 72.8%)이다.
> 기본이 꺼짐이므로 **켜는 사람이 알고 켜야 한다** - 그래서 이 두 구간을 `--tier1` 의
> `--help` 와 [README](README.md) 에 함께 실었다.
> **이월 23번은 닫지 않았다 - 관찰 자체는 유효하다.** v0.2 항목으로 미뤘고
> [아래 절](#이월-23번---예산-10에서-tier-1-이-전체-recall-을-깎는다)이 단일 출처다.
> 아래는 직전 세션의 기록이다.
>
> **이월 22번을 닫았다. 결함 ②는 해소되지 않았다 - 7배로 커졌다.**
> negation Recall 상승의 원인은 **밀어냄 완화가 아니라 유입 증가**다. 예산 30%에서
> negation 유실은 재설계 전후가 **똑같이 4건**이고 상승분 17건이 전부 유입(3 -> 20)에서
> 왔다. 예산 10%도 유입이 주다(0 -> 2, 유실은 1 -> 0).
> **밀어냄 자체는 커졌다** - 후보 밖에서 밀려난 세그먼트가 예산 10%에서 174건 ->
> **1,231건**(7.1배), 평균 Δrank +3.81 -> **+25.74**다. 원인은 후보의 출신 위치다:
> 재설계 전 후보는 위험도 상위라 50계단쯤 올라오는데, 재설계 후 후보는 극성 표지
> 보유라 회색지대 전역에서 **479계단**을 거슬러 온다. 순위는 보존량이라 올라온 만큼이
> 아래로 밀린다.
> **대가가 드러났다 - 예산 10%에서 전체 Recall 이 떨어진다.** 재설계 전에는 Tier 1 이
> 전체 오류를 **+21건** 늘렸는데(73.2% -> 77.4%) 후에는 **-2건**이다(72.8%).
> negation 2건을 얻고 glossary 16건·spec 8건을 잃었다. 예산 30%는 반대로 +16건이다.
> **이것을 이월 23번으로 열었다** - 09-06 에 보이지 않았던 이유는 리포트가 negation 과
> clean 부분집합만 싣고 **전체 Recall 을 싣지 않기** 때문이다.
> 재현성은 네 겹으로 확인했다 - 주입 트랙 SHA256 · 정답지 500건 · Tier 0 전 예산 ·
> Tier 1 negation Recall 이 전부 09-06 과 같다.
> **FR-4.2 는 여전히 🟡 다** - 막는 항목이 22번에서 23번으로 바뀌었다.
> (이 판정은 위 2026-09-12 종결 판정이 뒤집었다.)
> 아래는 직전 세션의 기록이다.
>
> **Tier 1 후보 선정을 재설계했고 실측이 효과를 확인했다. 이월 21번을 닫았다.**
> Tier 0 위험도 순위 대신 **극성 표지 보유 여부**로 후보를 우선 선정한다. 후보 안
> negation 이 무작위 기대값의 **4.37x(예산 10%)·4.91x(예산 30%)** 다. 이월 21번이
> 적은 같은 자리는 cap 적중 2건·3건이었고 이번에는 **17건·20건**이다.
> **negation Recall 도 올랐다** - 예산 10% 는 1.41% -> 4.23%, 30% 는
> 19.72% -> 42.25% 다(직전 판은 각각 0.00% 와 18.31% 로 **떨어졌다**).
> 다만 **설계 §9 는 Recall 상승을 판정 기준에서 뺐다.** 결함 ②(비대칭 밀어냄)가
> 남아 있기 때문이고, 이번 실행은 그 성격이 달라졌는지를 **재지 않았다** -
> 원인 규명은 이월 22번으로 열어 뒀다.
> Tier 0 는 전 예산에서 2026-09-05 와 소수점까지 같다(회귀 없음).
> **극성 표지는 `risk_score` 에 한 푼도 기여하지 않는다**(설계 D9). 바꾸는 것은
> "누가 비싼 Tier 1 검사를 받는가"뿐이고 "얼마나 위험한가"가 아니다.
> **FR-4.2 는 아직 🟡 다** - PR 이 열리지 않아 CI 가 돌지 않았고 이월 22번이 남았다.
> 아래는 직전 세션의 기록이다.
>
> **FR-4.2 를 구현했고 실측이 설계 결함을 드러냈다. 신호는 맞고 후보 선정이 틀렸다.**
> 역번역 신호는 의미 반전을 정확히 가려낸다(코사인 0.69 대 0.78). **그런데 Tier 1 을
> 파이프라인에 얹으면 negation Recall 이 오히려 떨어진다** - 예산 10% 에서
> 1.41% -> 0.00%, 30% 에서 19.72% -> 18.31% 다.
> 원인은 `select_tier1_candidates` 가 Tier 0 위험도 순위로 후보를 고르는 데 있다.
> Tier 0 는 의미 반전을 원리적으로 못 잡으므로 negation 을 무작위 위치에 놓고,
> 그래서 **71건 중 2~4건만 Tier 1 이 본다.** 상세는 아래 이월 21번이 단일 출처다.
> **이월 20번은 닫혔다** - 원자료 형식이 생겨 세그먼트별 역번역문·코사인·`selected`
> 가 남는다. 이제 라벨 몇 건이 바뀌어도 전체를 다시 돌릴 필요가 없다.
>
> **이월 19번을 닫았다. ja negation 정답지의 잡음은 자격 313건 중 129건(41.2%)이다.**
> 이월 항목이 적은 "표본 8건에서 2건"(25%)의 **1.6배**이고 부류도 둘이 아니라 다섯이었다.
> **그중 10건은 어색한 문장이 아니라 정답지가 스스로의 판단을 배신한 것이라 고쳤다** —
> 제외어가 표기 변이·개행에 뚫린 4건과, 부정을 떼도 뜻이 같은 「〜ませんか」 6건이다.
> 나머지 119건(비문·부자연)은 고치면 자격이 303 → 184건으로 무너지므로 **수치로 남긴다.**
> **FR-4.2 착수 시 정상 반전 184건에서의 Recall을 따로 보고하라** — 어색한 문장은
> 유사도를 쉽게 떨어뜨려 Recall을 낙관적으로 부풀린다.
> **en도 자격 524건 중 최소 34건이 같은 결함이다**(이월 19번은 ja만 적었다).
> 다만 **en 라벨은 한 건도 바뀌지 않았다** — en 규칙을 건드리지 않았기 때문이다.
> 아래는 직전 세션의 기록이다.
>
> **이월 18번을 닫았다. 역번역이 부정을 복원하는 비율은 en 21.8% · ja 17.9%다.**
> FR-4.2 임계값 설계에 들어갈 숫자가 생겼다 — **복원된 건에서 유사도 차이는
> 사실상 0이고 ja는 부호가 양수다**(오류 쪽이 원문에 더 가깝다). 임계값을 낮춰도
> 이 20%는 잡히지 않으므로 **Recall 목표의 상한이 80% 언저리**다.
> 같은 실행으로 **Q4 유보 ①의 로컬 2칸**도 닫았다(임베딩이 문자를 en +0.190 ·
> ja +0.180으로 이긴다). **상용 2칸은 열려 있다.**
> 아래는 직전 세션의 기록이다.
>
> **이월 17번을 닫았다. negation 정답지가 의미 반전이 아니었던 것을 고쳤다.**
> 주입기가 부정어를 못 찾으면 토큰 1번 자리에 영어 `not`을 무조건 끼워 넣었고,
> 그 경로가 `target_lang`을 받지 않아 일본어 문장에도 `not`이 들어갔다.
> **인수인계는 ja만 지목했으나 en도 61/71이 같은 결함이었다** — 이월 항목의
> 서술 범위를 믿지 말고 직접 재라는 이 리포의 규율이 또 한 번 맞았다.
> **삽입을 폐기하고 제거 전용으로 바꿨다.** 그러면 언어 인자가 필요 없다 —
> 부정 표현 자체가 언어를 식별하기 때문이다.
> **결론은 약해진 것이 아니라 강해졌다.** 전체 배수는 거의 그대로인데
> (7.24x → 7.32x) `negation` Recall은 예산 30%에서 "무작위와 구분 불가"였던 것이
> **무작위의 65%**로 갈렸다 — 옛 정답지의 이물질 `not`을 Tier 0가 우연히 잡아
> Recall을 부풀리고 있었다.
> **v0.1 완료 개수는 41 그대로다**(42개 중, 98%). 정답지 수정은 FR을 닫지 않는다.
> 상태 값은 여기 적힌 숫자가 아니라 아래 "현재 상태 재는 법"의 명령으로 직접 재라.
> 진척은 [WBS](docs/WBS.md), FR 번호의 출처는 [요구사항정의서](docs/요구사항정의서.md)다

## Current Status

| 단계 | 상태 | 산출물 |
| --- | --- | --- |
| **FR-4.2 — 역번역 유사도 신호** | ✅ **완료 (2026-09-12)** | 종결 PR [#44](https://github.com/withwooyong/cuesift/pull/44) (문서와 `--tier1` help 만, 코드 로직 0줄). 판정 근거는 요구사항정의서 §5.4 FR-4.2 행의 "2026-09-12 종결 판정"이다 — 우선순위 「권장」·기본값 꺼짐인 선택 기능이라 예산 구간별 부호 차이가 완료를 막지 않는다. 구현 산출물은 PR [#37](https://github.com/withwooyong/cuesift/pull/37) · 커밋 `51280e9` · `src/cuesift/embed/` · `signals/backtranslation.py` · 테스트 1808 → **1889** |
| **이월 20번 — 역번역 원자료 형식** | ✅ **닫혔다** | 같은 PR. `{pair}.backtranslation.json` 이 9개 필드와 메타 4종을 담는다 |
| **이월 21번 - Tier 1 후보 선정 결함** | ✅ **닫혔다 (2026-09-06)** | 브랜치 `feat/tier1-candidate-selection`, 커밋 `78c7690`. 다시 열 조건(재설계)이 충족됐다 - [아래 절](#이월-21번--tier-1-실측-신호는-맞고-후보-선정이-틀렸다)은 사료로 남기고, [이월 22번](#이월-22번---결함-2-비대칭-밀어냄-원인-규명이-남았다)이 후속이다 |
| **이월 22번 - 결함 ②(비대칭 밀어냄) 원인 미규명** | ✅ **닫혔다 (2026-09-12)** | PR #43. 상승 원인은 **유입 증가**이고 밀어냄은 완화되지 않고 **7.1배로 커졌다**. [아래 절](#이월-22번---결함-2-비대칭-밀어냄-원인-규명이-남았다)이 단일 출처 |
| ~~**이월 23번 - 예산 10%에서 Tier 1 이 전체 Recall 을 깎는다**~~ | ✅ **닫혔다 (2026-09-13).** 결함 ②(비대칭 밀어냄)를 `reselect_protecting_tier0` 으로 고쳤다 - **Tier 1 은 Tier 0 가 아무 신호도 내지 못한 칸(위험도 0)만 가져간다.** 두 언어쌍 × 다섯 예산 **열 칸 어디에서도 전체 Recall 이 떨어지지 않고**, 예산 15% 이상의 이득은 여섯 칸 전부에서 수정 전과 같다. 착수의 첫 질문(「나머지 부류 순증이 예산의 함수인가」)은 예산 지점을 다섯으로 늘려 **기각됐다** | 예산 10% en-ko **-2 -> +0** · ja-ko +0 -> +0. 대가는 그 예산의 negation 이득(en 2건 · ja 4건)이고 그때는 경고를 낸다. [아래 절](#이월-23번---예산-10에서-tier-1-이-전체-recall-을-깎는다)이 단일 출처. [설계](docs/superpowers/specs/2026-09-13-tier1-pushout-protection-design.md) |
| **Tier 1 실측 (en-ko, 후보 선정 재설계)** | ✅ **재측정했다 (2026-09-06)** | [`bench/results/en-ko-2026-09-06.md`](bench/results/en-ko-2026-09-06.md) · 원자료 492건(교차검증용). 후보 안 negation 4.37x(10%)·4.91x(30%) |
| **Tier 1 실측 (en-ko, 2026-09-05 판)** | ✅ **돌렸다 — 사료** | [`bench/results/en-ko-2026-09-05.md`](bench/results/en-ko-2026-09-05.md) · 역번역 496건. **`ja-ko` 는 돌리지 않았다** — 순위 분석이 같은 결과를 예고했다 |
| **이월 19번 — 정답지 잡음 계수 + A·F 픽스** | ✅ **머지됨** | PR [#35](https://github.com/withwooyong/cuesift/pull/35) · 커밋 `5541277` · `bench/inject.py` · 테스트 1799 → 1808 |
| **이월 18번 — 역번역 복원율 측정** | ✅ **쟀다** | PR [#34](https://github.com/withwooyong/cuesift/pull/34) · [`bench/results/backtranslation-spike-2026-09-04.json`](bench/results/backtranslation-spike-2026-09-04.json) · 역번역 426건 |
| **Q4 유보 ① — 로컬 2칸** | ✅ **닫았다** | 같은 PR·같은 실행. 상용 2칸은 열려 있다 |
| 직전 인수인계 갱신 | ✅ 머지됨 | PR [#30](https://github.com/withwooyong/cuesift/pull/30) · [#31](https://github.com/withwooyong/cuesift/pull/31) |
| **이월 17번 — negation 정답지 정정** | ✅ **머지됨** | PR [#32](https://github.com/withwooyong/cuesift/pull/32) · 커밋 `7b5bb2a` |
| **벤치 재측정** | ✅ **두 트랙 완료** | `bench/results/{en-ko,ja-ko}-2026-09-04.{md,json}` |
| 이 문서 + `CLAUDE.md` 두 줄 | PR [#33](https://github.com/withwooyong/cuesift/pull/33) | 코드 변경 0건. `CHANGELOG.md`는 PR #32에 이미 실렸다 — 중복 추가하지 말 것 |

**이 문서의 갱신은 PR #37 과 별개 PR 이다.** 인수인계 문서는 자기 자신이 실릴
PR 의 번호를 알 수 없어, 번호는 머지 뒤 두 번째 PR 로만 채울 수 있다. 이번에도
그렇게 했다 — 표의 `#37` 행들이 그 결과다.

**이번 세션은 코드를 바꿨다.** 직전 두 세션과 달리 `CHANGELOG.md`에 넣을 것이
있었고 PR #32에 함께 담았다.

## 현재 상태 재는 법

**첫 일은 직전 작업이 어디까지 갔는지 확인하는 것이다.**

```bash
gh pr list --state all --limit 5 --json number,title,state,mergedAt
git branch --show-current                     # main
git status --short                            # clean
git log --oneline -3
```

## 이월 19번 — negation 정답지가 신호에 주는 잡음을 셌다

**"먼저 몇 건인지 세라"가 이월 항목의 요구였고, 세어 보니 예상의 1.6배였다.**
이월 19번은 "표본 8건에서 2건 관측"(25%)을 근거로 두 부류를 적었는데,
실측은 **ja 자격 313건 중 129건(41.2%)** 이고 부류도 다섯이었다.

### 표본이 아니라 모집단을 셌다

라벨 71건은 한 시드가 뽑은 표본이다. `_negation`은 `rng`를 쓰지 않아
**자격 판정이 시드와 무관하므로**, 5,000 세그먼트 전부에 적용하면 시드에
흔들리지 않는 비율이 나온다. 같은 표본에서 50.7%가 나온 것은 우연 범위다
(모비율 41.2%, 표준편차 5.8%p).

| 부류 | ja 313건 | 표본 71건 | 성질 | 이번에 고쳤나 |
| --- | ---: | ---: | --- | --- |
| **A** 제외어 우회 | 4 (1.3%) | 3 | 제외 결정이 표기 변이로 무력화 | ✅ |
| **F** 반전 아님 | 6 (1.9%) | 1 | 라벨이 거짓 — 뜻이 안 바뀐다 | ✅ |
| **B** 고정형 파손 | 27 (8.6%) | 10 | 비문 (`なければなります`) | ❌ 수치로 남긴다 |
| **C** 호응부사 잔류 | 30 (9.6%) | 11 | 비문 (`全く分かります`) | ❌ 수치로 남긴다 |
| **D** `ではあります` | 62 (19.8%) | 11 | 부자연(비문은 아님) | ❌ 수치로 남긴다 |
| E 정상 반전 | 184 (58.8%) | 35 | | — |

**A와 F만 고친 이유는 성질이 다르기 때문이다.** B·C·D는 "의미는 반전되되
문장이 어색하다"이고, A·F는 **정답지가 이미 내린 판단을 배신한다** —
A는 제외하기로 결정된 것이 표기 때문에 들어왔고, F는 의미가 반전되지 않는데
라벨이 붙었다. 픽스 후 자격이 313 → 303으로 정확히 10건 줄었고
**정상 반전 184건은 그대로 유지됐다.**

### A — 문자열 완전 일치가 표기 변이와 개행에 뚫린다

```text
かも知れません     (한자)        → かも知れます      ja-03545
にすぎません       (히라가나)     → にすぎます        ja-00578 · ja-01814
じゃありませんか   (구어)        → じゃありますか    ja-02517
現れるかもし\nれません (개행이 쪼갬) → …かもし\nれます   ja-02192
```

**자막은 어절 중간에 줄바꿈이 들어간다.** 이것이 문자열 매칭 게이트를 조용히
통과시키는 경로다 — 개행 한 글자에 제외 목록 전체가 무력해진다.
표기 변이는 정규식으로 묶고, 판정은 개행을 지운 텍스트에 한다
(치환은 원본에 하므로 위치는 어긋나지 않는다).

### F — 부정 의문문은 부정을 떼도 뜻이 같다

```text
紙管で教会を再建しませんか？  (제안) → 再建しますか？        ja-01056
引き継いでもらえませんか？    (의뢰) → 引き継いでもらえますか？ ja-01791
```

제안·의뢰·확인의 「〜ませんか」는 긍정형과 뜻이 같다. 라벨이 붙으면
**Recall의 분모가 오염된다.** 기존 제외어에 `ではありませんか` 하나만 있었던
것이 이 부류를 한 자리에서만 막고 있었다는 뜻이다.

### en도 같은 결함을 갖는다 — 이월 17번과 같은 자리다

이월 19번은 ja만 서술했다. **en 자격 524건 중 최소 34건(6.5%)이 비문이다.**

| en 부류 | 524건 중 | 예 |
| --- | ---: | --- |
| NPI 잔류 | 34 (6.5%) | `this is theory anymore` · `we did study any of these animals` · `has even been distributed yet` |
| 다중 부정 후보 | 46 (8.8%) | 표본 9건 중 4건이 실제 결함(`no, do do that` · `you are/are`) |

**en 규칙은 이번에 건드리지 않았으므로 en 라벨은 한 건도 바뀌지 않았다.**
그래서 이월 18번의 en 복원율 21.8%는 재측정 없이 유효하다.

### FR-4.2에 무엇을 뜻하나

어색한 문장은 역번역·임베딩 유사도를 **원래보다 쉽게 떨어뜨린다.** 지금
정답지로 negation Recall을 재면 ja는 자격의 39.3%가 그런 건이므로
**낙관적으로 부풀려진다.** 착수하면 **정상 반전 184건에서의 Recall을 따로
보고해야** 진짜 수치다. B·C·D를 전부 고치지 않는 이유도 여기에 있다 —
고치면 자격이 303 → 184건으로 줄어 라벨 수급이 무너진다.

### 판정 기준을 코드로 고정했고, 오탐 9건·미탐 4건을 원문 대조로 잡았다

눈으로만 세면 다음 세션에서 재현되지 않는다. 분류를 규칙으로 쓰고 돌린 뒤
**모든 건을 원문과 대조해** 규칙 자체의 결함을 찾았다.

| 잡은 것 | 무엇이 틀렸나 |
| --- | --- |
| 오탐 5건 | `しか` 패턴이 접속사 「**しか**し」를 잡았다 — **CJK에는 단어 경계가 없다** |
| 오탐 2건 | 문두 `Yet,`(그러나)와 `but yet`(그렇지만)은 NPI가 아니라 접속사다 |
| 오탐 1건 | 앞 절 부정이 살아 있으면 `either`는 여전히 호응 대상을 갖는다 |
| 오탐 1건 | 「礼にかなう」는 긍정형이 실제로 쓰인다 — 고정형이 아니다 |
| 미탐 1건 | 축약형은 `\bn't\b`로 못 잡는다(`don't`의 `n` 앞이 단어 경계가 아니다) |
| 미탐 3건 | 「しか」가 술어와 떨어진 경우 · 형용사 `くありません` · 「〜ませんか」 |

**분류 스크립트는 리포에 넣지 않았다** — 일회성 측정이고, 판정 기준 자체가
일본어 문법 지식에 의존해 게이트로 쓸 수 없다. 재현이 필요하면 이 절의
부류 정의와 위 오탐·미탐 목록으로 다시 짠다.

## 이월 18번 — 역번역이 부정을 복원하는 비율을 쟀다

**이것은 버그가 아니라 역번역 신호의 원리적 상한이고, 이제 수치가 있다.**
FR-4.2 임계값을 정할 때 "왜 이 값인가"를 답할 근거가 여기에 있다.

측정 재료는 새 정답지(PR #32)의 `negation` 라벨 71건 × 2트랙이다.

> **이월 19번의 픽스가 ja 라벨 71건 중 4건을 교체했다**(`ja-01791`·`ja-01814`·
> `ja-02192`·`ja-03545` → `ja-01615`·`ja-02710`·`ja-03950`·`ja-04573`).
> **en 라벨은 바뀌지 않았으므로 en 21.8%는 그대로 유효하고, ja 17.9%만 재측정
> 대상이다.** 다만 아래 결과 파일에 **세그먼트별 원자료가 없어 4건만 교체해
> 다시 셀 수 없다** — 전체를 다시 돌려야 한다(이월 20번).
결과 파일은 [`bench/results/backtranslation-spike-2026-09-04.json`](bench/results/backtranslation-spike-2026-09-04.json)이다.

### 설계 — 절대값이 아니라 같은 세그먼트의 차이로 잰다

```text
A (bad)      injected target -> ko 역번역   부정이 제거된 번역
B (ok-pair)  clean    target -> ko 역번역   같은 세그먼트의 정상 번역
C (무라벨)    clean    target -> ko 역번역   source 길이를 A 에 맞춰 고른 대조군

  복원 판정 : 같은 세그먼트에서 n_A >= n_B   (부정이 하나 줄어야 정상)
  AUC       : 위험도 = 1 - 유사도, A 가 양성 · C 가 음성
```

**착수 조사에서 표본을 눈으로 본 것이 설계를 두 번 바꿨다.**

| 발견 | 사례 | 설계에 준 영향 |
| --- | --- | --- |
| 원문과 번역이 대응하지 않는 세그먼트가 있다 | `en-03247` — source는 "조언해 준게 감사해요", target은 "his will to live wasn't" | TED2020 자막의 정렬 오류다. **절대 코사인은 정렬 잡음을 신호로 싣는다** — 그래서 같은 세그먼트의 B를 기준선으로 빼는 `delta`를 함께 낸다 |
| 다중 부정 문장이 있다 | `en-01164` — `can't`만 제거되고 `don't`가 **둘 남았다** | "역번역에 부정이 있는가"로 물으면 전부 오탐이다. **개수 비교**여야 한다 |

**자격 조건이 세 번째 함정을 막았다.** `n_B == 0`이면 `n_A >= n_B`가 `0 >= 0`으로
늘 참이라 **"아무것도 측정되지 않음"이 "복원됨"으로 새어 들어간다.**
en 첫 41쌍에서 복원이 15건으로 나왔는데 자격 조건을 넣자 8건이 됐다 —
빠진 7건은 판정기가 어휘적 부정("무관심")을 못 세거나 역번역이 코드 스위칭으로
깨진 건이었다. **이 리포의 "픽스처가 규격 경계를 넘으면 통과가 아무것도 시도되지
않음이다"와 같은 부류다.**

### 결과

| 지표 | en-ko | ja-ko | 무엇을 뜻하나 |
| --- | --- | --- | --- |
| 자격 (B 역번역에 부정 있음) | 55/71 | 56/71 | 나머지는 판정 불가라 표본에서 뺐다 |
| **복원** | **12건 = 21.8%** | **10건 = 17.9%** | 역번역이 제거된 부정을 되살린 비율 |
| 복원(오염 제외) | 8/39 = 20.5% | 3/26 = 11.5% | 코드 스위칭된 건을 빼도 판정이 흔들리지 않는다 |
| A·B 역번역 완전 동일 | 2건 | 3건 | 옛 정답지에서 관측된 "글자까지 동일"이 재현됐다 |
| **`delta_restored`** | **-0.005** | **+0.011** | **복원된 건에서 신호는 0이다** |
| `delta_not_restored` | -0.117 | -0.095 | 복원 안 된 건은 신호가 정상 작동한다 |
| 코드 스위칭 (A) | 18/71 = 25% | 28/71 = 39% | 파킹 2의 26%·33%와 같은 수준이다 |

**`delta_restored`가 이 측정의 핵심 숫자다.** 복원된 건에서 오류 문장과 정상
문장의 유사도 차이가 **사실상 0이고, ja는 부호가 양수다** — 오류 쪽이 원문에 더
가깝게 측정된다는 뜻이다. 가장 극단적인 사례는 이렇다.

```text
[ja-01891] removed='ません'   원문: 하지만 늘 그런 것만은 아닙니다.
  A(bad, 부정 제거 → あります) : 그러나 반드시如此일 수는 없습니다   <- 부정이 되살아났다
  B(ok,  정상)                : 그렇지도 않아요
  cos A=0.842  B=0.694   delta=+0.148                          <- 오류 쪽이 원문에 가깝다
```

**임계값을 아무리 낮춰도 이 20%는 잡히지 않는다.** 신호를 세게 만드는 것으로
해결되지 않으므로, FR-4.2의 Recall 목표는 negation 오류의 **약 80%를 상한으로**
잡아야 한다. 실제로 복원 건을 표본에서 빼면 AUC가 en 0.758 → **0.793**,
ja 0.689 → **0.717**로 오른다 — 복원이 AUC를 각각 0.035·0.027 깎고 있다.

### 함께 나온 것 — Q4 유보 ①

유보 ①은 "1차 측정이 옛 라벨로 돌았으니 새 정답지로 다시 재라"였다.
같은 역번역 실행으로 잴 수 있어 함께 담았고, **문자 단위도 같은 표본·같은
역번역문에 재서 비교를 성립시켰다**(추가 호출 0회, `difflib` + NFKC).

| AUC | 옛 라벨 (n=50) | **새 라벨 (n=142)** |
| --- | --- | --- |
| 문자 단위 en-ko | 0.511 | **0.569** |
| 임베딩 en-ko | 0.730 | **0.758** |
| 문자 단위 ja-ko | 0.700 | **0.509** |
| 임베딩 ja-ko | 0.792 | **0.689** |

**결론은 유지된다 — 임베딩이 en +0.190 · ja +0.180으로 이긴다.**
다만 **닫힌 것은 로컬 2칸뿐이다.** 옛 1차는 로컬과 상용을 함께 쟀으므로
상용 2칸은 여전히 옛 라벨 기준이다. 요구사항정의서 §12 Q4에 그렇게 적었다.

**ja-ko의 절대값이 옛 라벨보다 떨어진 것을 성능 저하로 읽지 마라.** 옛 ja 라벨은
71건 전부가 영어 `not`이 끼워진 이물질이었고 그것을 임베딩이 쉽게 잡아냈다.
새 라벨은 진짜 의미 반전이라 더 어렵다 — **문제가 어려워진 것이지 모델이 나빠진
것이 아니다.**

## 이월 17번 — 무엇이었고 어떻게 고쳤나

### 결함

```text
_negation(seg, glossary, profile, rng)        # target_lang 인자가 없다
  ├ _NEGATIONS_EN 에서 부정어 발견 → 제거       ← en 10건만 이 경로
  └ 없으면 tokens.insert(1, "not")            ← ja 71/71, en 61/71 이 경로
```

| 트랙 | negation 라벨 | 삽입 경로 | 제거 경로 | 자격 미달(옛) |
| --- | --- | --- | --- | --- |
| en-ko | 71 | **61** | 10 | 237 |
| ja-ko | 71 | **71 (전부)** | 0 | 1,524 |

en 삽입 표본 12건을 눈으로 확인하니 실제 의미 반전은 **4건**뿐이었다.

```text
en-00347  I'm hopeful for two reasons.     -> I'm not hopeful ...        ✅
en-00113  No longer.                       -> No not longer.             ❌ 문법 파괴
en-00290  He knew we had to get involved.  -> He not knew we ...         ❌ 문법 파괴
en-00508  Yet, I cannot use the toilet.    -> Yet, not I cannot ...      ❌ 이중 부정
ja-00277  （笑） この子達は利口です          -> （笑） not この子達は...    ❌ 외국어 토큰
```

일본어는 공백 토큰 경계가 없어 **삽입 위치조차 의미가 없었다.** 그리고 삽입된
`not`과 공백이 폭을 4자 늘려 규격을 넘기는 바람에 **ja는 1,524건을 건너뛰고
짧은 문장만 골라 주입됐다** — 라벨이 틀린 것에 더해 표본까지 편향돼 있었다.

### 수정

**삽입을 폐기하고 제거 전용으로 바꿨다.** 언어 인자를 추가하지 않은 것이 핵심이다 —
영어 문장에 `ません`이 없고 일본어 문장에 `don't`가 없으므로 **매칭되는 규칙이
곧 언어 판정**이고, 규칙이 하나도 안 걸리면 자격 미달이다.

| 규칙 | 내용 |
| --- | --- |
| 영어 축약형 | `can't`·`won't`·`don't`·`doesn't`·`didn't`·`isn't`·`aren't`·`wasn't`·`weren't`·`couldn't`·`wouldn't`·`shouldn't`·`haven't`·`hasn't`·`hadn't` (15종) |
| 영어 그 밖 | `cannot`→`can` · `never`→`always` · ` not `→` ` |
| 일본어 | `ませんでした`→`ました` · `ません`→`ます` **둘만** |
| ja 제외어 | `かもしれません`·`すみません`·`いけません`·`過ぎません`·`ではありませんか`·`てなりません` |
| 대소문자 | 첫 글자를 원본에 맞춘다(`Don't`→`Do`) |

**순서가 의미를 바꾼다.** `ませんでした`가 `ません`보다 먼저 와야 하고(뒤집히면
`ますでした`라는 없는 형태가 나온다), 영어 축약형이 ` not `보다 먼저 와야 한다.
`\b`는 **ASCII 규칙에만** 쓴다 — CJK에 적용하면 단어 경계가 전부 깨진다.

자격은 **en 524건 / ja 313건**이고 quota는 72건이다.

## 재측정 결과 — 두 날짜는 직접 비교할 수 없다

| 지표 | 2026-07-29 | 2026-09-04 |
| --- | --- | --- |
| 전체 Recall @10% (en / ja) | 72.40% / 75.20% | **73.20% / 75.60%** |
| 전체 배수 @10% (en / ja) | 7.24x / 7.52x | **7.32x / 7.56x** |
| `negation` Recall @10% (en) | 1.41% | **1.41%** (그대로) |
| `negation` Recall @30% (en) | 29.58% | **19.72%** |
| 같은 예산의 무작위 기준선 @30% | 29.68% | 30.44% |
| 판정 @30% | "무작위와 구분 불가" | **무작위의 65%** |
| negation 자격 미달 (ja) | 1,524 | **1,172** |

**옛 정답지의 이물질 `not`을 Tier 0 신호가 우연히 잡아 Recall을 부풀리고
있었다.** 정답지를 고치자 격차가 드러났다 — FR-4.2 착수 근거가 약해진 것이
아니라 강해졌다.

**두 날짜의 수치를 직접 빼면 안 된다.** `inject`가 유형 처리 순서를 **자격
희소도**로 정하므로 negation 자격률이 바뀌자 **negation 외 6개 유형의 세그먼트
배정도 함께 달라졌다.** 그 사실을 리포트 caveat(`_LABEL_REVISION_CAVEAT`)에
실었고 2026-07-29 리포트는 지우지 않고 남겨 두었다.

### 함께 갱신한 문서

| 문서 | 무엇을 |
| --- | --- |
| `README.md` | 최상단 배수·실측 표 6행·`negation` 서술 2곳·리포트 링크 |
| `docs/요구사항정의서.md` §12 Q4 | 정답지 정정 문단 추가. **유보 ①은 닫지 않았다**(아래) |
| `docs/superpowers/specs/2026-08-17-tier1-signals-design.md` | §1 근거 수치·§3.1 표 전면 |
| `CHANGELOG.md` | Fixed 절에 항목 1개 |

**요구사항정의서 §12 Q4의 유보 ①을 닫지 않은 이유**: Q4 spike의 1차 측정은
**옛 라벨로 돌았다.** 주입기를 고쳤다고 그 측정이 소급해 유효해지지 않는다.
되살리려면 새 정답지로 1차를 다시 재야 한다.

## 이번 세션이 배운 것

**직전 세션의 ⓐ·ⓑ·ⓒ는 [CLAUDE.md](CLAUDE.md)의 규율로 올라갔으므로 여기서
반복하지 않는다** — 테스트가 설명 문자열을 자기 안에서 짓는 문제, 픽스처가 규격
경계를 넘으면 "통과"가 "아무것도 시도되지 않음"이 되는 문제, 이월 항목의 서술
범위를 의심해야 한다는 것 셋이다. 아래는 이번 세션이 새로 배운 것이다.

### ⓐ 문자열 완전 일치 게이트는 표기 변이와 개행 한 글자에 뚫린다

`_JA_MASEN_EXCLUSIONS`는 제외 판단을 **이미 내린** 목록이었는데, 판정이 문자열
완전 일치라 같은 말의 다른 표기가 그대로 통과했다.

| 뚫린 경로 | 사례 |
| --- | --- |
| 한자 ↔ 히라가나 | `かもしれません`은 막고 `かも知れません`은 통과 |
| 구어 표기 | `ではありませんか`는 막고 `じゃありませんか`는 통과 |
| **어절 중간 줄바꿈** | `かもし` + 개행 + `れません` |

**세 번째가 이 프로젝트에 특유하다.** 자막은 화면 폭에 맞춰 어절 중간에서
줄바꿈되므로, 자막 텍스트에 문자열·정규식을 적용하는 모든 자리가 같은 구멍을
갖는다. 판정은 개행을 지운 사본에 하고 치환만 원본에 한다.

### ⓑ CJK에는 단어 경계가 없어 부분 문자열 패턴이 다른 단어를 잡는다

분류기에 「しか」(부정 호응)를 넣었더니 접속사 「**しか**し」를 5건 잡았다.
CLAUDE.md의 "제안된 `\b` 단어 경계가 CJK를 전부 깨뜨렸다"와 **반대 방향의 같은
문제**다 — 경계가 없으니 `\b`도 못 쓰고, 그래서 부분 문자열이 이웃 단어를 문다.
`しか(?!し)`처럼 **뒤따르는 글자를 배제**하는 것이 이 언어에서 쓸 수 있는 경계다.

### ⓒ 집계값만 남긴 측정은 입력이 조금 바뀌어도 전부 다시 돌려야 한다

이월 18번의 결과 JSON에는 복원율·AUC 같은 **집계값만** 있다. 이번에 ja 라벨
71건 중 **4건**이 교체됐는데, 그 4건만 다시 재는 것이 불가능해 213회 역번역을
통째로 다시 돌려야 하는 상태가 됐다(이월 20번으로 열었다).

**측정 스크립트가 리포에 없는 것이 그 절반이다.** 스크래치에서 돌고 사라져
"다시 돌린다"가 "다시 짠다"가 됐다. 재측정 가능성은 결과 형식과 스크립트 위치
**둘 다**에 달려 있다.

### ⓓ 설명 문자열을 테스트가 자기 안에서 지으면 코드와 갈라져도 통과한다

`tests/test_bench_report.py`의 caveat 테스트 두 건은 문구를 테스트 안에서 지어
`render_markdown`에 넘겼다. **렌더링만 검사한 것이다.** 그래서 주입기가 제거
전용으로 바뀐 뒤에도 `bench/run.py`의 실제 caveat은 "부정어 삽입이 CPS를
넘기면"이라는 **일어나지 않는 실패**를 편향 원인으로 제시한 채 남았고,
전 스위트 1,792건이 통과했다.

새 테스트가 `bench.run.CAVEATS`를 **임포트해** 그 연결을 만든다.
RED를 확인하고 고쳤다.

### ⓔ 픽스처가 규격 경계를 넘으면 "통과"가 "아무것도 시도되지 않음"이다

RED 확인 중에 가짜 통과를 **두 번** 잡았다.

| 픽스처 결함 | 무슨 일이 났나 |
| --- | --- |
| `end_ms=8000` (`max_duration_ms`는 7000) | 원본이 이미 `duration_long` 위반이라 주입기가 전부 `None`을 반환. 테스트 2건이 "버그를 잡아서"가 아니라 "아무것도 시도되지 않아서" 통과 |
| 공백 없는 일본어 문장 | 삽입 경로가 `len(tokens) < 2`면 시도조차 안 한다. 버그가 아니라 "토큰이 모자람"을 재고 있었다 |

**실패를 확인할 때 실패 사유를 읽어야 한다.** `None`이 반환된 이유가 셋(규칙
없음·규격 위반·토큰 부족)인데 테스트는 `is None` 하나만 보므로, 어느 사유인지
모르면 게이트가 무엇을 재는지 모르는 것이다.

### ⓕ 이월 항목의 서술 범위도 의심한다

이월 17번은 "**ja-ko** 라벨 71건 전부가 의미 반전이 아니다"라고 적혀 있었다.
그 문장은 **참이었다.** 거짓인 것은 읽히는 함의였다 — en은 괜찮다는 뜻으로
읽혔는데 **en도 61/71이 같은 경로**였다.

CLAUDE.md의 "부류로 묶인 서술은 각 원소가 확인됐다는 뜻이 아니다"가 이번엔
**반대 방향**으로 작동했다. 원소가 아니라 **범위**가 좁게 적혀 있었다.

### ⓖ handoff 수집 스크립트의 HIGH 신호에 오탐이 있다

`[HIGH] ENV_KEYS_IN_CODE_BUT_NOT_IN_README: OPENAI_API_KEY`가 떴으나
`grep -rn "OPENAI_API_KEY" src/ scripts/ bench/`는 **0건**이다. 이 키는
`.claude/settings.local.json`의 개인 설정이고 Q4 spike의 throwaway 스크립트가
쓴 것이다. **제품이 읽지 않는 변수를 README에 넣으면 문서가 거짓이 된다.**

**2026-09-04 세션 종료 시점에 다시 확인했다** — 같은 신호가 또 떴고,
`grep -rn OPENAI_API_KEY`는 여전히 `.claude/settings.local.json` 한 건뿐이다.
그 파일은 `.gitignore:99`의 `.claude/`가 막고 있고 히스토리에도 없다.
**이 신호는 앞으로도 매 세션 뜨므로 그때마다 무시한다.**

`ARCHIVE_NEEDED=1`도 이 리포에서는 작동하지 않는다 — 줄 수(366 > 200)만 보는
신호인데 버전 절이 `[Unreleased]`·`[2026-07-28]`·`[2026-07-27]` **3개뿐**이라
"최신 3개만 남긴다" 규칙으로는 밀려날 절이 없다. 아직 릴리스가 없어 314줄이
`[Unreleased]` 하나에 쌓여 있는 구조다.

## 이월 트리아지 — **13건** (22번이 닫히고 23번이 열렸다. 23번은 2026-09-12 에 v0.2 로 미뤘고, **미룬 것은 닫은 것이 아니므로 개수는 그대로다**)

**번호는 원래 번호를 유지한다** — 다시 매기면 이전 세션의 기록이 다른 항목을 가리킨다.

| # | 무엇 | 왜 지금 안 했나 | 다시 열 조건 |
| --- | --- | --- | --- |
| 2 | `bench/track_io.py`의 `_FIELDS`가 `source_from_stt`를 모른다 — 왕복에서 **조용히 `False`로 리셋** | 도달 경로가 없다(벤치 코퍼스는 자막) | STT 트랙을 벤치에 넣을 때. `tests/test_bench_track_io.py`의 왕복 동등성이 그 순간 실패한다 |
| 3 | `pyproject.toml`에 **`filterwarnings`가 없어 경고가 게이트를 통과한다** | `["error"]`는 스위트 전체에 영향 | 별도 과제. **"검사하지 않고 통과하는 게이트" 규율에 직접 걸린다** |
| 4 | `translate/provider.py`의 `RetryableProviderError.__init__`도 `isinstance`+`isfinite`를 쓴다 — 거대 정수에서 `OverflowError` 누출 **미확인** | 기존 코드 | `translate`를 손대는 다음 패키지 |
| 5 | `Transcript.__post_init__`이 `cues`의 **컨테이너 타입만** 보고 원소 타입은 안 본다 | 범위 밖 | 프로바이더를 하나 더 붙일 때 |
| 6 | `pyproject.toml:69`의 **죽은 `stt` extra** | 의존성 고정 규율상 채울 것이 없다 | 패키징을 손볼 때 |
| 8 | FR-1.5(원문 언어 자동 감지)가 반쪽 — STT 응답의 `language`를 **기록만** 한다 | `IngestResult.source_lang`은 선언값을 쓴다 | 자막 파일 입력까지 함께 닫을 때 |
| 9 | API 키가 **공백 한 칸**이면 `Authorization` 헤더가 `Bearer` 뒤에 공백만 붙은 채 나가 401이 "키가 틀렸다"로 오독된다 | 번역 경로도 같아서 한쪽만 고치면 비대칭이 된다 | 키 처리를 손볼 때. `_resolve_stt_key`와 `_require_ascii_api_key` 양쪽을 함께 |
| 10 | `translate --media X --to ko`처럼 **대상이 원문과 같으면** 전사를 마친 뒤 exit 2 | 출력 경로 충돌 검사가 입력 이름을 알아야 한다 | 되돌릴 수 없는 비용 앞의 검사를 정리할 때 |
| 11 | 설정의 `input.media` + `--dry-run` 조합에 **CLI 탈출구가 없다** | 설정 무효화 옵션은 이 한 자리만의 문제가 아니다 | 설정 무효화 규칙을 전반적으로 정할 때 |
| 14 | `cli.py` 자막/영상 자리의 `if "input" in losers:`는 **도달 불가능한 죽은 코드** | 위치 인자는 `default_map`에 실리지 않아 `_from_config("input")`이 늘 거짓이다 | `_resolve_exclusive` 호출부를 정리할 때. 지우는 것이 맞는지부터 판정한다 |
| 15 | `review.json`에 **스키마 버전 필드가 존재한 적이 없다** | 그래서 "이번에 올리지 않았다"가 **표현 불가능한 결정이다** | 하류 소비자가 생길 때 |
| 16 | `policy_kind` 분기의 `else`가 **threshold로 조용히 떨어진다** | 지금은 넷째가 `ValueError`로 죽어 도달 불가다 | **넷째 축이 추가될 때** |
| ~~17~~ | ~~negation 삽입이 언어를 모른다~~ | — | ✅ **닫혔다 (2026-09-04, PR #32).** 제거 전용으로 바꿨다 |
| ~~18~~ | ~~역번역이 오류를 조용히 교정한다~~ | — | ✅ **쟀다 (2026-09-04).** 복원율 **en 21.8% · ja 17.9%**. 상한은 수치가 됐고 [아래 절](#이월-18번--역번역이-부정을-복원하는-비율을-쟀다)이 단일 출처다 |
| ~~19~~ | ~~ja 부정 제거가 어색한 일본어를 만든다~~ | — | ✅ **셌고 A·F 10건을 고쳤다 (2026-09-04).** 남은 119건은 수치로 남겼다 — [위 절](#이월-19번--negation-정답지가-신호에-주는-잡음을-셌다)이 단일 출처다 |
| ~~20~~ | ~~역번역 스파이크가 세그먼트별 원자료를 남기지 않았다~~ | — | ✅ **닫혔다 (2026-09-05, 브랜치 `feat/fr-4-2-backtranslation`, 커밋 `b97eb8f`).** `bench/run.py`의 Tier 1 통합이 `data/bench/{pair}.backtranslation.json`에 세그먼트별 원자료(9개 필드 + 메타 4종, `selected` 플래그 포함)를 남긴다 — 파일만으로 Recall@Budget을 되계산할 수 있다 |
| ~~21~~ | ~~Tier 1 후보 선정이 Tier 0 순위에 의존해, 원리적으로 못 잡는 오류 부류(의미 반전)를 무작위 위치에 놓는다~~ | - | ✅ **닫혔다 (2026-09-06, 브랜치 `feat/tier1-candidate-selection`, 커밋 `78c7690`).** 다시 열 조건("후보 선정 방식 자체를 재설계해야 한다")이 충족됐다 - `select_tier1_candidates`에 극성 표지 우선순위 통로(`priority_ids`)를 열어 회색지대 안에서 극성 표지 보유 세그먼트를 먼저 채우도록 재설계했다(설계 [tier1-candidate-selection-design.md](docs/superpowers/specs/2026-09-06-tier1-candidate-selection-design.md)). en-ko 재측정에서 후보 안 negation이 예산 10% **4.37x**·30% **4.91x**로 무작위 기대값을 유의하게 넘는다(이 표의 cap 적중 2건·3건에서 17건·20건으로) - 설계 §3.3의 예측과 일치한다. **다만 이 종결이 FR-4.2를 완료로 올리지는 않는다** - 결함 ②(비대칭 밀어냄)가 남아 [이월 22번](#이월-22번---결함-2-비대칭-밀어냄-원인-규명이-남았다)으로 잇는다. 상세는 [아래 절](#이월-21번--tier-1-실측-신호는-맞고-후보-선정이-틀렸다)에 사료로 남긴다 |
| ~~22~~ | ~~결함 ②(비대칭 밀어냄)의 원인이 규명되지 않았다~~ | - | ✅ **닫혔다 (2026-09-12, PR #43).** 다시 열 조건("FR-4.2 후속 착수 시 원인을 규명한다")이 충족됐다 - 상승 원인은 **유입 증가**이고(예산 30%에서 유실은 전후 똑같이 4건, 유입만 3 -> 20) 밀어냄은 완화되지 않고 **7.1배로 커졌다**(후보 밖 밀려남 174 -> 1,231건). 상세는 [아래 절](#이월-22번---결함-2-비대칭-밀어냄-원인-규명이-남았다)이 단일 출처다. **다만 이 종결이 FR-4.2 를 완료로 올리지는 않는다** - 측정이 새 결함을 드러내 [이월 23번](#이월-23번---예산-10에서-tier-1-이-전체-recall-을-깎는다)으로 잇는다 |
| **23** | **예산 10%에서 Tier 1 이 전체 Recall 을 깎는다** - 이월 21번의 재설계가 negation 2건을 얻는 대신 glossary 16건·spec 8건을 잃어, 전체 오류 순증이 재설계 전 **+21건**(73.2% -> 77.4%)에서 **-2건**(72.8%)이 됐다. 예산 30%는 반대로 +16건이라 **예산에 따라 부호가 갈린다** | 리포트가 negation 과 clean 부분집합만 싣고 **전체 Recall(Tier 0 vs Tier 0+1)을 싣지 않아** 2026-09-06 에 보이지 않았다. `render_tier1_comparison` 의 구멍이다 | **판정했다 (2026-09-12): v0.2 로 미룬다.** 이 항목을 연 것은 요구사항이 아니라 **실측이 드러낸 역효과**이고, 그 역효과는 우선순위 「권장」·기본값 꺼짐인 선택 기능의 한 예산 구간에서만 난다 - 그래서 FR-4.2 의 완료를 막는 조건으로는 과하다. 다만 Recall@Budget 이 핵심 지표라는 사실은 변하지 않으므로 **닫지 않는다.** **다시 열 조건**: v0.2 에서 Tier 1 을 손댈 때, 또는 ja-ko 에서도 같은 부호가 확인될 때. 착수 시 먼저 할 것은 `render_tier1_comparison` 에 전체 Recall 행을 더하는 것이다. **2026-09-12 에 그 둘을 했다** - 행을 더했고(순증 건수·실제 검수 비율 포함) ja-ko 를 쟀는데 **같은 부호가 나오지 않았다**(예산 10% 전체 순증이 en-ko -2건 대 ja-ko +0건, 예산 30%는 en-ko +16건 대 ja-ko +12건으로 양쪽 다 이득). 그래서 **ja-ko 조건은 닫혔고** 남은 다시 열 조건은 "v0.2 에서 Tier 1 을 손댈 때" 하나다 |

**미룬 이유가 다시 열 조건을 정한다.** 3·14는 **사람이 날을 잡아야** 하고, 15·16은
그때가 오면 **저절로 걸리며**, 나머지는 해당 영역을 손대는 작업이 함께 닫는다.
**FR-4.2 앞은 비었고 2026-09-12 에 FR-4.2 자체도 ✅ 로 닫혔다** — 17·18·19·20·21·22가
차례로 닫혔고, 마지막에 남은 23번은 **닫은 것이 아니라 v0.2 로 미뤘다.**

**막는 항목이 21 → 22 → 23으로 옮겨 다닌 것이 이 9일의 구조다.** 하나를 닫을 때마다
그 측정이 다음 하나를 드러냈고, 드러난 것이 그때마다 완료 조건으로 승격됐다 —
**요구사항이 늘어난 것이 아니라 합격선이 오른 것이다.** 완료 기준은 직전 실측이 아니라
**요구사항의 우선순위 등급과 기본값에서 읽는다**(§5.4의 「권장」, `--tier1`의 `False`).
그렇게 하지 않으면 측정을 잘할수록 FR이 멀어진다.
요구사항정의서 §5.4 FR-4.2 행이 여전히 🟡인 이유가 이것이다.

### 이월 21번 — Tier 1 실측: 신호는 맞고 후보 선정이 틀렸다

`en-ko` 벤치를 Tier 1로 돌렸다(exit 0). 신호가 잡히는데도 파이프라인 Recall이
떨어지는, 신호 결함이 아니라 **후보 선정 결함**이 드러났다.

| 예산 | 지표 | Tier 0 | Tier 0+1 |
| --- | --- | ---: | ---: |
| 10% | negation Recall | 1.41% | 0.00% |
| 10% | clean 부분집합 (n=61) | 1.64% | 0.00% |
| 30% | negation Recall | 19.72% | 18.31% |
| 30% | clean 부분집합 (n=61) | 22.95% | 19.67% |

원자료(`data/bench/en-ko.backtranslation.json`, 496건)에서 역번역 코사인은 신호가
올바른 방향임을 보여준다.

| 예산 | negation 평균 코사인 | 그 외 평균 코사인 | 방향 |
| --- | ---: | ---: | --- |
| 10% | 0.6909 | 0.7752 | 올바름 |
| 30% | 0.6698 | 0.7989 | 올바름 |

점수도 negation이 높다(예산 10%: 0.31 대 0.22, 예산 30%: 0.33 대 0.20). Q4 실측
(AUC 0.73~0.79)과 일치한다.

문제는 후보 선정이다. 후보 249건 중 negation은 전체 71건 중 **2건뿐**이었다.
`select_tier1_candidates`(`src/cuesift/triage/policy.py:181`)가 회색지대를 **Tier 0
위험도 내림차순으로 정렬해 상위 cap 건**을 취하는데, Tier 0는 의미 반전을 원리적으로
못 잡으므로 negation을 무작위 위치에 놓는다. LLM 없이 Tier 0만으로 두 트랙을 재어
확인했다.

| 트랙 | 예산 | 회색지대 안 순위 중앙값 | cap 적중 | 무작위 기대값 |
| --- | --- | --- | ---: | ---: |
| en-ko | 10% | 2338 / 4500 (52%) | 2 | 3.9 |
| en-ko | 30% | 1723 / 3500 (49%) | 3 | 4.1 |
| ja-ko | 10% | 2291 / 4500 (51%) | 3 | 3.8 |
| ja-ko | 30% | 1456 / 3500 (42%) | 4 | 4.1 |

네 칸 전부에서 중앙값이 회색지대의 절반 지점이고 적중이 무작위 기대값과 같거나
낮다. 컷라인 아래를 고르는 것도 Tier 0 순위에 의존하므로, Tier 0가 무작위로 흩어
놓는 부류에는 어느 구간을 골라도 무작위 표본이 된다. **`select_tier1_candidates`의
독스트링은 "위험도 상위를 후보로 삼으면 Tier 1은 이미 잡힌 것만 다시 본다"고 옳게
지적하고 컷라인 아래로 옮겼는데, 같은 논리가 컷라인 바로 아래에도 적용된다는 것을
보지 못했다.**

10% 예산에서 1.41% → 0.00%로 떨어진 이유도 여기 있다. Tier 1 점수를 받은 249건 중
negation이 아닌 247건의 점수가 올라가면서, 원래 선별됐던 negation 1건을 큐에서
밀어냈다.

**재현**: 위 두 표 중 코사인 분포는 `data/bench/en-ko.backtranslation.json`(자막
원문 포함, `.gitignore` 대상이라 커밋되지 않음)을 다시 돌려야 재현된다. 반면
**순위 분석은 LLM 호출이 필요 없다** — Tier 0만으로 회색지대 순위를 계산하면
되므로 몇 분이면 끝난다. 아래가 그 전부이며, 이월 19번의 분류 스크립트와 같은
이유로 리포에 넣지 않는다(일회성 측정이다).

```python
profile = load_builtin(f"ted-{lang}")                      # lang = "en" | "ja"
glossary = load_glossary(Path("bench/glossary.ted.yaml"), lang)
ctx = SignalContext(profile=profile, glossary=glossary, source_lang="ko", target_lang=lang)
segments = load_track(Path(f"data/bench/{lang}-ko.clean.json"))
mutated, labels, _ = inject(segments, glossary, profile, rate=0.10, seed=20260729)
signals = collect_all(mutated, ctx)
risks = [fuse(s.id, signals[s.id]) for s in mutated]
neg = {lb.segment_id for lb in labels if lb.kind == "negation"}

scored = select_by_budget(risks, budget)                   # budget = 0.10 | 0.30
gz = gray_zone(scored)                                     # 컷라인 아래, 위험도 내림차순
cap = int(len(scored) * 0.05)                              # TIER1_MAX_RATIO
pos = [i for i, r in enumerate(gz) if r.segment_id in neg]

# 판정은 둘이다. 중앙값이 len(gz)의 절반 근처인가,
# 그리고 cap 적중(sum(p < cap for p in pos))이 무작위 기대값
# len(pos) * cap / len(gz) 와 다른가.
```

**핵심은 무작위 기대값과의 비교다.** 적중 건수만 보면 "적지만 있긴 하다"로
읽히는데, 기대값과 나란히 놓아야 **선정이 무작위와 구별되지 않는다**는 것이
드러난다.

`ja-ko`는 이번에 돌리지 않았다 — 위 순위 분석이 이미 같은 결과를 예고했고, 두
시간을 더 써서 얻는 것은 확증뿐이다.

**다시 열 조건**: FR-4.2 재착수 시 `select_tier1_candidates`의 후보 선정 방식
자체를 재설계해야 한다(예: Tier 0가 원리적으로 판별 못 하는 오류 유형은 무작위
또는 별도 표본으로 후보에 포함하는 방식). 이 태스크(문서화 전용)는 그 재설계를
포함하지 않는다.

**이 조건은 2026-09-06(브랜치 `feat/tier1-candidate-selection`)에 충족됐다** —
아래 절이 그 재설계와 재측정을 기록한다.

### 이월 22번 - 결함 2 비대칭 밀어냄 원인 규명이 남았다

이월 21번의 재설계(`select_tier1_candidates`에 `priority_ids` 통로를 열어 극성
표지 보유 세그먼트를 회색지대 안에서 먼저 채운다, 설계
[tier1-candidate-selection-design.md](docs/superpowers/specs/2026-09-06-tier1-candidate-selection-design.md))를
en-ko에 적용해 재측정했다.

| 예산 | 회색지대 | cap | 후보 | 극성 표지 보유 | 채움분 | 후보 안 negation | 무작위 기대값 | 배수 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 10% | 4,500 | 250 | 250 | 250 | 0 | **17** | 3.89 | **4.37x** |
| 30% | 3,500 | 250 | 250 | 250 | 0 | **20** | 4.07 | **4.91x** |

**단일 출처는 [`bench/results/en-ko-2026-09-06.md`](bench/results/en-ko-2026-09-06.md)의
「Tier 1 후보 구성」표다.** 설계 §3.3이 예측한 en 트랙 4.55x·4.46x와 맞는다.
채움분이 0인 것은 우선 집합(P1 풀 847건·661건)이 cap보다 커서 D6 채움 경로가
발동하지 않았다는 뜻이고, 이것도 설계가 예측한 그대로다.

negation Recall도 함께 올랐다.

| 예산 | 2026-09-05 (Tier 0 → 0+1) | 2026-09-06 (Tier 0 → 0+1) |
| --- | --- | --- |
| 10% negation Recall | 1.41% → **0.00%** | 1.41% → **4.23%** |
| 10% clean 부분집합 (n=61) | 1.64% → 0.00% | 1.64% → **4.92%** |
| 30% negation Recall | 19.72% → **18.31%** | 19.72% → **42.25%** |
| 30% clean 부분집합 (n=61) | 22.95% → 19.67% | 22.95% → **45.90%** |

Tier 0는 전 예산에서 2026-09-05와 소수점까지 같다(`review_ratio`·`recall` 둘 다) —
Tier 0 경로는 건드리지 않았다.

교차 검증(원자료 `data/bench/en-ko.backtranslation.json`, 492건, Tier 1 비용을
실제로 치른 집합 — 아래 참고)도 방향이 올바르다.

| 예산 | 원자료 | negation | negation 평균 코사인 | 그 외 평균 | 방향 |
| --- | --- | --- | --- | --- | --- |
| 10% | 246 | 16 | 0.6873 | 0.7839 | 올바름 |
| 30% | 246 | 20 | 0.6816 | 0.7798 | 올바름 |

**이 Recall 상승의 원인을 이 실측이 규명하지는 않는다.** 설계 §9(완료 판정)는
Recall 개선을 조건으로 걸지 않았다 — 결함 ②(비대칭 밀어냄: 우선 집합에만 점수를
더하면 나머지가 상대적으로 밀려난다)가 남아 있기 때문이다. **그런데 실제로는
올랐다.** 이번 실행이 관찰한 사실은 "후보 250건이 전부 극성 표지 보유"라는 것뿐이고,
밀어냄의 성격이 달라졌는지는 재지 않았다. **"결함 ②가 해소됐다"고 단정하지 말 것** —
원인 규명은 회색지대 안에서 우선 집합 **밖** 세그먼트의 순위가 재설계 전후로 어떻게
이동했는지를 별도로 측정해야 답할 수 있다.

**리포트의 17건과 원자료의 16건은 불일치가 아니다.** 예산 10%에서 리포트는 후보
안 negation을 **17건**으로 싣는데 원자료는 **16건**(총계도 500이 아니라 492)이다 —
차이는 **역번역이나 임베딩이 신호를 못 낸 분**이다(번역 실패분이 아니다 -
네 건 모두 `target_text`가 정상이라 Tier 1 비용을 치렀고, `bench/run.py::_collect_raw`의
`bt is None` 필터에서 떨어졌다). **그중 `en-00952`는 negation 라벨이다** - 재설계가
골라내는 데는 성공했으나 백엔드가 신호를 못 준 건이고, 이것이 이월 22번의 입력이다.
`CandidateReport.candidate_ids`는 **선정된** 집합이고
원자료는 **신호를 낸** 집합이다(태스크 3 리뷰 I-4에서 의도적으로
정한 구분 — 설계 §3.2~§3.4의 기대값 계산이 전부 선정 집합 기준이라, 분모를 바꾸면
리포트가 설계 표와 대조되지 않는다).

이 브랜치가 한 일: 극성 표지 판정 모듈(`src/cuesift/polarity.py`, 설계 D1~D5,
커밋 `fa2bda6`·`c26400c`) · `select_tier1_candidates`의 `priority_ids` 통로(D6~D8,
`25a8cea`) · `tier1.py` 배선·`CandidateReport`·미지원 언어 경고·`tier1_cap()` 공유
함수(`1417082`·`ee9a294`·`99c62b0`) · 벤치 리포트 후보 구성 표(D10, `8b23227`·
`92d9fe5`) · 이번 재측정(`78c7690`). **극성 표지는 `risk_score`에 한 푼도 기여하지
않는다**(D9) — 바꾸는 것은 "누가 비싼 Tier 1 검사를 받는가"뿐이고 "얼마나
위험한가"가 아니다. `DEFAULT_WEIGHTS`는 전부 1.0 그대로다. 지원 언어는 ko·en·ja
뿐이고, 그 밖의 언어에서는 경고를 내고 빈 우선 집합으로 되돌아간다(D5).

**재현**: `bench/results/en-ko-2026-09-06.md`를 다시 내려면 캐시가 있는 채로

```bash
.venv/Scripts/python.exe -u -m bench.run --pair en-ko --seed 20260729 --tier1 \
  --base-url http://localhost:11434/v1 --model qwen2.5:3b --embed-model bge-m3 \
  --cache-dir data/bench/cache --audit-dir data/bench
```

를 돌린다. 결함 ②의 원인 규명(밀려난 세그먼트의 순위 이동)에는 별도 계산이
필요했고, 2026-09-12 에 `bench/pushout.py` 와 `--pushout` 플래그로 만들었다.
같은 명령에 `--pushout` 을 더하면 재설계 전후를 한 실행에서 비교한다.

### 규명 결과 (2026-09-12) - 다시 열 조건이 충족돼 닫는다

**측정 방법**: `triage_with_tier1` 에 `priority_ids` 주입 통로를 열어 재설계 전
조건(`frozenset()` 주입 - 극성 판정을 건너뛰므로 후보가 회색지대 위험도 순으로만
뽑힌다)을 재현하고, 같은 Tier 0 스냅샷을 기준선으로 두 조건의 세그먼트 단위 순위
이동을 비교했다. 단일 출처는
[`bench/results/pushout-en-ko-2026-09-12.md`](bench/results/pushout-en-ko-2026-09-12.md) 다.

**① 결함 ②는 해소되지 않았다. 7배로 커졌다.**

| 예산 | 조건 | 후보 밖 밀려남 | 평균 Δrank | 후보 안 Δrank 중앙값 |
| --- | --- | ---: | ---: | ---: |
| 10% | 재설계 전 | 174 | +3.81 | -50.5 |
| 10% | 재설계 후 | **1,231** | **+25.74** | **-479.0** |
| 30% | 재설계 전 | 1,160 | +46.81 | -841.5 |
| 30% | 재설계 후 | **2,268** | **+75.97** | **-1,484.5** |

**원인이 후보 안 Δrank 에 드러나 있다.** 재설계 전 후보는 위험도 상위라 이미
컷라인 근처였고 점수가 올라도 50계단쯤 움직인다. 재설계 후 후보는 극성 표지
보유라 **회색지대 전역에 흩어져 있어** 479계단을 거슬러 올라온다. 순위는
보존량이므로 올라온 만큼이 아래로 밀린다 - 후보 밖의 `올라감` 이 두 조건 모두
**0건**인 것이 비대칭의 정의다(noisy-or 는 점수를 올리기만 하고 Tier 1 은 후보에만
신호를 더하므로 후보 밖에서 위로 갈 방법이 구조적으로 없다).

**② 상승 원인은 밀어냄 완화가 아니라 유입 증가다.**

| 예산 | 조건 | negation 유입 | negation 유실 | 순증 |
| --- | --- | ---: | ---: | ---: |
| 10% | 전 -> 후 | 0 -> **2** | 1 -> 0 | -1 -> +2 |
| 30% | 전 -> 후 | 3 -> **20** | **4 -> 4** | -1 -> +16 |

**예산 30%에서 유실이 4건으로 똑같다.** 상승분 17건이 전부 유입에서 왔다.
예산 10%도 유입이 주이고 유실 감소는 1건뿐이다. **"밀어냄이 완화돼 Recall 이
올랐다"는 설명은 실측에 근거가 없다.**

**③ 대가가 있다 - 예산 10%에서 전체 Recall 이 떨어진다.** 상세는
[이월 23번](#이월-23번---예산-10에서-tier-1-이-전체-recall-을-깎는다)이 단일 출처다.

**재현성은 네 겹으로 확인했다** - 주입 트랙 SHA256 · 정답지 500건(다른 것은
`commit` 필드뿐) · Tier 0 전 예산 · Tier 1 negation Recall 이 전부 2026-09-06 과
같다. 원자료도 492건·negation 16·20건으로 동일했다.

**`en-00952` 는 원자료에 없는 것이 확인됐다** - 후보에는 선정됐으나 신호를 못 내
`bt is None` 필터에서 떨어졌다. 리포트의 17건과 원자료의 16건을 가르는 그 1건이고,
이 차이를 리포트가 설명하지 않던 것(M-1)을 이번에 고쳤다.

**다시 열 조건**: 없다. 물은 것(원인)에 답했다. 결함 ② 자체의 **수정**은 설계 §10 이
"결함 ① 수정 후의 실측을 먼저 얻는다"며 미뤄 둔 별건이고, 이제 그 실측이 생겼다.

### 이월 23번 - 예산 10%에서 Tier 1 이 전체 Recall 을 깎는다

이월 22번 측정이 드러낸 새 결함이다. **negation 만 보면 재설계가 성공인데, 전체
오류로 보면 예산 10%에서 손해다.**

| 예산 | 조건 | 전체 오류 (Tier 0 -> Tier 0+1) | 전체 Recall |
| --- | --- | --- | --- |
| 10% | 재설계 전 | 366 -> 387 (**+21**) | 73.2% -> **77.4%** |
| 10% | 재설계 후 | 366 -> 364 (**-2**) | 73.2% -> **72.8%** |
| 30% | 재설계 전 | 440 -> 439 (-1) | 88.0% -> 87.8% |
| 30% | 재설계 후 | 440 -> 456 (+16) | 88.0% -> **91.2%** |

부류별로 보면 무엇을 주고 무엇을 받았는지가 보인다(예산 10%).

| 부류 | 재설계 전 순증 | 재설계 후 순증 | 차이 |
| --- | ---: | ---: | ---: |
| glossary | +16 | +0 | **-16** |
| spec | +2 | -6 | **-8** |
| number | +4 | +2 | -2 |
| negation | -1 | +2 | **+3** |
| **합계** | **+21** | **-2** | **-23** |

**예산에 따라 부호가 갈리는 것이 이 항목의 핵심이다.** 예산 30%에서는 전체도
+16건으로 올랐고 그 16건이 전부 negation 이다. 예산 10%에서만 손해가 난다 -
회색지대가 4,500건으로 더 넓어 극성 표지 보유 후보가 더 멀리서 올라오고(Δrank
중앙값 -479), 그만큼 더 많이 밀어내기 때문이다.

**왜 2026-09-06 에 안 보였나**: `render_tier1_comparison` 이 negation Recall 과
clean 부분집합만 싣고 **전체 Recall(Tier 0 vs Tier 0+1)을 싣지 않는다.** 이월 21번의
판정 기준(설계 §9)이 후보 구성이었고 Recall 은 "결함 ② 를 판단할 자료"로만 기록하기로
했으므로, 전체 Recall 이 표에 없는 것 자체는 당시 설계와 어긋나지 않았다. 그러나
**Recall@Budget 은 이 프로젝트의 핵심 지표이고**(요구사항정의서 §9.1), 한 부류가
오른 것보다 전체가 떨어진 것이 무겁다.

**착수 시 먼저 할 것**: `render_tier1_comparison` 에 전체 Recall 행을 더한다. 지표가
표에 없으면 다음 재설계도 같은 자리에서 같은 것을 놓친다 - 이번에 보인 것도 별도
측정(`bench/pushout.py`)을 만들었기 때문이지 리포트가 알려 준 것이 아니다.

#### 판정 (2026-09-12) - v0.2 로 미룬다. 닫지는 않는다

**이 항목을 연 것은 요구사항이 아니라 실측이 드러낸 역효과다.** 그 구분이 판정을 정한다 -
요구사항이 요구한 것이라면 FR-4.2 의 완료를 막아야 하지만, 실측이 드러낸 역효과는
**그 FR 의 등급이 허용하는 범위 안인지를 먼저 봐야 한다.**

| 물음 | 답 | 출처 |
| --- | --- | --- |
| FR-4.2 의 우선순위는 | **「권장」** (필수가 아니다) | 요구사항정의서 §5.4 |
| 이 기능은 기본으로 켜지는가 | **아니다.** `--tier1` 기본값이 `False` 다 | `src/cuesift/cli.py` |
| 역효과가 나는 구간은 | **예산 10% 하나.** 예산 30%는 +16건으로 이득이다 | 위 표 |
| 사용자가 이 사실을 아는가 | **알게 했다.** `--help` 와 README 에 두 구간을 함께 실었다 | 이 브랜치 |

**그래서 「모든 예산 구간에서 이득일 것」을 완료 조건으로 적용하지 않는다.** 이 기준은
기본으로 켜지는 필수 기능에는 맞지만, 사용자가 명시적으로 켜는 「권장」 기능에는 과하다 -
적용하면 측정이 결함을 하나 드러낼 때마다 합격선이 함께 올라 FR 이 영원히 닫히지 않는다.

**대신 관찰은 유효하므로 닫지 않는다.** Recall@Budget 이 이 프로젝트의 핵심 지표라는
사실은 그대로이고, 한 예산 구간에서 그 지표가 떨어진다는 것은 v0.2 가 반드시 다뤄야 할
사실이다. [WBS](docs/WBS.md) "연기된 항목" 표에 실었다.

**다시 열 조건**: v0.2 에서 Tier 1 을 손댈 때, 또는 ja-ko 에서도 같은 부호가 확인될 때.
그때의 선택지는 셋이다 - 전체 Recall 이 떨어지는 예산 구간에서 Tier 1 을 끄거나, 결함 ②
를 구조적으로 고치거나(설계 §10), 재설계 자체를 재검토한다. **이 세 갈래는 이번 측정만으로
고를 수 없다** - ja-ko 에서도 같은 부호가 나오는지를 먼저 봐야 한다.

#### 착수 (2026-09-12, v0.2) - 전체 Recall 행을 더하고 ja-ko 를 쟀다

**착수 시 먼저 할 것 두 가지를 다 했다.** 아래 두 절이 각각의 결과다.

##### ① 전체 Recall 행을 더했다

`render_tier1_comparison` 의 첫 행이 전체 Recall 이고 비율·건수·순증을 함께 낸다.
검수 비율 행도 넣었다 - hard fail 이 예산을 우회하므로 두 조건의 큐 크기가 갈릴 수
있고, 갈리면 Recall 차이에 큐가 커진 몫이 섞인다. **빠진 키는 `KeyError`, 두 조건의
정답 건수가 다르면 `ValueError` 로 세운다** - `.get(key, 0.0)` 은 값이 없는 것과
Recall 이 0 인 것을 표가 구별하지 못하게 만드는데, 이 항목 자체가 "지표가 표에
없었다"에서 나왔으므로 같은 부류의 누락을 조용히 넘기지 않는다.

**게이트는 변이 5종으로 확인했다** - 순증 부호 뒤집기 · 순손실 판정 뒤집기 · Tier 0
칸에 Tier 1 값 넣기 · 검수 비율 경고 제거 · 분모 불일치 검사 제거. 다섯 다 잡혔다.
테스트 1957 -> 1967.

**교차 확인이 통과했다.** en-ko 를 같은 캐시로 다시 돌려, 새 행이 낸 값이 독립
측정(`bench/pushout.py`)의 값과 같은지 대조했다 - 예산 10% **366 -> 364 (-2건)** ·
예산 30% **440 -> 456 (+16건)** 으로 이월 22·23번의 기록과 일치한다. **두 경로가 같은
수를 낸다.** 더불어 `bench/results/en-ko-2026-09-12.md` 의 diff 가 **코드 커밋 해시와
새 행뿐**이다 - Tier 0 전 예산도 negation Recall 도 clean 부분집합도 한 글자도 바뀌지
않았으므로, 이 실행은 전체 Recall 행을 더한 것 말고는 2026-09-12 판의 완전한 재현이다.

##### ② ja-ko 실측 (2026-09-12, `qwen2.5:3b` · `bge-m3`)

정답지 500건 · negation 라벨 71건 · 트랙 5,000건이다.

| 예산 | 조건 | 전체 오류 | 전체 Recall | negation Recall |
| --- | --- | ---: | ---: | ---: |
| 10% | Tier 0 | 378 | 75.60% | 2.82% |
| 10% | Tier 0+1 | **378 (+0)** | **75.60%** | **8.45%** |
| 30% | Tier 0 | 424 | 84.80% | 19.72% |
| 30% | Tier 0+1 | **436 (+12)** | **87.20%** | **36.62%** |

실제 검수 비율은 네 칸 모두 요청 예산과 같았다(10.00% · 30.00%). 후보 250건은 두
예산 모두 **전부 극성 표지 보유**였고 채움분이 0건이다 - en-ko 와 같다.

##### ③ 두 언어쌍 대조 - **같은 부호가 나오지 않았다**

순증은 전부 건수다. `나머지 부류` 는 측정이 아니라 `전체 - negation` 의 **정확한
뺄셈**이다(둘 다 건수라 반올림이 없다).

| 예산 | 언어쌍 | 전체 | negation | 나머지 부류 |
| --- | --- | ---: | ---: | ---: |
| 10% | en-ko | **-2** | +2 (1 -> 3) | **-4** |
| 10% | ja-ko | **+0** | +4 (2 -> 6) | **-4** |
| 30% | en-ko | +16 | +16 (14 -> 30) | 0 |
| 30% | ja-ko | +12 | +12 (14 -> 26) | 0 |

**예산 10%에서 en-ko 는 손해였고 ja-ko 는 본전이다.** 다시 열 조건으로 적어 둔
"ja-ko 에서도 같은 부호가 확인될 때"는 **충족되지 않았다.**

**갈린 것은 대가가 아니라 이득이다.** 나머지 부류의 순증이 예산 10%에서 양쪽 다
정확히 -4건이고 예산 30%에서 양쪽 다 0건이다 - 네 칸에서 언어쌍이 아니라 **예산**이
값을 정했다. 전체 부호를 가른 것은 negation 을 몇 건 더 담았느냐였고, ja-ko 가 4건으로
en-ko 의 2건보다 많아 -4 를 상쇄했다.

**「나머지 부류 순증 0」을 「밀어냄이 없다」로 읽으면 안 된다.** 순증이 0인 것은 유입과
유실이 상쇄됐다는 뜻이다 - en-ko 예산 30% 실측에서 후보 밖 밀려남은 **2,268건**이었고
(이월 22번 표), 부류별로도 glossary 는 유입 7 · 유실 7 로 오갔다. 순증은 그 오감의
차일 뿐이다.

**ja-ko 의 유입·유실 분해는 재지 않았다.** `--pushout` 없이 돌렸으므로 이 표의 ja-ko
행은 순증만 말한다 - en-ko 행의 `+2 (1 -> 3)` 는 이월 22번이 유입 2 · 유실 0 으로
분해한 값이지만, ja-ko 의 `+4` 는 그 분해를 갖고 있지 않다. **부류로 묶인 서술이 각
원소를 확인했다는 뜻이 아닌 것과 같은 이유로**, 두 행을 같은 강도로 읽으면 안 된다.

##### ④ 후보 선정은 두 언어쌍에서 다 작동한다

| 예산 | 언어쌍 | 회색지대 | 후보 안 negation | 무작위 기대값 | 배수 |
| --- | --- | ---: | ---: | ---: | ---: |
| 10% | en-ko | 4,500 | 17 | 3.89 | **4.37x** |
| 10% | ja-ko | 4,500 | 11 | 3.83 | **2.87x** |
| 30% | en-ko | 3,500 | 20 | 4.07 | **4.91x** |
| 30% | ja-ko | 3,500 | 13 | 4.07 | **3.19x** |

**ja-ko 의 배수가 낮은데 negation 순증은 오히려 크다**(예산 10%에서 후보 11건에 +4건
대 후보 17건에 +2건). 후보에 negation 을 많이 넣는 것과 그중 몇 건을 컷라인 위로
올리는 것은 다른 일이라는 뜻이고, **이 실측은 그 이유를 규명하지 않는다** - 규명하려면
ja-ko 를 `--pushout` 으로 다시 돌려 Δrank 를 봐야 한다.

##### ⑤ 세 갈래 중 어디로 가는가

이월 23번이 적어 둔 선택지 셋을 이번 실측에 비춰 본다. **고르지는 않는다** - 아래는
근거가 어느 쪽으로 기울었는지까지다.

| 선택지 | 이번 실측이 말하는 것 |
| --- | --- |
| 전체 Recall 이 떨어지는 예산 구간에서 Tier 1 을 끈다 | **약해졌다.** 떨어지는 구간이 언어쌍마다 다르다 - 예산 10%에서 en-ko 는 -2 인데 ja-ko 는 +0 이라, 예산만으로는 끌 구간을 정할 수 없다 |
| 결함 ② 를 구조적으로 고친다 (설계 §10) | **가장 강해졌다.** 나머지 부류의 순증이 언어쌍과 무관하게 예산으로만 갈린다(10%에서 -4 · 30%에서 0). 언어에 따라 달라지지 않는 값은 언어 처리가 아니라 **구조**에서 나온다 |
| 재설계 자체를 재검토한다 | **약해졌다.** 후보 선정은 두 언어쌍 네 칸 모두에서 무작위의 2.87~4.91배다. 재설계가 하려던 일은 하고 있다 |

**다만 네 칸은 네 칸이다.** 「예산이 값을 정한다」는 언어쌍 2개 × 예산 2개에서 관찰한
것이고, -4 가 두 번 나온 것이 우연일 수 있다. **세 번째 언어쌍도 세 번째 예산 지점도
없으므로, 이것을 구조라고 부르려면 예산 지점을 늘려 같은 관계가 이어지는지 봐야 한다.**

**다시 열 조건 (갱신)**: ja-ko 조건은 **닫혔다**(같은 부호가 나오지 않았다). 남은
조건은 "v0.2 에서 Tier 1 을 손댈 때"이고, 그 착수의 첫 질문은 **결함 ② 의 구조적
수정**이다. 먼저 할 것은 예산 지점을 늘려(예: 5% · 15% · 20%) 나머지 부류 순증이
예산의 함수인지 확인하는 것이다 - 그것이 참이면 고칠 대상이 무엇인지가 정해진다.

### 종결 (2026-09-13) - 결함 ② 를 구조적으로 고쳤다

**단일 출처는 [설계 스펙](docs/superpowers/specs/2026-09-13-tier1-pushout-protection-design.md)
이다.** 아래는 그 스펙이 갖지 않은 것 - 판정의 경위와 다음 세션이 알아야 할 것 - 만 적는다.

#### ① 착수의 첫 질문은 기각됐다

위 ⑤ 표가 「나머지 부류 순증이 예산으로만 갈린다(10%에서 -4 · 30%에서 0)」를 결함 ②
수정의 **가장 강한 근거**로 적었다. 예산 지점을 다섯으로 늘리자 그 관계가 사라졌다.

| 예산 | en-ko 나머지 | ja-ko 나머지 |
| --- | ---: | ---: |
| 5% | +0 | +0 |
| 10% | **-4** | **-4** |
| 15% | +1 | -1 |
| 20% | -1 | -1 |
| 30% | +0 | +0 |

**-4 는 예산 10% 에만 있다.** 두 칸이 같은 값을 냈다고 해서 그 사이가 이어져 있는 것은
아니었다 - **가운데를 재지 않았을 뿐이다.** 결론(결함 ② 를 고친다)은 바뀌지 않았지만
**그 결론에 이른 근거는 틀렸다**. 근거가 틀린 채 결론이 맞는 것은 다음 판단에서 위험하다.

#### ② 고친 규칙에는 임계 상수가 없다

거래의 손익은 `(승격 중 오류) - (밀려남 중 오류)` 이고 **밀려나는 쪽은 언제나 컷라인
바로 위**다. 그 구간의 밀도를 정하는 것은 컷라인의 Tier 0 점수였다.

| 예산 | 컷라인 점수 | en-ko 밀도 | ja-ko 밀도 |
| --- | ---: | ---: | ---: |
| 5% | 1.0000 | 84% | 76% |
| 10% | 0.5000 | 28% | 16% |
| 15% 이상 | 0.0000 | 2% | 4% |

점수 분포가 **0 과 0.5 사이에서 비어 있다** - 결정론적 신호가 하나라도 발화하면 0.5
이상이고 하나도 없으면 0 이다. 그래서 규칙이 값 없이 적힌다: **Tier 1 은 Tier 0 가
아무 말도 하지 못한 칸만 가져간다.** 임계 상수를 두면 그 값을 벤치 Recall 로 맞추게
되고, 그것은 이 리포가 금지한 「같은 데이터에서 맞춘 가중치」와 성질이 같다.

**후처리 정책 9종은 전부 실패했다**(증거 문턱 τ · 상위 q% · 자기 상대 문턱).
승격 적중률이 최대 21% 인데 밀려남 적중률이 23~25% 라, 어떤 문턱으로 승격을 줄여도
거래의 부호가 뒤집히지 않는다. **고칠 곳은 누구를 올리느냐가 아니라 누구를 밀어내느냐였다.**

#### ③ 예정에 없던 결함을 하나 함께 닫았다

⑧ 무음 열화 경고를 더하자 `tests/test_cli_tier1.py` 5건이 깨졌다. CLI 가 `warn` 이
불렸는지로 「Tier 1 이 안 돌았다」를 추론하고 있었고, 그래서 **LLM 을 부르고 토큰을 쓴
실행이 `cost.includes` 에 `["translation"]` 만 실었다.** 화면 어디에도 신호가 없는
종류의 거짓말이다.

**같은 함정이 `_POLARITY_UNSUPPORTED` 에도 이미 있었다** - 극성 표지 목록이 없는
언어에서도 Tier 1 은 돈다. 테스트가 ko→en/ja 만 써서 드러나지 않았을 뿐이다.
`_tier1_warn` 의 독스트링이 이 실패를 **예고하고 있었다**: 「`warn` 이 다른 사유로도
불리게 되는 날 이 기록은 거짓이 된다」. 전용 콜백 `on_skip` 으로 갈랐다.

**좁힌 대상만 돌렸다면 못 봤다** - `tests/test_triage_policy.py` 와
`tests/test_tier1.py` 는 전부 통과했다. 「무엇을 대상으로 통과했나」가 한 번 더 발동했다.

#### ④ 다음 세션이 알아야 할 것

- **예산 10% 이하에서 Tier 1 은 아무 일도 하지 않는다.** 고장이 아니라 설계다 -
  그 구간의 컷라인 점수가 0.5 이상이라 내어줄 칸이 0 건이고, 실행이 그 사실을 경고한다.
  **켜서 이득을 보려면 예산 15% 이상이어야 한다**(`--help` 와 README 가 그렇게 말한다).
- **밀도 곡선은 이 트랙의 것이다.** 실제 자막에서 Tier 0 신호가 더 둔하면 점수 0
  구간에도 오류가 더 남는다 - 그때 이 규칙은 **더 보수적으로** 동작할 뿐 손해로
  뒤집히지는 않는다(설계 §7).
- **자가일관성 점수는 후보 집합에 의존한다.** 온도 1.0 재번역을 배치로 돌리므로 같은
  세그먼트라도 예산이 다르면 값이 다르다(실측: `en-00806` 이 예산 10%에서 0.1786,
  15%에서 0.2387). 오프라인 재구성으로는 **수집이 끝난 뒤의 후처리**만 흉내낼 수 있고,
  후보 집합 자체를 바꾸는 안은 이 방법으로 잴 수 없다.

## 승계 항목

| 항목 | 상태 |
| --- | --- |
| **Q4**(자가일관성 유사도 측정 수단) | ✅ **닫혔다(2026-09-04).** 임베딩이 필요하다. 요구사항정의서 §12 Q4가 단일 출처. **유보 ①은 로컬 2칸이 닫혔고 상용 2칸이 남았다** — 옛 1차가 로컬·상용을 함께 쟀기 때문이다 |
| **FR-4.2**(역번역) | ✅ **완료 (2026-09-12).** v0.1 대상 42개가 전부 닫혔다. **닫은 근거는 새 실측이 아니라 요구사항의 등급이다** - 우선순위 「권장」(요구사항정의서 §5.4)이고 `--tier1` 기본값이 `False` 인 선택 기능이라, 「모든 예산 구간에서 이득일 것」은 이 등급에 과도한 기준이라 적용하지 않았다. **실측된 트레이드오프**: 예산 30%에서 negation Recall 19.72% → **42.25%** · 전체 오류 **+16건**(88.0% → 91.2%), 예산 10%에서 negation 1.41% → **4.23%** · 전체 **-2건**(73.2% → 72.8%) - **예산에 따라 부호가 갈린다.** 기본이 꺼짐이므로 이 두 구간을 `--tier1` 의 `--help` 와 [README](README.md) 에 함께 실었다. **남은 것은 v0.2 다** - 이월 23번(예산 구간별 이득 판정 · 결함 ② 의 구조적 수정 · `render_tier1_comparison` 에 전체 Recall 행)을 미뤘고 닫지는 않았다. 여기에 이르기까지의 경과(이월 20·21·22번)는 [위 절들](#이월-22번---결함-2-비대칭-밀어냄-원인-규명이-남았다)이 사료로 보유한다. **2026-09-13: 이월 23번이 닫혔고 「예산에 따라 부호가 갈린다」는 더 이상 성립하지 않는다** - 결함 ② 를 고쳐 두 언어쌍 × 다섯 예산 열 칸 어디에서도 전체 Recall 이 떨어지지 않는다. `--help` 와 README 문구도 함께 갱신했다 |
| **FR-8.5 R3**(Windows 콘솔의 `\r`) | 구조는 확인됐고 **육안 관측이 남아 있다.** 진짜 콘솔 창이 필요하다 |
| **FR-8.3 R3**(STT live 검증) | **열려 있다.** `/v1/audio/transcriptions`가 이 서버에서 **400**을 낸다(404 아님) — 경로는 라우팅된다. **400이 "요청이 틀렸다"인지 형태만 받는지는 안 갈랐다** |
| **`engine.py::_run_single`의 전역 index** | 확인됐고 안 고쳤다. `main`에 있다 |
| `segments[].reasons`의 순서 미검증 | NFR-3 재현성 문제. 열려 있다 |
| 파킹 2 — 권장 모델 `qwen2.5:3b`의 한국어 출력 안정성 | 250건 기준 오염률 en 26% · ja 33%. **다만 판정력을 떨어뜨리지는 않는다**(다국어 임베딩이 코드 스위칭을 흡수한다) |
| 파킹 4 — `COLUMNS=88` 아래에서 옵션 이름이 잘린다 | rich의 표 렌더링. 폭 88을 게이트로 못 박았다 |
| **CHANGELOG 아카이브** | `ARCHIVE_NEEDED=1`이 뜨지만 **실행할 것이 없다**(위 ⓓ). 첫 릴리스 태그가 생기면 그때 유효해진다 |

## 벤치를 다시 돌리는 법

트랙은 `data/bench/`에 남아 있어 **코퍼스 재빌드가 필요 없다.** 주입 단계부터
다시 돈다.

```bash
.venv/Scripts/python.exe -m bench.run --pair en-ko
.venv/Scripts/python.exe -m bench.run --pair ja-ko
```

**Tier 1(이월 21번 재현 포함)을 다시 돌릴 때는 `--cache-dir`을 꼭 줄 것** — 역번역·임베딩은
호출당 비용이 커서 en-ko 한 쌍이 캐시 없이 1시간 48분이 걸리고, 중간에 죽으면 전량
유실된다. 캐시 디렉터리를 지정해 두면 재실행 시 이미 계산된 항목을 건너뛴다.

**예산 지점은 `--tier1-budgets` 로 바꾼다**(2026-09-13 신설, 기본값은 10%·30%).
쉼표로 준다 - `--tier1-budgets 0.05,0.10,0.15,0.20,0.30`. 상수를 손으로 고쳐 재면
그 변경이 작업트리에 남고, 그대로 커밋하면 다음 실행의 기본 비용이 조용히 오른다.

**리포트 파일명에 실행 날짜가 들어가므로 같은 날 두 번 돌리면 덮어쓴다.**
그리고 `--audit-dir` 기본값이 트랙과 같은 디렉터리라 **`data/bench/*.injected.json`과
`*.labels.json`을 덮어쓴다** — 옛 정답지와 비교할 일이 있으면 먼저 복사해 둘 것.

**긴 명령의 종료 코드를 볼 때 `; echo "exit=$?"`를 붙이지 마라** — 파이썬이
죽어도 뒤의 `echo`가 성공해 exit 0으로 보고된다(직전 세션 실측). 파이프로
`tail`을 붙이는 것도 같은 문제다. 파일로 리다이렉트하고 `echo "EXIT=$?"`를
**다음 줄에** 둔다.

## 개발 환경 메모 (승계)

**Python 실행은 반드시 `.venv/Scripts/python.exe`를 쓴다.** 시스템 Python은 3.14라 다르다.
게이트는 CI와 같은 대상 `.`으로 돌린다 — **`src tests`로 좁히면 안 된다**(그 차이로 CI가
5회 연속 실패한 전례가 있다).

**로컬과 CI의 게이트 수치는 1건 다르다.** 2026-09-06 기준 로컬 `1938 passed`,
CI 는 1건이 skip 된다(`tests/test_bench_glossary.py:47`이 트랙을 요구하는데
`data/`가 `.gitignore`다). **수집 개수 `1943 / 5 deselected`는 양쪽이 같아야 한다** -
이쪽이 갈리면 진짜 문제다.

**문서를 추가했으면 `git add` 뒤에 링크 체커를 돌린다.** 이번에도 실제로 갈렸다 —
새 리포트 2개를 추적하기 전에는 링크 체커 **45개** vs markdownlint **47개**였다.
**두 도구의 파일 개수가 같은지를 본다.**

**리포 루트에 `cuesift.yaml`을 만들지 마라.** 자동 탐색이 그것을 읽는다.
`conftest.py`의 autouse가 인프로세스 테스트는 막지만 **서브프로세스는 못 막는다.**

**변이 실험은 백업본을 만들고 복원을 `finally`에 둔다.** 스크래치 사본에서 할 때는
`PYTHONPATH`를 강제한다 — editable install의 `.pth`가 사본을 가려 "생존" 오탐을 낸다.
**`git checkout --`로 복원하지 마라** — 미커밋 작업을 날린 전례가 있다.

**콘솔 출력이 cp949라 한글·한자가 `UnicodeEncodeError`로 터진다.** 판정이 필요한
출력은 **UTF-8 파일로 받아서 읽는다**(`open(..., encoding="utf-8", newline="")`).
이번 세션에서도 일본어 표본을 찍다가 한 번 죽었다.

**Git Bash의 `/tmp`와 파이썬의 `/tmp`는 다른 곳이다.** 파이썬이 `/tmp/x.txt`에
쓰면 실제 경로는 `C:\tmp\x.txt`이고, Git Bash의 `cat /tmp/x.txt`는 그것을 못 찾는다.
`/c/tmp/x.txt`로 읽거나 스크래치 디렉터리를 쓴다.

**파이썬 스크립트로 문서를 고칠 때 `newline=""`을 준다.** `Path.write_text`의 기본값이
`newline=None`이라 Windows에서 `\n`을 `\r\n`으로 번역해 **2줄 수정이 수백 줄 변경으로** 찍힌다.
**`CHANGELOG.md`·`HANDOFF.md` 둘 다 LF다**(실측).

**긴 한글 문서는 heredoc이 아니라 `Write` 도구로 쓴다.** 여러 줄 커밋 메시지는
`git commit -F <파일>`로 넘긴다 — heredoc은 조용히 깨진다. **Bash heredoc 안의
파이썬에 한글 리터럴을 넣지 마라** — `SyntaxError: (unicode error)`로 죽는다.
한글·일본어가 필요하면 `\uXXXX` 이스케이프를 쓰거나 `Write`로 스크립트를 만든다.
**f-string 안에 조건식을 넣는 것도 heredoc에서 파서 오류를 냈다** — 문자열 연결로 쓴다.

**Bash heredoc 안의 파이썬에 Windows 경로를 넣지 마라.** 역슬래시가 한 겹 먹혀
`tests\\fixtures`가 `tests\fixtures`(폼피드)로 바뀐다. 경로가 섞인 편집은 `Edit` 도구로 한다.

**한글을 curl 커맨드라인에 넣지 마라.** Git Bash의 `curl -d`가 CP949로 인코딩해
모델이 `"random characters and gibberish"`라고 답한다. LLM 백엔드에 한글을 보내는 것은
`httpx`로 한다(런타임 의존성에 이미 있다).

**로컬 Ollama는 긴 입력에서 `ReadTimeout`을 낸다.** 재시도가 없으면 한 건의 타임아웃이
측정 전체를 죽이고 **결과 파일이 아예 쓰이지 않는다.**
장시간 측정 스크립트에는 재시도와 백오프를 넣는다.

### 로컬 LLM

```powershell
curl.exe http://localhost:11434/api/tags      # 설치된 모델과 capabilities
```

현재: `qwen2.5:1.5b`·`qwen2.5:3b`(둘 다 `completion`·`tools`) · `bge-m3`(`embedding`,
1024차원). **임베딩은 `capabilities`에 `embedding`이 있는 모델만 낸다** — 없으면
`/v1/embeddings`가 **501**이다(404가 아니다. 경로는 있고 모델이 못 하는 것이다).

### 상용 API

**키는 `.claude/settings.local.json`의 `env.OPENAI_API_KEY`에 있다**(`.gitignore` 99번째 줄에
걸려 추적되지 않는다). 확인은 값을 찍지 말고 길이만 본다 — 기대값 `len 164`.

```powershell
.venv/Scripts/python.exe -c "import os; k=os.environ.get('OPENAI_API_KEY'); print('len', len(k) if k else None)"
```

`None`이면 Claude Code를 재시작한다. **`setx`로는 되지 않는다** — 이미 떠 있는
프로세스 트리의 환경 블록을 갱신하지 않기 때문이다(실측). 단가는
`gpt-4o-mini` 입력 $0.15/1M · 출력 $0.60/1M, `text-embedding-3-large` $0.13/1M이고,
**충전한 $5는 사실상 그대로다.**

**이 키를 제품 문서에 넣지 마라.** `src/`·`scripts/`·`bench/` 어디도 읽지 않는다 —
spike 전용 개인 설정이다(위 ⓓ).

STT live 테스트는 `-m live` · `CUESIFT_LIVE_STT_*` 환경변수로 돌고
**오디오는 리포에 넣지 않는다**(D10, `CUESIFT_LIVE_AUDIO`로 받는다).

## 다음 세션 시작 절차

```bash
gh pr list --state all --limit 3 --json number,title,state,mergedAt
git branch --show-current                 # main
git status --short                        # clean
git log --oneline -3
```

**`main`에 직접 푸시하지 않는다.** CI의 `push` 트리거가 `branches: [main]`뿐이라
직접 푸시하면 머지된 **뒤에야** CI가 돈다 — 게이트가 아니라 사후 통보다.
PR 절차는 [CLAUDE.md](CLAUDE.md)의 "PR 절차"에 있다.

**다음 작업은 FR-4.2(역번역)다.** v0.1의 마지막 한 개이고, 착수 조건이 이번에
전부 갖춰졌다.

| 조건 | 상태 |
| --- | --- |
| 유사도 측정 수단 | ✅ 임베딩 코사인 (Q4, 2026-09-04) |
| 역번역 모델 | ✅ 로컬로 충분 (상용을 쓸 근거가 실측에 없다) |
| 정답지 | ✅ 고쳤다 (이월 17, PR #32) |
| **원리적 상한의 이해** | ✅ **쟀다** (이월 18, 2026-09-04) — 복원율 en 21.8% · ja 17.9% |
| 정답지가 주는 잡음 | ✅ **셌다** (이월 19번) — ja 자격 303건 중 119건(39.3%). **정상 반전 184건에서의 Recall을 따로 보고하라** |

**착수할 때 이월 18번의 `delta_restored`를 임계값 근거로 인용하라.** 복원된
건에서 신호가 0이므로 **임계값을 낮추는 것으로는 그 20%를 잡지 못한다** —
Recall 목표를 80% 언저리로 잡고, 못 잡는 부류가 무엇인지 설계 스펙에 적는다.

**착수하면 역번역 원자료를 세그먼트별로 남겨라**(이월 20번). 이번에 ja 라벨 4건이
교체됐는데 결과 JSON에 집계값만 있어 **그 4건만 다시 잴 수 없었다.**
