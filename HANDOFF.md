# Session Handoff

> Last updated: 2026-07-29 (KST)
> Branch: `main` (작업 트리 깨끗, stash 없음)
> Latest commit: `c746098` — 계획: noisy-or 융합과 숫자 NFKC 정규화 구현 계획
> **다음 세션은 코드부터 시작한다** — 설계·계획이 모두 확정됐다

## Current Status

**이번 세션은 코드를 한 줄도 바꾸지 않았다.** 다음 작업의 설계를 확정하고,
프로젝트 전체 진척을 처음으로 문서화했다.

```text
  411b14d  설계 스펙      noisy-or 융합 + 숫자 NFKC 정규화
  a3d234a  WBS           v0.1까지 남은 작업 전체 분해
  c746098  구현 계획      태스크 6개 · 리뷰 3축
```

제품 상태는 직전 세션과 같다 — **Tier 0 신호 엔진과 벤치마크 하네스가 완성됐고
CLI는 여전히 배선되지 않았다**(모든 서브커맨드가 종료 코드 `70`).

| 예산 10% (현재 실측) | en-ko | ja-ko |
| --- | --- | --- |
| 실제 검수 비율 | 10.00% | 10.00% |
| Recall | 69.60% | 70.60% |
| **무작위 대비 배수** | **6.96x** | **7.06x** |

이 숫자는 **다음 세션의 작업으로 바뀐다.** 아래 계획이 그것을 재측정한다.

저장소: <https://github.com/withwooyong/cuesift> (Public).
로컬이 `origin/main`보다 **3커밋 앞서 있다** — 푸시는 명시적 요청이 있을 때만.

## 다음 세션에 할 일

**`docs/superpowers/plans/2026-07-29-noisy-or-fusion-nfkc.md`를 Task 0부터 실행한다.**

```text
  Task 0  스펙 §5 표 정정 (w=0 행)              문서
  Task 1  A2 — _numbers() NFKC 정규화           src/cuesift/signals/structural.py
  Task 2  A2 단독 측정                          --out-dir data/bench/tmp (커밋 없음)
  Task 3  A1 — noisy-or 융합                    src/cuesift/risk/fuse.py
  Task 4  최종 측정 · 리포트 갱신                bench/results/
  Task 5  문서 갱신                             README·CHANGELOG·HANDOFF·WBS·요구사항정의서
```

계획에 **모든 테스트 코드와 커밋 메시지가 그대로 들어 있다.** 기대값 5건은
현재 코드에서 실제로 실행해 확인했고, 전부 예측대로 실패한다.

### 이 계획에서 특히 조심할 것

| 항목 | 내용 |
| --- | --- |
| **커밋 순서** | Task 3(noisy-or)의 커밋이 Task 2(A2 단독 측정)보다 먼저 들어가면 A/B가 무너진다. 병렬 구현은 되지만 커밋 순서는 강제해야 한다 |
| **`--out-dir`** | 중간 측정에 반드시 준다. 기본값이 `bench/results`라 그대로 돌리면 **커밋된 리포트를 덮어쓴다** |
| **기준 테스트 수** | **277**이다. 이전 HANDOFF의 271은 낡았다 |
| **불변식** | `check_invariants()`가 위반 시 예외를 던져 리포트를 내지 않는다. 숫자를 맞추려 완화하지 않는다 |

## 전체 진척 — `docs/WBS.md`

**"얼마나 더 하면 끝나는가"에 답하는 문서를 처음 만들었다.**

```text
  v0.1 대상 FR 42개 중 16개 완료 (38%)

  WP1 Tier 0 신호 엔진   ✅   WP4 인제스트  ⬜   WP7 번역 계층  ⬜
  WP2 벤치마크 하네스     ✅   WP5 출력      ⬜   WP8 Tier 1     ⬜
  WP3 융합·검출 정정      🔨   WP6 CLI 배선  ⬜   WP9 STT        ⬜
```

의존 그래프가 두 가지를 드러냈다.

| 발견 | 뜻 |
| --- | --- |
| **WP4(인제스트)가 병목** | 출력·번역·STT 셋이 전부 여기 걸린다. 자막 파일을 `Segment`로 바꾸지 못하면 Tier 0 엔진에 실데이터가 들어가지 않는다 |
| **`check`는 번역 없이 완결된다** | FR-8.2는 규격 검사만 한다. 인제스트 + 배선만으로 **쓸 수 있는 제품**이 나오며, v0.1 전체를 기다리지 않고 중간 산출물을 낼 수 있는 유일한 지점이다 |

권장 순서: WP3(진행 중) → WP4 → `check` 배선 → WP7 → WP8 → WP5·WP6 → WP9.

**WBS는 요구사항정의서의 파생물이지 병렬 출처가 아니다.** FR이 WBS에서만 바뀌면 갈라진다.

## Key Decisions Made

- **noisy-or 채택을 재측정 결과에 걸지 않는다.** "증거가 늘면 위험도가 내려간다"는 것은
  지표 이전에 논리적 결함이다. 벤치 숫자를 보고 융합식을 고르면 그것이 곧 **벤치
  데이터로 제품을 튜닝하는 것**이고, `DEFAULT_WEIGHTS`를 전부 1.0으로 둔 이유와
  같은 함정이다. 재측정의 목적은 판정이 아니라 기록이며, **Recall이 내려가도 그
  숫자를 README에 싣는다.**
- **가중치는 지수로 넣는다** — `1 - ∏(1 - sᵢ)^wᵢ`. 점수 스케일 가중(`1 - ∏(1 - wᵢ·sᵢ)`)은
  `wᵢ·sᵢ > 1`에서 `(1 - wᵢ·sᵢ)`가 음수가 되어 곱의 부호가 뒤집힌다. `w ≤ 1` clamp를
  강제해야 하고 그러면 "신호를 강화한다"를 표현할 수 없다.
  `w=0`이 "관측하지 않음"이 되어 **ablation의 신호 끄기와 의미가 일치**하는 것도 지수 쪽이다.
- **NFKC는 추출된 숫자 문자열에만.** 텍스트 전체를 정규화하면 `½`(카테고리 `No`)가
  `1⁄2`가 되어 **원문에 없던 숫자 1과 2가 생긴다.** `\d`는 `Nd`만 잡으므로 추출 후
  정규화는 그 경로를 열지 않는다. 변경 지점이 하나면 재측정 결과의 원인 후보도 하나다.
- **`--fusion` 플래그를 만들지 않는다.** A/B는 두 커밋에서 각각 측정하는 것으로 대신한다.
  쓰이지 않을 설정 경로를 제품에 남기지 않는다.
- **2단계로 측정한다.** A2와 A1의 효과가 반대 방향일 수 있다(A2는 hard fail을 줄여
  예산에 여유를 만들고, A1은 그 여유를 채우는 순서를 바꾼다). 1회 측정은 상쇄되면
  "변화 없음"으로 읽힌다.
- **WBS의 작업 패키지를 FR 번호에 묶는다.** 진척 주장을 반증 가능하게 만들기 위해서다.
  "인제스트 60% 완료"는 아무도 반박할 수 없지만 "FR-1.1 완료"는 코드와 테스트로 확인된다.

### 이전 세션에서 이어지는 결정 (변경 없음)

초기 언어쌍 **ko→en/ja**(Q2) · 로컬 LLM은 **OpenAI 호환 엔드포인트로 일원화**(Q3) ·
규격 1차 출처는 **Netflix TTSG**(Q5) · LLM 연동은 **자체 얇은 어댑터**(Q6) ·
배수 헤드라인은 **예산 10%**에서 뽑는다 · **측정을 끝낸 뒤 제품 코드를 고치지 않는다**.

## 이번 세션이 문서에서 찾아낸 결함

코드를 안 고쳤는데도 **셋을 잡았다.** 전부 "실제로 확인해 보는" 과정에서 나왔다.

| # | 결함 | 어떻게 드러났나 |
| --- | --- | --- |
| 1 | 새 스펙이 **링크 검사를 받지 않았다** | 링크 체커 12개 vs markdownlint 13개. `git ls-files` 기준이라 미추적 파일이 빠진다 |
| 2 | 스펙 §5의 `w=0` 행이 **틀렸다** | 계획을 쓰며 값을 실제로 계산. 두 산식이 일치하는 지점이라 PASS다 |
| 3 | 스펙이 **교체해야 할 기존 테스트를 놓쳤다** | `test_weighted_average_of_multiple_signals`·`test_weights_shift_the_result`가 가중 평균을 값으로 고정하고 있다 |

1번이 이 프로젝트의 규율이 실제로 작동한 사례다 — **`깨진 링크 없음`만 읽었다면
검사받지 않은 파일을 "통과"로 오해했을 것이다.** 두 게이트가 다른 방식으로 파일을
수집(`git ls-files` vs glob)하기 때문에 그 차이가 신호가 됐다.

2·3번은 **계획을 쓰는 행위 자체가 스펙의 검증**이라는 것을 보여준다. 스펙은
"이 테스트는 실패할 것"이라고 적었지만 계획은 실제 값을 적어야 하므로 계산을 강제한다.

## Known Issues

### `struct.number_missing`이 전각 숫자를 놓친다 — **다음 세션 Task 1에서 해결**

`src/cuesift/signals/structural.py:97`의 `_numbers()`가 NFKC 정규화를 하지 않아
`'５０' != '50'`이다. 두 자리라 `multi_digit` → **hard fail** → 검수 예산 우회.
**ja-ko 자연 오탐 41건 중 13건(31.7%)이 이 경로다.**

한자 수사(`十`·`百`·`万`, 4건 9.8%)는 같은 계열이나 **정규화로는 해결되지 않는다** —
파서가 필요하고 `十分に`(≠ 10분)·`万一`(≠ 10001) 같은 관용구에서 새 오탐을 만든다.
계획에서 명시적으로 범위 밖으로 뒀다.

### 가중 평균 융합의 구조적 문제 — **다음 세션 Task 3에서 해결**

문제를 하나 더 찾으면 위험도가 **내려간다** — 규격 위반(1.0)에 용어 위반(0.5)을
더하면 0.75가 된다. 오라클 대비 달성률이 예산 10%에서만 69.6%로 꺼지고
(1~5% 86.4%, 20~30% 86~89%), 그 구간이 기본 운영점이다.

**노이지-or로 바꾼 뒤에도 남을 문제**: ablation에서 `length.ratio`가 두 언어쌍 모두
**−6.0%p**다(끄면 Recall이 오른다). 가중 평균의 희석이 결함이었지만 오탐 신호에
한해서는 우연히 완충으로 작동했으므로, noisy-or에서 **악화될 가능성이 실재한다.**
답은 융합식을 되돌리는 것이 아니라 `length.ratio` 자체를 고치는 것이다 —
짧은 원문에서의 계통 오탐은 한 번 수정됐고(`3718e3a`) 남은 −6.0%p는 그 수정이
불완전하다는 신호다. **별도 과제로 이관한다.**

### `spec.overlap`은 여전히 판정 불가

계획 A 재리뷰가 이 신호의 캐스케이드(단일 타임코드 오타가 트랙 50~97%를 flag)를
발견하고 "벤치 하네스의 ablation으로 A/B 판정할 것"이라 남겼으나, **`build_track`이
겹침 0건으로 트랙을 합성하고 주입기의 `spec` 유형은 duration을 줄이므로 겹침을
만들지 않는다.** 기여도 `+0.0%`는 "쓸모없다"가 아니라 "측정하지 못했다"이다.

판정하려면 **겹침이 있는 실제 자막 트랙**이 필요하다 — WP4(인제스트) 이후에 가능해진다.

### 한자 수사가 리포트 원인 목록에 없다 — **Task 4 Step 4에서 함께 닫힌다**

ja-ko 자연 오탐의 9.8%(4/41)가 한자 수사 경로인데 리포트의 원인 목록 4갈래에 없다.
A2가 NFKC 경로를 제거하면 원인 구성이 바뀌므로 목록을 재작성해야 하고,
그 과정에서 함께 해결된다.

### 이전 세션에서 이어지는 이슈 (여전히 유효)

- **외부 URL은 아무도 검사하지 않는다.** 링크 체커는 내부 상대 링크만 본다.
  착수 시점에 사람이 재확인해야 한다
- `scripts/check_links.py`는 **앵커를 검사하지 않는다.** 절 번호를 바꾸면 앵커가 조용히 깨진다
- **링크 체커는 `git ls-files` 기준이라 미추적 파일을 건너뛴다.** 새 문서를 만들면
  `git add` 후에 돌려야 검사된다 — 두 게이트의 파일 개수를 대조하는 것이 유일한 탐지 수단
- `logprobs`는 백엔드에 따라 조용히 사라진다(Ollama 미지원)
- 자가일관성 샘플링은 `n>1` 단일 호출이 아니라 **N회 개별 호출**이어야 이식성이 유지된다
- **로컬 venv가 Python 3.14인데 CI는 3.11/3.12를 검증한다**

## Context for Next Session

**이번 세션의 요청은 "다음 작업을 알려 달라"였고, 선택지 제시 → 결정 → 설계 →
계획 순으로 진행했다.** 사용자가 A1+A2 묶음을 골랐고, 세션 중간에
**"전체 진척이 안 보인다"**는 지적이 나와 WBS를 신설했다.

### 사용자가 명시한 것

- **전체 작업 항목을 리스트업하고 완료 여부를 체크하며 진행할 것.** 단위 작업은
  따라갈 수 있으나 전체 중 어디쯤인지, 얼마나 더 하면 끝나는지 알 수 없다는 것이
  이유였다. → `docs/WBS.md`(세션 간) + 세션 내 태스크 목록(실시간) 두 층으로 대응했다
- **푸시는 명시적 요청이 있을 때만.** 커밋과 푸시를 한 명령에 묶지 않는다
- 커밋 메시지는 한글
- **콘솔 응답에는 ASCII 도식, `.md` 파일에는 Mermaid** — 터미널은 Mermaid를 렌더하지 못한다
- **서브에이전트로 나눌 수 있는 작업은 기본적으로 나눈다**(글로벌 규칙)
- 선택지와 근거를 먼저 제시하고 결정을 받는 방식이 잘 맞는다 — 이번 세션에 4번의
  결정 요청이 있었고 전부 즉시 답이 왔다

### 다음 세션이 세션 내 추적을 이어가려면

세션 내 태스크 목록은 **세션이 끝나면 사라진다.** 다음 세션 착수 시
계획 문서의 태스크 6개를 태스크 목록으로 다시 만들고, 완료할 때마다
체크하면서 진행한다. 그리고 **작업이 끝나면 `docs/WBS.md`의 WP3 상태를
✅로 바꾼다**(계획 Task 5 Step 4).

### 실행 방식 제안 (계획에 적혀 있음)

| 태스크 | 분할 |
| --- | --- |
| 0 · 2 · 4 · 5 | 컨트롤러 (문서·측정) |
| 1 · 3 | 병렬 구현자 2명 (파일이 겹치지 않는다) |
| 리뷰 | **3축** — 산식 정합성 · 기존 계약 파괴 · 측정 무결성 |

두 태스크 합쳐 30줄이라 병렬 이득이 작다. **커밋 순서가 헷갈릴 것 같으면 순차로 돌린다.**

**리뷰어에게 금지할 것**: 코드 수정 · `git stash` · 기본 `--out-dir`로 벤치 실행.
**셋 다 직전 세션에서 실제로 일어난 사고다.**

## Files Modified This Session

```text
docs/superpowers/specs/2026-07-29-noisy-or-fusion-nfkc-design.md   신규 (340줄)
docs/superpowers/plans/2026-07-29-noisy-or-fusion-nfkc.md          신규 (821줄)
docs/WBS.md                                                        신규 (138줄)
CLAUDE.md                                                          문서 지도에 WBS 등록
CHANGELOG.md                                                       이번 세션 3항목
HANDOFF.md                                                         전면 갱신
```

**제품 코드와 테스트는 하나도 바뀌지 않았다.** `git diff 61e99b9..HEAD --stat -- src tests`가
0건임을 확인했다.
