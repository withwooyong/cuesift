# Session Handoff

> Last updated: 2026-07-28 (KST)
> Branch: `main`
> Latest commit: `11dbfe4` — 문서: AI 오픈소스 비교 §5를 요구사항 결정에 동기화

## Current Status

**문서 체계를 정리한 세션이다. 코드·CI 변경은 없다.**
최상위에 흩어져 있던 마크다운을 `docs/`로 모으고, 세 문서에 걸친 용어 정의를 단일 출처로 통일했으며,
조사 문서에 남아 있던 열린 질문을 요구사항정의서의 결정에 맞췄다.

CI 3잡(`test 3.11`·`test 3.12`·`docs`)이 통과 상태이며, 작업트리는 깨끗하다.
CLI는 여전히 **스텁 단계**로 모든 서브커맨드가 종료 코드 `70`(미구현)을 반환한다 — 이 세션에서 바뀌지 않았다.

저장소: <https://github.com/withwooyong/cuesift> (Public)

## Completed This Session

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | 조사 문서 3종을 `docs/`로 이동 | `5ffda8d` | 문서 3개 이동, `README.md`·`HANDOFF.md` 링크 |
| 2 | 용어 풀이 §0 신설 2건 + 색상 이름 표기 + 문서 버전 0.4 | `cc8f109` | `docs/` 2개, `HANDOFF.md`, `CHANGELOG.md` |
| 3 | AI 문서 §5 조사 질문 6개를 결정 사항에 동기화 | `11dbfe4` | `docs/AI_자막검수_오픈소스_비교.md`, `CHANGELOG.md` |
| 4 | CHANGELOG 날짜 섹션 확정 · HANDOFF 갱신 | (이 커밋) | `CHANGELOG.md`, `HANDOFF.md` |

## In Progress / Pending

| # | Task | Status | Notes |
|---|------|--------|-------|
| 1 | **Q4 + AI 문서 §5 #2 — 통합 측정 과제** | 차단됨 | 두 문서에 따로 적혀 있던 질문이 같은 실험 하나로 닫힌다는 것이 이번 세션에 확인됐다. "자가일관성 유사도를 편집거리(무료)로 잴 수 있는가"(Q4)와 "xCOMET 없이 경량 신호만으로 트리아지가 성립하는가"(#2)는 같은 측정에서 동시에 답이 나온다 |
| 2 | TED2020에서 ko-en·ko-ja 병렬 코퍼스 확보 | 미착수 | **위 1번의 선행 조건.** Q2가 ko→en/ja로 범위를 확정해 대상이 정해져 있다 |
| 3 | 측정 하네스 작성 — 합성 오류 주입 + Recall@Budget | 미착수 | 방법은 요구사항정의서 §9.1·§9.2에 확정돼 있다. 오류 유형 5종(숫자 변조·용어 위반·의미 반전·규격 초과·미번역) |
| 4 | `specs/ko.yaml`·`en.yaml`·`ja.yaml`·`ted.yaml` 생성 | 미착수 | **조사 불필요.** §8.3.1에 값과 출처가 확정돼 있어 옮기기만 하면 됨 |
| 5 | CLI 스텁 3종(`translate`·`check`·`transcribe`) 구현 | 미착수 | 인자 스키마와 종료 코드 계약은 테스트 6건이 고정하고 있음 |

## Key Decisions Made

- **용어 정의는 단일 출처로 둔다.** 공통 용어(QE·MQM·CPS·화자분리·자막 포맷)의 상세는 `AI_자막검수_오픈소스_비교.md` §0에만 두고, `요구사항정의서.md` §0.5는 한 줄 요약 + 포인터만 갖는다. 같은 용어를 두 문서에서 각각 정의하면 한쪽만 고쳐져 반드시 갈라진다.
- **용어 항목에 근거 FR·Q 번호를 병기한다.** `hard fail (FR-6.2)`, `char_counting (FR-5.2, Q5)` 식. 요구사항이 바뀌면 어느 용어를 손봐야 하는지 문자열 검색으로 드러난다. 뜻만 적힌 용어집은 스펙이 움직일 때 반드시 뒤처진다.
- **`README.md`·`CHANGELOG.md`·`HANDOFF.md`는 루트에 남긴다.** README는 `pyproject.toml`의 `readme` 필드와 GitHub 랜딩이 참조한다. 나머지 둘은 루트에서 찾는 관례를 따른다.
- **상호 참조하는 문서는 함께 옮긴다.** 조사 문서 3종은 서로를 형제 상대경로로 참조하므로 통째로 내리면 링크가 유지되고, 바깥에서 들어오는 링크만 고치면 된다.
- **상태 기호를 재사용하지 않는다.** §5 조사 질문의 상태는 ✅⏸🔬⚪로 표기했다. 문서 최상단이 🟢·🟡을 **검증 수준**에 이미 묶어 뒀으므로 같은 색 동그라미를 쓰면 한 문서 안에서 기호가 이중 의미를 갖는다.
- **"조사로 닫히는 질문"과 "측정으로 닫히는 질문"을 섞지 않는다.** §5의 6개 목록에서 유일하게 착수 가능한 #2가 나머지 5개에 묻혀 계속 미뤄지고 있었다.

### 이전 세션에서 이어지는 결정 (변경 없음)

- 초기 대상 언어쌍 **ko→en/ja** (Q2) · 로컬 LLM은 **OpenAI 호환 엔드포인트로 일원화**(Q3) · 규격 프로파일 1차 출처는 **Netflix Timed Text Style Guide**(Q5) · LLM 연동은 **자체 얇은 어댑터**(Q6) · 벤치마크용 `specs/ted.yaml` 분리.

## Known Issues

- **`docs/번역관리_TMS_솔루션_비교.md:5`의 `글로벌_OTT_플랫폼.md` 링크가 깨져 있다.** 해당 파일은 이 저장소에 존재한 적이 없다(외부 문서 참조). **이번 세션의 `docs/` 이동과 무관한 기존 문제**이며 손대지 않았다. 링크를 지울지, 문서를 들여올지 결정 필요.
- **`docs/요구사항정의서.md`에 개정일 필드가 없다.** 버전만 0.4로 올렸고 `작성일: 2026-07-27`은 그대로다. 버전이 언제 올랐는지는 CHANGELOG로만 추적된다.
- **markdownlint 링크 검사는 하지 않는다.** 깨진 상대 링크는 CI가 잡지 못한다. 이번 세션에는 임시 Python 스크립트로 18개를 수동 검증했다. 상시 검사가 필요하면 `markdown-link-check` 계열 잡 추가를 검토할 것.

### 이전 세션에서 이어지는 이슈 (여전히 유효)

- **`logprobs`를 위험 신호로 쓰면 백엔드에 따라 조용히 사라진다.** Ollama 미지원, vLLM 지원. 능력 탐지와 리포트 명시가 구현될 때까지 이 신호를 도입하면 안 된다.
- **자가일관성 샘플링은 `n>1` 단일 호출로 구현하면 안 된다.** Ollama가 `n`을 지원하지 않으므로 N회 개별 호출이어야 이식성이 유지된다.
- **로컬 Python이 3.14.6이라 STT·QE 확장 설치가 막힐 수 있다.** `[stt]`·`[qe]` 작업 시 3.11 또는 3.12 가상환경을 별도로 만들 것.

## Context for Next Session

**사용자의 원래 의도** — 이 세션은 "최상위 md 파일을 폴더로 옮겨 달라"에서 시작해, 문서를 실제로 읽으며 발견한 결함을 차례로 메우는 흐름으로 진행됐다. 즉 요청은 파일 정리였지만 실질은 **문서 정합성 복구**였다.

**드러난 실패 유형 — 다음 세션에서도 경계할 것**

이 세션에서 발견한 문제 3건이 전부 같은 뿌리였다. **결정은 한 문서에만 기록되고 다른 문서는 조사 시점에 멈춰 있었다.**

1. AI 문서 §5가 "저자원 언어 성능이 최우선 검증 대상"이라고 주장 → Q2가 범위를 ko→en/ja로 좁힌 뒤라 틀린 말
2. §5 #3(PySubtrans)이 열린 질문으로 남아 있음 → Q6에서 이미 닫힘
3. §5 #4·#5가 조사 대상으로 남아 있음 → `FR-1.6`·`FR-5.5`가 v0.3이라 아직 시점 아님

**요구사항정의서 §12에서 Q가 닫히면, 조사 문서 §5도 함께 봐야 한다.** 두 문서가 서로를 링크하고 있으므로 한쪽만 갱신하면 독자는 상반된 두 주장을 만난다. 이번에 §5 각 항목에 근거 Q·FR 번호를 달아 뒀으니 다음부터는 역추적이 가능하다.

**다음에 할 일은 조사가 아니라 측정이다.** §5에 남은 유일한 질문(#2)은 남의 리포지토리를 확인해서 답이 나오지 않는다. TED2020 코퍼스를 받아 합성 오류를 주입하고 Recall@Budget을 실측해야 한다. 이 측정 하나가 Q4와 #2를 동시에 닫고, 동시에 README 최상단에 들어갈 "무작위 베이스라인 대비 배수"라는 숫자를 만든다 — 즉 **"AI 래퍼가 아니다"를 증명하는 첫 산출물**이다.

**사용자가 표현한 제약과 선호 (이전 세션에서 이어짐, 이번에도 유지됨)**

- **푸시는 명시적 요청이 있을 때만.** 커밋과 푸시를 한 명령에 묶지 않는다.
- 커밋 메시지는 한글로 작성한다.
- 범위를 넘는 변경은 임의로 하지 않는다 — 문서 버전 0.4 상향, `글로벌_OTT_플랫폼.md` 깨진 링크는 각각 확인을 받거나 이슈로 남겼다.

**검증 습관 (이전 세션의 교훈, 이번에도 적용됨)** — "통과했나"가 아니라 **"무엇을 대상으로 통과했나"** 를 본다. 이번 세션에도 markdownlint 실행마다 `Linting: 6 files`를 확인했고, 링크는 별도 스크립트로 개수(`OK=18`)를 세어 확인했다. 특히 `cuesift check` CI 게이트를 구현할 때 이 기준을 그대로 적용할 것 — **검사하지 않고 통과하는 게이트는 없는 게이트보다 나쁘다.**

## Files Modified This Session

```text
CHANGELOG.md                        2026-07-28 섹션 추가, Unreleased 비움
HANDOFF.md                          전면 갱신
README.md                           문서 링크 4곳에 docs/ 접두어
docs/AI_자막검수_오픈소스_비교.md    §0 용어 풀이 신설 · 최상단 색상 이름 · §5 전면 동기화
docs/요구사항정의서.md               §0 용어 풀이 신설 · 문서 버전 0.4
docs/번역관리_TMS_솔루션_비교.md     이동만 (내용 변경 없음)
```

코드·테스트·CI 설정(`src/`·`tests/`·`.github/`·`pyproject.toml`)은 **변경 없음**.
