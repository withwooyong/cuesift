# Session Handoff

> Last updated: 2026-07-29 (KST)
> Branch: `main` (작업 트리 깨끗, stash 없음)
> Latest commit: 아래 "Files Modified" 참고
> **다음 세션은 WP4(인제스트)부터 시작한다** — WP3이 완료됐다

## Current Status

**Tier 0 융합을 noisy-or로 바꾸고 숫자 검출 결함을 고쳐 재측정했다.**
README 최상단 배수가 갱신됐다.

| 예산 10% | en-ko | ja-ko |
| --- | --- | --- |
| 실제 검수 비율 | 10.00% | 10.00% |
| Recall | 69.60% → **72.40%** | 70.60% → **75.20%** |
| **무작위 대비 배수** | 6.96x → **7.24x** | 7.06x → **7.52x** |

제품 상태는 **CLI가 여전히 배선되지 않았다**(모든 서브커맨드가 종료 코드 `70`).
자막 파일 파싱(`ingest`)과 번역(`translate`) 계층이 없다.

테스트 **277 → 282건**. 저장소: <https://github.com/withwooyong/cuesift> (Public).
로컬이 `origin/main`보다 앞서 있다 — 푸시는 명시적 요청이 있을 때만.

## 이번 세션에 한 일

```text
  411b14d  설계 스펙      noisy-or 융합 + 숫자 NFKC 정규화
  a3d234a  WBS           v0.1까지 남은 작업 전체 분해
  c746098  구현 계획      태스크 6개 · 리뷰 3축
  0c91987  인수인계
  939f560  Task 0        스펙 §5 표 정정 (w=0은 두 산식이 일치)
  2314fc6  Task 1        A2 — _numbers() NFKC 정규화
  ────────  Task 2        A2 단독 측정 (커밋 없음, --out-dir data/bench/tmp)
  78921d4  Task 3        A1 — noisy-or 융합
  1c6473f  Task 4a       리포트 원인 목록을 실제 잔여 한계로 교체
  57bbded  Task 4b       재측정 결과 (코드 커밋과 분리)
```

계획을 **Task 0부터 5까지 순서대로 완주했고 fix 루프가 0회였다.**
계획에 테스트 코드와 기대값이 실측으로 들어 있어 구현이 기계적이었다.

## 다음 세션에 할 일

**WP4 — 인제스트**(`docs/WBS.md` 참고). FR-1.1·1.3·1.5.

WP4가 **병목이다** — 출력·번역·STT 셋이 전부 여기 걸린다.
그리고 WP4 다음의 `check` 서브커맨드(FR-8.2)가 **번역·STT 없이 완결되는 유일한 경로**라,
v0.1 전체를 기다리지 않고 실사용 가능한 도구를 낼 수 있다.

착수하려면 **브레인스토밍 → 스펙 → 계획** 순서를 다시 밟아야 한다.
이번 세션이 그 순서로 결함 셋을 구현 전에 잡았다(아래 참고).

## 전체 진척 — `docs/WBS.md`

**"얼마나 더 하면 끝나는가"에 답하는 문서를 처음 만들었다.**

```text
  v0.1 대상 FR 42개 중 16개 완료 (38%)

  WP1 Tier 0 신호 엔진   ✅   WP4 인제스트  ⬜   WP7 번역 계층  ⬜
  WP2 벤치마크 하네스     ✅   WP5 출력      ⬜   WP8 Tier 1     ⬜
  WP3 융합·검출 정정      ✅   WP6 CLI 배선  ⬜   WP9 STT        ⬜
```

WP3은 기존 FR(6.1·3.4)의 **정정**이라 완료 개수는 16/42 그대로다.

의존 그래프가 두 가지를 드러냈다.

| 발견 | 뜻 |
| --- | --- |
| **WP4(인제스트)가 병목** | 출력·번역·STT 셋이 전부 여기 걸린다. 자막 파일을 `Segment`로 바꾸지 못하면 Tier 0 엔진에 실데이터가 들어가지 않는다 |
| **`check`는 번역 없이 완결된다** | FR-8.2는 규격 검사만 한다. 인제스트 + 배선만으로 **쓸 수 있는 제품**이 나오며, v0.1 전체를 기다리지 않고 중간 산출물을 낼 수 있는 유일한 지점이다 |

권장 순서: ~~WP3~~ → **WP4** → `check` 배선 → WP7 → WP8 → WP5·WP6 → WP9.

**WBS는 요구사항정의서의 파생물이지 병렬 출처가 아니다.** FR이 WBS에서만 바뀌면 갈라진다.

## Key Decisions Made

- **noisy-or 채택을 재측정 결과에 걸지 않는다.** "증거가 늘면 위험도가 내려간다"는 것은
  지표 이전에 논리적 결함이다. 벤치 숫자를 보고 융합식을 고르면 그것이 곧 **벤치
  데이터로 제품을 튜닝하는 것**이고, `DEFAULT_WEIGHTS`를 전부 1.0으로 둔 이유와
  같은 함정이다. 재측정의 목적은 판정이 아니라 기록이며, **Recall이 내려가도 그
  숫자를 README에 싣는다.**
- **가중치는 지수로 넣는다** — `1 - ∏(1 - sᵢ)^wᵢ`. 점수 스케일 가중(`1 - ∏(1 - wᵢ·sᵢ)`)은
  `wᵢ·sᵢ > 1`에서 `(1 - wᵢ·sᵢ)`가 음수가 되어 곱의 부호가 뒤집힌다. `w ≤ 1` clamp를
  강제해야 하고 그러면 "신호를 강화한다"를 표현할 수 없다.
  `w=0`이 "관측하지 않음"이 되어 **ablation의 신호 끄기와 의미가 일치**하는 것도 지수 쪽이다.
- **NFKC는 추출된 숫자 문자열에만.** 텍스트 전체를 정규화하면 `½`(카테고리 `No`)가
  `1⁄2`가 되어 **원문에 없던 숫자 1과 2가 생긴다.** `\d`는 `Nd`만 잡으므로 추출 후
  정규화는 그 경로를 열지 않는다. 변경 지점이 하나면 재측정 결과의 원인 후보도 하나다.
- **`--fusion` 플래그를 만들지 않는다.** A/B는 두 커밋에서 각각 측정하는 것으로 대신한다.
  쓰이지 않을 설정 경로를 제품에 남기지 않는다.
- **2단계로 측정한다.** A2와 A1의 효과가 반대 방향일 수 있다(A2는 hard fail을 줄여
  예산에 여유를 만들고, A1은 그 여유를 채우는 순서를 바꾼다). 1회 측정은 상쇄되면
  "변화 없음"으로 읽힌다.
- **WBS의 작업 패키지를 FR 번호에 묶는다.** 진척 주장을 반증 가능하게 만들기 위해서다.
  "인제스트 60% 완료"는 아무도 반박할 수 없지만 "FR-1.1 완료"는 코드와 테스트로 확인된다.
- **리포트의 서술을 지울 때는 대체물을 찾아 넣는다.** NFKC 수정으로 "검출기가 전각
  숫자를 다른 값으로 본다"가 거짓이 됐지만, 지우기만 하면 **오탐은 여전히 남는데
  설명이 없는 절**이 된다. 한자 수사로 갈아 끼워 독자가 남은 오탐을 귀속할 수 있게 했다.
- **회귀 방지선은 사실이 아니라 계약을 고정한다.** 리포트 테스트가 `"검출기의 알려진
  한계"`를 검사했기에 한계의 정체가 NFKC → 한자 수사로 바뀌어도 한 줄도 안 고쳤다.
  `assert "NFKC" in md`였다면 **수정과 함께 실패해 개선을 방해했을 것이다.**

### 이전 세션에서 이어지는 결정 (변경 없음)

초기 언어쌍 **ko→en/ja**(Q2) · 로컬 LLM은 **OpenAI 호환 엔드포인트로 일원화**(Q3) ·
규격 1차 출처는 **Netflix TTSG**(Q5) · LLM 연동은 **자체 얇은 어댑터**(Q6) ·
배수 헤드라인은 **예산 10%**에서 뽑는다 · **측정을 끝낸 뒤 제품 코드를 고치지 않는다**.

## 이번 세션이 문서에서 찾아낸 결함

코드를 안 고쳤는데도 **셋을 잡았다.** 전부 "실제로 확인해 보는" 과정에서 나왔다.

| # | 결함 | 어떻게 드러났나 |
| --- | --- | --- |
| 1 | 새 스펙이 **링크 검사를 받지 않았다** | 링크 체커 12개 vs markdownlint 13개. `git ls-files` 기준이라 미추적 파일이 빠진다 |
| 2 | 스펙 §5의 `w=0` 행이 **틀렸다** | 계획을 쓰며 값을 실제로 계산. 두 산식이 일치하는 지점이라 PASS다 |
| 3 | 스펙이 **교체해야 할 기존 테스트를 놓쳤다** | `test_weighted_average_of_multiple_signals`·`test_weights_shift_the_result`가 가중 평균을 값으로 고정하고 있다 |

1번이 이 프로젝트의 규율이 실제로 작동한 사례다 — **`깨진 링크 없음`만 읽었다면
검사받지 않은 파일을 "통과"로 오해했을 것이다.** 두 게이트가 다른 방식으로 파일을
수집(`git ls-files` vs glob)하기 때문에 그 차이가 신호가 됐다.

2·3번은 **계획을 쓰는 행위 자체가 스펙의 검증**이라는 것을 보여준다. 스펙은
"이 테스트는 실패할 것"이라고 적었지만 계획은 실제 값을 적어야 하므로 계산을 강제한다.

### 구현·측정 단계가 추가로 잡은 것 셋

| # | 발견 | 어떻게 |
| --- | --- | --- |
| 4 | **직전 판의 "13건(31.7%)"이 과대평가** | 수정 후 재분류. 실제 8건(18.2%) |
| 5 | **오탐이 우연히 진짜 오류를 잡고 있었다** | ja-ko 낮은 예산 Recall이 1건 떨어져 추적 |
| 6 | **스펙 §7.1의 예측이 빗나갔다** | `length.ratio`가 악화가 아니라 완화 |

5번이 가장 값진 것이다. 잃은 1건은 `glossary` 주입 오류인데 `160년` ↔ `１６０年`은
**같은 숫자다** — 숫자는 애초에 누락되지 않았고 spurious hard fail이 다른 유형의
오류를 우연히 잡고 있었다. **이전 50.00%는 검출기가 옳아서 나온 숫자가 아니었다.**

6번은 예측을 문서에 남겨 뒀기에 **빗나갔다는 것을 알 수 있었다.** 적어 두지 않았으면
"원래 그럴 줄 알았다"가 됐을 것이다. 이유도 밝혀졌다 — 가중 평균에서는 오탐이 진짜
신호를 **희석해 끌어내렸고**, noisy-or에서는 **자리만 차지한다.** 후자가 덜 해롭다.

## Known Issues

### ~~`struct.number_missing`이 전각 숫자를 놓친다~~ — **해결** (`2314fc6`)

`_numbers()`가 추출 결과를 NFKC 정규화한다. ja-ko hard fail 오탐률 0.911% → 0.778%.

**직전 판의 "13건(31.7%)"은 과대평가였다.** 수정 후 재분류에서 실제는 **8건**이다
(발화 44건 기준 18.2%). 전각 숫자를 포함한 세그먼트는 16건이지만 그중 8건은
전각과 무관한 단위 환산(`80마일` → `1３0キロ`)·표기 체계(`30%` → `３割`)였다.
**"전각 숫자가 있다"와 "전각 때문에 오탐이다"는 다르다.**

한자 수사(`十代`)는 **여전히 미탐이다** — 정규화로 해결되지 않고, 파서를 붙이면
`十分に`(≠ 10분)·`万一`(≠ 10001)에서 새 오탐이 생긴다. 리포트 원인 목록에
명시했다(아래 항목이 이것으로 닫혔다).

### ~~가중 평균 융합의 구조적 문제~~ — **해결** (`78921d4`)

noisy-or(`1 - ∏(1 - sᵢ)^wᵢ`)로 교체했다. 예산 10% Recall이 en-ko 69.60% → 72.40%,
ja-ko 70.60% → 75.20%로 올랐다.

### `length.ratio`가 여전히 해롭다 — **별도 과제**

ablation에서 두 언어쌍 모두 **음수다**(끄면 Recall이 오른다).

| | 기준 | +A2 | +A1 (현재) |
| --- | --- | --- | --- |
| en-ko | −0.0600 | −0.0600 | **−0.0560** |
| ja-ko | −0.0600 | −0.0880 | **−0.0680** |

**스펙 §7.1의 예측은 빗나갔다** — "noisy-or에서 악화될 가능성이 실재한다"고 적었으나
오히려 완화됐다. 가중 평균에서는 오탐이 진짜 신호를 **희석해 끌어내렸고**,
noisy-or에서는 **자리만 차지한다.**

그래도 음수다. 답은 융합식이 아니라 `length.ratio` 자체를 고치는 것이다 —
짧은 원문에서의 계통 오탐은 한 번 수정됐고(`3718e3a`) 남은 음수는 그 수정이
불완전하다는 신호다. **가중치를 낮추는 것은 답이 아니다**(벤치 데이터로 튜닝하는 것).

### `spec.overlap`은 여전히 판정 불가

계획 A 재리뷰가 이 신호의 캐스케이드(단일 타임코드 오타가 트랙 50~97%를 flag)를
발견하고 "벤치 하네스의 ablation으로 A/B 판정할 것"이라 남겼으나, **`build_track`이
겹침 0건으로 트랙을 합성하고 주입기의 `spec` 유형은 duration을 줄이므로 겹침을
만들지 않는다.** 기여도 `+0.0%`는 "쓸모없다"가 아니라 "측정하지 못했다"이다.

판정하려면 **겹침이 있는 실제 자막 트랙**이 필요하다 — WP4(인제스트) 이후에 가능해진다.

### ~~한자 수사가 리포트 원인 목록에 없다~~ — **해결** (`1c6473f`)

NFKC 수정으로 원인 구성이 바뀌면서 목록을 재작성했다. 이제 한자 수사가
**검출기의 알려진 한계**로 명시돼 있고, 해소된 전각·반각 차이는 그렇게 표기된다.

**리포트 테스트는 한 줄도 고칠 필요가 없었다.** 직전 세션의 I-2 수정이
`assert "billion" in md`를 값 연동으로 바꾸면서 `"검출기의 알려진 한계"`라는
**계약**만 고정해 뒀기 때문이다. `assert "NFKC" in md`로 고정했다면 이 수정과
함께 실패했을 것이다 — **회귀 방지선은 사실이 아니라 계약을 고정해야 한다.**

### 이전 세션에서 이어지는 이슈 (여전히 유효)

- **외부 URL은 아무도 검사하지 않는다.** 링크 체커는 내부 상대 링크만 본다.
  착수 시점에 사람이 재확인해야 한다
- `scripts/check_links.py`는 **앵커를 검사하지 않는다.** 절 번호를 바꾸면 앵커가 조용히 깨진다
- **링크 체커는 `git ls-files` 기준이라 미추적 파일을 건너뛴다.** 새 문서를 만들면
  `git add` 후에 돌려야 검사된다 — 두 게이트의 파일 개수를 대조하는 것이 유일한 탐지 수단
- `logprobs`는 백엔드에 따라 조용히 사라진다(Ollama 미지원)
- 자가일관성 샘플링은 `n>1` 단일 호출이 아니라 **N회 개별 호출**이어야 이식성이 유지된다
- **로컬 venv가 Python 3.14인데 CI는 3.11/3.12를 검증한다**

## Context for Next Session

**이번 세션의 요청은 "다음 작업을 알려 달라"였고, 선택지 제시 → 결정 → 설계 →
계획 → 구현 → 측정 순으로 완주했다.** 사용자가 A1+A2 묶음을 골랐고, 세션 중간에
**"전체 진척이 안 보인다"**는 지적이 나와 WBS를 신설했으며,
그 뒤 푸시와 함께 계획 실행을 지시했다.

### 사용자가 명시한 것

- **전체 작업 항목을 리스트업하고 완료 여부를 체크하며 진행할 것.** 단위 작업은
  따라갈 수 있으나 전체 중 어디쯤인지, 얼마나 더 하면 끝나는지 알 수 없다는 것이
  이유였다. → `docs/WBS.md`(세션 간) + 세션 내 태스크 목록(실시간) 두 층으로 대응했다
- **푸시는 명시적 요청이 있을 때만.** 커밋과 푸시를 한 명령에 묶지 않는다
- 커밋 메시지는 한글
- **콘솔 응답에는 ASCII 도식, `.md` 파일에는 Mermaid** — 터미널은 Mermaid를 렌더하지 못한다
- **서브에이전트로 나눌 수 있는 작업은 기본적으로 나눈다**(글로벌 규칙)
- 선택지와 근거를 먼저 제시하고 결정을 받는 방식이 잘 맞는다 — 이번 세션에 4번의
  결정 요청이 있었고 전부 즉시 답이 왔다

### 세션 내 추적을 이어가려면

세션 내 태스크 목록은 **세션이 끝나면 사라진다.** 다음 세션 착수 시
WP4의 태스크를 목록으로 만들고 완료할 때마다 체크하면서 진행한다.
그리고 **작업이 끝나면 `docs/WBS.md`의 상태 칸을 근거 커밋과 함께 고친다.**

이번 세션에 두 층이 실제로 작동했다 — 태스크 목록이 "지금 6개 중 4번째"를,
WBS가 "전체 9개 중 3개 완료"를 답했다.

### 실행 방식 — 이번엔 나누지 않았다

계획은 Task 1·3을 병렬 구현자 2명으로 제안했으나 **컨트롤러가 직접 구현했다.**

| 판단 | 근거 |
| --- | --- |
| 나누지 않음 | 두 태스크 합쳐 30줄, 각각 한 파일 안의 작은 수정 |
| 계획에 코드가 있음 | 구현이 기계적이라 서브에이전트가 깨뜨릴 맹점이 적다 |
| **커밋 순서 제약** | Task 3이 Task 2보다 먼저 커밋되면 A/B가 무너진다 |

**결과적으로 맞는 판단이었다** — fix 루프 0회. 다만 **독립 리뷰를 받지 않았다**는
것은 남는 위험이다. WP4(인제스트)는 파서 3종이라 규모가 다르므로 **분할과 리뷰
3축을 다시 적용해야 한다.**

**리뷰어에게 금지할 것**: 코드 수정 · `git stash` · 기본 `--out-dir`로 벤치 실행.
**셋 다 이전 세션에서 실제로 일어난 사고다.**

## Files Modified This Session

```text
docs/superpowers/specs/2026-07-29-noisy-or-fusion-nfkc-design.md   신규 · §5 정정
docs/superpowers/plans/2026-07-29-noisy-or-fusion-nfkc.md          신규 (821줄)
docs/WBS.md                                                        신규 · WP3 완료 반영
CLAUDE.md                                                          문서 지도에 WBS 등록

src/cuesift/signals/structural.py    A2 — _numbers() NFKC 정규화
src/cuesift/risk/fuse.py             A1 — noisy-or 산식
bench/report.py                      hard fail 오탐 원인 목록 교체
tests/test_signals_structural.py     ctx_ja 픽스처 + 2건
tests/test_risk_fuse.py              2건 교체 + 3건 추가
tests/test_bench_report.py           독스트링·주석 정정 (단언은 그대로)
bench/results/{en-ko,ja-ko}-2026-07-29.{md,json}   재측정

README.md · CHANGELOG.md · HANDOFF.md              숫자·귀속 표·상태
docs/요구사항정의서.md                              §12 Q4 근거 강화
docs/AI_자막검수_오픈소스_비교.md                    §5 #2 배수 갱신
```

테스트 **277 → 282건**. `data/` 아래는 하나도 커밋되지 않았다.
