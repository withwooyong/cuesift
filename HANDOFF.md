# Session Handoff

> Last updated: 2026-08-29 (KST)
> **브랜치 `fix/failure-diagnostics`에 번역 실패 진단 구현이 끝나 있다. 게이트 전부 통과.**
> 커밋 수는 `git rev-list --count main..HEAD`로 센다 — 여기 숫자를 적으면
> 그 문장을 고치는 커밋이 자기 자신을 틀리게 만든다(실측으로 한 번 겪었다).
> **브랜치가 `origin`에 있는지, PR이 있는지, CI가 돌았는지는 아래 "배포 절차"의 명령으로 직접 재라.**
> 값이 아니라 명령이다.
> 진척은 [WBS](docs/WBS.md), FR 번호의 출처는 [요구사항정의서](docs/요구사항정의서.md)다

## Current Status

**파킹 #1·#2·#3(번역 실패 사유 미출력 덩어리)이 닫혔다.** FR 개수는 움직이지 않는다 —
FR-2.6은 이미 ✅였고 이 작업은 그 FR이 만든 데이터를 화면과 종료 코드로 **내보내는** 일이었다.

| 단계 | 상태 | 산출물 |
| --- | --- | --- |
| 착수 조사 | ✅ | 파킹 노트 **두 줄을 정정**했다(아래 ⓐ) |
| [구현 계획](docs/superpowers/plans/2026-08-29-failure-diagnostics.md) | ✅ 커밋 `0787397` | 태스크 4개 · 게이트 수치 고정 |
| 구현 (T1~T3) | ✅ | 종료 코드 분리 · 사유 렌더링 · dry-run 진단 |
| 이중 리뷰 (품질 축 · **계약 축**) | ✅ 2라운드 | CRITICAL 1 · HIGH 1 · MEDIUM 5 · LOW 5 → 전부 해소 |
| 검증 5관문 | ✅ | 아래 "게이트 실행 기록" |
| 문서 (T4) | ✅ | CHANGELOG · 이 파일 |
| 푸시 · PR · CI | ⬜ **아래 "배포 절차"로 직접 재라** | — |

**`main` 머지는 사용자 승인 항목이다.**

## 무엇이 바뀌었나

| 부분 | 무엇 |
| --- | --- |
| 종료 코드 | `translate`의 번역 실패가 **`1` → `3`**(`EXIT_TRANSLATION_FAILURE`). `check`의 1(규격 위반)과 갈라졌다. **⚠ 파괴적 변경** |
| 합산 규칙 | 대상 언어별 코드를 `max()`가 아니라 `_EXIT_PRIORITY`(**70 > 69 > 66 > 3**)로 합친다 |
| 화면 | 실패 세그먼트를 **사유별로 묶고** 시도 횟수를 붙인다. 이전에는 ID만 나열했다 |
| dry-run | `Tier 1 재번역 요청 최대 0회`에 **사유 줄**이 붙는다. 실행 경로와 문구를 공유한다 |

## 이번 세션이 배운 것

### ⓐ 파킹 노트가 조건을 좁게 적으면 다음 사람이 우선순위를 낮게 본다

착수 조사가 파킹 노트 세 줄 중 **둘을 정정했다.**

| # | 노트가 적은 것 | 실제 |
| --- | --- | --- |
| 1 | "번역 **전량** 실패가 `exit 1`" | `cli.py`의 `return 1 if translated.failures else 0` — **4000개 중 1개만 실패해도 1**이다 |
| 2 | "실패 사유 0줄" | 맞다. 다만 **데이터는 이미 있었다** — 새로 만들 것이 아니라 배선이었다 |
| 3 | "dry-run에 사유가 없다" | 맞되 부분적. 파라미터는 말하고 **왜 0인지**를 안 말한다 |

**그리고 노트가 "머리말과 충돌한다"고 적은 것은 틀렸다** — `cli.py` 머리말 표는 1이 두 뜻을
겸한다고 **이미 명시**하고 있었다. 진짜 문제는 다른 데 있었다: 그 표의 **근거 칸이 실제 요구를
과장했다.** FR-7.5는 `check`만 말하고(§514) FR-2.6은 종료 코드를 **한 글자도 언급하지 않는다**(§449).
**`translate`의 exit 1을 요구하는 FR은 없었다** — 구현 선택이 요구처럼 적혀 있었던 것이다.

### ⓑ "재시도하라"고 말하기 전에 재시도가 되는지 재 봐야 한다 — `EX_TEMPFAIL`

처음 고른 값은 **75**(`sysexits.h` `EX_TEMPFAIL`, "재시도하라")였고 근거는
*"실패 3종이 전부 재시도 여지가 있고 캐시가 성공분을 보존하므로 재실행이 싸다"*였다.
**계약 축 리뷰가 실측으로 무너뜨렸다.**

| 사유 | 근거가 주장한 것 | 실측 |
| --- | --- | --- |
| `invalid_response` | 재시도하면 될 수도 | **캐시가 깨진 응답을 보존**해 영원히 같은 실패 |
| `empty_translation` | 〃 | 〃 |
| `provider_error` | 캐시가 성공분 보존 | **캐시 디렉터리조차 안 생긴다**(응답이 없다) |

스텁 서버로 `invalid_response`를 만든 뒤 **서버를 죽이고** 같은 캐시로 재실행하니
`캐시 히트 3 · 실제 호출 0 · invalid_response 2건 · 종료 코드 동일`이 나왔다.
**CI가 이 코드를 "재시도"로 읽으면 API 호출 0회짜리 무한 루프가 된다.**

게다가 `engine.py:15`가 이미 반박하고 있었다 — *"`invalid_response`는 **모델을 바꿔야 하고**"*.
**표준 코드를 다른 뜻으로 쓰는 것은 전용 코드를 새로 파는 것보다 나쁘다.** `sysexits.h`에는
"산출물은 나왔는데 일부가 비었다"에 해당하는 값이 아예 없어서, 없는 것을 억지로 맞추다
거짓 약속을 하게 된 것이다. **`3`은 재시도 여부를 주장하지 않는다.**

### ⓒ 명시적 우선순위가 반증 불가능한 게이트가 될 뻔했다

`max()`를 `_EXIT_PRIORITY` 튜플로 바꾼 뒤 변이를 걸었더니 **죽는 테스트가 0건**이었다.
값이 `(70, 69, 66, 3)`이라 **값의 내림차순과 정확히 일치**해서, 등록된 넷만으로는
`max()`가 같은 함수가 됐기 때문이다.

**튜플을 지워도 아무 테스트가 안 죽는 상태**였고, 그러면 나중에 값 순서와 어긋나는 코드를
추가할 때 회귀가 조용히 들어온다. 둘을 가르는 유일한 입력인 **미등록 코드**로 테스트 2건을
세워 닫았다 — 프로덕션에서 도달 불가라는 것을 **주석에 밝히고** 남겼다
(`tier1.py:284`의 "측정할 수 없는 근거라고 밝힌 채 남긴다"와 같은 형태다).

### ⓓ 주석이 자기 실측과 모순한 것을 두 번 잡았다

둘 다 **수정 라운드에서 새로 쓴 주석**이 거짓이었다.

| 어디 | 적혀 있던 것 | 실측 |
| --- | --- | --- |
| `cli.py` 호출부 | "`max()`로 되돌리면 **75**가 69를 이겨…" | 75는 사라진 값이고, 3에서는 그 일이 **불가능**하다 |
| `test_cli_exit_codes.py` | "3은 값이 가장 작아 `max` 변이로 **세 쌍**이 함께 죽는다" | 죽는 줄은 **2개**뿐, 9개가 산다. 3이 최솟값이라 `max`와 답이 같다 |

두 번째는 **같은 파일의 다른 주석이 이미 정반대를 적고 있었다**. 고친 뒤 검증 단계에서
스크래치 사본에 변이를 다시 걸어 `max` 2줄 · 역순 6줄로 **주장이 참이 된 것**을 확인했다.

### ⓔ 축을 나눈 리뷰가 서로 다른 것을 잡았다

| 축 | 잡은 것 |
| --- | --- |
| 품질 | `_EXIT_PRIORITY`가 `70 > 69`를 뒤집은 것 · `세그먼트 수(N)`가 호출자마다 다른 수를 말하는 것 |
| 계약 | **`EX_TEMPFAIL`이 거짓인 것** · README에 남은 `exit 1` · 이름이 "partial"이라 거짓말하는 것 |

**계약 축 리뷰어가 자기 1라운드 누락도 신고했다** — 1차판 `_EXIT_PRIORITY`가 `(69, 70, 66, 75)`로
문서화된 계약을 조용히 뒤집고 있었는데, 역순 변이만 돌리고 **순서 자체가 옳은지는 안 봤다**고
적었다. 그 쌍은 품질 축이 잡았다. **한 명에게 다 맡겼으면 둘 중 하나는 못 봤다.**

## 확정된 결정

| 축 | 결정 | 근거 |
| --- | --- | --- |
| 종료 코드 값 | **3.** `sysexits.h`를 쓰지 않는다 | 해당하는 값이 없고, `EX_TEMPFAIL`은 실측상 거짓이다(ⓑ) |
| 이름 | `EXIT_TRANSLATION_FAILURE` | 조건이 `if translated.failures`라 **전량 실패도 걸린다** — "partial"은 거짓말이다 |
| 합산 | `_EXIT_PRIORITY = (70, 69, 66, 3)` | 70은 설정을 고쳐도 안 사라지고 69는 사라진다. 69는 조기 break를 걸어 70을 숨긴다 |
| 사유 정렬 | 개수 내림차순 · 같으면 이름순 | 삽입 순서를 쓰면 배치 스케줄에 따라 화면이 바뀐다(NFR-3) |
| dry-run 사유 | **하나만 낸다.** 실행 경로와 문구를 공유한다 | 나머지 넷은 채점된 `SegmentRisk`가 있어야 판정된다. 복제하면 한쪽만 고쳐져 갈라진다 |
| ADR | **만들지 않는다** | 이 저장소는 결정을 설계 스펙의 D 표와 CHANGELOG에 적어 왔다. `docs/ADR/`을 새로 열면 종료 코드 계약의 출처가 `cli.py` 머리말·CHANGELOG와 **셋으로 갈린다** |

## 배포 절차 — **"푸시했다"와 "CI가 돌았다"는 다르다**

```bash
git rev-list --count main..HEAD          # 이 브랜치의 커밋 수
git ls-remote --heads origin fix/failure-diagnostics   # 비면 아직 푸시 안 됨
gh pr list --head fix/failure-diagnostics              # 비면 PR 없음
gh pr checks --watch                                    # CI 통과 대기
```

**`main`에 직접 푸시하지 않는다.** CI의 `push` 트리거가 `branches: [main]`뿐이라
직접 푸시하면 머지된 **뒤에야** CI가 돈다 — 게이트가 아니라 사후 통보다.

## 게이트 실행 기록

| 게이트 | 이 세션 | 비고 |
| --- | --- | --- |
| `python -m compileall src tests` | 통과 | |
| `ruff check .` | **All checks passed!** | 대상은 `.` — `src tests`로 좁히지 않는다 |
| `ruff format --check .` | **114 files already formatted** | 신규 테스트 2개로 112 → 114 |
| `pytest --cov=cuesift` | **1571 passed · 3 deselected** · 커버리지 **99%**(2484문 중 31 미도달) | 착수 기준선 1547 → **+24** |
| 런타임 스모크 | 죽은 호스트 → **exit 3** · dry-run → exit 0 + 사유 줄 · `check` → exit 0 · `--help` → exit 0 | `check`가 3을 내지 않는 것까지 확인했다 |
| `python scripts/check_links.py` | 마크다운 **38개** · 상대 링크 **190개** · 깨진 링크 **0** | 계획서 추가로 37 → 38 |
| `npx markdownlint-cli2` | **38 files** · **0 issues** | 두 도구의 파일 수가 **일치**한다 |

**두 도구가 같은 파일 수를 세는지 본다.** 어긋나면 새 `.md`가 아직 `git add`되지 않은 것이고,
그 문서는 링크 검사를 **아예 받지 않는다**. 실측으로 32 vs 31로 갈린 전례가 있다.

**CI는 1건 적게 센다.** `data/`가 `.gitignore`에 있어 깨끗한 체크아웃에는 벤치 트랙이 없고
`tests/test_bench_glossary.py`가 1건을 skip한다 — CI의 기대값은
**1570 passed · 1 skipped · 3 deselected**다.

### 변이 증명

| 변이 | 사망 |
| --- | --- |
| `_combine_exit_codes` → `max(...)` | **2건** (미등록 코드가 낀 줄) |
| `_EXIT_PRIORITY` 역순 | **6건** (우선순위를 주장하는 줄) |
| `_format_failure_lines`가 헤더 줄만 | **4건** |
| `explain_zero_bound` → 항상 `None` | **2건** |
| dry-run 사유 블록 제거 / `if True:`로 무력화 | **각 1건** (양방향) |
| `_zero_by_floor`의 `noun`을 양쪽 통일 | **각 1건** (양방향) |
| `EXIT_TRANSLATION_FAILURE = 1` 회귀 | **2건** |

**앞의 둘이 서로 다른 줄을 죽인다** — 어느 하나만으로는 표를 다 지키지 못한다.

## 파킹된 finding

**#1·#2·#3이 닫혔다.** 셋이 한 덩어리("실패했는데 왜인지 안 말한다")였고 이 브랜치가 전부 냈다.

| # | 무엇 | 왜 지금 안 했나 | 다시 열 조건 |
| --- | --- | --- | --- |
| 4 | **`COLUMNS=88` 아래에서 옵션 이름이 잘린다** | rich의 표 렌더링이라 범위 밖이다. 대신 폭 88을 게이트로 못 박았다 | 옵션을 더 붙여 88이 깨지는 날 |
| 5 | 화면 토큰(번역만) vs `review.json` `cost`(합계)가 **다른 객체** | 화면에 합계 줄을 넣을지는 설계 결정이다 | 차이를 설명할지 없앨지 정하는 일 |
| 6 | `inf` 메시지가 원인을 틀리게 말한다 | 문구 문제, 작지만 별건 | 조합 검증은 **7규칙 / 8종 문자열**이다 |
| 7 | `cli.py`의 줄번호 인용이 조용히 낡을 수 있다 | 이 저장소 `src/`에 같은 형태가 **15건**이라 관행이다 | 관행 전체를 바꾸는 작업 |
| 8 | 종료 코드 69 단축이 **Tier 1 실패에도** 걸린다 | 단축은 `EXIT_UNAVAILABLE`에서만 걸리고 건너뛴 언어를 stderr로 명시한다 | 언어별로 다른 프로바이더를 쓸 수 있게 되는 날 |
| 9 | **`default_map` 값의 타입 변환에 게이트가 없다** | 게이트를 만들면 click 내부 의존이 한 겹 더 는다 | `Path` 전용 메서드를 쓰게 되는 날 |
| 11 | **`output.progress`에 스칼라가 아닌 YAML을 주면 트레이스백 + `exit 1`** | **기존 결함 부류의 세 번째 사례다** — `dry_run: [1]`·`signals.tier1.enabled: [1]`이 똑같이 행동한다 | 설정 파일 값 검증을 **부류 전체**로 손보는 작업 |
| 12 | **닫힌 파이프 + 실제 번역 + 진행 켬** 조합이 미검증 | `_TolerantOutput`이 한 층 아래에서 `EPIPE`를 삼켜 위험이 낮다 | 파이프 계약을 다시 손볼 때 |
| **13** | **캐시가 실패 응답을 보존한다 — 재실행이 같은 실패를 무료로 영구 반복한다** | `store/`는 이 작업의 대상이 아니고 이 브랜치가 만든 것도 아니다. **다만 종료 코드 값을 3으로 바꾼 근거가 바로 이것이다** | **다음 작업 후보 1순위.** 파킹 2번(권장 모델이 대량 실패)과 겹친다 |

### #13 재현 — 그대로 돌릴 수 있다

```text
① 스텁 LLM 서버를 세워 지시를 어긴 응답(200 + 잡문)을 내게 하고 `translate --cache-dir C`를 돌린다
   → exit 3 · invalid_response 2건 · 캐시 파일 3개 생성.
② 서버 프로세스를 죽인다(포트 응답 없음).
③ 같은 `--cache-dir C`로 재실행 → `캐시 히트 3 · 실제 호출 0 · invalid_response 2건 · exit 3`.
```

원인은 `src/cuesift/store/provider.py`의 `complete()`가 성공·실패를 가리지 않고
`_store_or_warn`으로 저장하는 것이다(미스 경로에 분기가 없다). `invalid_response`는 이 계층에서
정상 `Completion`이라 저장되고, `provider_error`는 예외로 먼저 빠져 저장되지 않는다.

**결과: 모델을 바꿔도 캐시를 지우기 전까지 같은 실패가 무료로 영구 재생된다.**

## 남은 관측 하나 — FR-8.5의 R3

설계 스펙 §9 R3(Windows 콘솔의 `\r`)은 **구조는 확인됐고 육안 관측이 남아 있다.**
`!`로 돌린 실행은 stderr가 파이프라 `plain` 경로를 탔다(그것대로 "비대화형에서 `\r`이 새지
않는다"는 계약은 확인됐다). 대화형 갱신은 **진짜 콘솔 창**이 있어야 한다.

```powershell
cd C:\Users\aeby\vscode\cuesift
.venv\Scripts\python.exe -m cuesift translate tests\fixtures\ingest\ten_cues.srt --to en --out . --base-url http://h/v1 --model m1 --progress
```

**확인하지 않은 것을 확인했다고 적지 않는다.**

## 승계 항목 — 이 브랜치가 건드리지 않았다

| 항목 | 상태 |
| --- | --- |
| **Q4**(자가일관성 유사도 측정 수단) | 여전히 열려 있다. 판정은 벤치마크에 Tier 1을 태우는 별도 작업의 몫 |
| **FR-4.2**(역번역) | 구현 안 함. 문자 단위 유사도로는 `llm.retranslation_gap`이 **역방향으로 작동**한다 |
| **FR-8.3**(`transcribe`) | STT 어댑터(WP9)가 선행이지만 **FR-8.3 자신은 WP6에 남는다** — §5.8("CLI") 소속이다. **그래서 WP6은 🟡다**(5개 중 4개) |
| **`engine.py::_run_single`의 전역 index** | 확인됐고 안 고쳤다. `main`에도 있다 |
| `segments[].reasons`의 순서 미검증 | NFR-3 재현성 문제. 열려 있다 |

## 개발 환경 메모 (승계)

**Python 실행은 반드시 `.venv/Scripts/python.exe`를 쓴다.** 시스템 Python은 3.14라 다르다.
게이트는 CI와 같은 대상 `.`으로 돌린다 — **`src tests`로 좁히면 안 된다**(그 차이로
CI가 5회 연속 실패한 전례가 있다).

**리포 루트에 `cuesift.yaml`을 만들지 마라.** 자동 탐색이 그것을 읽는다.
`conftest.py`의 autouse가 인프로세스 테스트는 막지만 **서브프로세스는 못 막는다**.

**변이 실험은 스크래치 사본에서 하고 `PYTHONPATH`를 강제한다.** editable install의 `.pth`가
사본을 가려 "생존" 오탐을 낸다. 리포에서 직접 할 때는 **파일 복사본으로 복원하라** —
`git checkout --`가 미커밋 작업을 날린 전례가 있다.

**콘솔에서 한글 출력이 깨져 보이는 것은 표시 문제이지 버그가 아니다.** 판정이 필요하면
파일로 받아 `read_bytes()` 후 utf-8·cp949 순으로 디코드해 읽는다.

**파이썬 스크립트로 문서를 고칠 때 `newline=""`을 준다.** `Path.write_text`의 기본값이
`newline=None`이라 Windows에서 `\n`을 `\r\n`으로 번역해 **2줄 수정이 1967줄 변경으로** 찍힌다.

Ollama는 트레이 앱 겸 백그라운드 서비스로 자동 기동해 `127.0.0.1:11434`를 듣는다.
PATH에 없으면 `$env:LOCALAPPDATA\Programs\Ollama\ollama.exe`를 직접 부른다.

| 모델 | 크기 | 용도 |
| --- | --- | --- |
| `qwen2.5:3b` | 1.9GB | 번역·Tier 1 신호용. **실측: 3큐 중 2큐 실패**(파킹 2번) |
| `qwen2.5:1.5b` | 986MB | 폴백 관찰용. 번역기로는 못 쓴다(실측 5/15) |

**결정론이 필요하면 스텁 서버를 쓴다.** OpenAI 호환 `/v1/chat/completions`에
`{"translations":[{"id":N,"text":…}]}`를 돌려주면 된다.
**리포 밖에 두어야 게이트를 오염시키지 않는다.**

live 실행 명령:

```powershell
$env:CUESIFT_LIVE_BASE_URL="http://localhost:11434/v1"
$env:CUESIFT_LIVE_MODEL="qwen2.5:3b"
.venv/Scripts/python.exe -m pytest -m live -v -s
```

## 다음 세션 시작 절차

```bash
git checkout fix/failure-diagnostics
git log --oneline | head -3
git status --short                        # clean이어야 한다
git ls-remote --heads origin fix/failure-diagnostics   # 푸시됐나
gh pr list --head fix/failure-diagnostics              # PR이 있나
```

머지가 끝났으면 **다음 작업은 파킹 #13(캐시가 실패를 보존한다)이 1순위다.**
파킹 2번(README 권장 모델이 대량 실패)과 같은 뿌리이고, 이번 세션의 CRITICAL이
바로 그 결함의 증상이었다. 그 다음이 **FR-8.3**(WP6의 마지막 조각, STT 어댑터가 선행)이다.
