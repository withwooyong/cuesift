# Session Handoff

> Last updated: 2026-08-01 (KST)
> Branch: `main` (작업 트리 깨끗, stash 없음)
> Latest commit: 아래 "Files Modified" 참고
> **다음 세션은 WP6 부분(FR-8.2 `check` 배선)부터 시작한다** — WP4가 완료됐다

## Current Status

**WP4(인제스트)를 완료했다.** 자막 파일(SRT·WebVTT·ASS/SSA)이 처음으로 파이프라인에 들어온다.

| | 착수 | 완료 |
| --- | --- | --- |
| v0.1 FR 완료 | 16/42 (38%) | **19/42 (45%)** |
| 테스트 | 282 | **316** |
| 인제스트 픽스처 | 0 | **12종** |

제품 상태는 **CLI가 여전히 배선되지 않았다**(모든 서브커맨드가 종료 코드 `70`).
번역(`translate`) 계층도 없다. 다만 **`check` 서브커맨드는 이제 배선만 하면 동작한다** —
Tier 0 엔진·규격 프로파일·인제스트가 모두 있기 때문이다.

저장소: <https://github.com/withwooyong/cuesift> (Public).

## Completed This Session

| # | 태스크 | 커밋 | 산출물 |
| --- | --- | --- | --- |
| — | 설계 스펙 (자체 리뷰 2회 포함) | `735c277`·`a3c6926`·`974fe90` | `specs/2026-07-31-ingest-design.md` |
| — | 구현 계획 (사전 스캔 포함) | `292db91`·`9123005` | `plans/2026-07-31-ingest.md` |
| 1 | 픽스처 10종 + 픽스처 자체 검증 | `42a126f` | `tests/fixtures/ingest/` · `test_ingest_fixtures.py` |
| 2 | 최소 로더 | `bb105b7` | `src/cuesift/ingest/{__init__,loader}.py` |
| 3 | 입력 종류 판별 (FR-1.3) | `be51706` | `not_found`·`video_input` |
| 4 | 로드 실패 번역 | `52acf5b` | `decode`·`parse` |
| 5 | 이벤트 필터와 `empty` | `d51cb3c` | `_keep_displayed` · `event_index` |
| 6 | `bad_timecode` | `1cbc21a` | 1-based 큐 번호 보고 |
| 7 | 오염 차단 음성 테스트 | `26f7517`·`6ad9366` | `test_ingest_contamination.py` |
| 8 | 문서 갱신 | `07876f9` | `docs/WBS.md` · `CHANGELOG.md` |
| — | 최종 리뷰 수정 웨이브 | `50e62cf`·`f9c51d8` | 회귀선 3종 + 설계 §2.1·§9.3·§10 정정 |

계획 커밋 3건(`9123005`·`0480167`·`c91bdb8`)은 **계획서 자신의 결함을 고친 것**이고
코드 커밋과 분리돼 있다.

## In Progress / Pending

| # | 작업 | 상태 | 비고 |
| --- | --- | --- | --- |
| 1 | **WP6 부분 — FR-8.2 `check` 배선** | ⬜ 다음 | 이것만 하면 **처음으로 쓸 수 있는 제품**이 나온다. 아래 "함정" 반드시 읽을 것 |
| 2 | WP5 출력 (FR-7.1~7.5) | ⬜ | `subs` + `event_index`로 라운드트립 가능함을 실측 확인. 단 태그 보존은 별도 경로 필요 |
| 3 | WP7 번역 → WP8 Tier 1 | ⬜ | Q4(자가일관성 유사도)가 여기서 닫힌다 |
| 4 | WP9 STT | ⬜ | 런타임 의존성 4개 고정과 충돌 — 호출 방식 미결 |

## Key Decisions Made

- **인제스트는 단일 진입점이다** — 포맷별 어댑터 3개를 만들지 않았다. pysubs2가 포맷
  자동 판별과 태그 정규화를 전부 하므로 **분기할 대상이 0개**다. WBS의 "파서 3종"은
  요구사항(3개 포맷)의 서술이지 구조(3개 모듈)의 서술이 아니었고, 라이브러리를 실제로
  찔러 보기 전에는 그 둘이 구분되지 않는다. 규모 추정이 M에서 S로 내려갔다
- **원본 `SSAFile`과 `event_index`를 `IngestResult`에 담는다.** FR-7.1이 "입력과 동일
  포맷 출력"을 요구하므로 WP5가 원본을 되쓴다. 필터가 `segments`와 `subs`를 어긋나게
  하므로 대응표가 없으면 **첫 주석 줄 이후로 전부 한 칸씩, 예외 없이 조용히 밀린다**
- **이상 입력은 엄격 실패 + 진단 메시지.** 건너뛰기는 호출자가 개수를 안 읽으면 손실이
  된다. `IngestError`는 단일 예외 + `reason` 코드이고 **테스트는 `reason`만 단언한다** —
  문구를 고정하면 메시지를 개선할 때 회귀 테스트가 함께 실패해 개선을 방해한다
- **`encoding` 파라미터를 만들지 않았다.** utf-8 고정이다. cp949 자막은 사용 불가가
  되지만 `cli.py`에 `--encoding` 플래그가 없어 **아무도 설정할 수 없는 인자**를 만드는
  것이고, `--fusion`을 만들지 않은 것과 같은 판단이다
- **파싱한 파일은 항상 `source_text`에 들어간다** — `check`가 번역 결과물을 검사할
  때도 마찬가지다. 필드가 상황마다 바뀌면 하위 모듈이 어느 쪽을 볼지 매번 판단해야 한다.
  **이 결정이 아래 "함정"을 낳는다**
- **자동 언어 감지를 넣지 않았다**(FR-1.5). 문구가 "자동 감지**하거나** 명시적으로
  지정받는다"이고 Q2가 원문을 한국어로 고정했으므로 감지할 것이 사실상 없다.
  직접 구현하면 한자만 있는 짧은 자막에서 ko/ja가 갈리지 않고 **그 실패는 조용하다**

### 이전 세션에서 이어지는 결정 (변경 없음)

초기 언어쌍 **ko→en/ja**(Q2) · 로컬 LLM은 **OpenAI 호환 엔드포인트로 일원화**(Q3) ·
규격 1차 출처는 **Netflix TTSG**(Q5) · LLM 연동은 **자체 얇은 어댑터**(Q6) ·
배수 헤드라인은 **예산 10%**에서 뽑는다 · **가중치는 튜닝하지 않는다**.

## Known Issues

### 🔴 WP6이 밟을 함정 — 인제스트 결과를 신호 엔진에 그대로 넣으면 전량 hard fail

**`load_subtitle` 결과를 `collect_all`에 넣으면 모든 세그먼트가 `struct.empty` hard fail이다.**

```text
  00000  [('struct.empty', True, 1.0)]
  00001  [('struct.empty', True, 1.0)]
```

Tier 0 신호 9종 중 8종은 `if not seg.target_text: return None`으로 시작하는데
`struct.empty`만 `target_text`가 없으면 hard fail을 낸다. 인제스트 직후는
`target_text=None`이므로 전부 걸린다.

**왜 치명적인가**: hard fail은 FR-6.2에 따라 **검수 예산을 우회**한다. 검수 비율이
100%가 되고, 그 비율이 배수의 분모이므로 **Recall@Budget 지표 자체가 무의미해진다.**

**대응 선택지**: `check` 배선이 파싱한 파일을 `target_text`로도 채우거나,
`SignalContext`에 원문-전용 모드를 두거나, `check`가 쓸 수집기를 명시적으로 고른다.
설계 [§10](docs/superpowers/specs/2026-07-31-ingest-design.md)에 기록해 뒀다.

### WP5가 알아야 할 것 — 라운드트립은 되지만 태그 보존은 안 된다

`subs` + `event_index`로 **SRT·VTT·ASS·SSA 4포맷 전부 라운드트립이 성립**하는 것을
실측했다(load → `event_index`로 되쓰기 → 동일 포맷 저장 → 재적재). 다만 보존 범위는
설계 초판이 적은 것보다 좁다.

| 항목 | 보존되나 |
| --- | --- |
| 스크립트 헤더 · 스타일 정의 · 주석 줄 · 타임코드 · 포맷 | ✅ |
| ASS 오버라이드 태그 `{\an8}` | ⚠️ **건드리지 않은 이벤트만.** `plaintext` setter가 텍스트를 통째로 대체한다 |
| VTT cue settings `line:0 position:20%` | ❌ `SSAEvent`에 담을 필드가 없다 |
| 화자 태그 `<v Alice>` | ❌ 로드 시점에 버려진다 |

**WP5가 태그를 보존한 채 되쓰려면 `plaintext` setter에 기대지 않는 별도 경로가 필요하다.**

### `spec.overlap` 판정 — 준비는 됐고 벤치는 아직

`overlap.vtt` → `check_overlaps` 경로가 열렸다(`test_overlapping_cues_reach_spec_check_overlaps`).
겹침 1000ms가 검출된다. 다만 **벤치 하네스는 여전히 겹침 0건인 합성 트랙을 쓴다** —
기여도 `+0.0%`를 다시 재려면 `bench/build_track.py`나 주입기가 겹침을 만들어야 한다.

### 미뤄 둔 것 (최종 리뷰가 전부 "이후로 미룸" 판정)

| 항목 | 판정 근거 |
| --- | --- |
| `_reject_non_subtitle`이 **디렉터리 입력도 `not_found`** 로 보고 | 존재하는데 "없다"는 틀린 진단이지만 조용히 통과하진 않는다. 올바른 처리 위치는 `typer.Option(dir_okay=False)`가 있는 **WP6** |
| `IngestError.reason`이 자유 `str` | **이 리포에 타입 체커가 없다**(`pyproject.toml`·`ci.yml` 확인). `Literal`은 아무 게이트도 강제하지 않는 장식이 된다. mypy 도입 시 함께 |
| `loader.py`의 `_load`·`_reject_non_subtitle` 정의 순서가 호출 순서와 반대 | 6커밋 누적의 유일한 흔적 |
| 설계 §10이 지시한 "`id` 한계를 주석에 적는다"가 코드에 미이행 | `id`가 인덱스 기반이라 파일 편집 시 흔들린다는 서술이 없다 |
| 테스트 독스트링 일부에 FR·§ 번호 병기 누락 | 표기 통일 수준 |

### 이전 세션에서 이어지는 이슈 (여전히 유효)

- **외부 URL은 아무도 검사하지 않는다.** 링크 체커는 내부 상대 링크만 본다
- `scripts/check_links.py`는 **앵커를 검사하지 않는다.** 절 번호를 바꾸면 조용히 깨진다
- **링크 체커는 `git ls-files` 기준이라 미추적 파일을 건너뛴다.** 새 문서를 만들면
  `git add` 후에 돌려야 검사된다 — 두 게이트의 파일 개수 대조가 유일한 탐지 수단
- `logprobs`는 백엔드에 따라 조용히 사라진다(Ollama 미지원)
- 자가일관성 샘플링은 `n>1` 단일 호출이 아니라 **N회 개별 호출**이어야 이식성이 유지된다
- **로컬 venv가 Python 3.14인데 CI는 3.11/3.12를 검증한다**

## Context for Next Session

**이번 세션의 요청은 "다음 작업을 알려 달라"였고, 사용자가 중간에 실행 방식을 바꿨다.**

### 사용자가 명시한 것

- **WBS 전체를 한 번에 무개입 개발하는 안을 검토하다가 "리스크가 큰 것 같다"며 직접
  철회하고 WP4 단독으로 좁혔다.** 사전 확정이 불가능한 항목(Q4는 벤치 판정 대기, STT는
  의존성 제약과 충돌)이 뒤쪽 WP에 남아 있고, 앞 WP의 실코드를 못 본 채 내린 결정이
  뒤 작업의 전제가 되면 되돌리기 비용이 커진다는 점을 확인한 직후의 판단이었다
- **판단 지점은 사전에 전부 못 박는다** — 착수 전에 스펙·계획을 충분히 돌려 판단을
  소진시킨 뒤 구현 구간만 무개입으로 돌린다
- **서브에이전트 주도 실행**을 선택했다 (태스크마다 새 구현자 + 독립 리뷰어)
- 커밋 메시지는 한글 · **푸시는 명시적 요청이 있을 때만**
- 콘솔 응답에는 ASCII 도식, `.md` 파일에는 Mermaid

### 실행 방식 — 무엇이 실제로 결함을 잡았나

| 단계 | 잡은 결함 |
| --- | --- |
| 스펙 자체 리뷰 | 4건 (중의성 3 · 내부 모순 1) |
| **계획서 작성** | 2건 — `load()`의 실제 동작이 스펙 주장과 다름, 27자 드로잉은 CPS를 안 넘어 음성 테스트가 성립 안 함 |
| 태스크별 리뷰 8회 | 1건 (Task 7의 이름-단언 불일치) + minor 7건 |
| **최종 전체 브랜치 리뷰** | **4건 — 태스크 리뷰가 전부 놓친 것** |

**최종 리뷰가 가장 값졌다.** 4건 중 3건이 계획·설계의 결함이지 구현 실수가 아니었다 —
`.ssa`는 계획서 픽스처 목록에 **없었고**, `overlap.vtt`는 **어느 태스크에도 배정되지
않았으며**, `struct.empty` 함정은 **아무도 실행해 보지 않았다.** 빈 칸은 전체를 봐야 보인다.

각 태스크 리뷰는 자기 diff만 본다. **그것이 구조적 한계이지 리뷰어의 태만이 아니다.**

### 리뷰어에게 반드시 넘길 것

지목만으로는 부족하고 **검증 방법까지** 적어야 한다. 이번에 효과가 확인된 형태:

- "diff로 판정할 수 없는 것"을 미리 지목한다 — BOM·CRLF·인코딩은 diff 텍스트에 없다
- **"테스트가 통과해도 이 결함이 있을 수 있다"**를 명시한다 — `reversed.srt`에 주석이
  없어 `raw_index`와 `index`가 같으므로 틀리게 써도 통과했다
- **선판정하지 않는다.** 구현자가 우려를 보고했을 때 "문제없다"를 덧붙이면 리뷰어가
  그 결론을 확인하는 쪽으로 기운다. 판정 기준만 주고 결론은 비운다
- 금지 사항: **코드 수정 · `git stash` · 작업 트리 변경**

### 컨트롤러가 겪은 것

- **`TaskUpdate`로 소유자를 기록하면 구현자에게 새 할당 신호로 전달된다** — 완료 보고
  후 소유자를 적었더니 세 구현자가 "이미 끝낸 작업인데 재할당됐다"고 되물었다
- **리뷰어가 보고 없이 대기 상태로 전환되는 일이 반복됐다.** 프롬프트에 "보고서를
  `SendMessage`로 team-lead에게 보내라"를 명시한 뒤로 해결됐다

## Files Modified This Session

```text
docs/superpowers/specs/2026-07-31-ingest-design.md   신규 · 자체 리뷰 2회 · 최종 리뷰 정정
docs/superpowers/plans/2026-07-31-ingest.md          신규 · 자체 정정 3회
docs/WBS.md                                          WP4 완료 · 규모 S · Mermaid WP3 정정
docs/AI_자막검수_오픈소스_비교.md                     §5 #4에 pysubs2 실측 결과 반영

src/cuesift/ingest/__init__.py                       신규 — 공개 API 재수출
src/cuesift/ingest/loader.py                         신규 — 로드·필터·변환·오류 번역
tests/fixtures/ingest/                               신규 12종
tests/test_ingest.py                                 신규 26건
tests/test_ingest_fixtures.py                        신규 5건 — 픽스처 자체 검증
tests/test_ingest_contamination.py                   신규 3건 — 오염 차단 음성 테스트
.gitattributes                                       *.ssa -text 추가

README.md · CHANGELOG.md · HANDOFF.md                상태·항목·인수인계
```

테스트 **282 → 316건**. `data/` 아래는 하나도 커밋되지 않았다.
