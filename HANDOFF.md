# Session Handoff

> Last updated: 2026-08-17 (KST)
> Branch: **`fix/translate-id-type-coercion`** — 커밋 2개, `main`에 아직 안 올라갔다.
> **이전 세션의 🔴 최우선 항목이 닫혔다** — `translate/`가 처음으로 실제 네트워크를 탔고
> `tests/test_translate_live.py`가 처음으로 통과했다.
> 진척은 [WBS](docs/WBS.md), FR 번호의 출처는 [요구사항정의서](docs/요구사항정의서.md)다

## Current Status

**Ollama를 붙여 live를 처음 실행했고, 그 한 번이 결함을 잡았다.**

이전 세션은 "858개가 `httpx.MockTransport` 위에서만 초록이고 live는 단 한 번도 통과한 적이
없다"를 최우선 항목으로 남겼다. 이번 세션이 그것을 실행했고, **첫 실행에서 번역이 3/3 실패했다.**

| | 이전 세션 | 이번 세션 |
| --- | --- | --- |
| live 테스트 | **단 한 번도 통과 없음** | **통과** (`qwen2.5:3b`·`1.5b` 양쪽 `calls=1`) |
| 테스트 | 858 passed + 1 deselected | **868 passed + 1 deselected** |
| 폴백 실물 검증 | 없음 | **구제 경로 관찰** (1.5b·15세그먼트에서 5건 구제) |
| v0.1 FR 완료 | 28/42 (67%) | 28/42 — FR 진척은 없다. 이번 세션은 **결함 수정과 관찰**이다 |

```mermaid
flowchart TD
    A["live 첫 실행<br/>3/3 invalid_response"] --> B["경계 계측<br/>원시 응답 확보"]
    B --> C["근본 원인<br/>모델: {id: \"0\"} 문자열<br/>파서: int 요구<br/>프롬프트: 타입 미명시"]
    C --> D["prompt.py<br/>예시를 정수 리터럴 + 타입 문장"]
    C --> E["batch.py<br/>_normalize_id 왕복 일치"]
    D --> F["live 재실행 calls=1 통과"]
    E --> F
    F --> G["1.5b·15세그먼트로<br/>폴백 구제 경로 관찰"]
```

### 무엇이 결함이었나

모델은 JSON·구조·개수·순서·번역을 **전부 맞혔고 어긋난 것은 JSON 타입 하나**였다
(`{"id": "0"}`). 직접 원인은 모델이 아니라 **프롬프트가 파서의 정수 요구를 전달하지
않은 것**이다 — 파서는 `isinstance(item_id, int)`를 거는데 시스템 프롬프트는
`{"id": <번호>}`라는 자리표시자만 주고 타입을 말하지 않았다.

**이 실패 양식에서 개별 폴백은 무력하다.** 타입 습관은 세그먼트 1개짜리 호출에서도
재현되므로 호출만 `(1+N)`배가 되고 결과는 같다. 설계 §6.2가 상정한 폴백의 사각지대다.

### 왜 813건의 테스트가 이것을 놓쳤나

**단위 테스트는 가짜 프로바이더를 쓰므로 응답을 테스트 작성자가 만든다.** 작성자는 파서
계약을 알고 있어 언제나 `{"id": 0}`을 냈고, 프롬프트와 파서가 어긋났다는 사실이 드러날
자리가 **구조적으로 없었다.** 이 저장소가 여러 번 물린 "변이 배터리는 자기가 상정한
변이만 잡는다"의 일곱 번째 사례다.

## 🔴 즉시 해야 할 것 — PR을 만들어야 CI가 돈다

```bash
git push -u origin fix/translate-id-type-coercion
gh pr create --base main
gh pr checks --watch     # test 3.11 · test 3.12 · docs
gh pr merge --squash
```

**로컬 venv는 3.14이고 CI는 3.11·3.12다.** 이번 변경은 f-string을 새로 쓰지 않아
버전 함정 위험이 낮지만, 임시 3.11 문법 검사는 계속 유효하다.

```bash
.venv/Scripts/python.exe -c "import ast,pathlib; [ast.parse(p.read_text(encoding='utf-8'), feature_version=(3,11)) for p in list(pathlib.Path('src').rglob('*.py'))+list(pathlib.Path('tests').rglob('*.py'))]; print('3.11 OK')"
```

## 다음 사람이 반드시 알아야 할 것 4가지

### ① live 테스트는 폴백 **구제** 경로를 검증하지 못한다

세그먼트가 3개라 배치도 1개(`DEFAULT_BATCH_SIZE=10`)이고 `before`/`after`가 **빈 튜플**이다.
따라서 폴백이 발동해도 `_fallback_individually`의 **맥락 재계산**이 실질적으로 돌지 않는다.

실측으로 확인했다 — 3b·1.5b **둘 다 `calls=1`** 로 배치 한 번에 성공한다.
세그먼트를 15개(배치 10+5)로 늘려야 폴백이 돈다.

| 조건 | 모델 | 호출 | 결과 |
| --- | --- | --- | --- |
| 세그먼트 3개 (현행 live) | 3b | 1회 | 통과, 폴백 미발동 |
| 세그먼트 3개 (현행 live) | 1.5b | 1회 | 통과, 폴백 미발동 |
| 세그먼트 15개 | 1.5b | **17회** | 성공 5/15, **폴백이 5건 구제** |

17회 = 배치 1 + 개별 10 + 배치 1 + 개별 5로 상한 계산과 정확히 일치한다.
**미결**: 이 15세그먼트 케이스를 `-m live`에 넣을지. `failures == ()`로 단정할 수 없어
(모델마다 다르다) 관찰용 출력 위주가 된다.

### ② 소형 모델의 실패 양식은 "전역 인덱스 추종 실패"다

`qwen2.5:1.5b`는 개별 호출 `ids=1`·`ids=10`·`ids=13`에 전부 `{"id": 0`으로 답하고,
**배치 `ids=10~14`도 `{"id": 0`부터 센다.** 프롬프트가 `[10] …`으로 번호를 명시해도 못 따라온다.
`_run_single`의 주석이 "0부터 다시 세면 두 번째 배치의 폴백이 전부 누락 판정을 받는다"고
예견한 그대로다.

**시스템 프롬프트 예시를 자리표시자로 되돌리지 마라.** 위 현상을 보고 "예시의 구체적 `0`을
모델이 복사하는 것 아닌가"를 의심해 비교했고 **가설은 기각됐다**:

| 프롬프트 | 성공 | 개별 id 정확 |
| --- | --- | --- |
| 현재 판 (예시가 정수 `0`) | 5/15 | 5/15 |
| 자리표시자 `<번호>` | **1/15** | **0/15** |

2회 반복 동일. 자리표시자는 타입 문장이 함께 있어도 열등하다.

**실용 결론: 번역에는 3b를 쓰고 1.5b는 폴백 관찰 도구로만 쓴다.**

### ③ 파서 완화의 경계는 `int()`가 아니라 **왕복 일치**다

`_normalize_id`는 `str(int(s)) == s`인 문자열만 정수로 바꾼다. `int()` 성공만 보면 안 되는
이유는 **파이썬 `int()`가 JSON 숫자 문법보다 넓기** 때문이다 — `"1_0"`→10, `" 0"`·`"+0"`→0,
`"٠"`(아랍-인도 숫자)→0.

왕복을 뺀 사본으로 변이를 돌리자 경계 테스트 8건 중 **5건이 죽었고**, 그중
`" 0"`·`"+0"`·`"٠"` 셋은 **예외도 폴백도 없이 0번으로 조용히 접혔다**(`DID NOT RAISE`).
id 집합 검사는 접힌 뒤의 값만 보므로 이 오매핑을 잡지 못한다.

### ④ `pytest` 수치 읽는 법 (이전 세션에서 승계)

마지막 줄이 `N passed, M deselected`이고 **두 수를 같이 읽어야 한다.**
`tests/conftest.py`에 게이트 방어 3겹(임포트 시점 / `pytest_configure` 훅 / 테스트)이 있고,
설정을 건드리면 **exit 4로 수집 전에 선다.** `-c`·`--override-ini`도 걸리므로 설정 변경은
`pyproject.toml`을 직접 고쳐야 한다.

## In Progress / Pending — WP7b·WP8이 막히는 지점 (승계)

**넷 다 `translate/` 안에서는 옳고 경계 밖에서만 보인다.**

| WP | 무엇이 막히나 | 근거 |
| --- | --- | --- |
| **WP7b 리포트** | **실패분을 triage 입력에 넣으면 Recall@Budget이 0이 된다.** `target_text=None` → `struct.empty`가 `hard_fail=True` → quota 소진 | 실패 20건(전체 10%)에서 **재현율 100% → 0%** [실측]. **이번 세션이 이것을 실물로 재현했다** — 1.5b 15세그먼트에서 실패 10건(67%) |
| **WP7b 재개** | `iter_batches`가 **연속 구간**을 전제한다. "안 된 것만 넘긴다"가 **프롬프트를 바꾼다** | 같은 세그먼트의 프롬프트 SHA가 `27077083` vs `8cb1100c` [실측] |
| **WP7b 캐시 키** | `Provider`에 **모델·설정 접근자가 없다**(`name`만 공개). CLI가 자기 값을 따로 들고 있어야 한다 | — |
| **WP8 자가일관성** | **차단 수준.** Tier 1은 상위 25%만 도는데 그것이 **비연속**이다. 이웃이 맥락이 아니라 **번역 대상**이 되어 **프롬프트 차이**를 잰다 | **Q4 판정이 이 측정에 걸려 있다** |
| **WP8 재사용 범위** | `build_messages`·`parse_translations`는 공개인데 **재시도·백오프는 비공개**다 | — |
| WP8 온도 | **막히지 않는다** — `temperature` 노출, `n`·`logprobs` 미전송(§12 Q3 정합) | — |

## 파킹 목록 — 후속 작업 후보

### 이번 세션이 잠금을 푼 것

| # | 내용 |
| --- | --- |
| **S-4 (해금)** | **`prompt.py`의 `_escape_newlines`가 단사가 아니다** — 원문 `C:\name` → `C:<진짜개행>ame`. 고치려면 프롬프트 계약을 바꿔야 하고 약한 모델의 형식 위반이 는다. **"Ollama로 폴백률을 관측한 뒤가 순서다"라는 선행 조건이 이번 세션으로 풀렸다** — 기준선은 3b `calls=1`(폴백 0), 1.5b 15세그먼트 `calls=17`(폴백 15) |
| (신규) | **live 테스트에 폴백 구제 케이스 추가 여부** — 위 ① 참조 |
| (신규) | **번역문에 번호 마커가 새어 들어간다** (`"text": "[0] That night…"`). `text`는 문자열이면 계약을 통과하므로 **id가 맞았다면 그대로 살아남는다.** 파서가 아니라 Tier 0 신호의 몫이다(설계 §7.2가 나눈 경계) |

### WP7b 브리프로 옮길 것 (문서·주석, 코드 동작은 옳다)

| # | 내용 |
| --- | --- |
| **R-1** | **`iter_batches` 독스트링 처방 앞 절반이 NFR-4·FR-4.3과 모순된다.** "전체 트랙을 넘긴 뒤 결과를 거르거나"는 재개와 Tier 1 둘 다에서 성립하지 않는다. **틀린 처방은 처방이 없는 것보다 나쁘다 — 옮길 때 반드시 정정할 것** |
| **R-2** | `provider.py`의 `content` 가드 주석 **근거가 거꾸로다.** 열거 밖 타입(`object()` 등)이 새어 `TypeError`가 된다 |
| **R-3** | `translate/__init__.py`의 `ValueError` 표 **2행이 `translate_segments` 경로가 아니다.** 제목 "다섯"과 표 6행 불일치 |
| **R-4** | 일부 `# noqa: ANN001, ANN202`가 **켜지지 않은 규칙**을 억제한다(`select = E,F,I,UP,B,SIM`) |
| — | `engine.py`의 **인자 4단 수동 전달** — 10개 키워드 인자가 4개 함수로 손 복사된다. frozen 설정 객체가 대안 |
| — | NFR-2 정밀도 3건: `Completion`에 완료 사유 없음 · 실패 호출 토큰 과소계상 · `TokenUsage.calls=0` 기본값 |
| — | `size` vs `batch_size` 네이밍 갈림 · `parse_translations`만 키워드 전용 `*` 없음 |
| — | `test_translate_live.py`의 `prompt_tokens > 0`이 `_extract_usage`의 "못 읽어도 0" 설계와 충돌 — usage를 안 주는 서버에서 **거짓 빨강**. **Ollama는 usage를 준다**(`prompt_tokens=240` 실측)이므로 이 경로는 아직 안 밟혔다 |
| — | `SegmentFailure.reason`이 검증 없는 맨 `str` · `attempts`가 폴백에서 배치 호출분을 빼고 센다(`usage.calls`는 정확) |

### 별도 태스크 (`translate/` 밖, v0.1 전)

| # | 내용 | 실측 |
| --- | --- | --- |
| **S-1** | **`_contains_term` 정규식 사전 컴파일.** `re._MAXCACHE = 512`를 넘으면 FIFO 축출로 **매 호출 전량 재컴파일** | 큐 1000개: 용어 **510개 0.769초 → 520개 18.111초 (23.5배)**. **게이트를 시간이 아니라 컴파일 횟수로 잡을 것** |
| **S-2** | **`load_glossary`가 빈 `source`를 검증하지 않는다** — 모든 큐에 매치 | **`select_by_threshold`가 임계 ≤0.5에서 붕괴**(review_ratio 10% → **100%**). 예산 경로는 무사 |
| **S-3** | 중복 `source` **병합은 의미를 바꾼다.** 용어집 스키마의 의미론 결정이 먼저다 | — |

### 안 하기로 한 것

| 내용 | 근거 |
| --- | --- |
| ast 리터럴 위장 방어 | 좁히는 방법이 **그 자체로 또 이름 매칭**이다 |
| 게이트 우회 M8+M4 · 하드코딩 URL live 파일 | **완전한 정적 판정은 없다** — 강화 규칙 후보가 `test_translate_api.py` 자신을 첫 오탐으로 만들었다 [실측]. 설계 §9.2에 한계로 명시 |

## Known Issues — 배운 것

### 거짓 전제가 일곱 번째로 나왔다 (이번 세션)

`test_translate_live.py` 독스트링이 **"3B급 모델은 번호를 빠뜨리거나 코드펜스를 씌우거나
설명을 덧붙이는 것을 실제로 한다"** 고 단정했으나, **실물이 낸 첫 변이는 셋 다 아니었다.**
정정하면서 상정 목록을 단정형으로 다시 쓰지 않았다 — 다음 빈틈도 목록 밖에 있을 가능성이 높다.

### 반복된 실패 형태 (승계)

| 형태 | 사례 |
| --- | --- |
| **변이 배터리는 자기가 상정한 변이만 잡는다** | 이번 세션이 일곱 번째 사례다 — 가짜 프로바이더가 언제나 `{"id": 0}`을 냈다 |
| **선언된 불변식에 테스트가 없으면 주석이지 계약이 아니다** | 주석이 "위치로 짝지으면 안 된다"고 적었는데 위치 짝짓기 변이가 35개를 전부 통과 |
| **테스트 데이터가 미탐을 만든다** | 순서 데이터가 하필 정렬 순과 같아 `sorted()` 변이를 못 잡음 |
| **거짓말하는 가짜가 테스트를 통째로 무효화한다** | 5회 |
| **커버리지 100%는 "실행됐다"이지 "관찰됐다"가 아니다** | `engine.py`가 100%인 채 **변이 12종이 살아남았다** |

### 게이트를 만들면 반드시 실패시켜 봐야 한다 (이번 세션에서 발동)

새 경계 테스트 8건은 **RED 단계에서 통과했다** — 현행 코드가 모든 문자열을 거부하니 당연하다.
그래서 왕복을 뺀 변이 사본으로 **5건이 실제로 죽는 것**을 확인했다. 그 확인이 없었다면
"8건 초록"이 게이트인지 장식인지 구분되지 않았다.

## Key Decisions Made

| 결정 | 근거 |
| --- | --- |
| **프롬프트와 파서를 둘 다 고친다** | 프롬프트는 계약을 전달하지 않은 **결함**이고, 파서 완화는 **그물**이다. 프롬프트는 지시이지 강제가 아니다 |
| **완화 경계는 `str(int(s)) == s` 왕복** | `int()`는 JSON 숫자 문법보다 넓다. 왕복이 없으면 `" 0"`·`"+0"`·`"٠"`가 **조용히** 0번으로 접힌다 |
| **문자열이 아닌 값은 손대지 않는다** | 실수 `0.0`을 0으로 접으면 `1.5`가 정수 자리에 들어올 길이 열리고 반올림이 세그먼트를 정한다 |
| **거부보다 수용이 보수적이다 (이 자리에서는)** | 번역 실패는 `struct.empty`가 hard fail로 판정해 검수 예산을 우회하고 **§9.1 배수의 분모**를 부풀린다 |
| **타입 문장과 정수 리터럴 예시를 둘 다 넣는다** | 각각 단독으로도 3/3이었으나 어느 쪽이 듣는지는 모델마다 다를 수 있다 |
| (승계) **재시도 소진 후에는 폴백하지 않는다** | 폴백은 "모델이 지시를 어김"의 처방이지 "서버가 죽음"의 처방이 아니다 |
| (승계) **설정 오류는 맨 `ValueError`다** | `ProviderError`는 "프로바이더 **호출** 실패의 최상위"인데 이건 설정이다 |

## 게이트 실행 기록

| 게이트 | 수치 |
| --- | --- |
| `ruff check .` | All checks passed |
| `ruff format --check .` | **73 files** already formatted |
| `pytest -q` | **868 passed, 1 deselected** |
| `pytest -m live --collect-only` | **1/869 (868 deselected)** |
| `pytest -m live` (Ollama `qwen2.5:3b`) | **1 passed** · `calls=1` `prompt_tokens=240` |
| `pytest -m live` (Ollama `qwen2.5:1.5b`) | **1 passed** · `calls=1` |
| `python scripts/check_links.py` | 마크다운 **21개 파일** · 상대 링크 **85개** / 깨진 링크 0 |
| `npx markdownlint-cli2` | Linting: **21 files** / 0 issues |

**두 문서 게이트가 21로 일치한다** — 갈라지면 추적 안 된 문서가 있다는 뜻이다.

## 개발 환경 메모

Ollama는 이번 세션에 설치했다(`winget install --id Ollama.Ollama -e`, 0.32.13).
Windows판은 트레이 앱 겸 백그라운드 서비스로 자동 기동해 `127.0.0.1:11434`를 듣는다 —
`ollama serve`를 따로 칠 필요가 없다. 설치 직후 현재 터미널에는 PATH가 안 잡히므로
새 터미널을 열거나 `$env:LOCALAPPDATA\Programs\Ollama\ollama.exe`를 직접 부른다.

| 모델 | 크기 | 콜드 스타트 | 용도 |
| --- | --- | --- | --- |
| `qwen2.5:3b` | 1.9GB | 22.2초 | **번역용.** live 통과 |
| `qwen2.5:1.5b` | 986MB | — | **폴백 관찰용.** 번역기로는 못 쓴다(5/15) |

이 PC는 GTX 960 / VRAM 2GB · RAM 31.8GB다. 3b는 VRAM에 걸쳐 일부가 CPU로 넘어가지만
세그먼트 몇 개짜리 테스트에는 지장이 없다.

**live 실행 명령은 [README](README.md)의 "개발 환경 > 실제 LLM 엔드포인트 테스트"에 옮겨 뒀다**
(앵커를 걸지 않은 것은 링크 체커가 앵커를 검사하지 않기 때문이다 — 검사받지 않는 링크는 남기지 않는다).
독스트링의 bash 예시(`VAR=값 명령`)는 PowerShell에서 동작하지 않는다 — `$env:`로 따로 세팅해야 한다.
