# Session Handoff

> Last updated: 2026-07-29 (KST)
> Branch: `feat/ted2020-benchmark-harness` (main 미병합)
> Latest commit: 이 세션의 커밋(측정 실행 스크립트 · 문서 반영) — 아래 "Files Modified" 참고

## Current Status

**TED2020 벤치마크 하네스가 완성됐고, Tier 0 신호 엔진의 첫 Recall@Budget 실측치가 나왔다.**
공개 병렬 코퍼스(TED2020/OPUS, en-ko·ja-ko 각 5,000건)에 합성 오류를 주입해 측정했다.

| 예산 | en-ko 실제/Recall/배수 | ja-ko 실제/Recall/배수 |
| --- | --- | --- |
| 1%·2%·5% | 6.32% / 54.60% / 8.64x | 5.82% / 50.00% / 8.59x |
| **10%** (헤드라인) | 10.00% / **69.60%** / **6.96x** | 10.00% / **70.60%** / **7.06x** |
| 20% | 20.00% / 86.60% / 4.33x | 20.00% / 83.60% / 4.18x |
| 30% | 30.00% / 89.40% / 2.98x | 30.00% / 85.60% / 2.85x |

hard fail 자연 오탐률: en-ko **0.9556%** / ja-ko **0.9111%** (목표 "≈ 0"에 부합, 불변식 4는 2% 이하만 통과시킨다).

**README 헤드라인은 예산 10%(약 7배)를 쓴다.** 예산 1~5%가 배수는 더 크지만(8.6x) hard fail만으로
채워져 요청 예산(1~5%)과 실제 검수 비율(6.32%/5.82%)이 갈라진다. **요청 예산과 실제 검수 비율이
처음 일치하는 지점**이 10%라 인용에 가장 안전하다.

**이 배수는 의미 오류에는 적용되지 않는다.** `negation`(의미 반전)만 떼어 유형별 무작위
기준선(100시드)과 비교하면 **Tier 0가 무작위보다 못하다** — 예산 10%에서 1.41% vs 무작위
9.61%(en-ko), 예산 20%에서도 9.86%/11.27% vs 무작위 19.49%/19.85%(en-ko/ja-ko). Tier 0가
다른 오류(미번역·빈 값·규격 위반)를 상위로 올리면서 문법적으로 완벽한 문장(의미만 뒤집힌
것)을 큐에서 오히려 밀어내기 때문이다 — README 한계 절과 `bench/results/`의
"`negation` 무작위 기준선과의 비교" 절에 실려 있다.

**여전히 CLI에 배선되지 않았다.** 모든 서브커맨드는 여전히 종료 코드 `70`을 반환한다.
자막 파일 파싱(`ingest`)과 번역(`translate`) 계층이 없다 — 측정은 벤치 하네스가
`bench/track_io.py`로 만든 JSON 트랙에서 `Segment`를 직접 로드해 이뤄졌고, CLI 경로를 거치지 않았다.

**ko 자막의 약 절반이 TED 규격에 물리적으로 담기지 않는다(스펙 §4.4).** en-ko는
399,414쌍 중 194,463쌍(**49.91%**), ja-ko는 358,635쌍 중 168,613쌍(**48.09%**)이
21자×2줄 규격을 못 채워 트랙에서 빠졌다 — FR-5.4(v0.2 규격 자동 교정)를 정량적으로
정당화하는 숫자이고, 남은 표본이 짧은 문장으로 기운다는 뜻이기도 하다.

저장소: <https://github.com/withwooyong/cuesift> (Public)

## Completed This Session (Task 9 — 마지막 태스크)

앞선 8개 태스크(코퍼스 획득 → 클린 트랙 → 오류 주입 → 측정 → 리포트, `dd2826c`..`dfa12ca`)가
이미 이 세션 이전에 커밋돼 있었다. 이번 태스크는 그것들을 엮어 **실측**하고 결과를 문서에 반영했다.

| 산출물 | 내용 |
| --- | --- |
| `bench/run.py` | 오케스트레이션. `python -m bench.run --pair {en-ko,ja-ko}`. 브리프 원본 코드에는 `random_baseline` 호출 인자가 실제 시그니처(`segments, error_ids, ratio`)와 달랐고 `RunMeta.hard_fail_false_positive_rate`(필수 필드)가 누락돼 있어 둘 다 고쳐서 구현했다 |
| `bench/results/{en-ko,ja-ko}-2026-07-29.{md,json}` | 실측 리포트. 재현 정보·예산 스윕·유형별 Recall·**오라클 대비 달성률**(신규)·ablation |
| `bench/report.py` | "오라클 대비 달성률" 절 + "`negation` 무작위 기준선과의 비교" 절(유형별 무작위 기준선, `by_kind_baseline` 신규 키워드 인자) 추가. 기존 12개 리포트 테스트는 그대로 통과(둘 다 기본값 `None`인 키워드 전용 인자라 하위 호환) |
| `README.md` | 최상단에 "실측: Recall @ Budget" 절 신설 — 배수·예산 스윕 표·리포트 링크. 한계 절에 "이 배수는 의미 오류에 적용 안 됨"(`negation` 무작위보다 못함) 명시 |
| `docs/요구사항정의서.md` §12 Q4 | 정량 근거 추가·강화(의미 반전 Recall 1.41%@예산10%, **무작위 기준선 9.61%보다 낮음**). **열어 둔 채로** |
| `docs/AI_자막검수_오픈소스_비교.md` §5 | 조사 질문 #2를 ✅ 닫힘으로 갱신. "Q4와 같은 실험" 서술 정정. `specs/ted.yaml` 단수 → 3파일 정정 |
| `CHANGELOG.md` | Unreleased에 벤치 하네스 전체 + 이번 세션 문서 반영 기록 |
| `bench/build_track.py` | **(fix 라운드 1)** 트랙과 나란히 사이드카 `{pair}.clean.stats.json`을 쓴다 — 스펙 §4.4 부수 산출물(코퍼스 규격 미충족률)이 리포트 어디에도 없던 것을 바로잡았다 |
| `bench/report.py` | **(fix 라운드 1)** `RunMeta.excluded` → `injection_skipped`로 이름 정정("표본 제외"가 아니라 "주입 자격 미달"). `corpus_stats` 필드와 "코퍼스 제외 (스펙 §4.4)" 절 추가 |

## In Progress / Pending

| # | Task | Status | Notes |
| --- | --- | --- | --- |
| 1 | **CLI 서브커맨드 배선** | 미착수 | 인자 스키마와 종료 코드 계약은 테스트가 고정하고 있다 |
| 2 | **자막 파일 파싱(`ingest`)** | 미착수 | `pysubs2`가 의존성에 있으나 `src/` 어디서도 임포트되지 않는다 |
| 3 | **번역 계층(`translate`)** | 미착수 | LLM 어댑터(Q3 확정: OpenAI 호환 엔드포인트) 미구현 |
| 4 | **Tier 1 자가일관성 · Q4** | 미착수 | 아래 "Q4는 왜 안 닫혔는가" 참고. 다음 단계의 본체 |
| 5 | **`spec.overlap` A/B 판정** | **미해결로 남음** | 아래 "빠진 판정" 참고 — 계획 A 재리뷰가 요구했으나 이 벤치의 데이터로는 답할 수 없었다 |
| 6 | **가중평균 vs noisy-or A/B** | 미해결 | U자 곡선(아래)이 정량적 정황 증거는 남겼지만, 융합 규칙을 바꾼 브랜치와의 직접 비교는 아직 없다 |
| 7 | 브랜치가 main에 병합되지 않았다 | 대기 | 사용자 병합 결정 대기 |

## Q4는 왜 안 닫혔는가

`docs/AI_자막검수_오픈소스_비교.md` §5의 조사 질문 #2("xCOMET 없이 경량 신호만으로 트리아지가
성립하는가?")는 이번 실측으로 **✅ 닫혔다** — Tier 0 결정론적 신호 9종만으로 예산 10%에서
무작위 대비 약 7배를 낸다.

**요구사항정의서 §12 Q4("자가일관성의 의미 유사도 측정 수단")는 다른 질문이라 안 닫힌다.**
Q4는 **LLM 재번역 결과끼리 얼마나 다른지 재는 방법**을 묻는데, 이 벤치는 LLM 호출을 전혀
하지 않는 Tier 0 전용이다. 이번 실측이 Q4에 준 것은 **투자 근거**뿐이다 — 결정론적 신호로는
의미 반전(`negation`)을 예산 10%에서 **1.41%**밖에 못 잡는다. 처음엔 "낮다" 정도로만 적었는데,
Task 8 리뷰어가 유형별 무작위 기준선을 100시드로 재측정해 **더 정확한 진술**이 나왔다 —
**같은 예산에서 무작위로 뽑는 것(9.61%)보다도 낮다.** Tier 0가 다른 오류를 상위로 올리면서
문법적으로 완벽한 문장(의미만 뒤집힌 것)을 오히려 큐에서 밀어내기 때문이다(예산 20%에서도
9.86%/11.27% vs 무작위 19.49%/19.85%). 이 숫자가 Tier 1(자가일관성·역번역)의 필요성을
뒷받침하고, Q4는 그 Tier 1을 실제로 구현해야 답할 수 있다. **이전 버전의 조사 문서가
"Q4와 #2는 같은 실험에서 답이 나온다"고 적어놨던 것은 틀린 서술이었다** — 이번 세션에서 정정했다.

## fix 라운드 1 — §4.4 부수 산출물이 리포트에서 빠져 있었다

컨트롤러 계획의 `run.py` 의사코드가 `excluded=dict(skipped)`였는데, 이는 `inject`의
**주입 자격 미달** 건수(`bench/inject.py`의 `skipped`)이지 스펙 §4.4가 요구하는
**코퍼스 규격 미충족 제외**(`build_track`의 `unfittable`)가 아니었다. 두 가지가 같은
"제외"라는 말로 뭉뚱그려져 있었다 — 필드 이름(`excluded`)도, 리포트 절 제목("제외
건수")도 이 둘을 구분하지 못했다.

**바로잡은 방법**: `bench/build_track.py`가 트랙을 쓸 때 사이드카 JSON
(`{pair}.clean.stats.json`)에 §4.4 통계를 함께 쓰고, `bench/run.py`는 그 값을 **그대로
읽어서** 쓴다 — 코퍼스를 다시 훑어 재계산하지 않는다. 재계산하면 트랙을 만든 실행과
측정 실행이 다른 숫자를 낼 수 있고, 그때 어느 쪽이 맞는지 알 방법이 없어진다.
`RunMeta.excluded`는 `injection_skipped`로 이름을 바꿔 의미를 분명히 했다.

**검증 순서(TDD)**: `tests/test_bench_report.py`에 새 절·필드명을 요구하는 테스트
4건을 먼저 추가해 `TypeError`로 실패하는 것을 확인했고, `tests/test_bench_build_track.py`
(신규 파일)에 사이드카 테스트 2건을 추가해 `ImportError`로 실패하는 것을 확인한 뒤
구현했다. 트랙을 재생성해 시드가 같으므로 SHA-256이 재생성 전과 **완전히 동일함**을
확인했다(en-ko `7fb304ec…`, ja-ko `1dade65a…`).

**실측(콘솔·사이드카 일치 확인)**: en-ko 원본 399,414쌍 → 필터 후 389,598쌍 →
규격 미충족 제외 **194,463건(49.91%)** → 가용 195,135. ja-ko 원본 358,635쌍 → 필터 후
350,634쌍 → 규격 미충족 제외 **168,613건(48.09%)** → 가용 182,021. 팀장이 미리 알려준
en-ko 수치(194,463건/49.91%)와 정확히 일치했다.

**주의 — 이 라운드 도중 작업트리 충돌이 실제로 발생했다.** `bench/report.py`를
두 번째로 Edit하려는 시점에 파일이 외부에서 갱신돼(다른 에이전트가 같은 파일을 건드린
것으로 추정) 방금 적용한 첫 번째 Edit(RunMeta 필드 선언부)이 사라져 있었다. 즉시
재적용하고 테스트를 다시 돌려 확인했다. **CLAUDE.md의 경고("구현자가 도는 동안
작업트리는 내 것이 아니다")가 실제로 재현된 사례다** — 다음 세션도 공유 작업트리에서
파일을 편집한 직후에는 재확인 없이 다음 단계로 넘어가지 말 것.

## `spec.overlap`이 미해결로 남은 이유

계획 A(Tier 0 엔진) 재리뷰가 남긴 미결 항목 중 하나였다 — 단일 `end_ms` 오타가 트랙의
50~97%를 flag하는 캐스케이드를 실측했고, `ablation()`이 on/off 하락폭을 낼 수 있으니
이 벤치가 판정하기를 기대했다. **실제로 돌려본 결과 두 언어쌍 모두 `spec.overlap`의 기여도가
`+0.0%`다** — 판정이 나온 게 아니라 **판정할 데이터가 없다**는 뜻이다. 합성 트랙은 겹침이
0건이고, 벤치의 `spec` 주입기는 `duration`을 줄이기만 해서(§5.5 배타성 유지 목적) 겹침을
만들지 않는다. `spec.overlap`이 발화할 세그먼트 자체가 이 벤치 데이터에 없다.
**겹침이 실제로 있는 자막 트랙이 있어야 판정할 수 있다** — 다음 세션의 후보 과제.

## 오라클 대비 달성률의 U자 곡선

이번 태스크가 `bench/report.py`에 새로 추가한 절이다. 오라클(그 예산에서 가능한 최댓값) 대비
실제 Recall의 비율을 예산별로 뽑으면 U자 모양이 나온다.

| 예산 | 오라클 대비 (en-ko) | 오라클 대비 (ja-ko) |
| --- | --- | --- |
| 1~5% | 86.4% | 85.9% |
| **10%** | **69.6%** (저점) | **70.6%** (저점) |
| 20% | 86.6% | 83.6% |
| 30% | 89.4% | 85.6% |

낮은 예산 구간은 hard fail만으로 채워지고 hard fail 오탐률이 0.9%대로 매우 낮아 거의 낭비가
없다. hard fail을 다 쓰고 나머지를 **가중평균 점수 순**으로 채우는 구간(정확히 이 프로젝트의
기본 운영점인 예산 10%)에서 정밀도가 떨어져 저점을 찍는다. **이것이 계획 A가 남긴 미결
항목("가중평균 vs noisy-or")의 정량적 근거다** — 융합 규칙을 바꾼 브랜치와의 직접 A/B는
아직 없지만, 가중평균이 정확히 어느 지점에서 얼마나 희석되는지는 이 곡선으로 드러났다.

## Key Decisions Made (이번 세션)

- **README 헤드라인은 가장 큰 배수(8.64x)가 아니라 예산 10%(6.96x/7.06x)를 쓴다.** 배수는
  요청 예산이 아니라 실제 검수 비율로 나누는 프로젝트 규율의 연장이다 — 어느 행을 헤드라인으로
  뽑느냐도 같은 정직함을 요구한다. 1~5% 구간은 요청과 실제가 갈라져 "1% 예산에 8.6배"로
  읽히면 거짓이 된다.
- **조사 문서 #2를 닫으면서 Q4를 억지로 같이 닫지 않았다.** 두 질문이 다른 계층(Tier 0 vs
  Tier 1)을 묻는다는 것을 실측이 확인해줬다 — 실측 전에는 "같은 실험"이라고 적혀 있었는데
  그게 틀렸다는 것도 이번에 알았다.
- **`bench/run.py`는 브리프의 의사코드를 그대로 베끼지 않았다.** `random_baseline`과
  `RunMeta`의 실제 시그니처가 브리프 작성 시점과 달라져 있었다(태스크 7·8이 반환값을
  넓혔다). 브리프보다 현재 `bench/measure.py`·`bench/report.py`의 실제 시그니처를 우선했다.

### 이전 세션에서 이어지는 결정 (변경 없음)

초기 언어쌍 **ko→en/ja**(Q2) · 로컬 LLM은 **OpenAI 호환 엔드포인트로 일원화**(Q3) ·
규격 1차 출처는 **Netflix TTSG**, 벤치마크는 `specs/ted-{ko,en,ja}.yaml` 분리(Q5) ·
LLM 연동은 **자체 얇은 어댑터**(Q6) · **가중치는 튜닝하지 않는다**(균등 1.0).

## Known Issues (변경 없음, 여전히 유효)

- **가중 평균 융합에 구조적 문제가 있다.** 이번 세션의 U자 곡선(위)이 정량적 정황 증거를
  더했지만, noisy-or과의 직접 A/B는 여전히 없다.
- **신호 9종 중 어느 것도 `Span`을 채우지 않는다.** FR-7.3 리포트 하이라이트는 아직 안 된다.
- **`_RATIO_MIN_RELATIVE_DEVIATION`(0.25)·`_RATIO_MIN_SOURCE_WIDTH`(4.0)·
  `_RATIO_MIN_SAMPLES`(8)의 근거가 어림값이다.** 이번 벤치가 스윕까지는 하지 않았다 —
  `ablation()`은 신호 on/off만 재고 상수 스윕은 별도 스크립트가 필요하다.
- **로컬 venv가 Python 3.14인데 CI는 3.11/3.12를 검증한다.**
- **외부 URL은 아무도 검사하지 않는다.** (`translations.ted.com` DNS 실패 건, 이전 세션 기록)
- **`logprobs`를 위험 신호로 쓰면 백엔드에 따라 조용히 사라진다**(Ollama 미지원).
- **자가일관성 샘플링은 `n>1` 단일 호출로 구현하면 안 된다** — N회 개별 호출이어야 이식성 유지.

## Context for Next Session

**다음 후보 작업 (우선순위 순)**

1. **CLI 배선** — `ingest`·`check`가 가장 먼저 값이 있다. Tier 0 엔진과 벤치 하네스가
   이미 검증됐으므로 나머지는 배관 작업에 가깝다.
2. **Tier 1 자가일관성 프로토타입** — Q4를 실제로 닫을 유일한 방법. `bench/`가 이미
   측정 하네스를 갖췄으므로 LLM 어댑터만 있으면 같은 트랙·라벨로 바로 A/B가 된다.
3. **겹침 있는 벤치 트랙** — `spec.overlap`을 판정하려면 필요하다. TED2020 합성 트랙이
   아니라 실제 SRT/WebVTT 샘플이 있어야 한다.
4. **가중평균 vs noisy-or 직접 A/B** — `bench/measure.py`의 `ablation()`이 이미 신호
   on/off는 재므로, 융합 규칙(`cuesift.risk.fuse`)만 브랜치를 나눠 같은 트랙으로 재측정하면 된다.

**사용자가 표현한 제약과 선호 (변경 없음)**

- 푸시는 명시적 요청이 있을 때만. 커밋과 푸시를 한 명령에 묶지 않는다
- 커밋 메시지는 한글
- 문서 작성 시 도식과 표를 적극 활용한다
- **가장 큰 숫자가 아니라 가장 오해가 적은 숫자를 헤드라인으로 고른다** — 이번 세션에 R4
  ("AI 래퍼가 아니다" 증명)의 실전 적용으로 다시 확인됐다

## Files Modified This Session

```text
bench/run.py                          신규 — 벤치마크 오케스트레이션(python -m bench.run),
                                       negation 유형별 무작위 기준선 · 사이드카 로드 포함
bench/report.py                       "오라클 대비 달성률" · "negation 무작위 기준선과의
                                       비교" · "코퍼스 제외 (스펙 §4.4)" 절 추가.
                                       RunMeta.excluded → injection_skipped 이름 정정
bench/build_track.py                  (fix 라운드 1) 사이드카 {pair}.clean.stats.json 기록
tests/test_bench_report.py            corpus_stats·injection_skipped 테스트 4건 추가
tests/test_bench_build_track.py       신규 — 사이드카 테스트 2건
bench/results/en-ko-2026-07-29.md     신규 — en-ko 실측 리포트(재생성, fix 라운드 1 반영)
bench/results/en-ko-2026-07-29.json   신규(재생성)
bench/results/ja-ko-2026-07-29.md     신규 — ja-ko 실측 리포트(재생성, fix 라운드 1 반영)
bench/results/ja-ko-2026-07-29.json   신규(재생성)
README.md                             "실측: Recall @ Budget" 절 신설, 상태 경고 현행화,
                                       §4.4 코퍼스 제외 한 줄 추가
CHANGELOG.md                          Unreleased에 벤치 하네스 전체 + 이번 반영 기록
docs/요구사항정의서.md                  §12 Q4 정량 근거 추가(열어 둠)
docs/AI_자막검수_오픈소스_비교.md        §5 #2 닫힘, Q4/Q5 서술 정정
HANDOFF.md                            전면 갱신
```

**`src/cuesift/`는 이 세션에서 손대지 않았다.** 측정만 했고 제품 코드는 그대로다.
**`data/bench/{en-ko,ja-ko}.clean.stats.json`은 로컬에만 있다** — `data/`가
`.gitignore:73`에 걸려 커밋 대상이 아니다. 다음 세션이 벤치를 재실행하려면
`bench.build_track`을 먼저 돌려야 사이드카가 다시 생긴다(트랙 자체는 시드 고정으로
항상 동일하다 — 이번 세션에 SHA-256으로 확인).
