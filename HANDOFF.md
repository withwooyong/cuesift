# Session Handoff

> Last updated: 2026-07-28 (KST)
> Branch: `feat/tier0-signal-engine` (main 미병합)
> Latest commit: `e2c7d39` — 문서: 구현 계획에 "구현 중 바뀐 결정" 절 추가

## Current Status

**Tier 0 신호 엔진이 완성됐다.** 요구사항정의서 §4의 3계층 중 첫 계층 — 무료 결정론적
신호 전체다. 순수 모듈 6개, 신호 9종, 테스트 172건. 네트워크·LLM 의존이 0이라 오프라인에서
전량 통과한다.

**직전 세션까지 `src/`에는 CLI 스텁만 있었다.** 이 세션에서 `segment`·`spec`·`glossary`·
`signals`·`risk`·`triage`가 생겼고, 파이프라인이 `collect_all` → `fuse` → `select_by_budget` →
`review_ratio`로 끝까지 연결된다(최종 리뷰가 300건 합성 트랙으로 실측 확인).

**아직 CLI에 배선되지 않았다.** 모든 서브커맨드는 여전히 종료 코드 `70`을 반환한다.
자막 파일 파싱(`ingest`)과 번역(`translate`) 계층이 없다.

저장소: <https://github.com/withwooyong/cuesift> (Public)

## Completed This Session

| # | Task | Commit | 핵심 |
|---|------|--------|------|
| 0 | 벤치마크 하네스 설계 스펙 | `f19c76d` | 5단계 파이프라인, 오류 7종 주입, 불변식 4종 |
| 0 | Tier 0 구현 계획 (태스크 10개) | `cb9b891` | TDD 사이클, 테스트 127건 예정 |
| 0 | TED 규격 값 조사 | `845a9a9` | ko·ja 21자/10 CPS 확정 (출처 URL 소멸) |
| 1 | 세그먼트 데이터 모델 | `198e90a` | §7.3의 4개 모델, 정수 밀리초 |
| 2 | 문자 폭 계산 3종 | `e3b5352` | FR-5.2, 악센트 라틴 일관성 수정 |
| 3 | 규격 프로파일 + YAML 6종 | `c79f752` | §8.3.1의 42개 수치 전수 대조 |
| 4 | 규격 검사 | `740f433` | FR-5.1, 겹침 스윕 수정 |
| 5 | 용어집 | `0784e20` | FR-3.7, 룩어라운드 경계 매칭 |
| 6 | 수집기 인터페이스·레지스트리 | `a49c3e5` | FR-6.5, 프로토콜 2종 |
| 7 | 구조 신호 5종 | `34f4abc` | FR-3.1~3.5, 전부 hard fail |
| 8 | 파생 신호 3종 | `198cd29` | FR-3.6~3.8, MAD 척도 3회 수정 |
| 9 | 위험도 융합 | `95b5ff3` | FR-6.1·6.2·6.4, nan 방어 |
| 10 | 트리아지 선별 | `bfa0f72` | FR-6.3, API 재설계 |
| 11 | 최종 리뷰 수정 5건 | `3718e3a`..`cace3d4` | 겹침 신호 연결, `Span.side` 추가 |
| 12 | 계획 문서 동기화 | `e2c7d39` | D-1~D-21 결함 기록 |

## In Progress / Pending

| # | Task | Status | Notes |
|---|------|--------|-------|
| 1 | **최종 수정 5건이 재리뷰를 받지 않았다** | 미검증 | 사용자가 백그라운드 에이전트를 중지한 시점이 재리뷰 직전이었다. 코드·테스트·린트는 통과한다. 검토하려면 `bfa0f72..cace3d4` diff만 보면 된다 |
| 2 | **브랜치가 main에 병합되지 않았다** | 대기 | 최종 리뷰 판정은 "병합 가능(Critical 0건)"이다 |
| 3 | 벤치마크 하네스 (계획 B) | 미착수 | 스펙은 `docs/superpowers/specs/2026-07-28-...`에 확정돼 있다. **아래 "빠진 조각" 참고** |
| 4 | Tier 1 자가일관성 · Q4 | 미착수 | 계획 B 이후 |
| 5 | CLI 서브커맨드 배선 | 미착수 | 인자 스키마와 종료 코드 계약은 테스트 6건이 고정하고 있다 |

## Key Decisions Made

- **hard fail 신호에서는 미탐이 오탐보다 낫다.** FR-6.2에 따라 hard fail은 검수 예산을
  우회하므로, 오탐은 실제 검수 비율을 부풀려 §9.1 지표 자체를 파괴한다. Task 7의 수정
  5건을 전부 미탐 쪽으로 기울인 근거다.
- **점수 스케일을 통해 몰래 가중을 넣지 않는다.** 계획 §6.3이 "가중치는 튜닝하지 않는다"고
  못 박고 `DEFAULT_WEIGHTS`를 균등으로 뒀는데, 신호마다 다른 점수 스케일이 실질 가중이 되고
  있었다. 공용 `_violation_score`로 통일했다.
- **`select_by_*`는 전체 목록을 반환한다.** 선별된 것만 반환하면
  `review_ratio(select_by_budget(...))`가 항상 1.0을 내는데, 그 값이 README 배수의 분모다.
  `SegmentRisk.selected` 필드가 있는 이유와도 맞는다.
- **리뷰어의 지적이 옳아도 해법은 틀릴 수 있다.** 제안된 `\b` 단어 경계가 CJK 문자를
  전부 `\w`로 취급하는 파이썬 정규식 특성 때문에 ko·ja 용어를 전부 미매칭으로 만들 뻔했다.
  **이 프로젝트의 언어 조건(ko→en/ja)에서 동작하는지 반드시 확인할 것.**
- **계획 문서의 코드 블록을 최종 코드로 갈아 끼우지 않는다.** 그러면 계획이 "구현과 같은
  것을 두 번 적은 문서"가 되고 실제 코드가 항상 더 정확하니 존재 이유가 사라진다.
  남길 가치가 있는 것은 차이 그 자체이며 "구현 중 바뀐 결정" 절이 그 역할을 한다.
- **TED 프로파일 출처 URL이 죽었다.** `translations.ted.com`이 DNS 해석에 실패한다.
  값은 검색 색인 스냅샷으로만 확인했고, 그 사실을 YAML 주석과 테스트에 남겼다 —
  다시 확인할 수 없는 수치는 테스트가 유일한 보존 수단이다.

### 이전 세션에서 이어지는 결정 (변경 없음)

초기 언어쌍 **ko→en/ja**(Q2) · 로컬 LLM은 **OpenAI 호환 엔드포인트로 일원화**(Q3) ·
규격 1차 출처는 **Netflix TTSG**(Q5) · LLM 연동은 **자체 얇은 어댑터**(Q6).

## Known Issues

- **가중 평균 융합에 구조적 문제가 있다.** 문제를 하나 더 찾으면 위험도가 **내려간다** —
  규격 위반 3건(1.0)에 용어 위반 1건(0.5)을 더하면 0.75가 된다. 예산이 빠듯한 트리아지에서
  문제가 가장 많은 세그먼트가 탈락한다. 합성 데이터 20시드 평균으로 예산 10%에서
  **재현율 5.2%p 손실**이 측정됐고 noisy-or 계열이 구조적으로 맞다는 분석이 나왔다.
  **지금 바꾸지 않는다** — 첫 측정 전에 규칙을 바꾸면 베이스라인이 없어 개선을 입증할 수 없다.
  벤치 하네스의 첫 측정 항목으로 A/B 판정할 것.
- **신호 9종 중 어느 것도 `Span`을 채우지 않는다.** 현 상태로 FR-7.3의 리포트 하이라이트는
  성립하지 않는다. 다만 대부분 싸게 채울 수 있다 — 데이터가 이미 계산돼 있고 버려질 뿐이다
  (`findall` 대신 `finditer`, `re.search`의 `match.span()` 등).
  `Span.side`가 그 준비로 추가돼 있다.
- **`_TAG` 정규식이 `x<y>z` 같은 공백 없는 부등호를 가짜 태그로 잡는다.** 발화 조건이 좁고
  (`<` 직후 라틴 문자 + `>`) 비교가 대칭이라 원문의 가짜 태그가 번역문에 보존되면 발화하지
  않는다. 고치려면 한 글자 태그명을 막아야 하는데 그러면 가장 흔한 `<i>`·`<b>`가 빠진다.
- **`_RATIO_MIN_RELATIVE_DEVIATION`(0.25)·`_RATIO_MIN_SOURCE_WIDTH`(4.0)·
  `_RATIO_MIN_SAMPLES`(8)의 근거가 어림값이다.** 벤치 하네스가 실데이터로 재보정할 값이다.
- **로컬 venv가 Python 3.14인데 CI는 3.11/3.12를 검증한다.** 이 계획은 extra를 쓰지 않아
  무관했으나, `[stt]`·`[qe]` 작업 시 3.11 또는 3.12 가상환경을 별도로 만들 것.

### 이전 세션에서 이어지는 이슈 (여전히 유효)

- **외부 URL(56개 / 24개 호스트)은 아무도 검사하지 않는다.** 의도된 설계이지만 링크 부패는
  실제로 발생한다 — 이번 세션에 `translations.ted.com`이 정확히 그렇게 됐다.
- **`scripts/check_links.py`는 앵커를 검사하지 않는다.** 절 번호를 바꾸면 앵커가 조용히 깨진다.
- **`logprobs`를 위험 신호로 쓰면 백엔드에 따라 조용히 사라진다**(Ollama 미지원).
- **자가일관성 샘플링은 `n>1` 단일 호출로 구현하면 안 된다** — N회 개별 호출이어야 이식성 유지.

## 다음 계획(벤치 하네스)을 위한 인계

### 준비된 것

파이프라인이 **수동 변환 지점 없이** 종단까지 연결된다. 최종 리뷰가 300건 합성 트랙으로
실측한 결과(합성 데이터이므로 진짜 숫자가 아니라 **계산이 된다는 확인**이다):

| 요청 예산 | 실제 검수 | Recall | 배수 |
|---|---|---|---|
| 5% | 5.7% | 56.7% | 10.0x |
| 10% | 10.0% | 73.3% | 7.3x |
| 20% | 20.0% | 93.3% | 4.7x |

- **예산 스윕**: 같은 `risks` 목록에 예산 여러 개를 순차 적용해도 오염되지 않는다.
- **ablation**: `collect_all(segments, ctx, enabled=[...])`가 그대로 쓸 수 있고 오타는 `ValueError`로 잡힌다.
- **무작위 기준선**: `review_ratio()`가 곧 기대 재현율이다. 배수 = `recall / review_ratio`.

### 빠진 조각

| # | 항목 |
|---|---|
| 1 | **자막 파일 → `Segment` 로더가 없다.** `pysubs2`가 의존성에 있으나 `src/` 어디서도 임포트되지 않는다 |
| 2 | **원문·번역 정렬(pairing)이 없다.** `Segment`는 둘을 한 객체에 갖는데 실제로는 두 파일에서 온다 |
| 3 | **정답 집합과 오류 주입기가 없다.** 벤치의 핵심이다 |
| 4 | **`SegmentRisk`에 세그먼트 본문이 없다.** `review.json`을 쓰려면 리포트 계층이 `dict[str, Segment]`로 조인해야 한다 — 결함이 아니라 §7.2 모듈 경계 유지의 결과다 |

## Context for Next Session

**사용자의 원래 의도** — "TED2020 코퍼스를 받아 측정 하네스를 만들자"로 시작했다. 그런데
brainstorming 과정에서 **측정 하네스가 Tier 0 신호 엔진 전체를 선행 조건으로 요구한다**는
것이 드러났다(§9.1의 Recall@Budget을 계산하려면 `signals` → `risk` → `triage` 사슬이 있어야
한다). 그래서 계획을 둘로 나눴고 이번 세션은 **계획 A(Tier 0 엔진)** 만 실행했다.
**계획 B(벤치 하네스)는 아직 작성되지 않았다** — 계획 A가 확정할 함수 시그니처를 소비하므로
A 완료 후에 쓰기로 했다. 그 시점이 지금이다.

**사용자가 표현한 제약과 선호**

- **푸시는 명시적 요청이 있을 때만.** 커밋과 푸시를 한 명령에 묶지 않는다
- 커밋 메시지는 한글
- **문서 작성 시 도식과 표를 적극 활용한다** (이번 세션에 글로벌 규칙으로 등록됨)
- 선택지와 근거를 먼저 제시하고 결정을 받는 방식이 잘 맞았다 — 이번 세션에 12번의
  결정 요청이 있었고 전부 즉시 답이 왔다

**드러난 실패 유형 — 다음 세션에서도 경계할 것**

리뷰가 걸러낸 실질 결함 17건 중 **15건이 계획 자체의 결함**이었다. 구현자들은 브리프를
정확히 따랐다. 그리고 거의 전부 같은 형태였다 — **"흔한 입력에서는 맞는" 코드**다.
ASCII만 있으면 맞고, 자막이 서로 포함하지 않으면 맞고, 용어가 길면 맞는다.

**계획에 쓴 테스트가 정확히 그 흔한 경우만 담았기 때문에** 자체 검토로는 걸러지지 않았다.
테스트를 쓴 사람과 코드를 쓴 사람이 같으면 같은 맹점을 공유한다.

효과가 있었던 것은 **리뷰어에게 구체적 위험을 지목**한 것이다. "3개 이상 연쇄로 겹칠 때를
확인하라", "짧은 용어의 부분 문자열 오탐을 확인하라" 같은 지시가 그대로 적중했다.
다만 **지목이 검증을 보장하지는 않는다** — 한 재리뷰어가 지목받은 위험을 "코드를 읽어 보니
괜찮다"는 주장만으로 통과시켰고, 컨트롤러가 직접 시뮬레이션해서야 회귀가 드러났다.
이후 재리뷰 지시에는 **검증 방법까지** 적었다.

**컨트롤러(나)의 실패도 기록해 뒀다** — 구현자 실행 중 리포 파일 편집, 손으로 만든 4개
케이스만으로 수정안 검증(벤치마크 조건을 놓쳐 재현율이 절반으로 떨어지는 수정을 지시했다),
회귀를 못 잡는 회귀 테스트, 테스트 개수 오산 2회. 상세는 계획 문서의 "프로세스 기록" 절에 있다.

**작업 규약은 `CLAUDE.md`에 있다** — 이번 세션에 신설했다. 매 세션 컨텍스트에 실리므로
문서 지도·검증 습관·확정된 결정을 다시 설명할 필요가 없다.

## Files Modified This Session

```text
CLAUDE.md                             신규 — 프로젝트 작업 규약
README.md                             상태 경고 현행화 · 문서 표에 스펙·계획 추가
CHANGELOG.md                          Unreleased에 Tier 0 엔진 전체 기록
HANDOFF.md                            전면 갱신
docs/superpowers/specs/2026-07-28-ted2020-benchmark-harness-design.md   신규
docs/superpowers/plans/2026-07-28-tier0-signal-engine.md                신규 + 실행 후 동기화
src/cuesift/segment/                  신규 — 데이터 모델
src/cuesift/spec/                     신규 — 문자 폭 · 프로파일 · 규격 검사
src/cuesift/glossary/                 신규 — 용어집
src/cuesift/signals/                  신규 — 레지스트리 · 신호 9종
src/cuesift/risk/                     신규 — 위험도 융합
src/cuesift/triage/                   신규 — 검수 큐 선별
specs/                                신규 — 규격 프로파일 YAML 6종
tests/                                신규 8개 파일, 총 172건
pyproject.toml                        specs/ 를 휠에 force-include
```

**`src/cuesift/cli.py`는 변경되지 않았다.** 모듈은 만들어졌으나 CLI에 배선되지 않았다.
