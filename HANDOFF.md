# Session Handoff

> Last updated: 2026-07-29 (KST)
> Branch: `main` (계획 A·B 모두 병합 완료, 작업 브랜치 삭제됨)
> Latest commit: `5974558` — 병합: TED2020 벤치마크 하네스 (계획 B)

## Current Status

**Tier 0 신호 엔진의 첫 Recall@Budget 실측치가 나왔고 README 최상단에 실렸다.**

이 프로젝트의 핵심 주장 — *"AI 래퍼가 아니다"* — 을 뒷받침하는 숫자가 처음 생겼다. 공개 병렬 코퍼스 TED2020(OPUS)에서 언어쌍당 5,000건을 뽑아 합성 오류 500건을 주입하고 측정했다.

| 예산 10% | en-ko | ja-ko |
| --- | --- | --- |
| 실제 검수 비율 | 10.00% | 10.00% |
| Recall | 69.60% | 70.60% |
| **무작위 대비 배수** | **6.96x** | **7.06x** |

5단계 파이프라인이 전부 구현됐고 ④만 제품 코드다.

```text
  ① fetch    scripts/fetch_ted2020.py    코퍼스 78MB · SHA-256 manifest    ✓
  ② build    bench/corpus.py             399,414 + 358,635쌍 로드·필터      ✓
             bench/timing.py             3언어 CPS 동시 만족 · 줄바꿈       ✓
             bench/build_track.py        깨끗한 트랙 5,000 × 2 (위반 0건)   ✓
             bench/glossary.ted.yaml     용어집 30개 (대응률 ≥79.8%)        ✓
  ③ inject   bench/inject.py             오류 7종 · 라벨 500 × 2            ✓
  ④ measure  src/cuesift/                Tier 0 신호 9종 (계획 A)           ✓
  ⑤ report   bench/report.py             재현 헤더 · 정직한 한계            ✓
             bench/run.py                오케스트레이션                     ✓
```

**아직 CLI에 배선되지 않았다.** 모든 서브커맨드는 여전히 종료 코드 `70`을 반환한다. 자막 파일 파싱(`ingest`)과 번역(`translate`) 계층이 없다.

저장소: <https://github.com/withwooyong/cuesift> (Public) — **origin/main 동기화됨**(`361be6a`,
`git ls-remote`로 확인). 이전 판의 "58커밋 미푸시"는 낡은 정보였다.

## 이 벤치가 밝힌 것 넷

| # | 결론 | 근거 |
| --- | --- | --- |
| 1 | **Tier 0는 성립한다** | 예산 10%에서 무작위 대비 6.96x / 7.06x |
| 2 | **Tier 0로는 부족하다** | 의미 반전 Recall 1.41% — 같은 예산 **무작위 기준선 9.61%보다 낮다** |
| 3 | **어디를 고쳐야 하는가** | 오라클 대비 달성률이 예산 10%에서만 69.6%로 꺼진다(1~5% 86.4%, 20~30% 86~89%) |
| 4 | **무엇을 못 쟀는가** | `spec.overlap`은 합성 트랙에 겹침이 0건이라 **판정 불가** |

2번이 Tier 1·QE 투자의 근거다. Tier 0가 다른 오류(미번역·빈값·반복·숫자·규격)를 상위로 올리면서 **문법적으로 완벽하고 의미만 뒤집힌 문장을 큐에서 적극적으로 밀어낸다.** 무작위 선별은 편향이 없는데 Tier 0는 "정상으로 보이는 것"을 후순위로 보낸다.

3번은 계획 A 재리뷰가 남긴 미결 항목("가중평균 vs noisy-or")의 정량 근거다. U자의 바닥이 정확히 이 프로젝트의 기본 운영점이다.

## Completed This Session

| # | 작업 | 커밋 | 핵심 |
| --- | --- | --- | --- |
| 0 | 계획 A 재리뷰 | `097e1ec`, `c497d3a` | D-22 가중치 총합 오버플로 발견·수정 |
| 0 | 계획 A 병합 | `e185761` | Tier 0 엔진 30커밋 |
| 0 | 병합 후 정정 | `341b357`, `9baa3b7` | D-23 `Span.side` 검증, 재리뷰 기록 정정 |
| 1 | 계획 B 작성 | `cad22a2` | 태스크 9개. 착수 시 OPUS URL 직접 확인 |
| 1 | Task 1~8 | `dd2826c`..`3e80154` | fix 루프 **0회** |
| 1 | Task 9 + fix 3라운드 | `dfa12ca`..`f4b32ca` | 실측·문서 반영, 서술 교정 |
| 1 | 계획 B 병합 | `5974558` | 19커밋 |

테스트 **169 → 271건**. 계획 결함 **12건** 중 **6건을 구현 전에** 잡았다.

## In Progress / Pending

| # | 항목 | 상태 |
| --- | --- | --- |
| 1 | ~~origin 푸시~~ | **완료** — 로컬과 리모트가 `361be6a`로 일치 |
| 2 | CLI 서브커맨드 배선 | 미착수. 인자 스키마와 종료 코드 계약은 테스트가 고정하고 있다 |
| 3 | `ingest`(자막 파싱) · `translate` 계층 | 미착수. **벤치는 이것 없이 완결됐다**(moses 평문이 줄 정렬돼 있어 필요 없었다) |
| 4 | Tier 1 자가일관성 · Q4 | 미착수. **이 벤치가 그 투자 근거를 만들었다** |
| 5 | `_NUMBER`의 NFKC 미정규화 | **고치면 재측정 필요** — 아래 Known Issues |
| 6 | 융합 방식 (가중평균 → noisy-or) | 미착수. **정량 근거는 이미 있다**(오라클 달성률 U자 저점 69.6%, `length.ratio` ablation −6.0%p). 5번과 함께 재측정 1회로 묶는 편이 싸다 |

## Q4는 왜 안 닫혔는가

조사 문서 §5 #2("경량 신호만으로 트리아지가 성립하는가")는 **✅ 닫혔다.** 이 벤치가 답했다.

**요구사항정의서 §12 Q4(자가일관성 유사도 측정 수단)는 열려 있다.** 두 질문이 "같은 실험에서 답이 나온다"고 묶여 있었는데 **그 묶음이 틀렸다** — Q4는 LLM 재번역이 필요한 Tier 1 질문이고 이 벤치에는 그 계층이 없다. 두 문서를 함께 고쳐 각각 별개로 닫히는 이유를 명시했다.

Q4에는 이제 정량 근거가 붙었다 — *"결정론적 신호 9종으로 의미 반전을 예산 10%에서 1.41%만 잡고, 그것도 무작위(9.61%)보다 낮다"*.

## `spec.overlap`이 미해결로 남은 이유

계획 A의 재리뷰가 이 신호의 **캐스케이드**(단일 타임코드 오타가 트랙 50~97%를 flag)를 발견하고 **"벤치 하네스의 ablation으로 A/B 판정할 것"** 이라고 남겼다.

**이 벤치는 그 질문에 답할 수 없다.** `build_track`이 겹침 0건으로 트랙을 합성하고 주입기의 `spec` 유형은 duration을 *줄이므로* 겹침을 만들지 않는다. 그래서 이 신호는 한 번도 발화하지 않고 ablation 기여도가 정확히 `+0.0%`다.

**기여도 0은 "쓸모없다"가 아니라 "측정하지 못했다"이다.** 리포트에 `unmeasured`(FR-3.5, 일부러 뺌)와 **구분된 절**로 표기했다. 판정하려면 **겹침이 있는 실제 자막 트랙**이 필요하다.

## Key Decisions Made

- **배수 헤드라인은 예산 10%에서 뽑는다.** 예산 1%의 8.64x가 더 크지만 그 구간은 hard fail만으로 채워져 실제 검수 비율이 6.32%다. "1% 예산에 8.6배"로 읽히면 거짓이 된다. **계산은 이미 정직했지만(실제 비율로 나눔) 어느 행을 헤드라인으로 뽑느냐도 같은 정직함을 요구한다 — 가장 큰 숫자가 아니라 가장 오해가 적은 숫자를 고른다.**
- **hard fail 오탐 세그먼트를 트랙에서 빼지 않는다.** 빼면 `struct.number_missing` 성능이 좋아 보이지만, 숫자가 있고 번역이 자유로운 세그먼트를 체계적으로 제외하는 것이라 과대평가가 된다. 대신 **오탐률을 측정값으로 삼고**(en 0.96% / ja 0.91%) 불변식 4를 "≤ 2%" 임계로 바꿨다.
- **`negation` 주입은 규격을 깨지 않는 세그먼트에만.** 부정어 삽입이 CPS를 넘기면 `spec.violation`이 발화해 **의미 반전이 아니라 길이 증가를 잡은 것**이 Recall로 집계된다(정정 전 en 79.7% / ja 93.7%가 그렇게 발화했다). 고친 뒤 Recall이 80.3% → 1.41%로 떨어졌고 **그게 정직한 숫자다.**
- **측정을 끝낸 뒤 제품 코드를 고치지 않는다.** 벤치 데이터로 제품을 튜닝하는 것과 같다. `_NUMBER`의 NFKC 결함을 발견하고도 `src/cuesift/`를 건드리지 않았다.
- **무작위 기준선은 실제 검수 비율로 샘플링한다.** 비교 대상이 "같은 수를 무작위로 검수했다면"이므로, 실제로 검수하지 않은 요청 예산을 기준으로 삼으면 비교가 성립하지 않는다.
- **용어집 채택 기준에 대응률을 넣었다.** "사전적 1:1"만으로는 부족하다 — `의사`는 en 대응률 56.1%라 정상 번역의 44%를 위반 판정한다. **흔한 말일수록 번역 선택지가 많아 대응률이 낮다.**

### 이전 세션에서 이어지는 결정 (변경 없음)

초기 언어쌍 **ko→en/ja**(Q2) · 로컬 LLM은 **OpenAI 호환 엔드포인트로 일원화**(Q3) · 규격 1차 출처는 **Netflix TTSG**(Q5) · LLM 연동은 **자체 얇은 어댑터**(Q6).

## Known Issues

### `struct.number_missing`이 전각 숫자를 놓친다 — **고치면 재측정 필요**

`src/cuesift/signals/structural.py:59`의 `_NUMBER`가 NFKC 정규화를 하지 않아 `'５０' != '50'`이다.

```text
  _numbers('지금은 하루 50센트 이하입니다.')       -> ['50']
  _numbers('今では一日５０セント以下になりました')  -> ['５０']
```

집합 비교에서 누락 판정 → `multi_digit`이라 **hard fail**. **ja-ko 자연 오탐 41건 중 13건(31.7%)이 이 경로다.** 한자 수사(`十`·`百`·`万`, 4건 9.8%)도 같은 계열이나 정규화로는 해결되지 않는다.

**고치면 벤치를 재측정해야 한다** — 측정을 끝낸 뒤 제품을 고치면 그 측정이 무효다.

### ~~리포트 테스트 2건이 하드코딩 회귀를 못 잡는다~~ — **해결**

두 테스트를 고치고 **뮤테이션으로 RED를 확인**했다(`bench/report.py`에 하드코딩 2개를
심어 2 failed → 복원 후 22 passed). 구현 자체는 처음부터 정확했다.

**이 항목이 남긴 진짜 교훈은 수정 제안 쪽에 있었다.** 이전 판이 "1줄"이라 적어 둔 후자의
해법(픽스처 이름을 `zz.synthetic.probe`로)은 **그것만으로는 틀렸다** — 이름을 바꿔도
뮤테이션이 그대로 통과했다. 원인이 셋이었기 때문이다.

| 원인 | 왜 무력했나 | 수정 |
| --- | --- | --- |
| 픽스처가 하나 | 렌더된 값과 리터럴이 구분되지 않는다 | 두 번째 값(`0.0123`)으로 한 번 더 렌더 |
| 신호 이름이 실재 | `length.ratio`는 실측에서 음수가 나오는 이름이라 제품이 하드코딩할 수 있다 | `zz.synthetic.probe`로 교체 |
| **잘라내는 범위** | `## 신호별 기여도` 이후는 ablation **표**를 포함해 어떤 이름이든 거기 나타난다 | `**음수는` 이후 문단만 검사 |

세 번째가 이전 판이 보지 못한 것이다. **지적이 옳아도 해법은 틀릴 수 있다 — 뮤테이션을
돌리지 않았다면 그대로 통과했다.**

### 한자 수사가 리포트 원인 목록에 없다

ja-ko 자연 오탐의 **9.8%(4/41)** 가 한자 수사(`10대` → `十代`) 경로인데 리포트의 원인 목록 4갈래에 없다. 문장이 "원인은 여러 갈래다"로 열려 있어 거짓은 아니지만, ja-ko 리포트 독자가 약 10%를 어느 갈래에도 귀속시킬 수 없다.

### ~~주입 감사 산출물이 없다 (스펙 §5.7)~~ — **해결**

`bench/track_io.py`의 `dump_audit()`이 변조 트랙과 정답 라벨을 **한 번의 호출로 함께**
쓴다. `bench.run`이 **측정 전에** 호출하므로 불변식에서 실패한 실행도 주입 내역은 남는다.

| 결정 | 이유 |
| --- | --- |
| 두 파일을 한 함수로 | 짝이 아닌 산출물은 감사에 못 쓴다. 함수가 둘이면 한쪽만 호출하는 실수가 구조적으로 가능하다 |
| 변조 트랙은 `dump_track` 형식 | 감사자가 새 파서를 짜야 한다면 감사 수단이 아니다 — `load_track`으로 그대로 읽힌다 |
| 라벨에 트랙 SHA-256 기록 | 라벨만 있고 어느 트랙의 것인지 모르면 감사가 아니다. `load_labels()`가 대조한다 |
| `--audit-dir` 기본값 = 트랙 디렉터리 | `data/`는 gitignore라 리포를 오염시키지 않는다. 리포트의 `--out-dir`와 분리한 이유는 리포트는 커밋되지만 변조 트랙은 코퍼스 파생물이라 CC BY-NC-ND에 걸리기 때문 |

**실행 검증**: en-ko·ja-ko 양쪽에서 깨끗한 트랙과 변조 트랙의 차집합이 라벨 500건과
**양방향으로 완전히 일치**했다(라벨에만 0건 / 변조에만 0건). Recall 값은 기존 리포트와
동일해 산출물 추가가 측정을 바꾸지 않았음도 확인했다.

### 가중 평균 융합의 구조적 문제 (계획 A에서 이어짐, 이제 정량 근거 있음)

문제를 하나 더 찾으면 위험도가 **내려간다** — 규격 위반 3건(1.0)에 용어 위반 1건(0.5)을 더하면 0.75가 된다.

**이번 벤치가 그 비용을 쟀다**: 오라클 대비 달성률이 예산 10%에서만 69.6%로 꺼진다(1~5% 86.4%, 20~30% 86~89%). hard fail을 다 쓰고 **나머지를 가중평균 점수 순으로 채우는 유일한 구간**이 거기고, 바로 그 지점이 기본 운영점이다.

ablation에서 `length.ratio`가 두 언어쌍 모두 **−6.0%p**다 — 끄면 Recall이 올라간다.

### 이전 세션에서 이어지는 이슈 (여전히 유효)

- **외부 URL은 아무도 검사하지 않는다.** 링크 체커는 내부 상대 링크만 본다. 착수 시점에 사람이 재확인해야 한다 — 이번에 OPUS URL을 그렇게 확인했고 살아 있었다
- `scripts/check_links.py`는 **앵커를 검사하지 않는다.** 절 번호를 바꾸면 앵커가 조용히 깨진다
- `logprobs`는 백엔드에 따라 조용히 사라진다(Ollama 미지원)
- 자가일관성 샘플링은 `n>1` 단일 호출이 아니라 **N회 개별 호출**이어야 이식성이 유지된다
- **로컬 venv가 Python 3.14인데 CI는 3.11/3.12를 검증한다**

## ~~README / 문서 갱신 필요~~ — **해결**

README에 **"재현"** 절을 신설했다(실측 절 안, 한계 서술 뒤). 3단계 파이프라인을 Mermaid
흐름도 + 표 + 실행 명령으로 싣고, `python -m`으로 실행해야 하는 이유와 감사 산출물,
용어집 회귀 게이트 `scripts/glossary_verify.py`를 함께 문서화했다.

**적은 명령 4개는 전부 실제로 실행해 확인했다** — `scripts.fetch_ted2020` ·
`bench.build_track` · `bench.run` · `scripts.glossary_verify`(exit 0). 문서에 적기만 한
명령은 검사하지 않는 게이트와 같다.

`README.md`의 `cuesift.yaml` 참조는 **"CLI (설계 확정, 구현 예정)" 절의 설계 서술**이라
깨진 참조가 아니다 — 고치지 않았다.

## Context for Next Session

**이 세션의 원래 요청은 "0번(재리뷰+병합) → 1번(계획 B 작성)"이었고, 계획 B는 작성에 그치지 않고 실행·병합까지 끝났다.**

### 서브에이전트 방식으로 실행했다

사용자가 명시적으로 선택했고, 그 결과가 글로벌 `~/.claude/CLAUDE.md`에 규칙으로 등록됐다("서브에이전트 작업 규칙"). 구현자 9명 · 리뷰어 10명을 썼고 **Task 1~8의 fix 루프가 0회**였다.

**fix 루프가 0회인 것은 결함이 없어서가 아니라 구현 전에 잡혔기 때문이다.** 컨트롤러가 **구현자가 도는 동안 실데이터로 다음 태스크의 계획을 공격**했다. 리포 파일을 건드릴 수 없으니(작업트리는 구현자 것) 스크래치패드에서 측정만 하게 되고, 측정은 계획의 가정을 정면으로 검사한다.

### 결함의 발견 시점이 성격을 가른다

| 시점 | 무엇이 틀렸나 | 무엇을 해야 보이나 |
| --- | --- | --- |
| 구현 전 (6건) | 계획의 **가정** | 실데이터를 만진다 |
| 구현 중 (3건) | 계획의 **산수** | 코드를 돌린다 |
| 리뷰 중 (나머지) | 산출물의 **서술** | 문서를 읽는다 |

**마지막 단계가 가장 늦게 드러나고 가장 오래 남는다.** 코드 결함은 테스트가 잡지만 서술 결함은 사람이 읽어야 보이고, 한 번 인용되면 정정이 따라가지 못한다. **이 브랜치의 마지막 일곱 커밋이 전부 서술 교정이었다.**

상세는 `docs/superpowers/plans/2026-07-29-ted2020-benchmark-harness.md`의 **"구현 중 바뀐 결정"** 절에 있다 — 계획 결함 12건, 컨트롤러 프로세스 실패 6건, 리뷰가 잘 작동한 형태까지.

### 다음 세션에 경계할 것

- **비동기 에이전트에게 "지금 상태"를 말할 때 기준 커밋을 함께 말하라.** 이번 실행의 조율 실패 3건이 전부 이 문제였다 — `DONE`을 종료로 읽고 다음 구현자를 띄운 것, 지시 응답 전에 리뷰 패키지를 만든 것, "else 분기가 없다"고 보고했으나 이미 있었던 것
- **리뷰어 지시에 `git stash`·기본 출력 경로를 명시적으로 막아라.** "리포 파일을 만들거나 고치지 마라"는 그 경로를 안 덮는다. 실제로 리뷰어가 `python -m bench.run`을 기본 `--out-dir`로 돌려 다른 에이전트의 미커밋 작업 위에 덮어썼고, `git stash`가 구현자의 편집을 가져갔다
- **리뷰어의 오해를 정정할 때 그 오해가 산출물에도 있는지 확인하라.** 리뷰어가 틀린 추측을 했다는 것은 그렇게 읽힐 여지가 있다는 신호이고, 그 여지는 대개 산출물의 문구에서 온다
- **컨트롤러가 회귀 테스트를 써서 넘길 때도 버그 코드에서 실패하는지 확인하라.** 이번에 넘긴 3개가 전부 결함이었다(`seed=1`이 우연히 통과, 픽스처에 숫자가 없어 원본으로도 실패, `"a"*20`이 단일 토큰이라 검사 경로를 안 탐)

### 사용자가 표현한 제약과 선호 (변경 없음)

- **푸시는 명시적 요청이 있을 때만.** 커밋과 푸시를 한 명령에 묶지 않는다
- 커밋 메시지는 한글
- **콘솔 응답에는 ASCII 도식, `.md` 파일에는 Mermaid** — 터미널은 Mermaid를 렌더하지 못한다(글로벌 규칙 등록됨)
- **서브에이전트로 나눌 수 있는 작업은 기본적으로 나눈다**(글로벌 규칙 등록됨)
- 선택지와 근거를 먼저 제시하고 결정을 받는 방식이 잘 맞았다 — 이번 세션에 8번의 결정 요청이 있었고 전부 즉시 답이 왔다

## Files Modified This Session

```text
CHANGELOG.md                        계획 A·B 전체 기록
HANDOFF.md                          전면 갱신
README.md                           실측 절 신설 · 배수 7배 · 문서 표
~/.claude/CLAUDE.md                 서브에이전트 규칙 · Mermaid 매체 규칙 (전역)

bench/                              신규 9개 모듈 + 용어집 + manifest + results 4파일
scripts/fetch_ted2020.py            신규 — 코퍼스 획득 (표준 라이브러리만)
scripts/glossary_verify.py          신규 — 용어집 대응률 검증기
src/cuesift/risk/fuse.py            D-22 가중치 총합 오버플로 차단
src/cuesift/segment/models.py       D-23 Span.side 런타임 검증
src/cuesift/signals/derived.py      경계 조건 주석 보강
src/cuesift/triage/policy.py        경계 조건 주석 보강
tests/                              신규 9개 파일, 총 271건 (169 → 271)
pyproject.toml                      pytest pythonpath · ruff src 확장
.gitignore                          data/ (CC BY-NC-ND 4.0)

docs/superpowers/plans/2026-07-29-ted2020-benchmark-harness.md   신규 + 실행 후 동기화
docs/superpowers/plans/2026-07-28-tier0-signal-engine.md         재리뷰 결과 기록
docs/요구사항정의서.md                §12 Q4 정량 근거
docs/AI_자막검수_오픈소스_비교.md      §5 #2 ✅ 닫힘 · Q4 묶음 정정 · specs 정정
```

**`data/` 아래는 하나도 커밋되지 않았다** — TED2020은 CC BY-NC-ND 4.0이라 원본도 가공물도 배포할 수 없다. `git log --all -- data/`가 0건임을 확인했다. 리포에 들어간 것은 `bench/manifest.json`(SHA-256 기록)과 결과 숫자뿐이다.
