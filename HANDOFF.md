# Session Handoff

> Last updated: 2026-08-03 (KST)
> Branch: **`feat/check-cli`** (작업 트리 깨끗, stash 없음) — PR 열림, 미머지
> Latest commit: `9ef4869` 설계 스펙 + 이 인수인계 커밋
> **다음 세션은 `writing-plans`로 구현 계획을 작성하는 것부터 시작한다** — 설계는 확정됐다

## Current Status

**`check` 배선(FR-8.2)의 설계를 확정했다.** 코드는 아직 한 줄도 쓰지 않았다.

| | 이번 세션 |
| --- | --- |
| v0.1 FR 완료 | 19/42 (45%) — **변동 없음** |
| 테스트 | 316 — **변동 없음** |
| 산출물 | 설계 스펙 1건 (440줄) |

제품 상태는 그대로다 — **모든 서브커맨드가 종료 코드 `70`(미구현)**. 다만 `check`는
이제 **"무엇을 어떻게 만들지"가 전부 확정된 상태**이고 판단이 소진돼 있다.

저장소: <https://github.com/withwooyong/cuesift> (Public).

## Completed This Session

| # | 태스크 | 커밋 | 산출물 |
| --- | --- | --- | --- |
| 1 | 함정 실측 (3회) | — | HANDOFF 기록 2건 정정 · 사각지대 1건 발견 |
| 2 | 설계 스펙 (자체 리뷰 포함) | `9ef4869` | [`check` 배선 설계](docs/superpowers/specs/2026-08-03-check-cli-design.md) |

**이번 세션은 판단만 했다.** 브레인스토밍 스킬의 흐름(탐색 → 질문 4개 → 접근안 비교 →
설계 제시 → 스펙 작성 → 자체 리뷰 → 커밋)을 따랐고, 구현은 다음 세션으로 넘긴다.

## In Progress / Pending

| # | 작업 | 상태 | 비고 |
| --- | --- | --- | --- |
| 1 | **`check` 구현 계획 작성** (`writing-plans`) | ⬜ **다음** | 설계 §1.1의 산출물 4건이 태스크 분해의 입력이다 |
| 2 | `check` 구현 | ⬜ | 계획 승인 후. 이것이 끝나면 **처음으로 쓸 수 있는 제품**이 나온다 |
| 3 | **요구사항정의서 §3.2 S3 정정 제안** | ⬜ **사용자 승인 대기** | 설계 §9. `--spec th`가 실행되지 않는다 |
| 4 | WP5 출력 (FR-7.1~7.5) | ⬜ | `subs` + `event_index`로 라운드트립 가능. 태그 보존은 별도 경로 필요 |
| 5 | WP7 번역 → WP8 Tier 1 | ⬜ | Q4(자가일관성 유사도)가 여기서 닫힌다 |
| 6 | WP9 STT | ⬜ | 런타임 의존성 4개 고정과 충돌 — 호출 방식 미결 |

### 사용자가 아직 하지 않은 것 — 스펙 리뷰

브레인스토밍 스킬의 절차상 **사용자가 스펙 파일을 검토·승인한 뒤에 `writing-plans`로
넘어간다.** 이번 세션은 그 앞에서 끝났다. 다음 세션은 검토 요청부터 다시 꺼내야 한다.

특히 봐 달라고 지목한 절은 넷이다.

| 절 | 왜 |
| --- | --- |
| §9 요구사항정의서 정정 제안 | **문서를 고치는 제안**이라 승인이 필요하다 |
| §5.1 `hard`와 `any`가 같은 결과 | 플래그 값 3개 중 2개가 동의어다 |
| §11 R2 원문 자막은 규격을 흔히 어긴다 | 기본 호출이 늘 빨간불이면 게이트가 무시된다 |
| §8 `--spec`을 확장자로 가르는 규칙 | FR-5.3 도달성을 위해 범위를 약간 넓혔다 |

## Key Decisions Made

설계 스펙 §13에 결정 로그 12건이 근거와 함께 있다. 요약하면 넷이다.

- **`check`는 신호 엔진을 통과하지 않는다.** `collect_all` → `fuse` → `triage`를 쓰지 않고
  `spec/check.py`를 직접 부른다. 신호 계층이 얹는 넷(점수화·`hard_fail`·융합·트리아지)을
  `check`가 하나도 쓰지 않기 때문이다 — 심각도가 단일 등급이고 예산도 순위도 없다.
  **두 경로가 갈라지지 않는다** — 규격 판정의 원천이 `spec/check.py` 하나이고 양쪽이 그것을 쓴다
- **판정 범위는 규격 6종 + 빈 큐.** 비교할 번역문이 없어 `struct.*`는 성립하지 않는다.
  **이 설계가 새로 만드는 판정은 `check_empty_cues` 하나뿐이고 나머지는 전부 배선이다**
- **심각도는 단일 등급이고 등급을 발명하지 않았다.** `--fail-on hard`와 `any`가 v0.1에서
  같은 결과를 낸다. 프로파일 수치가 축을 가리지 않고 흔들려(§12.4) 배정의 출처가 없고,
  §11 R8이 "출처 없는 수치를 기본값으로 넣지 않음"을 명시한다
- **출력은 콘솔 요약만.** `review.json`(FR-7.2) 스키마를 규격만 아는 상태에서 못 박으면
  WP5가 그 결정을 물려받는다. 위반 목록은 **stdout**이다 — 오류 메시지가 아니라 산출물이다

### 이전 세션에서 이어지는 결정 (변경 없음)

초기 언어쌍 **ko→en/ja**(Q2) · 로컬 LLM은 **OpenAI 호환 엔드포인트로 일원화**(Q3) ·
규격 1차 출처는 **Netflix TTSG**(Q5) · LLM 연동은 **자체 얇은 어댑터**(Q6) ·
배수 헤드라인은 **예산 10%**에서 뽑는다 · **가중치는 튜닝하지 않는다** ·
인제스트는 **단일 진입점** · 파싱한 파일은 항상 **`source_text`**에 들어간다.

## Known Issues

### ✅ 해소됨 — WP6이 밟을 함정

이전 인수인계의 🔴 항목은 **설계 D3(신호 엔진 우회)로 구조적으로 소멸했다.**
`struct.*` 수집기가 `check` 경로에서 아예 실행되지 않는다.

다만 **그 과정에서 이전 기록이 두 군데 틀렸다는 것이 실측으로 드러났다.** 다음 세션이
같은 함정을 다시 만나지 않도록 남긴다.

| 이전 기록 | 실측 |
| --- | --- |
| "`struct.empty`만 발화한다" | **틀렸다.** 원문에 숫자·태그가 있으면 `struct.number_missing`·`struct.tag_lost`도 hard fail한다. 이전 픽스처에 숫자도 태그도 없었을 뿐이다 |
| "대응 선택지 ①: 파싱한 파일을 `target_text`로도 채운다" | **그대로는 안전하지 않다.** `struct.degeneration`이 `'네 네 네 알겠습니다'`에서 hard fail을 낸다 |

재현 방법은 설계 §12.1~12.2에 있다.

### 🟡 빈 큐는 현재 어느 경로로도 탐지되지 않는다

`empty_cue.srt`의 빈 큐를 세 구성으로 시험했고 **셋 다 놓친다**(설계 §12.3).

| 경로 | 왜 놓치나 |
| --- | --- |
| `struct.empty` | `source_text`가 비면 `None`을 반환한다 |
| `check_text` | 빈 텍스트의 줄·CPS 검사를 건너뛴다 |

`check_empty_cues`가 이것을 닫는다. **`check_text` 안에 넣으면 안 된다** — `translate`
경로에서 `struct.empty`와 이중 계산되고, 그 부풀림이 검수 비율을 밀어 올린다.

### 🟡 요구사항정의서 S3의 `--spec th`가 실행되지 않는다

내장 프로파일은 `en`·`ja`·`ko`·`ted-en`·`ted-ja`·`ted-ko` 여섯뿐이다. Q2가 언어쌍을
ko→en/ja로 확정했으므로 태국어는 v0.1 범위 밖이고, `--spec th`는 Q2 확정 이전의 잔재다.
**`--spec ja`로 정정 제안 — 사용자 승인 대기**(설계 §9).

### WP5가 알아야 할 것 — 라운드트립은 되지만 태그 보존은 안 된다

`subs` + `event_index`로 **SRT·VTT·ASS·SSA 4포맷 전부 라운드트립이 성립**한다(실측).
다만 보존 범위는 좁다.

| 항목 | 보존되나 |
| --- | --- |
| 스크립트 헤더 · 스타일 정의 · 주석 줄 · 타임코드 · 포맷 | ✅ |
| ASS 오버라이드 태그 `{\an8}` | ⚠️ **건드리지 않은 이벤트만.** `plaintext` setter가 텍스트를 통째로 대체한다 |
| VTT cue settings `line:0 position:20%` | ❌ `SSAEvent`에 담을 필드가 없다 |
| 화자 태그 `<v Alice>` | ❌ 로드 시점에 버려진다 |

### `spec.overlap` 판정 — 준비는 됐고 벤치는 아직

`overlap.vtt` → `check_overlaps` 경로가 열렸다. 겹침 1000ms가 검출된다. 다만
**벤치 하네스는 여전히 겹침 0건인 합성 트랙을 쓴다** — 기여도 `+0.0%`를 다시 재려면
`bench/build_track.py`나 주입기가 겹침을 만들어야 한다.

### 미뤄 둔 것 (여전히 유효)

| 항목 | 판정 근거 |
| --- | --- |
| `_reject_non_subtitle`이 **디렉터리 입력도 `not_found`** 로 보고 | **이번 설계가 갚는다**(D11) — `typer.Argument(exists=True, dir_okay=False)` |
| `IngestError.reason`이 자유 `str` | 이 리포에 타입 체커가 없다. `Literal`은 아무 게이트도 강제하지 않는 장식이 된다. mypy 도입 시 함께 |
| `loader.py`의 `_load`·`_reject_non_subtitle` 정의 순서가 호출 순서와 반대 | 6커밋 누적의 유일한 흔적 |
| 설계 §10이 지시한 "`id` 한계를 주석에 적는다"가 코드에 미이행 | `id`가 인덱스 기반이라 파일 편집 시 흔들린다는 서술이 없다 |

### 이전 세션에서 이어지는 이슈 (여전히 유효)

- **외부 URL은 아무도 검사하지 않는다.** 링크 체커는 내부 상대 링크만 본다
- `scripts/check_links.py`는 **앵커를 검사하지 않는다.** 절 번호를 바꾸면 조용히 깨진다
- **링크 체커는 `git ls-files` 기준이라 미추적 파일을 건너뛴다.** 새 문서를 만들면
  `git add` 후에 돌려야 검사된다 — **두 게이트의 파일 개수 대조가 유일한 탐지 수단이다.**
  이번 세션에도 실제로 썼다(양쪽 18개 일치)
- `logprobs`는 백엔드에 따라 조용히 사라진다(Ollama 미지원)
- 자가일관성 샘플링은 `n>1` 단일 호출이 아니라 **N회 개별 호출**이어야 이식성이 유지된다
- **로컬 venv가 Python 3.14인데 CI는 3.11/3.12를 검증한다**

## Context for Next Session

### 사용자가 이번 세션에 명시한 것

- **결정 4건을 직접 골랐다** — 판정 범위(규격+빈 큐) · 심각도(단일 등급) ·
  엔진 우회 · 출력(콘솔만). 전부 권장안을 채택했다
- **"이번 세션 종료하고 다음 세션에서 구현 계획 작성"** — 구현 계획은 다음 세션의 첫 작업이다
- **커밋·푸시를 명시적으로 승인했다** (이 세션 한정)
- 커밋 메시지는 한글 · 콘솔 응답에는 ASCII 도식, `.md` 파일에는 Mermaid

### 실행 방식 — 무엇이 실제로 결함을 잡았나

| 단계 | 잡은 것 |
| --- | --- |
| **함정 실측** | HANDOFF 기록 2건 정정 + 사각지대 1건 발견. **읽기만 했다면 셋 다 놓쳤다** |
| 프로파일 수치 실측 | 2등급 심각도 가설을 폐기시켰다 — 두 축 모두 흔들린다 |
| 문서 대조 | `--fail-on` 값 불일치 · `--spec th` 미존재 |

**서술을 액면가로 받지 않은 것이 이번 세션의 전부다.** HANDOFF의 함정 기록은 정확했지만
범위가 좁았고, 제안된 대응은 옳아 보였지만 새 오탐을 만들었다. 20줄짜리 스크립트를
세 번 돌린 것이 그 둘을 갈랐다.

### 다음 세션이 계획서에 반드시 넣어야 할 것

브리프는 계획서에서 잘라낸 것이라 **앞선 태스크에서 배운 것을 모른다.** 컨트롤러가 손으로
옮겨야 한다. 이번 설계에서 옮겨야 할 것은 다음 넷이다.

1. **`check_empty_cues`는 먼저 없는 상태에서 `empty_cue.srt`가 통과하는 것을 확인한 뒤
   추가한다.** 게이트를 만들면 반드시 실패시켜 봐야 한다
2. **큐 번호는 `event_index[segment_id] + 1`이지 `segment.index + 1`이 아니다.**
   주석이 없는 파일에서는 둘이 같아 **틀려도 테스트가 통과한다**(설계 §10.3)
3. **종료 코드 `2`와 `66`은 구분해서 단언한다.** `assert exit_code != 0`으로 쓰면
   뒤바뀌어도 통과한다
4. **`spec/check.py`는 순수해야 한다.** `ingest`·`typer`를 임포트하면 계약이 깨진다

### 리뷰어에게 반드시 넘길 것

지목만으로는 부족하고 **검증 방법까지** 적어야 한다. 이번 설계에서 diff로 판정할 수 없는
것은 설계 §10.3에 표로 정리해 뒀다 — 그대로 브리프에 옮긴다.

금지 사항: **코드 수정 · `git stash` · 작업 트리 변경.**

### 컨트롤러가 겪은 것 (이전 세션에서 이어짐)

- **`TaskUpdate`로 소유자를 기록하면 구현자에게 새 할당 신호로 전달된다**
- **리뷰어가 보고 없이 대기 상태로 전환되는 일이 반복됐다.** 프롬프트에 "보고서를
  `SendMessage`로 team-lead에게 보내라"를 명시한 뒤로 해결됐다
- **`Bash` 도구는 Git Bash다.** PowerShell here-string(`@'…'@`)을 쓰면 메시지 앞뒤에
  `@`가 남는다 — 이번 세션에 실제로 발생해 `--amend`로 고쳤다. 여러 줄 커밋 메시지는
  heredoc(`git commit --file=- <<'MSG'`)을 쓴다

## Files Modified This Session

```text
docs/superpowers/specs/2026-08-03-check-cli-design.md   신규 440줄 — 절 13개 · 결정 로그 12건
docs/WBS.md                                             WP6 행에 설계 스펙 근거 추가
CHANGELOG.md · HANDOFF.md                               세션 기록과 인수인계
```

코드는 변경하지 않았다. 테스트 **316건 유지**.

## 게이트 실행 기록

| 게이트 | 결과 |
| --- | --- |
| markdownlint | `Linting: 18 files` · 0 issues |
| 링크 체커 | 마크다운 18개 파일 · 상대 링크 60개 · 깨진 링크 0 |
| ruff · pytest | **돌리지 않았다** — 코드 변경이 없다. PR의 CI가 검증한다 |

## README/문서 갱신 필요

수집기가 낸 신호는 전부 `[LOW]`이고 검토 결과 **오탐이다.** 고치지 않았다.

| 신호 | 판단 |
| --- | --- |
| `SCRIPTS_DIR_MISSING_IN_README` — `check_links.py` | CI 게이트이지 사용자가 실행하는 스크립트가 아니다. `CLAUDE.md`에 문서화돼 있다 |
| 같은 신호 — `fetch_ted2020.py` · `glossary_verify.py` | README "재현" 절에 **이미 있다** |
| `UNRESOLVED_PATH_REFS` — `cuesift.yaml` | FR-8.4의 **아직 만들지 않은 파일**이다. 참조가 깨진 것이 아니다 |
| 같은 신호 — `sys.path` | 산문 속 용어이지 경로 참조가 아니다 |
