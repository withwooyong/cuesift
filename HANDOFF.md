# Session Handoff

> Last updated: 2026-08-28 (KST)
> **브랜치 `feat/config-file`이 계획서 태스크 9개를 전부 끝냈다.**
> 커밋 수는 `git rev-list --count main..HEAD`로 센다 — 여기 숫자를 적으면
> 그 문장을 고치는 커밋이 자기 자신을 틀리게 만든다(실측으로 한 번 겪었다).
> **FR-8.4가 닫혔다.** v0.1 완료 개수 35 → **36**. WP6에는 FR-8.3·8.5가 남아 여전히 🟡다.
> 다음 작업은 **FR-8.5**(진행 표시·CI 감지) — FR-8.3은 STT 어댑터(WP9)가 없으면 붙일 대상이 없다.
> **브랜치가 `origin`에 있는지, PR이 있는지, CI가 돌았는지는 아래 "배포 절차"의 명령으로 직접 재라.**
> 값이 아니라 명령이다.
> 진척은 [WBS](docs/WBS.md), FR 번호의 출처는 [요구사항정의서](docs/요구사항정의서.md)다

## Current Status

**FR-8.4를 닫았다. 계획서 태스크 9개 전부 완료.**

| # | 태스크 | 커밋 | 테스트 |
| --- | --- | --- | --- |
| — | [설계 스펙](docs/superpowers/specs/2026-08-28-config-file-design.md) | `ea84f91`·`82794de` | 1379 |
| — | [구현 계획](docs/superpowers/plans/2026-08-28-config-file.md) | `acc55d2` | 1379 |
| 1 | 매핑표와 상등 게이트 (`schema.py`) | `1e92019` | 1391 |
| 2 | 로더 — 미지 키 거부·가중치 검증 | `9393d40` | 1420 |
| 3 | 도메인 중첩 → 커맨드 중첩 | `4b058a4` | 1429 |
| 4 | `ctx.default_map` 배선 (우선순위) | `bebb038` | 1443 |
| 5 | 환경변수가 설정 파일을 이기게 | `bf8f245` | 1449 |
| 6 | Tier 0 융합에 가중치 전달 | `bc9ee5f` | 1456 |
| 7 | Tier 1 융합 2곳에 가중치 전달 | `efc240a` | 1462 |
| — | 이중 리뷰 반영 (HIGH 4 · MEDIUM 3 · LOW 1 · 서브프로세스 경계) | `a73ca9b`·`b5dce36`·`b6fff47` | **1475** |
| 8 | 문서 갱신 + §8.2 게이트 | `7736355` | **1479** |
| 9 | 실물 확인 | 이 커밋 | **1480** |

**§8.2 게이트는 Task 9가 한 번 더 조였다.** Task 8이 만든 것은 `load_config()`까지였고,
실물 확인이 **로드되지만 실행되지 않는** 예시를 찾아내 CLI까지 태우는 테스트를 더했다
(아래 ⓐ).

## 이번 세션이 배운 것

### ⓐ 로드되는 것과 실행되는 것은 다르다 — 배선이 문서의 오류를 보이게 했다

요구사항정의서 §8.2는 오래 `signals.tier1.max_ratio: 0.25`를 적고 있었다. 이 값은

| 층 | 결과 |
| --- | --- |
| `load_config()` | ✅ 통과 — 알려진 키이고 타입도 맞다 |
| `ctx.default_map` → `--tier1-max-ratio` | ✅ 도달 |
| **곱 게이트** (`samples x max_ratio < 0.3`, FR-4.3) | ❌ **종료 코드 2** |

**Task 8이 만든 게이트는 이것을 잡지 못했다** — 로더까지만 태웠기 때문이다.
실물 확인에서 `--config t1.yaml translate`가 exit 2로 죽고서야 드러났다.

**FR-8.4가 이 오류를 만든 것이 아니라 보이게 만들었다.** 설정 파일이 배선되기 전에는
그 값이 아무 데도 가지 않아 틀린 것이 드러날 자리가 없었다. **배선은 문서의 주장을
반증 가능하게 만든다** — FR-7.3의 `Span` 사건과 방향이 반대인 같은 교훈이다(그때는
스키마가 있는데 값이 없었고, 이번엔 값이 흐르기 시작하자 값이 틀렸다).

지금은 `tests/test_docs_config_example.py`가 §8.2 블록을 **CLI까지** 태운다.
실패시켜 확인했다 — `max_ratio`를 `0.25`로 되돌리면 `assert 2 != 2`로 죽는다.

### ⓑ 완화책을 실제보다 넓게 적으면 그 자체가 위험이다 — 스펙 R1을 좁혔다

스펙 §9 R1의 이전 판은 "P1~P4의 성질을 테스트가 직접 확인하므로 업그레이드 시
게이트에서 잡힌다"였다. **리뷰어가 `type_cast_value`를 패치해 실측한 결과 절반만 참이다.**

| P4의 두 성질 | 게이트가 잡나 |
| --- | --- |
| **검증** — 틀린 값이 종료 코드 2로 거부된다 | ✅ 잡는다 |
| **변환** — `"./x/y"` → `Path`, `"html"` → `ReviewFormat` | ❌ **못 잡는다** |

typer가 `default_map` 값의 타입 변환만 그만두는 회귀에서 `output.dir`이 `str`로
흘러도 **죽는 테스트가 하나도 없다.** v0.1에서는 무해하다(받는 쪽이 `str`도 받아
넘긴다). 위험한 것은 다음 사람이 "게이트가 잡는다"를 읽고 `Path` 전용 메서드를 쓰는
경우다 — 그때 처음 터지는데 원인은 typer 업그레이드라 한참 떨어진 곳에 있다.

**게이트로 만들지 않고 적어 두는 것을 택했다.** 잡으려면 파라미터의 런타임 타입을
단언해야 하고, 그것은 R1이 걱정하는 바로 그 click 내부 의존을 한 겹 더 늘린다.
스펙 §9.1에 그대로 남겼다.

### ⓒ 자동 탐색 차단은 **인프로세스 전용이다** — 서브프로세스에는 안 듣는다

`tests/conftest.py`의 autouse 픽스처가 `cli._discover_config`를 막아, 개발자 리포
루트에 `cuesift.yaml`이 있어도 테스트가 그것을 읽지 않는다. **그런데 `monkeypatch`라
서브프로세스에는 듣지 않는다.** `test_cli_pipe.py`가 `cwd=`를 임시 디렉터리로 넘겨
닫았다(`b6fff47`).

**앞으로 서브프로세스로 CLI를 띄우는 테스트를 추가하면 같은 조치가 필요하다.**
빠뜨리면 개발자 기계에서만, 그것도 리포 루트에 `cuesift.yaml`을 둔 사람에게만
깨진다 — CI는 초록이다.

### ⓓ 지목받는 것과 막혀 있는 것은 다르다

요구사항정의서 §8.2를 FR-6.1(`signals.weights`)과 FR-4.3(`signals.tier1.max_ratio`)이
**이름을 걸어** 지목하고 있어 "설정 파일이 열리면 둘도 닫힌다"로 읽혔다. 실제로는
착수 시점에 **둘 다 이미 ✅**였다 — FR-6.1은 수식어라 판정을 좌우하지 않았고,
FR-4.3은 `--tier1-max-ratio`로 축 1이 먼저 닫혀 있었다.

**개수를 움직이는 것은 지목이 아니라 막고 있는 것이다.** WP8b가 FR 둘을 함께 닫았던
것과 겉모습이 같아 보여도 구조가 다르다.

### ⓔ 문서 갱신이 갱신을 놓친 자리를 또 찾았다

WBS의 `축 | WP1 | WP4 | …` 표가 **낡아 있었다.** FR-7.3이 WP5를 4에서 5로 올렸는데
표는 `4`인 채로 남아 합이 34를 냈고, **바로 위 문단은 표제를 35로 적고 있었다.**
그 문단의 내용이 정확히 "산문이 낡으면 총계가 갈라진다"였다.

**경고문을 쓰는 것과 표를 고치는 것은 다른 행동이다.** 이번엔 반대 방향으로 갈렸다 —
산문이 맞고 표가 틀렸다. 갱신 시점에 둘을 함께 봐야 한다.

## Task 9 실물 확인 — 서브프로세스로 잰 것

**`pytest`는 실제 파일시스템의 현재 디렉터리에서 CLI를 띄우지 않는다.** 자동 탐색(D2)이
`CliRunner`가 아니라 진짜 프로세스에서 도는지는 서브프로세스로만 확인된다.

작업 디렉터리는 리포 밖이다 — **리포 루트에 `cuesift.yaml`을 만들면 게이트가 오염된다.**

| # | 명령 | 관측 |
| --- | --- | --- |
| 1 | `python -m cuesift translate a.ko.srt --dry-run` (옵션 없음) | stderr 첫 줄 `설정을 읽었다: cuesift.yaml`. **`--to` 없이 `[en]`으로 돌았다** — `Missing option`이 나오지 않는다(P3). `base_url`·`model`·`output.dir`·`review.out`이 전부 설정에서 왔다 |
| 2 | 같은 명령 + `--to ja` | 대상이 `[ja]`. **CLI가 이긴다** |
| 3 | 상위 디렉터리에 설정을 두고 하위에서 실행 | `설정을 읽었다` 줄이 **나오지 않는다**(D2) |
| 4 | `--config bad.yaml`(오타 키) | **exit 2** · `가까운 키: triage.review_budget`. 리다이렉트해도 2를 유지한다 |
| 5 | 스텁 프로바이더로 실주행 (`translate`, 예산 40%) | exit 0. `spec.violation` 1건 적발, `review.json` 산출 |
| 6 | **가중치를 바꿔 3회 재실행** | 아래 표 |
| 7 | `--config t1.yaml`(Tier 1 켬) | exit 0. 가중치 2.0이 그대로 반영됐고, 후보 0건 사유가 화면에 나갔다(세그먼트 3개라 `floor(3 x 0.05) = 0`) |
| 8 | `--config ollama.yaml`(**실제 엔드포인트** `qwen2.5:3b`) | 설정의 `review.format: both`가 먹어 `a.en.review.json`과 `a.en.report.html`이 **둘 다** 나왔다. exit 1 — 3세그먼트 중 2건 번역 실패(파킹 finding 2번, 이 브랜치와 무관) |

**⑥이 가중치 통로의 직접 증거다.** 세 실행 모두 `캐시 히트 1개 · 실제 호출 0개`라
번역문이 동일했고, **바뀐 것은 `signals.weights` 하나뿐이다.**

| `spec.violation` 가중치 | `review.json`의 `risk_score` | noisy-or 검산 `1 - (1-0.75)^w` |
| --- | --- | --- |
| 1.0 | 0.75 | 0.75 |
| 0.1 | 0.12944943670387588 | 0.129449… |
| 2.0 | 0.9375 | 0.9375 |

**값이 산식과 자릿수까지 일치한다** — "적용됐다"가 아니라 "이 식으로 적용됐다"까지 잰다.

**스텁 서버는 리포 밖에 뒀다**(`scratchpad/live/stub.py`). OpenAI 호환
`/v1/chat/completions`에 `{"translations":[{"id":N,"text":…}]}`를 돌려주면 된다
(`translate/prompt.py`의 계약). 큐가 많거나 결정론이 필요한 시나리오는 이 방법이
Ollama보다 낫다 — 실제로 ⑧의 `qwen2.5:3b`는 3큐 중 2큐를 실패했다.

## 배포 절차 — **"푸시했다"와 "CI가 돌았다"는 다르다**

**값이 아니라 명령으로 확인해라.** 이 문단은 직전 세션에서 세 번 틀렸다.

| 무엇 | 확인 명령 |
| --- | --- |
| 원격에 브랜치가 있나 | `git ls-remote origin feat/config-file` |
| 미푸시 커밋 | `git rev-list --count origin/feat/config-file..HEAD` |
| PR이 있나 | `gh pr list --head feat/config-file --state all` |
| CI가 돌았나 | `gh pr checks <번호>` — **PR이 없으면 안 돌았다** |

**푸시만으로는 CI가 안 돈다.** `.github/workflows/ci.yml`의 트리거는
`push: branches: [main]` · `pull_request:` · `workflow_dispatch:`뿐이라
**기능 브랜치 푸시는 어느 것도 건드리지 않는다.**

직전 작업 패키지 WP5 나머지(FR-7.3)는 PR [#12](https://github.com/withwooyong/cuesift/pull/12)로
`main`에 들어갔다.

**`main` 직접 푸시가 금지된 이유**: 직접 푸시하면 **머지된 뒤에야** CI가 돌아
게이트가 아니라 사후 통보가 된다. 로컬 venv는 3.14이고 CI는 3.11~3.14 매트릭스라
로컬 통과가 CI 통과를 뜻하지도 않는다.

### 남은 작업 순위

| WP | 상태 | 근거 |
| --- | --- | --- |
| ~~WP6 부분 (FR-8.4)~~ | **✅ 완료** | 이 브랜치가 닫았다 |
| **WP6 나머지 (FR-8.5)** | **1순위** | 진행 표시·CI(비대화형) 감지. 선행이 전부 풀린 유일한 항목이다 |
| WP6 나머지 (FR-8.3) | 보류 | `transcribe` 배선. **STT 어댑터(WP9)가 없으면 붙일 대상이 없다** |
| WP9 STT | 2순위 | FR-1.3이 "자막 우선"이라 마지막이어도 S1이 성립 |

**착수 전에 위 ⓐ를 읽어라.** FR-8.5도 "화면 출력 하나"로 읽히기 쉬운 형태다.
FR-7.3(렌더러 하나)과 FR-8.4(로더 하나)가 둘 다 그 함정에 있었다.

## 파킹된 finding — **원장이 아니라 여기가 영구 기록이다**

직전 세션들에서 이월된 8건은 **그대로 열려 있다.** 이 브랜치가 닫은 것은 없다.

| # | 무엇 | 왜 지금 안 했나 | 다시 열 조건 |
| --- | --- | --- | --- |
| 1 | **번역 전량 실패가 `exit 1`** | `main`에 이미 있는 기존 동작이다. `cli.py` 독스트링의 "1을 진단 실패에 쓰지 않는다"와 충돌하지만 이 브랜치가 만든 것이 아니다 | 종료 코드 체계를 손보는 작업이 열릴 때 |
| 2 | README 권장 모델(`qwen2.5:3b`)로 **번역이 대량 실패 + 실패 사유 0줄** | 진단 부재는 별도 작업. §12 Q3이 "로컬 LLM 능력은 균일하지 않아 **탐지·명시가 필요**"라 이미 말한다. **이번 live에서 3큐 중 2큐가 실패해 재현됐다** | 진단 출력 작업. **문서가 권하는 모델이 실패하는 것 자체가 사용자 신뢰 문제** |
| 3 | `--dry-run`의 `최대 0회`에 **사유가 없다** | 실주행엔 `_diagnose_empty_candidates`가 있는데 dry-run엔 없다 | 2번과 같은 작업에서 함께 |
| 4 | `COLUMNS=70`에서 옵션 이름이 잘린다 | rich 렌더링. 색 활성 재현이 안 돼 `[의심]`으로 남았다 | 색이 켜진 CI 로그에서 실제로 관측될 때 |
| 5 | 화면 토큰(번역만) vs `review.json` `cost`(합계)가 **다른 객체** | 화면에 합계 줄을 넣을지는 설계 결정이다 | 차이를 설명할지 없앨지 정하는 일 |
| 6 | `inf` 메시지가 원인을 틀리게 말한다 | 문구 문제, 작지만 별건 | 조합 검증은 **7규칙 / 8종 문자열**이다 |
| 7 | `cli.py`의 줄번호 인용이 조용히 낡을 수 있다 | 이 저장소 `src/`에 같은 형태가 **15건**이라 관행이다 | 관행 전체를 바꾸는 작업 |
| 8 | 종료 코드 69 단축이 **Tier 1 실패에도** 걸린다 | 단축은 `EXIT_UNAVAILABLE`에서만 걸리고 건너뛴 언어를 stderr로 명시한다 — 손실이 없다 | 언어별로 다른 프로바이더를 쓸 수 있게 되는 날 |

**1·2·3이 한 덩어리다** — 전부 "실패했는데 왜인지 안 말한다"이고, 2·3은 같은 작업에서 닫힌다.

### 이 브랜치가 새로 파킹한 것

| # | 무엇 | 판정 |
| --- | --- | --- |
| 9 | **`default_map` 값의 타입 변환에 게이트가 없다** | 위 ⓑ. 게이트를 만들면 R1이 걱정하는 click 내부 의존이 한 겹 더 는다. 스펙 §9.1에 적어 뒀다 |
| 10 | 요구사항정의서 §0.1의 "수혜자 규칙은 지금도 **FR-4.3**을 🟡로 붙들고 있다" | **낡았다.** FR-4.3은 2026-08-27 WP8b가 ✅로 올렸다. 이 브랜치 범위 밖이라 손대지 않았다 — 다음에 §0.1을 여는 사람이 고칠 것 |

## 브랜치 안에서 확정된 규칙 (Ruling)

| # | 무엇 |
| --- | --- |
| D1 | 우선순위 해결을 `ctx.default_map`에 위임한다. **병합 코드를 쓰지 않는다** — 손으로 짜려면 옵션 23개의 기본값을 전부 `None` 센티널로 옮겨야 하고 그러면 `--help`의 기본값 표시가 사라진다 |
| D2 | 자동 탐색은 현재 디렉터리 **한 칸**뿐이다. 상위로 올라가면 사용자가 존재를 모르는 파일이 Recall@Budget을 조용히 오염시킨다 |
| D4 | 모르는 키는 **종료 코드 2**로 거부하고 `difflib`로 가까운 키를 제시한다 |
| D5 | 타입 변환·값 검증은 click에 맡긴다. 로더가 판정하는 것은 `llm.provider`와 `signals.weights`의 값 타입 둘뿐 — **후자만 예외인 이유는 그것이 CLI 파라미터가 아니어서 click을 거치지 않기 때문이다** |
| D6 | `signals.weights`는 `default_map`이 아니라 `ctx.obj`로 간다. `--weights` 플래그를 만들지 않는다 |
| D12 | `cache.enabled`는 **긍정형**이고 로더가 `no_cache`로 뒤집는다. `no_cache: false`는 이중부정이라 손으로 쓰는 문서에서 매번 되짚게 된다 |
| D13 | 위치인자(`input`)는 설정 대상이 아니다. `default_map`은 인자에도 먹으므로 **명시적으로 빼야** 한다 |
| R5 | 매핑표 대상 집합과 click 옵션 집합은 **상등**이어야 한다. 부분집합만 보면 매핑표에 남은 죽은 행을 못 잡는다 |
| — | `RecursionError`를 `ValueError`로 감싼다. **테스트를 두지 않는 것은 의도다** — 재귀 한계가 파이썬 버전·플랫폼마다 달라 3.11~3.14 매트릭스에서 간헐 실패하는 게이트가 된다 |

## 승계 항목 — 이 브랜치가 건드리지 않았다

| 항목 | 상태 |
| --- | --- |
| **Q4**(자가일관성 유사도 측정 수단) | 여전히 열려 있다. 문자 단위 유사도는 **형태**를 재고 **의미**는 못 잰다(실측 7쌍이 negation 0.727~0.930 / paraphrase 0.759~0.800으로 범위가 겹친다). 판정은 벤치마크에 Tier 1을 태우는 별도 작업의 몫 |
| **FR-4.2**(역번역) | 구현 안 함. 문자 단위 유사도로는 `llm.retranslation_gap`이 **역방향으로 작동**한다 |
| **`engine.py::_run_single`의 전역 index** | 확인됐고 안 고쳤다. `main`에도 들어가 있다. 약한 로컬 모델에서 개별 폴백이 `index=0`을 뺀 전부에서 실패한다 |
| **`cli.py` 크기** | 이 브랜치가 또 키웠다. 분할한다면 별도 작업이다 — `config/`를 별도 패키지로 뺀 것이 그 방향의 첫 걸음이다 |
| `segments[].reasons`의 순서 미검증 | NFR-3 재현성 문제. 열려 있다 |

## 게이트 실행 기록

**이 세션이 브랜치 HEAD에서 직접 돌린 값이다.** 링크 수는 이 문서 자신을 포함하므로
**이 커밋 이후의 값**을 적는다.

| 게이트 | 수치 |
| --- | --- |
| `ruff check .` | All checks passed |
| `ruff format --check .` | **109 files** already formatted |
| `pytest -q` | **1480 passed, 3 deselected**(로컬) |
| `pytest --cov=cuesift` | 커버리지 **99%** |
| `python scripts/check_links.py` | 마크다운 **35개** 파일 · 상대 링크 · 깨진 링크 **0** |
| `npx markdownlint-cli2` | Linting: **35 files** · **0 issues** |

**두 도구가 같은 파일 수(35)를 세는지 본다.** 어긋나면 새 `.md`가 아직 `git add`되지
않은 것이고, 그 문서는 링크 검사를 **아예 받지 않는다**. 실측으로 32 vs 31로 갈린
전례가 있고 `깨진 링크 없음`만 읽었다면 통과로 보였다.

**CI는 1건 적게 센다.** `data/`가 `.gitignore`에 있어 깨끗한 체크아웃에는 벤치 트랙이
없고 `tests/test_bench_glossary.py`가 1건을 skip한다 — **`passed`가 하나 적고
`skipped` 하나가 붙는 것**이 CI의 기대값이다. 로컬 3.14 통과가 CI 3.11~3.13 통과를
뜻하지 않는 것과는 **별개의** 차이이므로 둘을 섞지 않는다.

**테스트가 1379 → 1480으로 101건 늘었다.** 계획서는 목표치를 못 박지 않았다 —
FR-7.3이 1332를 예고하고 1379를 냈다(+47).

## 개발 환경 메모 (승계)

**Python 실행은 반드시 `.venv/Scripts/python.exe`를 쓴다.** 시스템 Python은 3.14라 다르다.
게이트는 CI와 같은 대상 `.`으로 돌린다 — **`src tests`로 좁히면 안 된다**(그 차이로
CI가 5회 연속 실패한 전례가 있다).

**리포 루트에 `cuesift.yaml`을 만들지 마라.** 자동 탐색(D2)이 그것을 읽는다.
`conftest.py`의 autouse가 인프로세스 테스트는 막지만 **서브프로세스는 못 막는다**(위 ⓒ).

Ollama는 트레이 앱 겸 백그라운드 서비스로 자동 기동해 `127.0.0.1:11434`를 듣는다 —
`ollama serve`를 따로 칠 필요가 없다. PATH에 없으면
`$env:LOCALAPPDATA\Programs\Ollama\ollama.exe`를 직접 부른다.

| 모델 | 크기 | 용도 |
| --- | --- | --- |
| `qwen2.5:3b` | 1.9GB | 번역·Tier 1 신호용. **이번 세션 실측: 3큐 중 2큐 실패**(파킹 2번) |
| `qwen2.5:1.5b` | 986MB | 폴백 관찰용. 번역기로는 못 쓴다(실측 5/15) |

**결정론이 필요하면 스텁 서버를 쓴다.** OpenAI 호환 `/v1/chat/completions`에
`{"translations":[{"id":N,"text":…}]}`를 돌려주면 된다.
**리포 밖에 두어야 게이트를 오염시키지 않는다.**

live 실행 명령:

```powershell
$env:CUESIFT_LIVE_BASE_URL="http://localhost:11434/v1"
$env:CUESIFT_LIVE_MODEL="qwen2.5:3b"
.venv/Scripts/python.exe -m pytest -m live -v -s
```

## 다음 세션 시작 절차

```bash
git checkout main && git pull
git log --oneline | head -5
git status --short                            # clean이어야 한다
# docs/WBS.md 의 "다음 작업 순서" 1순위를 본다
```

**FR-8.4에는 코드에 손댈 것이 남아 있지 않다** — 로컬 게이트 5종이 통과했다.
다음 작업은 **FR-8.5**(진행 표시·CI 감지)다. FR-8.3은 WP9가 선행이다.

**착수 전에 위 ⓐ와 파킹 9·10번을 훑어라.**
